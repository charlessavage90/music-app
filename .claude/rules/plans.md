---
paths:
  - "docs/superpowers/plans/**"
  - "docs/superpowers/specs/**"
---

# Plans and specs — path-scoped rule (`DLS-T1` probe)

This rule exists to test whether a path-scoped rule loads when a session opens a plan or a
spec (`docs/superpowers/findings/2026-09-10-documentation-layer-strategy.md` §6). It carries
no guidance of its own. The guidance for writing and reviewing plans is `CLAUDE.md`, "Writing
and reviewing plans here", which stays authoritative. The test was read on 2026-09-24 (that document's §6c); this rule
remains only as a monitor until the owner removes it.
