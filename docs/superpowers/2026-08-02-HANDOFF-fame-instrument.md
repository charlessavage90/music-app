# Handoff — the fame-instrument track, 2026-08-02

**Role: ACTIVE — this is the CURRENT handoff.** Nothing supersedes it. Supersedes
[`2026-08-02-HANDOFF-tail-drop-and-candidate-census.md`](2026-08-02-HANDOFF-tail-drop-and-candidate-census.md)
on next actions. It does **not** state project status: for that read
[`NEXT.md`](NEXT.md), which owns it.

**This is a SEAM handoff.** The track completed: construct redefined, proxy adopted under
a passed validation, every verdict rendered by committed rules. Nothing is in flight, no
subagent is running, no port is listening, the tree is clean and pushed. Reasoning:
[`2026-08-02-fame-instrument-execution-log.md`](2026-08-02-fame-instrument-execution-log.md)
(§1–§9).

## The claims that must not be reverted

1. **"Fame" changed meaning on 2026-08-02, and old documents keep the old meaning.** The
   requirements Definitions entry is now **novelty-likelihood** (user-relative,
   audience-pinned); the worldly-fame construct is **retired**, its records stand
   un-re-read in their own currency, and **worldly-fame claims are barred from future
   criteria until an instrument exists** — three instruments were measured failing to
   order it (`RCS-`). A well-meaning editor "fixing" an old document's fame language to
   the new meaning, or vice versa, is the failure this line exists to prevent.
2. **`fame_lb_pctl` is the adopted novelty-likelihood proxy** — via `NOV-2` (15/15),
   under `NOV-AM1`, whose post-result disclosure is maximal and part of the record.
   `NOV-1` stands UNREADABLE and retired as mis-designed; do not re-run it.
3. **The `FAM-` verdict (NO ADOPTION) stands.** It answered "does LB measure worldly
   fame" — no. The later adoption answers a different construct. Both are correct;
   neither supersedes the other.
4. **Deezer and Wikipedia are dead as fame candidates under the `RCS-` rules** —
   re-proposal needs new grounds, never a re-run. `RCS-AM1`'s five-row list is closed;
   no third widening of its corroboration exists.
5. **The cap re-evaluation's owner inputs are now complete** — scope (both data sets),
   primary outcome (the bypass novelty gradient, `REQ-42` shape), coverage as guard-only
   at ~10,000, hubness-rising-with-depth as the per-arm kill, candidate families (a)–(d)
   including the tag-based limiter and staged unbounded, tags refine-never-create, router
   pricing in scope. All recorded in the execution log §1. **It is written by a fresh
   session, in the adopted currency, consuming `TB-P5H-7` for any pricing arm and the
   `FAM-AM1.3` quantisation floor (gradient claims ≥ 10× the ruler's measured step).**

## Already updated — do not re-edit

`PRODUCT-REQUIREMENTS.md` (Definitions entry, operational chain, `REQ-Q1` note, §10 item
3), `NEXT.md` (rewritten at this closeout), `TEST-QUEUE.md` (new entry), `docs/README.md`
(new documents classified), the three specs (`FAM-`, `RCS-`, `NOV-`, each with
amendments), the execution log, and the analysis README.

## What I know that is not in the durable record

Checked deliberately; the record is thorough because it was written per task. Three
residuals, folded here:

- **The 30-artist corpus's reuse rule**: hand values (Spotify) arbitrate *worldly-fame*
  questions only; the known/unknown marks arbitrate *novelty* questions; TJ Brown's late
  read (8,274 / ruler 218) is an addendum row usable by a future corpus only at design
  time. All three files are committed under `builder/analysis/2026-08-02-fame-instrument/`.
- **The Deezer id-map and the union snapshot cover the same union** (93,067), so the cap
  re-evaluation can score either population without a coverage trip; the snapshot's
  identity is its manifest sha (`d9d6d5d3…`), and a re-fetch is a new instrument
  (`FAM-AM1.7`).
- **The owner's marks imply famous-band interiors are ~73% novel to him** — a fresh
  session designing gradient criteria should read log §8 before assuming the famous band
  is a novelty dead zone; it is not, and that materially loosens what a successful arm
  must achieve.
