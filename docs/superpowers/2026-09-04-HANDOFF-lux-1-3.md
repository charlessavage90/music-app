# Handoff — `LUX-1`, `LUX-2` and `LUX-3` are built and in review, 2026-09-04

**Role: SUPERSEDED 2026-09-05 on NEXT ACTIONS ONLY** by [`2026-09-05-HANDOFF-doc-layer.md`](2026-09-05-HANDOFF-doc-layer.md), a maintenance session that changed no app code. **Everything this note records about `LUX-1..3` — the zero-diff promise, the retirements, `LUX-E4`'s undefined threshold, the damaged `TAS-` sample — still stands in full**, and its must-not-revert list is unchanged. Only its ranking as the current handoff has moved. Supersedes
[`2026-09-01-HANDOFF-cxr-revert.md`](2026-09-01-HANDOFF-cxr-revert.md) on **next actions
only** — that note remains authoritative for the `CXR-` revert's internals and its
must-not-revert list, all of which still stand. It does **not** state project status: for
that read [`NEXT.md`](NEXT.md), which owns it.

**A SEAM handoff.** The work concluded: all seven tasks are executed, each individually
reviewed, a whole-branch review ran, its findings were fixed in one wave and re-reviewed
16/16. **The degradation tell did not fire.**

Reasoning: [`2026-09-04-lux-1-3-execution-log.md`](2026-09-04-lux-1-3-execution-log.md) —
it owns the ten rulings, the deferrals and the deploy note. Governing scope:
[`specs/2026-09-03-launch-ux-scope.md`](specs/2026-09-03-launch-ux-scope.md). Plan:
[`plans/2026-09-04-launch-ux-1-3.md`](plans/2026-09-04-launch-ux-1-3.md) — **COMPLETE**.
Branch `launch-ux-1-3`, **PR #101, open against `main`**.

---

## Start here

**Nothing is assigned and nothing is blocked.** The next action is the owner's, and it is
either merging PR #101 or pressing the queued test. `TEST-QUEUE.md` holds **one live item**,
written 2026-09-04, and it is not exercisable until this is deployed.

## Which documents are now wrong, and in which direction

**None now — but the closeout doc-audit found seven, and all seven were fixed before this
note was final.** Recorded because "the audit was clean" and "the audit found things and they
were fixed" are different facts, and only the second one tells you the check was working.
Three were HIGH: the **map row** for the handoff this note supersedes still read "ACTIVE" with
no forward pointer (the *document* had been updated and its **row had not** — a defect of
absence, invisible to any grep); the **root `README.md`** still described the app as having
two bypass controls; and the **plan's role line** still read "ACTIVE" with no execution
status, which `docs-lint`'s own check 4 had flagged. Four were MEDIUM, of which one matters
to a future reader: `LUX-T4`'s task header still advertised parallel execution, which `PF-1`
had overridden — a reader starting from that task alone would not have seen the correction.
`docs-lint`'s hard checks pass.

Two directions of staleness to watch, because both bite the other way round:

- **"The app has two bypass buttons" is now FALSE of the present** and correct inside any
  record describing what shipped before 2026-09-04. Read for tense.
- **"`LUX-4` is unstarted" is TRUE and must stay true.** It is gated on `LUX-E1` and the
  scope document names a handoff seam before it. Do not start it from the `LUX-` plan, which
  deliberately excludes it.

## Claims an editor must NOT revert

- **The `dislike` mechanism, its wire contract and its `?dislike=` URL parameter all still
  exist and are deliberately unreachable from the UI.** Removing them would break every link
  ever shared. `exclusions.ts` and `pathfinding.py` both have **zero diff** across the whole
  branch, and that is load-bearing, not incidental.
- **`REQ-30`/`31`/`32` are RETIRED, not overturned.** If the second bypass signal is ever
  restored, they govern it again as written.
- **`LUX-E4`'s threshold is UNDEFINED, not passed.** Its denominator was empty — no
  lower-half artist reached a card. Any document softening this into a pass is wrong.
- **The committed `TAS-` 120-pair sample is damaged on the served map**, with the attrition
  almost entirely in the two obscure classes. `LUX-E2` reuses that file. `specs/…-launch-ux-
  scope.md` §5 now warns about it; do not remove the warning.
- **The two frozen two-button artifacts stay as written** — `frontend/design/2026-08-07-
  bypass-tray/` and `builder/analysis/2026-07-23-f6-trace-capture/resolve_trace.py`. They
  record their own era correctly.
- **Candidate de-duplication was promoted into scope deliberately**, on the spec's authority
  ("a *different* clip"). It is not scope creep and the plan now says so.

## What has already been updated — do not re-edit

`PRODUCT-REQUIREMENTS.md` (retirements + `REQ-45`/`REQ-46`, which live in **§3**, not §7),
`docs/README.md` (five new rows and two amended), `specs/2026-09-03-launch-ux-scope.md` §5, `api/README.md`,
`frontend/README.md`, `infra/README.md` (the deploy note), `TEST-QUEUE.md` (one new entry),
`NEXT.md`, and the plan itself (corrected twice in place during execution).

## What I know that is not in the durable record

**Empty, and checked rather than asserted.** Every ruling, deferral, the deploy note, the
Snyk gap and the shared-link trace are in the execution log; the measurement's figures are in
its own README; status is in `NEXT.md`. The SDD ledger was distilled into the log and then
deleted, and the two rulings made after the log was first written were appended to it before
deletion (commit `2912bf1`) — that gap was found and closed at closeout rather than shipped.

One judgement, recorded here because it is the kind that evaporates: **the wording of the
single bypass control was not put to the owner before shipping.** The scope document says
wording is his. It reads "Dig deeper — same vibe, less familiar" with "Rebuilds the whole
journey" beneath. Changing it is a one-line edit; the queued test asks him to read it before
pressing it.

## Owed, and by whom

- **`snyk_code_scan` on the modified `api/` code — the OWNER's**, because the tool is
  unauthenticated here and the auth flow needs his hands. The only undischarged gap.
- **Merging PR #101 — the owner's**, as always. It supersedes PR #100 rather than sitting
  beside it; #100 should be closed as superseded, and that is also his.
- **The queued use-the-app test — the owner's**, after deploy.
- **What to do about `LUX-E4`'s unreadable threshold — the owner's.** Re-run at press depth,
  redraw the pair sample, or accept the depth-zero reading. Nothing is blocked on it.
