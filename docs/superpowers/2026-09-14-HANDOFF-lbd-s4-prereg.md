# Handoff — the `LBD-S4` adoption pre-registration, 2026-09-14

**Role: ACTIVE — this is the CURRENT handoff.** Nothing supersedes it. Supersedes
[`2026-09-13-HANDOFF-lbd-a4.md`](2026-09-13-HANDOFF-lbd-a4.md) on next actions. It does **not**
state project status: for that read [`NEXT.md`](NEXT.md), which owns it.

**A seam, and the cleanest kind.** The pre-registration is **committed and unexecuted**. Nothing
was derived, emitted, built, censused or listened to. **The executing session reads it cold** —
that is the condition it was written for — and **the owner stop is the go/no-go read**, not
anything before it.

**It owns no figures and neither does this note.**

---

## What exists now

One new document:
[`specs/2026-09-14-lbd-s4-adoption-preregistration.md`](specs/2026-09-14-lbd-s4-adoption-preregistration.md),
identifiers **`LBA-`**, collision-checked across every ref on 2026-09-14 (the series and all six
sub-patterns: free, with nothing in the working tree either).

It fixes, before any result exists:

- **Nine arms** in a full 3 × 3 lattice — strength threshold (10, 7, 3) × population rule (`V`
  today's served artists, `P` the extended crawl's artists, `U` every artist the table names) —
  each cell with an isolating baseline differing in exactly one column, and each with its plain
  sentence.
- **Five measurements** `LBA-M1`–`M5`, each with a plain sentence and either a stated effect size
  or an explicit *reported descriptively, no threshold*.
- **Four gates** `LBA-G1`–`G4`, each with its own effect size and each with its bar's provenance
  stated — including the two that have no prior calibration and say so.
- **Ten reads** `LBA-R0`–`R9`, each naming the run state it presupposes, covering every outcome
  including *"the numbers say no"*.
- **The owner's three rulings, verbatim** (`LBA-D1`–`D3`), and six methodology decisions
  (`LBA-D4`–`D9`).
- **Eight named exposures** `LBA-X1`–`X8`, and a **claims check** resolving every function, file,
  config value and figures-owning document it names.

---

## ⚠ The three things a reader is most likely to get backwards

**1. The un-listenable drop list is NOT held constant, however the config reads.** It removes
nobody from `V` and nobody from `P` because it is *measured inert* there; it removes nobody from
`U` because over an uncensused population the guard **refuses** and the filter is switched **off**.
Two zeroes that look identical in a configuration dump and mean opposite things — and the arms
where it switches off are exactly the arms that grow the map. This is the pre-registration's §2.4,
the section `CLAUDE.md` requires because a factor table cannot see a dormant term. `LBA-M4` sizes
it, `LBA-G4` discounts `LBA-M3`'s supply gain when it is large, `LBA-X2` carries the bar, and
`ULC-F4` means every one of those figures **over-drops** relative to how the app actually resolves
clips.

**2. `LBA-M3` over a `V` arm is 1.0 by construction, and that is not a finding.** The added set is
exactly `P − V`, so no member of it is in `V`. It is the `V` rule's own definition made visible —
and it is arguably the most decision-relevant line on the page: *the population rule that keeps
today's artists is the rule that keeps none of the artists this track exists to help.* The
consequence is a bar: **`LBD-G2` is applied within a population rule and never across one**
(`LBA-X6`).

**3. `LBD-X6` still stands.** Its condition was discharged on 2026-09-13 and `R10` **fired**, which
confirmed the bar rather than releasing it. `LBA-D1` — the owner's ruling that every `S4` arm uses
ListenBrainz's own pairing — makes the point moot for these arms and **does not license a sentence
about the cheaper form.**

---

## Claims not to revert

- **The owner's three rulings are his and are quoted, not paraphrased.** The threshold is an
  **open factor**, not a choice this document makes; no read selects one. No listen is designed or
  run before the stop.
- **`LBA-A1` and `LBA-A3` are `LBD-A0V` and `LBD-A5V`** — reused, sha-verified, **not rebuilt**.
  Rebuilding them would add a build-date column the factor table does not have.
- **Threshold 7 is the three-listener bar**, by arithmetic on ListenBrainz's own SQL
  (⌈(t+1)/contribution⌉ = 3 for t ∈ {6,7,8}), and 7 is the value the committed descriptive curve
  already carries a cell for. **Thresholds 3 and 5 both require two listeners**, so an arm at 5
  would have been a dead arm — recorded in §2.1 because §6c's shape makes 5 look like the obvious
  middle value and it is not.
- **`LBA-M1`'s query-cost half is admissible without fame, and this was checked from source, not
  assumed.** At zero `known` presses the ramp multiplier is exactly `0.0` and the file's own
  comment records that `cost += 0.0 * fame` is numerically identical to omitting the term —
  measured, and pinned by `MSW-G2`. **It follows that only d0 is measured**, and the
  pre-registration says so rather than working around it.
- **`LBA-G2` may stop a cell only on its stated bar.** The executing session does **not** have the
  authority to reduce the lattice for any other reason — not "obviously dominated", not cost.
  `LBA-A9` is the corner and it is the only cell that can say *"there is no bigger map to have
  inside these rules."*
- **Nothing here re-reads any `LBD-` criterion.** `LBA-M3` uses `LBD-C2b`'s statistic and
  `LBD-G2`'s bar over new arms — a new measurement in an old currency, not a re-read.
- **`LBD-C1` is not cited as passed**, and every arm inherits whatever the reimplementation gets
  wrong that the synthetic sub-check cannot see. That sentence belongs in the results document.

---

## What I know that is not in the durable record

1. **No document on the record states how many artists the full pair table names.** `LBD-M1` was
   explicitly not taken at the table level — the similarity README's §6b says so in those words —
   and the two build READMEs count only within `P` and `V`. So **the `U` population's size is
   genuinely unknown**, which is why `LBA-D8` stage 1 exists and why it is the cheapest thing that
   could change the design. Do not go looking for that number in an older document; it is not
   there.
2. **`builder/analysis/census-coverage/ulf_coverage.json` is active data, not a frozen probe
   output.** The census scripts read **and write** it. Record its state before and after any
   census pass, or a later reader cannot tell what a run cost.
3. **The frozen census scripts are copied forward, never re-run in place.** The 2026-08-09
   precedent is in `2026-08-09-cex-task11-execution-log.md` §2: re-running would overwrite a frozen
   record and write a misleading provenance.
4. **The MusicBrainz release dump is ~322 GiB, not the ~17 GiB two census docstrings claim.** It
   is ~90 % of the bytes a census reads and therefore sets the floor on its cost. The correction is
   in the same execution log's §6 and in the live copy of the script; the frozen copy still says
   ~17 GiB.
5. **A dedicated duckdb venv exists at `C:\unsung-fast\lbd-venv`** (duckdb 1.5.5, matching every
   manifest in the track), outside the repo and absent from a fresh clone. Recreate with `uv venv`
   + `uv pip install duckdb` if a later session re-runs these scripts.
6. **Harness-tracked background jobs get killed on this machine under memory pressure.** Launch
   multi-hour work detached via `Start-Process`.

---

## Nothing is in flight

No background jobs beyond the `docs-lint` run recorded in the PR body, no dispatched subagents, no
half-written directories, no listeners on 8000 or 5173 — this session booted no app and needed
none. **No graph was built, no archive emitted, no pass run, no census run, and no journey
generated.**

---

## Artifacts

**None.** This session produced two documents and a `docs/README.md` row, all committed. It pinned
existing artifacts by sha256 inside the pre-registration (§10 lists all thirteen) and **verified
none of them** — verification is the executing session's first act, and the pre-registration says
so.

---

## Owed, and by whom

**The owner's**, in this order — and per `NEXT.md`'s rule this note does not record how far down
the list he has got:

1. **Run the queued use-the-app tests** — `TEST-QUEUE.md`, **15 unticked boxes**, live since the
   2026-09-08 deploy and untouched by this work. The two at the bottom are flagged as not being
   defect checks and are his to strike or keep.
2. **Merge this PR.**
3. **Decide whether `LBD-AM3`'s override extends to `LBD-A4`** as a statement about *absolute*
   fidelity. A scope question about his own earlier ruling, carried forward from the `LBD-A4`
   handoff. **Nothing here depends on it.**
4. **Decide whether `LBD-S4` is executed at all.** The pre-registration is committed and
   unexecuted; committing it does **not** start it. **Stopping the `LBD-` track here remains a
   complete outcome**, and no session proposes a route.

**A fresh session's, if he says execute:** read the pre-registration cold, verify the thirteen
sha256s in its §10, then run `LBA-D8` stage 1 and stop at `LBA-G2`. **The owner stop is the
go/no-go read**, which presupposes the `complete` run state — every cell either built or stopped on
`LBA-G2`'s stated bar with the bar recorded.

Branch `lbd-s4-adoption-prereg` (`gh` says where its PR is).
