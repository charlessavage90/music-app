# Documentation map

**Role: AUTHORITATIVE.** The current classification of every document in `docs/`. Where
another document disagrees with this one about a document's role, this wins.

**Read this before citing any document in `docs/`.** It says which documents are
authoritative, which are historical, and which must never be used as context.

Every document here has exactly one **role**. The role determines how much you can
trust it and whether you may act on it.

| Role | Meaning |
|---|---|
| **AUTHORITATIVE** | Current and correct. Cite it. If another document disagrees, this one wins. |
| **ACTIVE** | Work in progress. Current intent, but not yet reality. |
| **COMPLETE** | Describes work that shipped. Accurate as history; may not describe the code today. |
| **HISTORICAL** | Superseded. Retained so the reasoning can be audited. **Do not cite. Do not act on.** |
| **NARRATIVE** | Written for humans, not for engineering. **Never use as context for development.** |
| **EXTERNAL** | Not ours. Third-party reference material. **Not project documentation.** |

---

## The one rule that matters most

**Measured figures about similarity scoring, hub-seeking, and path quality live in
exactly one file:**

> ### `superpowers/findings/2026-07-21-scoring-adjudication.md`

Every other document **cites it by section number and does not restate its numbers.**
Its **§6** is a table marking prior claims as upheld, overturned, or unresolved —
check there before trusting any scoring claim you find anywhere else.

This rule exists because five documents each kept their own copy of the same figures
and drifted into three mutually contradictory positions. Two separate analyses were
invalidated before anyone noticed. **Restating a correct number is still a violation** —
that is how the drift began.

---

## Current state

> **⚠ For *what to do next*, read [`superpowers/NEXT.md`](superpowers/NEXT.md) — it owns
> status and sequencing, and this section does not.** The bullets below are retained for the
> **defect record and the standing hazards**, which are this map's job: what you must know
> before citing the documents it classifies. They lag: as of 2026-07-26 they do not mention
> Gate 2's Tracks A, D or B, and the handoff they name as current is superseded. Where they
> disagree with `NEXT.md` about status, `NEXT.md` wins; where they disagree about a *defect*,
> the owning document wins. Added 2026-07-26, when a status summary in `CLAUDE.md` went three
> tracks stale because three documents each kept a copy.
>
> ⚠ **2026-07-28: path-quality work is UNPAUSED by owner decision.** Every "the pause is
> intact" tail in the bullets and rows below was true when its document was written and is
> **not** a claim about today — the governing document for the resumed work is named in
> `NEXT.md` (the Track 3 pre-registration).

- ⛔ **Repair + retune is DONE. Track 1 was adopted; Track 2 (2026-07-24) and Track 2F
  (2026-07-25) both returned nulls; the ceiling **ordering** measurement (2026-07-25) came
  back **WIDE** — nothing adopted at any point.** Current handoff:
  [`superpowers/2026-07-25-HANDOFF-track2f-and-headroom.md`](superpowers/2026-07-25-HANDOFF-track2f-and-headroom.md),
  which supersedes the 2026-07-24 one **on next actions only**. **The execution log's last two
  entries are fresher than any handoff.** Track 2F ran the ceiling toll at full strength — the
  previous handoff's own recommendation, now executed — and exhausted that mechanism with its
  bound holding, which Track 2's corner arm never managed. The half it structurally could not
  reach, the ceiling's *ordering*, was then measured read-only and is WIDE. **Live candidate: a
  builder-side p99 rescale, still un-pre-registered and blocked behind the nameless-artist
  decision.** **Next action is the owner's.** **Before that rescale is pre-registered, read
  [`superpowers/findings/2026-07-25-router-ascent-gradient.md`](superpowers/findings/2026-07-25-router-ascent-gradient.md)** —
  a re-reading of the P8b review's ascent measurement, which argues the rescale is likelier to
  yield *better-chosen famous* artists than *more obscure* ones, and names two re-reads of
  already-committed runs that would settle it without a rebuild. It owns no figures and
  proposes no arm. The defect record and diagnosis below remain essential reading before touching
  Phase 1 — the three conflated quantities it warns about are still live hazards.
  **Read [`superpowers/2026-07-22-phase1-execution-log-and-graph-defect.md`](superpowers/2026-07-22-phase1-execution-log-and-graph-defect.md)
  §2 before any Phase 1 work — and read §2.12 first, because it retracts a central claim of
  §2.9 and corrects §2.10 and §2.11.**
  - **What is settled.** Two real defects in the **adopted-at-the-time** artifact (figures
    owned by the sections cited, not restated here): the most popular artists were among
    the least connected, caused by an MBID tie-break over p99-clipped scores (§2.8); and
    famous→obscure edges were depleted well below a structure-preserving null, caused by
    the mutual-k-NN reciprocity rule (§2.10). Routed paths never present an artist below a
    very high popularity percentile, at any bypass depth, on either graph tested (§2.9). ⚠ **§2.11's and §2.12's tables were measured on `graph-t15-capfix.bin`, not the adopted artifact** — see `superpowers/findings/2026-07-25-consulting-derivations.md` `DRV-1`, **narrowed 2026-07-26; it previously named §2.9–§2.12 and that was over-broad.** The conclusions are argued to survive (`DRV-2`); **those two sections'** individual figures must not be quoted as facts about the running app until re-measured. **§2.10 is NOT exposed, and its reciprocity attribution above stands for the graph in use:** its "§2.8's tie-break is not this" table measures the adopted artifact's own topology — the adoption record states the fix's topology is that of §2.8 Arm 2 and of `rankfix`, §2.13 C2 records those as identical, and the two manifest sidecars agree on node and edge counts while differing only in `similarity_rescale` — and §2.10's figures ride on a **fixed pre-cap popularity reference** rather than on emitted scores, so they do not turn on which rescale the artifact carries. **§2.9 needs no provenance caveat to be barred from quotation:** §2.10 supersedes its figures and §2.12 retracts its central claim, so it is already barred on its own terms.
  - **What it is NOT.** Not primarily a graph problem. Obscure artists are a small, fixed
    number of hops from every famous artist tested, and even the most-connected famous
    artists have a handful of admissible `known` substitutes (exact counts: §2.12).
    **§2.12 concludes this is a cost-function problem** — the router prices the exits
    correctly and declines them.
  - **Three quantities that are not interchangeable** and have each caused an error here:
    **degree ≠ fame** (§2.6), **popularity ≠ fame** at the top of the distribution (§2.11),
    and **raw popularity ≠ percentile** (§2.12). Do not trust any
    `top1pct_degree_frac` (named `hubfrac` before 2026-07-23) or payload figure,
    and check which currency a claim is in before acting on it.
  - **Decided 2026-07-23:** the owner chose **repair + retune** — governing design
    `superpowers/specs/2026-07-23-defect-remediation-and-cost-retune-design.md`; its §1
    also records the nine-names verdict (in-graph popularity ≠ fame at the top,
    confirmed) and the owner's relaxation of the listening-test prohibition.
  - **Track 1 is DONE, 2026-07-23: the §2.8 tie-break is fixed and the rebuilt artifact
    is adopted.** The app now routes on `graph-t15-tiebreakfix.bin` — identity and
    verification in `superpowers/findings/2026-07-23-tiebreak-fix-adoption.md` (the
    authoritative record for the adopted artifact's checksum). Radiohead is back;
    famous-artist neighbourhoods are score-ranked, not MBID-ranked.
  - **Track 2 is COMPLETE and returned R0, a full null, 2026-07-24.** All 15 arms scored over
    two stages; the best reached −0.177 against a −1.0 threshold. **Nothing adopted, no shipped
    code changed, no blind listen run.** Firmest finding, and it needs no fame measurement: the
    **floor device is never pivotal** — the two floor arms are byte-identical to their base
    across every scored cell while the floor term fires 3× more often than production's, so the
    only depth-graduated device in the cost function cannot do its job. **R0 does NOT license
    "the repricing family is exhausted"** — A17(c)'s falsifier fired. Figures are owned by
    `builder/analysis/2026-07-24-track2-arm-scorer/`; the execution log narrates them.
- **Gate 1 (personal use).** Phase 2 (path quality) is **COMPLETE**, 2026-07-22. All 16
  tasks executed on branch `phase2-path-quality`. The blind listening test was run and the
  owner adopted the **`capfix`** arm — `cap_strategy="mutual_knn"`,
  `similarity_rescale="p99_log_clip"`, `similarity_damping=0.0`. See execution log §16
  (verdict) and §17 (adoption). **That adoption is not overturned** — but §2 of the Phase 1
  log is information that was not available when it was made.
- **Phase 1 resumed 2026-07-23; Track 1 is done and adopted, and Track 2 has now run and
  returned a null.** C3
  (`w_floor` is a no-op; `known` degrades to a bare hard exclusion — a *pathfinding*
  defect rather than the UX item it was filed as) is now folded into Track 2's
  cost-function retune, per
  `superpowers/specs/2026-07-23-defect-remediation-and-cost-retune-design.md` §4. Current
  sequencing: see that spec and
  `superpowers/2026-07-23-repair-and-retune-execution-log.md`. **Clips (C1, C2) and the four
  frontend UX items are DONE and CLOSED 2026-07-25** — built in PR #19, completed in PR #20,
  and **confirmed in use**. ⚠ **C1 is nonetheless not fully closed:**
  [`superpowers/findings/2026-07-25-bypass-depth-use-run.md`](superpowers/findings/2026-07-25-bypass-depth-use-run.md)
  `BYP-13` records a card that played a clip by a *different artist of the same name*, found in
  use on 2026-07-25. C1 was closed on a worked example where a band name collided with a song
  title; this is an **artist-name collision aggravated by a rename**, a different route to the
  same failure, and it is live. Not path-quality work, not paused. Record:
  `superpowers/2026-07-25-gate1-clips-and-ux-execution-log.md` (§15–§19 carry the clip
  closure and two playback defects found in that use). **F1 — zero-intermediary paths — is
  the remaining Gate 1 item**: the owner has decided every journey needs at least one stop.
  Implementation has landed, on branch `f1-min-stop` — design
  `superpowers/specs/2026-07-25-f1-minimum-stop-design.md`, plan
  `superpowers/plans/2026-07-25-f1-minimum-stop.md` — and it is not inside the path-quality
  pause (§16). Not yet closed: closure needs the queued use-the-app check (plan Task 6),
  which only the owner can run.
  Two items carry in from Phase 2 with success conditions; see the roadmap's Phase 1 section.
- **Two Phase 2 predictions did not survive measurement**, and older prose still asserts
  them. Damping ("C4") was tested at 0.25 / 0.5 / 0.75 and **rejected** — the undamped arm
  won. Neighbour-set Jaccard was **not** adopted as the primary objective; the
  overlap-family metrics sign-flip between slices at these effect sizes (adjudication §6
  claims 41–42). The p99 ceiling defect **survives adoption** and is carried to Phase 1.
- `superpowers/plans/2026-07-22-phase2-revised-plan.md` governed Tasks 14–16 and supersedes
  the remaining tasks of the 2026-07-21 plan. Both are now historical.

---

## Every document, by role

### Authoritative

| Document | Covers |
|---|---|
| `superpowers/NEXT.md` | **The next action, and the only document that states it.** Gate state, what is closed and must not be re-planned, what is waiting on the owner, and the path-quality pause. `CLAUDE.md`'s orient table points here rather than restating it — that row went three tracks stale as a summary, 2026-07-26, which is why status now follows the same one-document rule as figures. Rewritten wholesale at every `closeout`; owns no figures. **Where a handoff or an execution log disagrees with it about status, the fresher of those two wins and this document is stale** — fix it rather than working around it. |
| `superpowers/findings/2026-07-21-scoring-adjudication.md` | **All** scoring, hub-seeking, and path-quality figures. The single quantitative record. |
| `superpowers/PRODUCT-REQUIREMENTS.md` | **The owner's product requirements — Must / Should / Expect, 2026-07-29.** The requirements layer above `WHAT-GOOD-LOOKS-LIKE.md`: **where the two disagree, this governs**, and its §10 lists every known disagreement (value 2's lengthening clause superseded as a documentation conflation; value 5 promoted to a Must). Identifiers **`REQ-`**; open questions **`REQ-Q`** (one is live: the currency of discovery payload, `REQ-Q1`). Owns no figures and sets no thresholds; its Expects are calibration and must never be read as targets. Carries the operational chain (novelty is reached through obscurity, fame currency) and the recorded structural conflict between REQ-37 and `DD-F1`. |
| `superpowers/WHAT-GOOD-LOOKS-LIKE.md` | **⚠ Partially superseded 2026-07-29 by `superpowers/PRODUCT-REQUIREMENTS.md` — where they disagree, that document governs; its §10 lists the disagreements. This file remains the calibration record (provenance, worked cases, protocol).** **What the owner means by a better path.** Calibration for the blind listening test — this project's strongest evidence class, which decided the graph twice where the offline metrics decided it zero times. Read before interpreting any listening verdict, **and before designing anything that scores a path**: it holds no thresholds and must not be mined for any, but a criterion that contradicts a value in it is wrong (worked example: the Track 2 pre-registration's C4). **Amended 2026-07-27 at the owner's instruction**, after the reference product it invokes was read for the first time: value 9's endpoint-tracking clause is **verified** and its caveat lifted; value 2's "Reference product: boilthefrog" claim is **struck in place** — that product's bypass does not do what value 2 describes — and a short table now delineates what is shared with boilthefrog, what is new here, and what is a deliberate deviation. **The values themselves are unchanged**; what was withdrawn is an appeal to precedent. Read that table before citing boilthefrog in either direction. |
| `superpowers/findings/2026-07-22-phase2-sweep-results.md` | Every figure from the Phase 2 six-arm sweep. Owns its numbers; linked from the adjudication's §6 (claims 41–45). |
| `superpowers/plans/2026-07-21-alpha-rollout-roadmap.md` | Gate structure and phase ordering. Its C4 is a pointer only — it holds no figures. |
| `superpowers/findings/2026-07-23-tiebreak-fix-adoption.md` | Identity (sha256) and verification record of the **adopted** 75k artifact, post tie-break fix. |
| `superpowers/findings/2026-07-22-configuration-model-null.md` | Authoritative for its own figures — the exact configuration-model null referenced by Phase 1 log §2.10/§7.1 item 1. |
| `superpowers/findings/2026-07-23-track2-protocol-analyst-review.md` | **AUTHORITATIVE for its own §2 measurements; ADVISORY on the protocol.** The `ml-graph-analyst` review of the Track 2 pre-registration, discharging its prerequisite **P8** and the design's §4.6 review gate. Verifies the pre-registration's §0 resolution table **in code**, and raises numbered protocol observations (O-series) and recommendations (PR-series) that the Track 2 session must read before running any arm. Scope is the **protocol only** — the harness is a separate review (P8b). Remit is derivation, not judgement: it says nothing about whether the experiment is worth running. Measurements: `builder/analysis/2026-07-23-track2-protocol-review/`. |
| `superpowers/findings/2026-07-23-a0-gate-analyst-review.md` | **AUTHORITATIVE for its own measurements; ADVISORY on the protocol.** The `ml-graph-analyst` review of the step-4 A0-vs-P result. Establishes that the raw floor's *value* is arm-invariant but its *effect* is not (a first-path change alters who gets bypassed, and that carries to depth); that the unconditional dead-bound is **7** `known` bypasses, not 6; that the d0–d2 floor asymmetry between a diving arm and production is real and large; and it corrects the claim-23 wording before it enters the adjudication. Figures: `builder/analysis/2026-07-23-track2-a0-review/`. |
| `../builder/analysis/2026-07-24-track2-p8b-harness-review/` | **AUTHORITATIVE for its own measurements; ADVISORY on the harness.** The `ml-graph-analyst` review of the Track 2 **Stage A scorer**, discharging pre-registration prerequisite **P8b** — the companion to the P8 protocol review above, and the gate that stood between the harness and the arms. Verdict: fit to run stage 1, not fit to run stage 2 as it then stood. Fourteen findings, two HIGH. **Owns** the blank-name and duplicate-name figures, the floor-lifetime depths, and the measurement showing the `X` arm does not bound what §1.4 claims. **Not a `findings/` document, deliberately** — a second copy would restate its figures, which the rule above forbids; cite this directory. Its consequences live in pre-registration **A17–A19**. |
| `../builder/analysis/2026-07-25-track2f-toll-ladder/` | **AUTHORITATIVE for its own measurements.** The Track 2F dose-response ladder on the similarity-ceiling toll, run 2026-07-25 after Track 2's A17(b) established that the toll had only ever run at half its intended magnitude. **Owns** the ladder's `C1`–`C4` figures, the isolating contrasts, and **`TF-D1`, the ceiling-hop usage measurement** — the finding that production takes 85 % of its hops on ceiling edges and that no price drives that below 28.86 %, because those hops are structurally forced. Result **`TFR0`**, a null on the primary criterion at `M*` = −0.198; **nothing adopted**. Unlike Track 2's corner arm, **its bound holds** (`TFR4`), so the null may be stated at mechanism strength. **Not a `findings/` document, deliberately** — a second copy would restate its figures. Cite this directory. |
| `../builder/analysis/2026-07-25-ceiling-ordering-headroom/` | **AUTHORITATIVE for its own measurements.** How much edge ordering the p99 clip destroys, measured read-only from the archive on 2026-07-25 — the untested half of the ceiling hypothesis that Track 2F's toll structurally could not reach. **Decision rule committed before the measurement ran** (`0e39522`); verdict **WIDE**, median erased cost spread **10.01 × `w_hop`** against a 1 × threshold, and **100 % of the pre-registered endpoints above 7.5 ×** — a magnitude Track 2F proved changes routing. Carries an **identity gate**: the in-memory rebuild reproduces the adopted artifact's sha256, so the figures are about the graph in use. **Owns** the mechanism statement — at a fully saturated node the similarity term contributes nothing, so the router discriminates on popularity alone. Licenses *pre-registering* the builder-side rescale; **not** adoption. Cite this directory. |
| `../builder/analysis/2026-07-28-asc5-path-ascent/` | **AUTHORITATIVE for its own measurements.** The ASC-5 discharge, run 2026-07-28 after the owner unpaused path-quality work: the path-level ascent re-read and the `X`-vs-`A7` isolating contrast (`PLA-` series; decision rules committed at `8562d15` before the run). **Owns** the geodesic-null comparison showing famous-pair first-path interiors are structurally forced, the flat bypass-depth popularity profile, and the one-hop `A7` extension. Constrains how the parked p99 rescale may be pre-registered. **Not a `findings/` document, deliberately** — a second copy would restate its figures. Cite this directory. |
| `superpowers/findings/2026-07-25-mutual-knn-stranding.md` | **AUTHORITATIVE for its own measurements. Records a mechanism; proposes nothing.** Why recognisable artists (Meat Loaf, Elbow, The Cult, The Streets, Nada Surf, Pretenders) hold one or a handful of connections: `mutual_knn` requires both artists to rank each other in their top 50, so an artist whose nearest neighbours are all more listened-to than it loses every edge in both directions. **Owns** the reciprocity figures (`MKS-3`: 43.7 % mean survival, 9.3 % of artists left with ≤1 edge), the degree-by-popularity table (`MKS-4`), and the no-detour pair counts (`MKS-6`: 6,599 total, 203 non-leaf) that make **F1 unsatisfiable for those pairs on this artifact**. **Distinct from the tie-break fix** (`MKS-5`), which repaired the very top and left this band untouched. **The mechanism itself is NOT new — cite Phase 1 log §2.10, which isolates the reciprocity rule by factorial and owns those figures; this document's `MKS-5a` records that an earlier draft wrongly claimed otherwise.** What is new is the per-artist view (§2.10 measures a distribution, not who is affected) and the F1 consequence. Explicitly **not** evidence that a looser rule would be better — `mutual_knn` won a blind listen, and §2.10's coupling (dropping reciprocity restores unbounded degree) binds any redesign (`MKS-5b`). ⚠ **Read "`mutual_knn` won a blind listen" in the light of `findings/2026-07-26-low-degree-synthesis.md` `SYN-6` (added 2026-07-26):** neither blind listen varied the both-ways rule against a bounded-degree alternative, so what the ear endorsed is the **degree bound**, not the reciprocity rule. `MKS-5b`'s force is unchanged; only its currency is. Nothing adopted; the path-quality pause is intact. Measurements: `builder/analysis/2026-07-25-mutual-knn-stranding/`. |
| `superpowers/findings/2026-07-26-low-degree-census.md` | **AUTHORITATIVE for its own interpretation; OWNS NO FIGURES** — every quantity belongs to `../builder/analysis/2026-07-26-low-degree-census/`, whose `REPORT.md` is the deliverable. **Cite that directory for any number.** Which artists can never appear mid-journey: an artist with one connection can only ever be one of the two the user typed, so the app can never introduce anyone to it (`DRV-4` stated this as structural and unmeasured; this measures it). **Closes three open questions**: `DRV-4`'s denominator (the two populations are **nested, not alternatives** — `pipeline.py:131,184` make every artifact node a crawled artist, and the fractions differ immaterially); that the count is post-largest-component-prune (confirmed observationally, not argued); and that non-artist entities are excluded (checked, not assumed — the nameless nodes are the one contaminant). Fame is the **adopted proxy unchanged** (§5 + A11 + A15, imported not reimplemented); **graph popularity ranks nothing** (§2.11) and was used only to screen who to ask — sound because popularity is accumulated *before* the mutual-kNN cap, so the reciprocity rule destroys degree and leaves popularity untouched. **Does not establish** that the lists are the true top of all low-degree artists: the cut was deliberately not deepened (owner's call), and a famous artist with low in-graph popularity is carried as a **candidate crawl-coverage gap**, not filed as instrument noise. Carries **`CNS-1`** in §3, recorded in passing per the `MKS-7` precedent: an artist can be unfindable under the name users know it by, because the graph stores one name and no aliases (Pretenders / The Pretenders). Nothing rebuilt, adopted or proposed; the pause is intact. |
| `superpowers/findings/2026-07-26-stranding-causes.md` | **AUTHORITATIVE for its own interpretation; OWNS NO FIGURES** — every quantity belongs to `../builder/analysis/2026-07-26-stranding-causes/`, whose `REPORT.md` is the deliverable. **Cite that directory for any number.** Separates *why* the degree-1/degree-2 artists are low-degree, which the census (above) counted but did not explain. Identifiers **`STC-`**. **`STC-1`: the crawl-frontier cause, as usually phrased, is nearly empty** — the dominant cause is that the source named almost no similar artists for them at all, and `STC-2` records that practically everything it did name had already been crawled. **`STC-3` is the reading rule**: the set is dominated by unfixable artists *by headcount* and by rule-fixable ones *by who anyone would recognise*, the two are almost perfectly stratified by popularity, and quoting either alone misleads. **`STC-4` is the strongest thing cutting against a cap-rule change** — most of the set is named by four artists or fewer, so even the most permissive rule reaches few of them, and the ones it does reach are all at the popular end. **`STC-6` corrects a claim an earlier draft of this document made**: SOURCE-THIN means "nothing above the co-occurrence threshold **we chose**" (`BuilderConfig.algorithm`), not "the source knows nothing" — so a lower-threshold re-crawl is inside project control, with an unmeasured payoff; the length limit was checked and provably cannot cause short lists. ⚠ **`STC-6`'s remedy is KILLED as of 2026-07-29** — the `algorithm` parameter is a closed enum and production already sits at the lowest available threshold, so there is no lower threshold to re-crawl at; the document carries a correction banner at its §6 and `superpowers/2026-07-29-algorithm-selection-execution-log.md` §9 owns the closure. **`STC-6`'s underlying distinction still stands** — only its remedy is dead. **`STC-5` records a limitation in `reciprocity.py`** (it does not model the builder's special-purpose exclusion), corrected here, **not invalidating** `findings/2026-07-25-mutual-knn-stranding.md`. The boundary between the populations is the session's stated choice and the report prints eleven alternatives. Nothing rebuilt, adopted or proposed; the pause is intact. |
| `superpowers/findings/2026-07-27-boilthefrog-source-review.md` | **AUTHORITATIVE for its own interpretation; OWNS NO FIGURES** — every quantity belongs to `../builder/analysis/2026-07-27-boilthefrog-reconstruction/`, whose `REPORT.md` is the deliverable. **Cite that directory for any number.** A source review of **BoilTheFrog** (`plamere/BoilTheFrog` at `1f2cb60`), the product named as the reference in `WHAT-GOOD-LOOKS-LIKE.md` values 2 and 9 and never previously read. Identifiers **`BTF-`**. **`BTF-2` lifts value 9's standing "not independently verified" caveat on its first clause** — endpoint-tracking is confirmed from the author's own design text, the code, and measurement — and records that the author's stated *purpose* for the shared `w_jump`-shaped term was to **suppress** obscurity ("back alleys that no music fan should dare to tread"). **`BTF-3` is the finding that cuts hardest against this project's own record: value 2's progressive-bypass clause is NOT exhibited by the reference product it names** — BoilTheFrog's bypass is single-signal, node-local, relaxes no popularity term, and reaches a fixed point within a few presses. That does not make value 2 wrong (it is preference, and self-flagged as gut instinct) but it means artistpath's two-signal bypass is **novel, not a re-implementation**. **`BTF-1`** is the structural half of value 9 nobody had: the famous artists are **near-leaves** (Ed Sheeran, degree 4), so the router cannot pass through them. **`BTF-4` explicitly does NOT answer `SYN-6` or weaken `MKS-5b`** — BoilTheFrog is reciprocity-free *and* degree-bounded, but the factor table shows its source list is capped at 20 by Spotify, so the unbounded-degree regime cannot arise there at all; what survives is a **third option absent from the resume brief and `SYN-1`**: change what the cap *selects* (popularity proximity vs similarity rank), measured to halve max degree and cut stranding at a fixed cap. ⚠ **Do not pick that option up off this row.** It is a **graph** intervention, so the resume brief's §2 blocker binds it exactly as it binds the degree floor — **if the router prices these artists out, changing what the cap selects changes nothing** — and it is a rebuild, which is the owner's call and currently blocked behind the nameless-artist decision. It was measured on a **different graph from a different source**, and is recorded as a named option with an unmeasured payoff, not a candidate. **`BTF-5` corrects an expectation** — the reference strands artists at a broadly comparable rate; only the *distribution* differs. `BTF-6`/`BTF-7` are outside path work: `BYP-13`'s defect class is structurally absent there because graph and clips share one identifier space, and `de_norm` is a ready-made, search-side, rebuild-free answer to `CNS-1`. **`BTF-8`** records the reference's own headline mechanism (track energy smoothing) as **never called in any shipped generation** — the `FMS-P1` class in a six-year-old public product — and that this document reproduced the class in its own factor table. Nothing adopted, proposed or pre-registered; the pause is intact. |
| `superpowers/findings/2026-07-30-lb-algorithm-semantics.md` | **AUTHORITATIVE for the semantics of the source `algorithm` string's parameters** — read from ListenBrainz's own Spark job (`listenbrainz_spark/similarity/artist.py`, quoted verbatim), which outranks the 2024 staff forum comment that prompted it and corrects that comment twice (`session` is 300 **seconds** not minutes; the per-pair cap is `contribution`, per user, not a per-session 50). Identifiers **`LBS-`**. **Owns one measurement series**: the candidate-list-length table (`builder/analysis/2026-07-30-lb-source-semantics/`) showing `ALG-C`'s `limit_50` lists still reach 100 entries — reconciled by the source's own "instructive only, upto 2x" clause, so **the k-sweep ceiling of 100 and `NEXT.md`'s closed list-length item both stand**. `LBS-4` is a **labelled inference, not evidence**: lowering `contribution` 5→3 raises the minimum distinct co-listeners for a pair from 3 to 4, a supply cut aimed at the obscure end — the mechanism-shaped hole in `RC-R1`. `LBS-5` supersedes every earlier paraphrase of these parameters; the `filter` token is left open deliberately (absent from current master, and it cannot bear on the `ALG-B` decision). **The Track B design cites this rather than re-deriving it.** Changes interpretation only; enum closure, `MKS-5b`, and the parked adoption untouched. |
| `superpowers/findings/2026-07-30-track-b-cap-selection-results.md` | **AUTHORITATIVE for Track B's results** — the four-part report (measured / inferred in plain language / weakest link / options) over the 24-cell cap-rule grid. **Raw record: `../builder/analysis/2026-07-30-track-b-cap-selection/cb_scores.json`** (committed before any read); cite one of those two for any Track B number. Headline verdicts under the pre-registered bars: the k-curve is null on `ALG-E` and decisive on `ALG-B` (`R0`); the incumbent loses at matched bound 50 to `trimmed_union` on coverage criteria with no hub flag (`R1`); reciprocity alone is nearly free at k = 100 (`R1a`, null); `R2` is the pre-committed router null on `ALG-E` (quota edges verified present at famous nodes and declined at production weights — **not** "the quota rule cannot work") and decisive nonzeros on `ALG-B` including under the production rule; the survival tilt is directionally as predicted and immaterial (`R3`); un-boundedness is priced (`R4`). **Its weakest link is stated in the document**: path criteria rest on famous pairs only after the `CRS-G3` attrition. Licenses a recommendation input only; `REQ-38`'s blind listen owed before any adoption. |
| `../CLAUDE.md` | How to work in this repo: commands, architecture, conventions. |

### Active

| Document | Covers |
|---|---|
| `superpowers/2026-07-30-HANDOFF-track-b-runs.md` | **⚠ SUPERSEDED 2026-07-30 on next actions by `2026-07-30-HANDOFF-fame-proxy-coverage.md` (row below)** — *this row still claimed CURRENT until the coherence-probe closeout caught it; the file's own role line was already correct.* Still the record of Track B's completion seam: all 24 cells built and scored, raw outputs committed before any read, six reads written in prereg order, report committed, nothing in flight. Supersedes the Track B design handoff below **on next actions**. Names the next state (**nothing queued for a working session; the next action is the owner's** — the parked cap-rule and `ALG-B` decisions now have their pre-registered inputs) and the claims an editor must not revert: the two pre-committed nulls (`R1a`; `R2`-on-`ALG-E` as a *router* finding), `PS100` ≡ `MK100` byte-identical, the famous-pair-only scope of the path evidence, the closed no-redraw decision on the `CRS-G3` attrition, and the measured tie-domination. Carries what is otherwise unwritten: the gitignored raw path walks, the companion's max-endpoint resolution in count currency, and runtimes. |
| `superpowers/2026-07-30-track-b-runs-execution-log.md` | **The retained execution log for Track B's runs and reads (`CB-5`/`CB-6`). ACTIVE.** Owns the run-phase decisions and the six reads' verdict statements with their run-state sentences; **figures are cited from `cb_scores.json` and the results note, never restated as findings here.** Records: the gates re-run green and red at the scoring commit; the `CRS-A2` baseline protocol resolving to zero in both famous classes (no blind bar amendment needed); **the pair-set attrition decision** (82 → 39; five `× lower` classes unreadable per `CRS-G3`; no redraw, with the reasoning); the `CRS-H1` tie operationalisation fixed before any cell's ties were computed, with the trim instrumentation validated per cell against the built artifacts; the direct verification that quota cells hold their reserved sub-decile edges at famous nodes; and the exposure map that closed the reads with nothing escalated. |
| `superpowers/2026-07-30-HANDOFF-coherence-tag-probe.md` | **ACTIVE — the CURRENT handoff, 2026-07-30 (latest).** Written at the coherence tag probe's seam: the kill gate fired, step 2's pre-registered retrodiction was **never run and stays unconsumed**, nothing in flight. Supersedes the fame-proxy-coverage handoff below **on next actions only**. Carries the claims an editor must not revert: the unexecuted `ct_retrodict.py` is the *outcome*; the kill is decisive and vocabulary-robust; **`COH-2` and `COH-3` travel together** the way `FPC-2`/`FPC-9` do; `COH-5` retires the 21-hour MB tag-collection cost assumption. |
| `superpowers/2026-07-30-coherence-tag-probe-execution-log.md` | **RETAINED EXECUTION LOG** for the coherence tag probe. **Owns no figures** (they live in the findings row below). Records: the gate outcome and the five-for-five confirmed predictions, the instrument decision (genre union, not raw tags) and its post-hoc vindication, the concurrent-collector glimpse and its handling, the two decided-against moves (running the retrodiction "descriptively" after the kill; re-gating on the `COH-3` population after seeing it), and the run-cost measurements. |
| `superpowers/findings/2026-07-30-coherence-tag-probe.md` | **ACTIVE — the coherence tag probe's findings. Identifiers `COH-`. Owns its figures** (instrument-coverage tables; probes in `builder/analysis/2026-07-30-coherence-tag-probe/`). The kill gate fired at band level; `COH-3` is the recorded counterpoint on delivered routes; `COH-4` is the committed-but-unrun retrodiction; `COH-5` the LB-transport result; `COH-6` the post-hoc vocabulary-robustness read. **Descriptive in the `CS-P0` sense — nothing adopted, no criterion fixed, neither open owner decision settled.** |
| `superpowers/2026-07-30-HANDOFF-fame-proxy-coverage.md` | **⚠ SUPERSEDED 2026-07-30 on next actions by `2026-07-30-HANDOFF-coherence-tag-probe.md` (row above)** — its §7 coherence thread's instrument half has now been probed and killed; its structural half stays parked. Supersedes `2026-07-30-HANDOFF-track-b-runs.md` **on next actions only**; Track B's own record is untouched and remains accurate. Names the two open owner decisions **with the retiring session's position on each** rather than as a menu (fix the instrument *before* the graph work, on `FPC-9`'s sequencing argument; leave prior fame-scored results standing, fixed in advance of any recompute), the claims a well-meaning editor must not revert (three REFUTED predictions recorded as refuted; `FPC-2` and `FPC-9` must be quoted together), and what is not otherwise in the durable record. |
| `superpowers/2026-07-30-fame-proxy-coverage-execution-log.md` | **RETAINED EXECUTION LOG** for the fame-proxy probes. **Owns no figures.** Its **§7 is the largest thing NOT done and the reason to read it**: the coherence thread — that the past failure of coherence metrics was *misdiagnosed* (Adamic–Adar and overlap coefficient are both topological, auditing the graph they were derived from, while the owner's §3.9 verdict notes are genre/era/scene judgments), and that the cost function assumes coherence is **additive along edges** while `PRODUCT-REQUIREMENTS`'s own second clause — "the whole path reads as one journey" — is not expressible as an edge sum. Names MusicBrainz tags as the untried independent-currency instrument **with the warning that its coverage thins in the same tail**. §2 tabulates seven pre-committed predictions and their outcomes; §8 records what was decided against. Neither half of §7 is a proposal; both are the owner's trigger. |
| `superpowers/findings/2026-07-30-fame-proxy-coverage.md` | **ACTIVE — descriptive scope probes on what the FAME instruments can see.** Identifiers **`FPC-`**. **Owns one measurement series**: the coverage and agreement tables (`builder/analysis/2026-07-30-fame-proxy-coverage/`). **Nothing is adopted, no criterion is fixed, no currency changed, and Track B is untouched**; a pre-registration is owed before any criterion is built on any instrument here. **Read its §0 first — it leads with a correction of the case that opened the work**: the Wikipedia floor is **not** actively corrupting fame-currency measurements. `FPC-2` measures floor reach among routed interiors *falling* with bypass depth (5.3%→2.4% distinct, d0→d20) because the router never descends into the blind region, and `FPC-1` finds the instrument correct 94.4% of the time — so **no Track 2 or Track 3 verdict changes**. What survives is `FPC-3`: **55.0% of the artifact carries no English article**, a **latent** blindness that binds on the population and would begin binding when the product succeeds at what `REQ-37`/`DD-F1` demand. `FPC-4`: ListenBrainz `POST /1/popularity/artist` covers **99.94%** of the artifact and 99.9% of the bottom band, MBID-keyed — the "drop-in improvement" the alpha design §4.1 named in advance. **Two of three pre-committed predictions were REFUTED and are recorded as such** (`FPC-2`, `FPC-5`). Standing hazard restated: LB and English Wikipedia share a population blind spot, so **their agreement is not corroboration**. |
| `superpowers/specs/2026-07-30-track-b-cap-selection-preregistration.md` | **⚠ EXECUTED 2026-07-30 — all 24 cells scored and all six reads written; read `findings/2026-07-30-track-b-cap-selection-results.md` beside it, which is fresher on outcomes.** It remains authoritative for *criteria, bars, gates and read order*, and its §8 amendments were all honoured as written (`CRS-A2`'s baseline measured zero in both famous classes, so no further §8 entry was needed). *(Original role: **ACTIVE pre-registration — Track B, cap-rule selection at production scale.** Identifiers **`CRS-`**, collision-checked. Committed before any comparative cell is scored; `CB-1`–`CB-3`'s instrument gates (green **and** red halves) and the design probe `CB-P1` ran first and are recorded in its §0. **Its §0 carries the per-family `DD-F1` bearing table** — fixed from `CS-P0c`'s own text plus `CB-P1`'s two-archive scan, the consultant items 1–2 validated: weakest-first trim provably deletes every reverse-only famous→obscure edge (checked in all 10 rows), so **`banded_quota` is the only selectable configuration bearing on `DD-F1`**, and a `CRS-C5` null elsewhere is confirmation, not a finding. 24 cells over three families × two archives + a barred `uncapped` reference row; matched-bound rule for cross-family reads; `CRS-C6` pre-registers the consultant's survival-split tilt with direction fixed by `LBS-4`. Resolves `PLA-R1` from its grounds (presence, not fame-of-interior). **Licenses a recommendation input only** — no adoption, no default change, no weight retune; `REQ-38`'s blind listen owed regardless.)* |
| `superpowers/specs/2026-07-30-tag-discrimination-probe-preregistration.md` | **ACTIVE pre-registration — the tag discrimination probe.** Identifiers **`TAS-`** (criteria), **`TAS-AM`** (amendments, deliberately distinct so an amendment can never be misread as a criterion). Committed before any measurement; **owns its figures** (`builder/analysis/2026-07-30-tag-discrimination/`). Asks the one question both candidate architectures — build-time tag-aware selection and router-side tag pricing — rest on: **does genre agreement discriminate between the candidates an artist already has**, or is it the similarity score wearing a different hat. **Descriptive: adopts nothing, fixes no criterion, changes no weight, default or currency, and requires no rebuild.** Explicitly **not** the router-side pricing track's pre-registration, which still owes its own consuming `TB-P5H-7`. **Read §8 before acting on any criterion:** `TAS-AM1` withdraws `TAS-4`'s original bar as **false** (per-artist swaps understate map change ~2.11×; the median reads 0 while 6% of the map moves) and replaces it with edge turnover ≤ 1%, the owner's materiality line; `TAS-AM2` fixes the substrate per criterion — selection-side on the pre-cap capture, map-side on the adopted artifact, **never compared across**. `TAS-1` is a **narrow** gate that deliberately does **not** inherit `COH-2`'s coverage bar: that bar governed a *sensor*, this is an *actuator*, and silence where labels are missing is safe in a way blindness is not. |
| `superpowers/plans/2026-07-30-tag-discrimination-probe.md` | **ACTIVE — Tasks 1–3 executed 2026-07-30, checkboxes annotated in place; Tasks 4–8 unrun.** The pre-registration above **governs wherever the two disagree** — it has already overtaken this plan's Task 4 once via `TAS-AM1`. Eight tasks with handoff seams named at authoring time. **Its value beyond sequencing is the record of four defects found in the plan itself by verifying rather than trusting** — a neutral-value formula computing the wrong quantity, two artifact accessors that do not exist, and a Wikidata assumption that was wrong twice over (`fp_wikidata.json` holds item presence and no genres; `ct_wikidata_genres.json` holds counts, not labels, so P136 **labels were never persisted** and needed their own collector). All four were confident prose about correct code, and none would have raised an error. |
| `superpowers/2026-07-30-HANDOFF-tag-discrimination.md` | **ACTIVE — the CURRENT handoff.** Nothing supersedes it. Supersedes `2026-07-30-HANDOFF-coherence-tag-probe.md` **on next actions only** (that probe's own record is untouched and accurate). A **seam** handoff: Tasks 1–3 ended with both gate reads resolved, nothing in flight, tree clean. Carries what the next session must not revert (no arm at bound 100 — ranking is provably never consulted there; the neutral rule is per-artist, never zero, and is the one dormant term; a `TAS-5` null is weak evidence about tags specifically), **the open architecture decision with the retiring session's position on it** rather than as a menu, and what is not otherwise in the durable record — including that the ~20 MB captures live in a session scratchpad and must be regenerated rather than hunted for. |
| `superpowers/2026-07-30-tag-discrimination-execution-log.md` | **RETAINED EXECUTION LOG** for the `TAS-` probe. **Owns no figures.** Its **§2 is the reason to read it**: four defects found in the plan *itself* by verify-against-source steps, none of which would have raised an error — the project's characteristic failure, tabulated. §1 records how the owner's three interventions changed the design (and why the bound-100 vote was reversed on evidence). §4 records the corrections this session made to its own committed pre-registration, including the withdrawal of a bar as **false** before it ran. §8 records the standing-layer measurement **and** that its own first draft fabricated both figures. |
| `superpowers/plans/2026-07-30-graph-rebuild-track-b.md` | **⚠ EXECUTED 2026-07-30 through its last task — the checkboxes are annotated in place; read the two execution logs beside it for what ran.** It remains authoritative for **its §0 owner scope rulings, which outlive the plan** — cap-RULE selection not k-tuning; the degree bound as a scoping constraint, not a settled product conclusion; the uncapped row barred from selection. *(Original role: **ACTIVE — the governing plan for Track B, the cap-rule selection simulation** (`MKS-5b`'s owed simulation, the successor plan Track A's strand 3 deferred). Identifiers **`CB-`**. **Its §0 records the owner's 2026-07-30 scope rulings — read them before touching anything cap-adjacent:** the track is cap-**rule** selection, not k-tuning (mutual k-NN is one candidate family among three, on `SYN-6`/`CS-P0f`/`BTF-4` grounds); **the degree bound is a scoping constraint of this track, not a settled product conclusion** — re-evaluating bounded-degree (router-priced unbounded graphs) is an open future track, owner's trigger, owing its own blind listen; and the uncapped graph runs as a **descriptive reference row barred from selection**. Six tasks, seam named at `CB-4` (the pre-registration, its own document, committed before any comparative cell). Everything analysis-only: no shipped default moves, archives are read-only (`GRT-A1`), and adoption still owes `REQ-38`'s blind listen regardless of outcome. Consumes `RC-A2`, `LBS-3`/`LBS-4`, `GRT-P3`/`GRT-P4`, and resolves `PLA-R1` applicability in the prereg rather than assuming it.)* |
| `superpowers/2026-07-30-HANDOFF-track-b-design.md` | **⚠ SUPERSEDED 2026-07-30 on next actions** by `2026-07-30-HANDOFF-track-b-runs.md` (row above) — the `CB-5`/`CB-6` work it named ran to completion the same day, its three order-sensitive obligations all discharged as written. Everything it records about the design phase remains accurate and its do-not-revert list stands. *(Original role: **The CURRENT handoff, 2026-07-30 — written at Track B's own named seam (`CB-4`): pre-registration committed, every instrument gate green AND red, nothing in flight.)*** Supersedes the Track A handoff below **on next actions**. Names the next work (`CB-5` runs then `CB-6` reads, fresh session, prereg read cold) and the three order-sensitive obligations that bind it (scorer dry-run first; `ALG-E`-`MK50`'s `C5` before any other `C5`, per `CRS-A2`; reads in the prereg's own order). Carries the claims an editor must not revert — the `LBS` corrections to the forum comment, the retracted "limit is not list length" read, the checked trim derivation, `CS-P0c`'s true scope, the band-pool baseline correction — and what is otherwise unwritten: the gitignored `cb-cells/` artifacts with manifest sidecars, the deliberately-unread `cb_bound_check.json` numbers, `CRS-A5`'s endpoint claim being the consultant's report not this session's verification, and the `CB-3` gate's two-class slice being deliberate. |
| `superpowers/2026-07-30-track-b-design-execution-log.md` | **The retained execution log for Track B's design phase. COMPLETE for the phase it records** — the runs phase has its own log (row above). *(Read "owns no comparative figures — those do not exist yet" as of its writing; they exist now and belong to the results note.)* LBS semantics → plan → `CB-1`–`CB-3` instruments → `CB-P1` probe → `CRS` pre-registration with `A1`–`A5`, one session, retired at the seam. Owns the design-phase gate-outcome table and the operational timings; **owns no comparative figures** — those do not exist yet by design. Records: the owner's widening of the track with grounds; this session's own mid-stream retraction ("limit is not list length", reversed by the source's 2x clause before commit); both consultant rounds with every item's verification outcome; four defects in this session's own output including the two blocking prereg/harness mismatches and **its own first draft asserting D4/D6 numbers from memory before running the commands**; the `banded_quota` origin (both proposed trim keys provably fail); and what was decided against (proximity-trim cells, a `q` axis, a second weight set). |
| `superpowers/2026-07-29-HANDOFF-graph-rebuild-track-a.md` | **⚠ SUPERSEDED 2026-07-30 on next actions** by `2026-07-30-HANDOFF-track-b-design.md` (row above) — the Track B work it named was designed through its seam, **and the owner re-scoped it on the way: cap-RULE selection, not k-tuning**, so its description of Track B as a k-simulation is dated. Everything it records about Track A remains accurate. *(Original role, retained in full: **The CURRENT handoff, 2026-07-29 — written at a seam after the graph rebuild plan was written AND executed to its last task in one session.** Supersedes the reciprocity handoff below **on next actions**. Names the next work (**Track B, the cap-selection simulation under `MKS-5b`**, which must consume `GRT-P2` and `RC-A2`; the `ALG-B` re-crawl decision remains PARKED and is the owner's trigger), and the claims an editor must not revert: **`RC-P2` is confirmed on its mechanism and REFUTED on its consequence** (R.E.M. holds 6 connections under `ALG-B` against 47 in the matched control, but the build does **not** refuse); **`GRT-P2`** — the guard cannot see the collapse it was written for, because popularity *is* score-weighted in-degree, so a collapsing artist drops out of the top-25 sample the degree floor inspects (**not established at production scale**); **`GRT-P1`** — `GRT-C4`'s ratio bar is undefined against a zero baseline, so the pre-registered read returned "not decisive" everywhere and is left standing as written; the production archive is **not** closed under one-hop neighbours (`GRT-A1`); a full build takes **~29 s**, not ~2 minutes. Records what is otherwise unwritten: **`snyk_code_scan` never ran and is owed** on three diffs; the scorer defect caught by dry-running the cheap arm while the expensive one collected; the cross-validation against the calibration harness; and that the production checkpoint no longer exists, so `GRT-A1`'s mechanism is hypothesis while its gap is measured.)* — Two claims inside that original description are dated by later work and correct in the handoff itself: `GRT-P2` **was** subsequently established at production scale (`GRT-P4`), and the owed `snyk_code_scan` **was** run and is clean. |
| `superpowers/plans/2026-07-29-graph-rebuild-track-a.md` | **⚠ EXECUTED 2026-07-29 — read the execution log beside it, which is fresher on everything that ran.** It remains authoritative for *task structure, the factor table and the strand ordering*. Its `GR-5` left two design decisions explicitly open (the control arm's archive; the trial target) and **the pre-registration closed both** — read that document, not this one, for what was actually run. *(Original role: ACTIVE, the governing plan for Track A.)* Identifiers **`GR-`**. Six tasks: the nameless-artist drop rule (pulled forward as the first build blocker), `--algorithm` selection, algorithm-scoped archive keys (`RC-H3`), a verification rebuild, the pre-registration, and the trial build. Declares its own seam after `GR-6` and defers strand 3 (cap selection) to a successor plan, because `RC-A2` shows the cap and the algorithm interact rather than compose. |
| `superpowers/2026-07-29-graph-rebuild-track-a-execution-log.md` | **The retained execution log for graph rebuild Track A. ACTIVE.** Owns this track's build-shape figures and nothing else; scoring and path-quality figures stay in `findings/2026-07-21-scoring-adjudication.md`. Records the baseline refusal captured **before** the drop rule landed (33 nameless, build refused, nothing written), the owner's MusicBrainz check that upgraded the drop decision from preference to only-available-option (all three sampled MBIDs return "Artist not found", so backfilling was never possible), the **36-vs-33 reconciliation** (different populations, both correct, and 3 stranded neighbours pruned — the clause's first production evidence), `GR-G1` passing, and the full `GRT` trial-build result with all three gates. Carries three findings against its own work in fixed wording: **`GRT-A1`** (the production archive is not neighbour-closed; a structural guard caught a naive control arm), the **scorer defect** found by dry-running the control while the trial arm still collected, and the cross-validation that followed. Corrects the record on build cost (~29 s, not ~2 min). §8 records that **`snyk_code_scan` was never run and is owed**. |
| `superpowers/specs/2026-07-29-algb-trial-build-preregistration.md` | **ACTIVE pre-registration — the `ALG-B` trial build.** Identifiers **`GRT-`**, namespaced and collision-checked. Committed **before any arm ran**; harness committed before either script produced a result. Its **§0 records the scope check that changed the design**: `RC-A2` already diagnosed reciprocation by hand and re-checking one artist costs ~101 requests, so the single irreplaceable output is **`GRT-C4`, component membership (`RC-H1`)**, which sampling structurally cannot reach — and without it the document says it would be withdrawn. Two arms differing in exactly one column (`ALG-B` vs a coverage-matched `ALG-E` control); every comparison against the adopted artifact is **barred** as a two-knob confound. Explains why the reads bypass `cmd_build` (a capped graph cannot satisfy `node_count`, and there is deliberately no CLI escape hatch). ⚠ **AMENDED three times, §8, append-only:** `GRT-A1` (pre-run — overlay archive, after the read-only guard refuted the document's own coverage assumption), `GRT-P1` and `GRT-P2` (post-hoc). No clause above §8 was edited. Licenses no adoption and no re-crawl. |
| `superpowers/2026-07-29-HANDOFF-reciprocity.md` | **⚠ SUPERSEDED 2026-07-29 (latest) on next actions** by `2026-07-29-HANDOFF-graph-rebuild-track-a.md` (row above) — the plan it handed over was written **and executed**, so **its named next action is spent**. One claim it carries is now corrected rather than merely superseded: it states that `RC-P2` predicts a build refusal, and a real build shows **no refusal** for the reason `GRT-P2` gives. Everything else it records remains accurate. *(Original role: the CURRENT handoff, 2026-07-29 (later) — written at a seam after the reciprocity measurement ran and was read.** Supersedes the algorithm-selection handoff below **on next actions**. Names the next work (write the graph rebuild plan cold — **no experiment now gates it**), the instrument change an editor must not revert (**the `--target`-capped trial crawl is retired for `AS-H2`**: its readable core is structurally famous, so it cannot answer a question about obscure artists — retired for that question only, since it remains the sole route to component membership), the two items inside the plan now known to be harder (the nameless-drop rule **gates the plan's own first build**; the cap simulation **interacts with** any algorithm change), the claims not to revert (`ALG-B` strands obscure artists **and** must not be summarised as simply worse; the cause is supply not reciprocation; `RC-P3`'s mechanism is unsupported by this session's own data; `RC-P1`'s scorer defect biased toward the null and both readings are reported together; `RC-P2` predicts a build refusal), and what the session knew that is otherwise unwritten (full candidate lists stored so a different `k` needs no new requests; the two scorers re-derive degree independently; the corrected reading of `crawl.py`'s stopping rule). |
| `superpowers/2026-07-29-HANDOFF-algorithm-selection.md` | **⚠ SUPERSEDED 2026-07-29 (later) on next actions** by `2026-07-29-HANDOFF-reciprocity.md` (row above). **Its own named next action — `AS-H2`'s `--target`-capped trial crawl — is RETIRED as an instrument and must not be revived from this document**; the successor answered `AS-H2` by sampling instead and records why. Everything else it states remains accurate. *(Original role: the CURRENT handoff, 2026-07-29 — written at a seam after the algorithm-selection measurement ran and was read.)* Supersedes the requirements/Track 3b handoff below **on next actions**. Names the next work (write the graph rebuild plan cold; the agreed next experiment is `AS-H2`'s trial crawl, recommended and NOT run), **the re-sequencing an editor must not revert** (cap-selection simulation first → third, on `CS-P0c`), the two levers now known impossible (`limit` above 100; `threshold` below 10 — so `STC-6` is KILLED, not deferred), the claims not to revert (`AS-R1`/`AS-R5` did not fire; `ALG-B` is not unambiguously better; `AS-C1` is a lower bound), and what the session knew that is otherwise unwritten (the trial crawl's shape; the raw records being committed so re-scoring needs no new requests; the endpoint's real latency; `ALG-C`/`ALG-D` needing no re-measurement). |
| `superpowers/2026-07-29-HANDOFF-requirements-track3b.md` | **⚠ SUPERSEDED 2026-07-29 (later) on next actions** by `2026-07-29-HANDOFF-algorithm-selection.md` (row above) — that session ran and **re-sequenced the task order this handoff names: the cap-selection simulation moved from first to third**, because `CS-P0c` showed it cannot fix `DD-F1`. Everything it records about the requirements rewrite and Track 3b remains accurate, and its do-not-revert list stands. *(Original role: the CURRENT handoff, 2026-07-29 — written at a seam after the requirements rewrite and Track 3b both ran to completion.)* Supersedes the Track 3 handoff below **on next actions**. Names the next work (graph rebuild planning, owner's instruction, with entry points and the now-due nameless-drop rule), the parked decisions (candidate pool, recompute, listen — owner's trigger), which documents are now wrong and in which direction, the overturned claims an editor must not revert, and what the session knew that is otherwise unwritten (the recompute's cost; its position on the candidate pool; the mirror knobs staying 0.0). |
| `superpowers/2026-07-29-track3b-execution-log.md` | **The retained execution log for Track 3b. ACTIVE.** §1: TB-P1 discharged — 15 findings (4 HIGH), all amended into the pre-registration pre-arm, marked in place; the prereg's §9 is the index. Carries the review's headline in fixed wording: **the pre-run forecast says TB-R2 (shortening still dominates) is the likely outcome** — a designed outcome with a pre-committed reading, and the run proceeds. F4's 0/252 sub-decile-victims figure was independently re-verified by the dispatching session. |
| `superpowers/2026-07-29-algorithm-selection-execution-log.md` | **The retained execution log for algorithm selection. COMPLETE** for the measurement it records. All six arms clean (`AS-G1` 200/200 each); `AS-G2` passes. **`ALG-B` (`days_7500 · contribution_3`, one parameter from production) is the named re-crawl candidate** — sub-decile candidate yield at the very top 1.175 → 11.25, sub-median 0.075 → 4.025. Carries two findings against itself in fixed wording: **`AS-H1`** (a read-structure gap — neither designed read fires, recorded as not-fired rather than re-labelled toward the flattering one, same shape as `TB-P5H-7`) and **`AS-H2`** (`ALG-B` returns **58% fewer candidates** below the median, so it plausibly strands MORE obscure artists — the `MKS-6`/`F1` and `MKS-3` mechanism; unmeasured, deferred on "before any adoption decision"). **`AS-R3` fires: this is a replacement graph, not a patch** — Jaccard 0.42 at the top, 16.8 never-seen artists per artist, so every path-quality figure the project holds was measured under `ALG-E`. Nothing adopted; nothing here measures whether the new edges are any *good* (`REQ-38`). |
| `superpowers/specs/2026-07-29-track3b-thresholded-toll-preregistration.md` | **ACTIVE pre-registration — Track 3b, the thresholded toll. ⚠ AMENDED 2026-07-29 per TB-P1, pre-arm — 15 findings folded in, marked in place; §9 is the amendment index; the review is `../builder/analysis/2026-07-29-track3b-thresholded-toll/TB-P1-protocol-review.md`. Committed before any arm; no arm runs until its prerequisites discharge in order (TB-P1 ✅ → TB-P2a fame extension → TB-P3 ceiling go/no-go → TB-P4 harness → arms → TB-P2b → TB-P5 harness review → read).** Identifiers **`TB-`**. The named successor from the Track 3 execution log §7, owner-triggered 2026-07-29; the first track designed against `superpowers/PRODUCT-REQUIREMENTS.md` (REQ-13/14/9 and the REQ-Q1 currency ruling). Device: `w · k · max(0, pctl(v) − 0.90)` — zero on sub-decile interiors at any strength, so length-neutral where Track 3's DD-D5 confound lives. One axis (dose {0.10, 0.30, 1.00}, derived to match Track 3's realised 5×/15×/50× ladder); floor deliberately NOT an axis (DD-D4: dead from k=3 on this pair set); knee fixed at 0.90; pair set inherited frozen (`pairs_v2.json`). TB-C6 (length) is **promoted to a criterion** — TB-R1 (mechanism confirmed) requires a pass to be unflagged. Schedules no blind listen, no adoption, no rebuild. |
| `superpowers/2026-07-29-reciprocity-execution-log.md` | **The retained execution log for reciprocity sampling. COMPLETE** for the measurement it records. **OWNS NO CRITERION FIGURES** — every `RC` measurement belongs to `../builder/analysis/2026-07-29-reciprocity-sampling/` (`rc_scores.json`, the pre-registered read; `rc_posthoc.json`, the post-hoc corrections) and `../builder/analysis/2026-07-29-trial-crawl-calibration/`. **It does own two things nothing else can**: §6's operational measurements (the endpoint's latency then versus now, collection counts, the corrected stopping-rule reading) and §8's standing-context-layer totals, which are the figures the *next* closeout compares against. Records why the capped trial crawl was **retired as an instrument** before any pre-registration existed, the replacement design and its cost, `RC-G1` **not fired** with its figure, all five bands readable under `RC-G2`, and `RC-R1` **fired** on both readings. Carries three findings against its own design — **`RC-P1`** (a denominator defect in the pre-registered scorer that biased toward the null; corrected effect roughly doubles, verdict unchanged, both figures reported together), **`RC-P2`** (`RC-C3` pre-registered only the median floor, missing the top-25 minimum and the canonical-names clause, so an `ALG-B` artifact is predicted to be refused by the build) and **`RC-P3`** (the mechanism this session proposed, unsupported by its own data — candidate obscurity does not predict reciprocation). Also carries the corrected reading of `crawl.py`'s stopping rule (`done == discovered == target`) and the endpoint's measured latency. Nothing built, nothing adopted. |
| `superpowers/specs/2026-07-29-reciprocity-sampling-preregistration.md` | **ACTIVE pre-registration — does `ALG-B` strand obscure artists?** Identifiers **`RC-`**, namespaced and disjoint by role (`RC-ARM-*` arms, `RC-C*` criteria, `RC-G*` gates, `RC-R*` reads, `RC-H*` hazards, `RC-P*` post-hoc). Committed **before any arm ran**. Answers `AS-H2`'s own instruction to a successor — *pre-register the rate alongside the count* — by fixing candidate count, reciprocation rate and their product per fame band, paired across two settings differing in one request parameter. Its §0 carries the dormant-term analysis: the p99 clip is **genuinely** inert here because the cap ranks on unclipped strengths (verified in source, not assumed), while `filter_special_purpose` is **not** automatically constant and is handled explicitly. ⚠ **AMENDED twice, §8, append-only:** `RC-A1` **pre-run** (adopt the AS sample as the seed set, making the seed side free and the drift gate free and stronger) and `RC-A2` **post-run** (`RC-P1`/`RC-P2`/`RC-P3`, plus the diagnosis that the famous-side collapse is a `k` interaction — **explicitly not a licence to raise `k`**, which `MKS-5b` governs). No clause above §8 was edited. Licenses no adoption and no re-crawl. |
| `superpowers/specs/2026-07-29-algorithm-selection-preregistration.md` | **ACTIVE pre-registration — source algorithm selection.** Identifiers **`ALG-`** (arms) and **`AS-`** (criteria, gates, reads, noise floor), disjoint by construction. Committed **before any sampled arm ran**. Fixes the six-arm factor table over the ListenBrainz `algorithm` enum, which `CS-P0e` validated as **closed** — `limit` cannot exceed 100 and `threshold` cannot go below 10, so two previously-planned levers do not exist. `ALG-C` is **barred from every one-knob attribution** (four columns differ, and the enum's closure means no isolating baseline can be built). `ALG-A` vs `ALG-F` (1.4% apart in window) is adopted as the noise floor `AS-N`, giving calibrated effect sizes. Its §1.3 dormant-term check caught a real confound: sub-decile counts measured against the current artifact silently drop candidates *absent* from it — inert under production, not under `ALG-B` — so absence is promoted to `AS-C2` and `AS-C1` is declared a lower bound. ⚠ **AMENDED post-run** with `AS-H1` (neither `AS-R1` nor `AS-R5` fires; both assumed the strata would agree) and `AS-H2` (the criteria are counts, not rates); §7 is the amendment index and no clause was edited. Licenses no adoption and no re-crawl. |
| `superpowers/2026-07-28-HANDOFF-track3-preregistered.md` | **⚠ NO LONGER THE CURRENT handoff — superseded 2026-07-28 on next actions by [`2026-07-28-HANDOFF-track3-executed.md`](superpowers/2026-07-28-HANDOFF-track3-executed.md), which executed the work this one handed over.** Its direction-changes remain in force; **its next action (execute Track 3) is spent.** *(Original role: the CURRENT handoff, written at a seam after ASC-5 was discharged and Track 3 pre-registered.)* Supersedes the Track 2F/headroom handoff **on next actions** (the path-work chain) and the redesign-deployed handoff below **as the document to read first only** — a different body of work, discharging none of it. Names what the successor executes (Track 3, from its pre-registration, read cold), the direction changes that must not be reverted (the pause is over; famous-pair first-path fame is barred as a criterion; nameless artists are dropped, not backfilled), and the implementation knowledge folded in from the retiring session (the DD-P1 instrument mostly exists; DD-P4 extends the committed mirror; DD-G4 must land before the new pairs' fame fetch). |
| `superpowers/specs/2026-07-28-track3-depth-descent-preregistration.md` | **⚠ EXECUTED 2026-07-28, and AMENDED during execution — read the execution log beside it, which is fresher on everything that ran.** It remains authoritative for *design intent, criteria and thresholds*; it is **wrong in four places the log corrects**: it claims 4 both-famous carry-over pairs (there are 6, DD-D1), its DD-P1 branch trigger is **unsatisfiable by construction** (DD-D3), two of its six arms are **bit-identical to two others at every scored depth** and were struck (DD-D4), and its §5 has **no length control**, without which no criterion can attribute a DD-C1 movement to descent rather than shortening (DD-D5 → DD-C6 adopted). *(Original role: ACTIVE, governs the next path-quality work; no arm runs until discharged in order DD-P2 → DD-P1 → DD-P3 → DD-P4.)* Identifiers **`DD-`**. The first device that **rewards descent with bypass depth** rather than permitting it (`w_known_ramp_pctl · k · pop_pctl(v)`, zero at k = 0 so the first path is production's by construction, gated not assumed). Six arms over dose × floor with per-row isolating baselines; its §0 declares the raw floor a **live interacting term**, not a constant. Criteria reuse Track 2's committed calibration; coherence is a tripwire, never a gate (Phase 1 log §3.8). Its reads pre-commit the null: DD-R2 bars magnitude escalation and names the rescale and the `STC-6` re-crawl as the successors on a null with headroom. Schedules no blind listen, no rebuild, no owner spend. |
| `superpowers/2026-07-28-HANDOFF-track3-executed.md` | **⚠ SUPERSEDED 2026-07-29 on next actions** by `2026-07-29-HANDOFF-requirements-track3b.md` (row above) — the pending owner decision it hands over was taken and reframed (requirements rewritten; Track 3b run; candidates PARKED; next work is the rebuild plan). Still the record of Track 3's execution, and its do-not-revert list stands. *(Original role: the CURRENT handoff, 2026-07-28 — written at a seam after Track 3 ran to a read.)* Supersedes the Track 3 pre-registration handoff **on next actions**. All prerequisites discharged, all gates closed, DD-R1 fired, both halves of DD-P3 complete, and the owner's consultant review closed on all six items. Carries the fixed verdict wording that no summary may soften, the six overturned claims a well-meaning editor must not revert (**DD-A4/DD-A5 struck; `pairs_v2.json` is the pair set; anchors unscored and held-out gates nothing; production's negative depth gradient RETRACTED; guard-G constancy corrected to untested; DD-D8 must appear in every summary**), what is already updated, and what was decided against. Its last section carries the transferable lesson: **the pre-registration scheduled its own analyst review third, and nearly everything that review found needed no measurements at all.** |
| `superpowers/2026-07-28-track3-depth-descent-execution-log.md` | **The retained execution log for Track 3. ACTIVE — fresher than the pre-registration on everything that was actually run.** §1–§2 the pair set and the headroom measurement, including **DD-D3: the pre-registration's own branch trigger is unsatisfiable by construction** (any permitted pair set contributes 33.3 % against a 30 % trigger), and **DD-F1**, the structural finding that superstar endpoints have *zero* edges below the top decile. §3 the protocol review, which **struck two of six arms** (DD-D4), exposed the length/obscurity confound (DD-D5) and forced the currency check (DD-D6). §4 the ceiling probe and the **value tension it exposes between WGLL values 1 and 2**. §5 the arms and the verdict. §6 held-out confirmation and the **descriptive anchor table — the device does nothing on famous pairs at any strength**. §7 **Track 3b, a named UNSTARTED option** (the thresholded toll) that needs its own pre-registration. §8 the harness review, carrying **DD-D8** and **two retractions of this log's own earlier claims**. Figures owned by the analysis directory; cited, never restated. |
| `../builder/analysis/2026-07-28-track3-depth-descent/` | **AUTHORITATIVE for its own measurements.** Track 3's measurement record: the pair draw and its re-draw, the headroom instrument, the `w → ∞` ceiling probe, the four arm walks, the pre-registered scorer, and the supplementary held-out/anchor tables. **Owns** every Track 3 figure — the sub-decile degree series, the headroom cells, the ceiling fame gap, and the criterion table. `REPORT.md` is the deliverable and carries the verdict; `README.md` says what is imported rather than reimplemented and **what the directory cannot conclude**. Both DD-P3 reviews live here (`DD-P3-analyst-review.md`, `DD-P3-harness-review.md`) with their probes. **Not a `findings/` document, deliberately** — a second copy would restate its figures. Cite this directory. |
| `superpowers/2026-07-28-asc5-path-ascent-execution-log.md` | **The retained execution log for the ASC-5 discharge and Track 3 pre-registration, 2026-07-28.** §0 records the status change (path work UNPAUSED, owner decision, his stated goal verbatim); §1 why ASC-5 was the first move and not the degree floor or the rescale prereg; §4 which pre-committed branches fired, with figures owned by `builder/analysis/2026-07-28-asc5-path-ascent/`; §6 the Track 3 ordering decision; §7 the owner's nameless-artist decision (**drop**), recorded at the tripwire. Also records this session's two logged defects (a serialization failure before any figure existed; a prerequisite-order misstatement corrected the same hour). |
| `superpowers/2026-07-28-HANDOFF-redesign-deployed.md` | **⚠ No longer the CURRENT handoff — superseded 2026-07-28 (late night) *as the document to read first only* by the Track 3 handoff above, a different body of work discharging none of this one.** Everything it records about the deployed site remains true; **the `--prune` pass and the queued use-the-app test (including the iPhone script) are still owed exactly as it says.** *(Original role: written at a seam, after the work was finished, merged and DEPLOYED.)* Supersedes the row below **on everything**. All 13 tasks are done, plus three owner copy/rail changes and `path_length` telemetry; live at `https://musicapp.cmiller.io` (`d68ef7a`). Carries what the execution log cannot: that **the `--prune` pass is TRIGGERED but must be a SEPARATE LATER RUN**, never part of a publish, because pruning while the old `index.html` may still be cached re-opens `FRO-1`; the six do-not-revert items, including that **`PathIntro`'s count assertions must read `textContent`** and that the **landing clip-length test asserts ABSENCE** by the owner's decision; and that the loading screen's rail was deliberately left alone when the journey rail changed. Records what was decided against, including recording The Soft Boys find (offered, owner declined — **do not cite it as evidence**) and moving the reroll notice. |
| `superpowers/2026-07-28-HANDOFF-frontend-mockup-adoption.md` | **⚠ HISTORICAL — superseded 2026-07-28 (night) on EVERYTHING by the row above.** *(This row read "The CURRENT handoff" until then — the same collision recorded against the Track C and Track D rows below, caught here by the closeout rather than by an audit.)* Its Tasks 1–7 status, its "nothing is deployed" line and its next actions are **all spent**; its do-not-revert list is carried forward by the successor rather than cancelled. *(Original role: the handoff at the PLANNED Task 7 seam. Its body still reads "Tasks 8–13 are not started" and "nothing is deployed" — **both false since 2026-07-28 night**, left in the document because a frozen handoff is a record of what was handed over, and governed by its role banner. This row previously restated those claims as if current, which is the defect the banner exists to prevent.)* Still the only record of what the Task 7 seam handed over, and **its do-not-revert list is carried forward, not cancelled**: the deliberately MIXED `UI-7` test, the `data-testid` locator in `responsive.spec.ts`, `UI-D3`, and the oxlint exclusion of the design export. Also still true and worth knowing: **`npm run lint` exits 0 when oxlint fails to parse its own config.** For everything else read the row above. |
| `superpowers/2026-07-28-frontend-mockup-adoption-execution-log.md` | **The retained execution log for the mockup adoption plan. ACTIVE — all 13 tasks done, merged and DEPLOYED. Fresher than the plan on status; where they disagree, this wins.** **Owns the suite counts for this work**, stated in exactly two tables (§1 at the seam, §9 at completion) and nowhere else. §14–§17 cover the owner's post-use changes and the deploy: §15's gate table includes **the check that is not in the runbook** — a production journey returning the identical path to the same journey locally, which is the only direct evidence that routing did not move — and §17 is the deferral state, including that the App Runner tag deferral was **tested against reality and did not fire**. **Owns no path-quality figures**; the pause is intact. Its §4 records the finding of the session: **`B3` mutation testing caught a vacuous test in this session's own output**, guarding `UI-7`, the rule that a decorative lookup must not be able to turn a working page into an error page. Removing the `.catch()` that *implements* the rule left the suite green, because a rejected `Promise.all` never runs `.then`, so state stayed at its **initial** `null` — which is what the assertion read. **The fifth documented instance of this class here, and the first found by mutation rather than review.** It also records why a red-first test is not automatically a real test: this one went red because the module did not exist, not because the behaviour was absent. §3 lists three defects found **in the plan itself**, §6 records that the **e2e gate was not run** at the seam and why that is the honest weak point, and §8 is the five things a successor must not get wrong. |
| `superpowers/plans/2026-07-28-frontend-mockup-adoption.md` | **COMPLETE — fully executed 2026-07-28. The plan for the frontend redesign.** Thirteen tasks, identifiers namespaced **`UI-`**, one seam after Task 7 **named at authoring time** rather than discovered — and the seam worked: the successor picked up cold and finished. **All 13 done.** Its Global Constraints section is the load-bearing part: the field labels stay literally `From`/`To` and are uppercased in CSS only (four test files resolve them by exact text), the journey stays an `<ol>` of `<li>` (three e2e specs index into it), the bypass markup stays a single copy (Playwright strict mode), and `viewport-fit=cover` is **not** added. **Read the execution log's §3 before executing any remaining task** — two of the plan's own code and test snippets are known wrong there. Owns no figures. |
| `superpowers/specs/2026-07-28-frontend-mockup-adoption-design.md` | **AUTHORITATIVE for the redesign's intent; owns no figures.** The design for adopting the owner's Claude Design mockup across all five screens. **Not a re-skin:** `frontend/src/index.css` already defined eight theme tokens and the mockup uses those exact eight values, so the work is type scale, spacing, states and five new structural pieces. **Its §2 records nine owner decisions so they are not reopened**, and §7 fixes every user-facing string **before any implementation existed**, so wording could not be reshaped to fit what got built — including the two per-signal reroll messages, which are flagged there as *accurate to the router rather than decorative*, so a change to `dislike`'s neighbourhood penalty or `known`'s floor relaxation puts them in its blast radius. `UI-1` requires the mockup export to be **committed** (`frontend/design/2026-07-28-mockup/`) rather than cited from a Downloads folder. §12 names two deploy-time consequences: **this will be the next FRONTEND publish, which is the `--prune` deferral's trigger**, and the API change means an App Runner deploy. §14 names the weakest link. |
| `2026-07-28-doc-audit-frontend-mockup-adoption.md` | **COMPLETE — a point-in-time audit record. ⚠ THREE OF ITS SIX FINDINGS WERE ALREADY FALSE WHEN WRITTEN**, and it carries a banner saying which. The `doc-auditor` run for the mockup-adoption closeout. It was dispatched **while the closeout was still editing the documents it audits**, so its two HIGH findings (four documents unclassified; the handoff's lint claim untrue) and one MEDIUM (`NEXT.md` silent on the frontend work) are **race artifacts, verified false immediately after it reported**. **The one real defect it records is its own** — as written it had no role marker, was unclassified, and carried a dead link, failing three of the six checks it was auditing; all corrected in place. **Retained rather than deleted because its verification half is real evidence**: code references in the spec and plan resolve against source, and the `UI-` series is collision-free. Two findings survived and were acted on (the supersession axis, and a heading). **Cite it only through its banner.** |
| `superpowers/2026-07-28-HANDOFF-password-removal-complete.md` | **⚠ NO LONGER THE CURRENT HANDOFF — superseded 2026-07-28 (evening) on next actions ONLY** by the mockup-adoption handoff above, which is a different body of work and discharges none of this one. **Everything it states about the deployed site remains true, and the iPhone test it says is owed is STILL OWED.** Originally written at the password-removal plan's last implemented task, 2026-07-28 — written at the plan's last implemented task.** Supersedes the Track B handoff below **on next actions and on status**. `PW-5`→`PW-8` complete, deployed and verified; **`PW-9` is the only unrun task** and is gated on the owner's approval. **The site password is gone and `https://musicapp.cmiller.io` is public**, behind a Cloudflare front door with a rate limit; no application code changed. Carries what the execution log cannot: that **both plans are historical and still read as though nothing has run** (leave them); that the plan is wrong in two named places (`PW-6`'s check 4 aims at the wrong leg, and its rate-limit period assumes 60-second windows the owner's Cloudflare plan does not offer); that **`~1.9 s per path request` is CORRECT** and was briefly and wrongly reported as refuted, because latency is driven by the artist **pair** and not by bypass depth; and what was decided against, including Cloudflare's post-quantum origin handshake — deferred only so `PW-6`'s gates ran with one variable, still worth enabling. |
| `superpowers/2026-07-28-HANDOFF-password-removal-track-b.md` | **⛔ SUPERSEDED 2026-07-28 on next actions AND on status** by the row above — Track B ran to completion the same day. Retained as the record of *why Track B was shaped as it is*: the additive-reversible ordering, and four things it warned a cold session would get wrong, all of which held. **⚠ One of its bullets was WRONG and is struck in place** — it said the `--prune` deferral came due at `PW-5`'s deploy; that deferral belongs to the *frontend* publish and has not come due. **Do not act on its next actions.** Originally written at the plan's own Track A/Track B seam. Track A (PW-1→PW-4, API-only) is complete, committed and verified; **Track B is not started and the password is still on.** Supersedes the migration handoff below **as the document to read first only** — a *different* body of work, discharging none of it. **Two live handoffs, deliberately:** the migration's Tasks 9→11 remain owed on their own axis. Carries what the execution log cannot: that **the password must stay on until PW-7** and the additive-reversible ordering before it is a safety property rather than bureaucracy; that **`max_size=2` must NOT be raised** — it reads like the obvious capacity fix and is the cost ceiling, and not raising it is the only reason `G3-A3` does not fire; that **`G3-S7`'s fix is a swap, not a deletion** (deleting the gate also deletes the SPA fallback and 403s every shared link); that **PW-6 step 6 check 4 is not optional** — it proves the Cloudflare secret reaches the origin *while the password is still on*; and that **the `--prune` deferral comes due at Track B's first deploy** with nothing currently scheduled to read it. Also records what was decided against: AWS WAF, spending Cloudflare's five spare rules, and a `Literal` for `ExclusionIn.reason`. |
| `superpowers/plans/2026-07-28-password-removal-load-hardening.md` | **COMPLETE except `PW-9` (owner-gated, blocks nothing, status owned by `NEXT.md`) — executed through `PW-8` and deployed 2026-07-28; role corrected 2026-07-30 by doc audit after reading ACTIVE for two days post-ship.** *(Original role: ACTIVE — the governing plan for removing the site password.)* Nine tasks, identifiers namespaced **`PW-`**, one seam after PW-4. Implements a **subset** of the Gate 2→3 review — the minimum load-related set that lets the password come off — and **explicitly does not open Gate 3**; where it describes a finding, the review wins. **Owns no figures.** Track A (PW-1→PW-4) is DONE; Track B (PW-5→PW-9) needs the owner's AWS and Cloudflare accounts. Records the scope reasoning that two blocking findings were argued *out*: **`G3-A3` does not fire** under a shed-load-at-the-edge design, and **`G3-A1`'s capacity half is deferred** because a per-IP limit cannot help against a dozen legitimate simultaneous users. Contains one **in-place correction made during execution** (the circuit breaker's cooldown reset, annotated at the code block) — a plan defect, not a code defect. |
| `superpowers/2026-07-28-password-removal-execution-log.md` | **The retained execution log for the plan above, appended per task. ACTIVE — Track A DONE, Track B not started, the live site unchanged.** Fresher than the plan on status. Records the two **design errors caught by red steps** and kept rather than quietly fixed: a **vacuous test** whose 422 assertion passed before the fix existed, because `build_path` already 422s for "exactly two" — but only *after* materialising the array, which was the entire finding; and the **breaker cooldown reset** that would have let a dead catalogue take five calls a minute instead of one. Also records that tests written *after* their implementation were verified by mutation rather than trusted green, the Snyk result (one Low, pre-existing, already triaged), and the five things a Track B session must not get wrong. **Owns the suite counts** — its "State at the seam" table is the only copy; three documents restated them and disagreed until the doc audit caught it. **Records two closeout findings against this closeout itself**: `A4` found an untested wiring path (`build_default_app` relies on a constructor default to arm the circuit breaker, and every test injected one explicitly — `G3-Q1`'s exact shape), and `D1` ran before Part B's fixes and reported a false green. |
| `superpowers/2026-07-27-HANDOFF-migration-phase-d.md` | **⚠ No longer the CURRENT handoff — superseded 2026-07-28 by the one above *as the document to read first only*. ⚠ **NOW FULLY SPENT as of 2026-07-28 — do not act on its next actions.** Task 9 was discharged by restore, and Tasks 10 and 11 are **CLOSED by owner decision**: the OneDrive tree is kept indefinitely as an archive, so there is no deletion to gate and Task 10 existed only to gate it.** Written at the plan's own Phase D seam. Phases A–C are complete and verified; **Phase D is not started, and two owner-owned gates stand before it** (Backblaze upload *completed*, and Task 10's few days of use). Supersedes the handoff below on next actions **and** on status. Carries what the execution log cannot: that **`MIG-10` / Task 12 was deliberately deferred — ⚠ it is DONE as of 2026-07-28 and this clause is spent.** Its defence (the statements were still true of the tree that was still the rollback) expired when Tasks 10/11 closed and the tree became a permanent archive. The live documents now name `C:\dev\music-app`; only historical logs still say OneDrive, and those must stay as they are. The one earlier exception taken (`CLAUDE.md`'s project-memory path, corrected because it was actively misleading a live session); that **`MIG-11` could not be actioned** — two old-tree API servers on ports 8138/8139 are still running and will make Task 11's deletion fail partially, the kill having been blocked by the tool-permission classifier; that a **third listener on 53342 belongs to a different project** and must not be killed with them; and that the Task 4 evidence lives in five files under `C:\dev\` that **cannot be committed** and must not be deleted with the old tree. |
| `superpowers/2026-07-27-HANDOFF-onedrive-migration.md` | **⚠ SUPERSEDED 2026-07-27 on next actions AND on status** by the handoff above. Its "no task in it has run" is no longer true and its four don't-get-this-wrong items are all consumed. **Retained because it is still the record of *why the plan is shaped as it is*** — the `clamp` evaluation, the `MIG-5` firing, and the archive-is-the-asset framing are unchanged and still correct. Do not act on its next-actions section. | Supersedes `superpowers/2026-07-27-HANDOFF-gate2-track-c.md` **on next actions only**; that document's four must-not-revert items are untouched and still govern. Carries the four things a cold session would otherwise get wrong — Task 0 is not bookkeeping (the clone is silently lossy against uncommitted work), `clamp` looks like it does Task 8 and does not, `MIG-5` already fired and must **not** be repaired, and the archive is the asset rather than the artifacts. Its "what is owed" section records that **a full `closeout` was not run** and names what is missing. Also records the two unrelated items in the same PR: the queue entry DONE per-step, and the **mangled-descriptions retraction** — with the warning not to reinstate it from the two Track C records, which are annotated but still contain the original text. |
| `superpowers/plans/2026-07-27-onedrive-migration.md` | **The OneDrive migration plan — twelve tasks, identifiers `MIG-`. ⚠ COMPLETE as of 2026-07-28, and **Phase D never ran and now never will** — Phases A–C executed 2026-07-27; Task 9 discharged and Tasks 10/11 **closed by owner decision** (the tree is kept as an archive); Task 12 done. **Read it as history, not as work outstanding** — the plan's own text still reads as though nothing has run. For what actually ran, read the execution log row below, which is fresher than this one. Moves the tree to `C:\dev\music-app` (owner's decision, 2026-07-27). **Touches no application code, no graph, no routing and no config default** — its §5 says so explicitly and no test-queue entry is owed. Its §0 states what the job is actually about: every tracked byte is already on GitHub, so the whole risk sits in the 1.6 GB git ignores, and within that in **`builder/scratch/graph-archive/`** — 75,000 files that cost a 4¼-hour rate-limited crawl and **cannot be reproduced**, because a fresh crawl of a live API is a different input. §1 is the risk register; **three of the ten fail silently** (`MIG-1` partial copy, `MIG-2` project memory orphaned by the path-keyed `~/.claude/projects/` slug, `MIG-3` unverified Backblaze coverage). It **clones rather than copies the tracked tree** as the structural mitigation for the `.git`-under-OneDrive corruption incident, and **names a hard seam before Phase D**, which is where the rollback copy gets deleted. `MIG-9` is accepted, not fixed: **26** frozen `builder/analysis/` scripts will stop resolving, which **weakens the stated justification for the shipped-code read-only aliases** — evidence toward their removal condition, not an argument to remove them now. **That count read 16 until the doc audit corrected it**, and §4 keeps the error rather than just the fix: a regex alternation over two slash styles matched nothing on the backslash branch and returned a plausible wrong number. Count with `grep -F` per variant. |
| `superpowers/2026-07-27-onedrive-migration-execution-log.md` | **The retained execution log for the migration plan above, appended per task. ACTIVE — Phases A–C DONE, Phase D not started. Fresher than the plan and the handoff on status; where they disagree, this wins.** **Owns the migration's own inventory figures**, which live nowhere else: `builder/scratch` is **75,052 files / 1,460,266,074 bytes**, of which `graph-archive` is **75,000 / 900,518,765**. Records the Task 4 pass (per-file manifest diff, **0 differing files**; all 18 `.bin` sha256 identical source-to-destination; `/health` from the new tree serving `4cb84ef9…`) and all four suites green from `C:\dev\music-app` (builder 115, api 195, infra 58, frontend 80 + 5 e2e). **Four things a successor needs that the plan does not carry:** (a) `MIG-4` names two gitignored files and there are **six** — the clone also drops `.superpowers/`, which holds `sdd/BLIND-MAPPING.json`, the arm-to-identity key for the blind listening tests, gitignored precisely because a tracked copy would unblind them (§4); (b) **`TaskStop` does not kill the process tree here** — a killed inventory script kept running and raced a second writer into one output file, producing interleaved mid-line corruption caught by eye rather than by any control (§12); (c) **`robocopy` exit code 1 means success** and is surfaced as a failure (§8); and (d) **`MIG-11` — two API servers from the OLD tree are still running** on ports 8138/8139 since 2026-07-20 and **will make Task 11's deletion fail partially**, since they hold that tree's `.venv` open. **The most safety-critical of the four**, because it is the only one still live at this seam; stopping them was blocked by the tool-permission classifier and is an owner action. **Task 8 is CLOSED** (§11): memory was *copied* to the predicted slug `C--dev-music-app` rather than renamed, and a session launched in the new tree then read all eleven `MEMORY.md` entries from it — the duplicate under the old slug is retained deliberately until Task 11. **Task 9 (Backblaze) is IN PROGRESS — the owner's, and it gates Task 11**; its exclusion-list check passed (§13a), and acting on it uncovered a **pre-existing backup failure unrelated to this project** (§13b). |
| `superpowers/2026-07-21-phase2-execution-log.md` | **The audit trail for Phase 2.** Every decision and why, defects found in the plan and in the prior record, gate pass/fail state, open items, deferred findings. **§16 is the blind-test verdict; §17 the adoption; §18 the closeout triage; §19 the fixture-seed defect found after closeout — which supersedes §17's checksums.** Where any other document disagrees with §16–19 **on Phase 2**, this wins. **No longer the freshest truth in this directory** — as of 2026-07-23 that is `2026-07-23-repair-and-retune-execution-log.md` and `findings/2026-07-23-tiebreak-fix-adoption.md`, which govern current status and the adopted artifact. Marked ACTIVE rather than COMPLETE because Phase 1 consumes its carried-forward items. |
| `superpowers/plans/2026-07-21-alpha-rollout-roadmap.md` | *(also listed as Authoritative)* Its Phase 1 section gives the **phase structure**, including the two items carried in from Phase 2 with success conditions; current sequencing within Phase 1 is governed by the 2026-07-23 repair+retune spec, not this document. |
| `superpowers/2026-07-22-phase1-execution-log-and-graph-defect.md` | ⛔ **Start here for Phase 1, and read §2 before any implementation.** One document by design: the **graph-structure defect that halted the work** (§2 — facts, discovery mechanism, suspected cause; deliberately no solutioning) **plus** the full Phase 1 progress record (§3 — including two independent ML-analyst reviews and what each overturned), what is closed (§4), artifacts (§5), and state of play (§6). Splitting it would let the progress record be picked up without the context that stopped it. **Owns its figures** — cite from here, do not restate. |
| `superpowers/2026-07-22-HANDOFF-phase1.md` | Written at Phase 2 closeout for a cold session: what was overturned and must not be reverted, and what the previous session knew that is not otherwise in the record. **Superseded on Phase 1 status by the log above** — that document governs where they disagree. Still valid on Phase 2 outcomes. |
| `superpowers/2026-07-23-HANDOFF-track2.md` | **SUPERSEDED 2026-07-24** by `2026-07-23-HANDOFF-track2.md`'s successor below — its loose ends are closed or folded forward. Retained for audit; do not act on it. *(Original role: loose ends written at the 2026-07-23 mid-track retirement.)* |
| `superpowers/2026-07-24-HANDOFF-track2.md` | **SUPERSEDED 2026-07-24 by `2026-07-24-HANDOFF-track2-scorer.md` below** — its one open unit (the sweep) is now half-built (the scorer exists) and re-described there. Retained for audit; do not act on it. *(Original role: fame-proxy/sweep seam handoff, P4 closed, A11 committed.)* |
| `superpowers/2026-07-24-HANDOFF-track2-scorer.md` | **SUPERSEDED 2026-07-24 (same day) by `2026-07-24-HANDOFF-track2-complete.md` below.** Its open unit — P8b, then the arms — is closed: P8b was discharged and all 15 arms ran. Its "no factorial arm has run" statements are now false. Retained for audit; **do not act on it.** *(Original role: handoff at the scorer/P8b seam.)* |
| `superpowers/2026-07-25-HANDOFF-track2f-and-headroom.md` | **The CURRENT handoff, 2026-07-25 — written at the Track 2F / headroom completion seam.** Track 2F ran the ceiling toll at full strength and returned **`TFR0`**, a null at `M*` = −0.198 — but **its bound holds**, unlike Track 2's corner arm, so the toll mechanism can be declared exhausted rather than merely untested at fifteen points. The ceiling's *ordering*, which no router-side arm can reach, was then measured read-only and came back **WIDE** (median 10.01 × `w_hop` erased). **Nothing adopted, no shipped code changed, no blind listen run, no threshold touched, no rebuild.** Carries the mechanism statement, the four things a successor must not get wrong, the open decision with the session's position, and what was decided *against*. Supersedes the 2026-07-24 handoff **on next actions only**. **Read the execution log's last three entries first**; where they disagree, the log wins. A seam handoff, not mid-flight. |
| `superpowers/2026-07-24-HANDOFF-track2-complete.md` | **⚠ PARTLY SUPERSEDED 2026-07-25** by the handoff above — **on next actions only.** Track 2's own result is untouched and this is still the record of it, but its §4 recommendation has been **executed** (that is Track 2F) and its §2 claim that the toll "has never run at full strength" is **no longer true**. Both are marked inline as well as bannered. *(Original role: the Track 2 completion-seam handoff, 2026-07-24.)* **Written at the completion seam.** The sweep ran to completion and returned **R0, a full null**: 15 arms, best at −0.177 against a −1.0 threshold, **nothing adopted, no shipped code changed, no blind listen run**. Carries what R0 does and does **not** license (A17(c)'s falsifier fired, so it does not reach "the family is exhausted"), the firmest finding (the floor device is never pivotal), the open owner decision with the session's recommendation, and the **three items now queued behind one rebuild**. **Read the execution log's Track 2 section first**; where they disagree, the log wins. A seam handoff, not mid-flight. |
| `superpowers/findings/2026-07-25-consulting-derivations.md` | **AUTHORITATIVE for `DRV-1` only; everything else is a labelled hypothesis with a named check.** Written when a consulting session was asked what it knew that was not written down. **`DRV-1` is verified and matters: §2.11's and §2.12's tables were computed on `graph-t15-capfix.bin`, while the app has run `graph-t15-tiebreakfix.bin` since 2026-07-23** (**narrowed 2026-07-26** from the original "§2.9–§2.12"; §2.10 measures the adopted topology directly and is not exposed, and §2.9 is barred by supersession rather than by provenance — see the Current State bullet above and the notice in `DRV-1` itself) — the scripts assert capfix's checksum, and `known_viability.py` was committed six hours *before* the tie-break artifact was adopted. The visible tell needs no tooling: §2.12's worked example is "The Beatles have 7 of 7 neighbours admissible", and `MKS-5` records the Beatles holding the full 50 on the adopted graph. **`DRV-2` argues this is a provenance defect, not a refutation** — the pricing leg is artifact-independent and the exposed legs move in the direction that *strengthens* §2.12 — **but any specific §2.9–§2.12 figure quoted as a fact about the current app is unsupported**, which this map's Current State does. Re-running two committed read-only scripts settles it. `DRV-3`–`DRV-7` were recorded at hypothesis strength, and **`DRV-4` is now RESOLVED (2026-07-26)** by `findings/2026-07-26-low-degree-census.md`, which measures the population its structural claim describes and closes its open denominator: the descent is a **round trip** and §2.12 priced it one-way; a **degree-1 artist can never be an interior card**, so it is undeliverable as a discovery (**`DRV-4`, resolved**); §2.12's substitute counts may be **leaf-inflated**; the **staircase model** that generated `BYP-11`; and **three discriminating signals go flat at the famous endpoints**, leaving popularity alone. §3 carries the session's opinions, fenced off from the findings. **§4 is the calibration**: the same session made three confident wrong claims that the owner caught, all from reasoning over summaries when the data was available — and it names which two hypotheses are most likely to fail the same way. Nothing adopted; the pause is intact. |
| `superpowers/findings/2026-07-25-bypass-depth-use-run.md` | **AUTHORITATIVE for its own measurements — the owner's own use of the shipped app, 2026-07-25.** **Two runs of 100 bypass presses** on one pair (Bob Dylan → Metallica): run 1 with the buttons mixed, run 2 **`dislike` only**. Dogfooding, not an experiment — one pair, one listener, nothing scored or pre-registered, and §1 states every protocol defect up front. **The headline (`BYP-3`): across 200 presses the app never presented an artist who is obscure by any ordinary reading** — floors of 372k and 168k Spotify monthly listeners. `dislike` finds more unfamiliar artists (10 v 7) and reaches ~2× deeper, confirming a prediction this document fixed *before* run 2 ran. **`BYP-11` is the most consequential reading**: genre departure precedes obscurity by ~50 presses, so the router appears to escape the famous stratum by crossing genres rather than descending within one — measured against `WHAT-GOOD-LOOKS-LIKE` value 8, that is buying novelty with incoherence. **`BYP-9`, confirmed and reproduced:** `dislike` held the path at 3 cards for nine consecutive presses, which at 3 cards is *forced* 1:1 swapping — the shape value 6 says `dislike` should show least. **`BYP-13` is a live defect and the most actionable item: a card played a clip by a different artist of the same name** (FERG / A$AP Ferg), which is **C1's class reached by a different route, so C1 is not fully closed** — not path-quality work, not paused. **Read §4 before citing any length claim**: `BYP-4` was written, corrected, then partly restored, and the settled statement is *an upward trend with large non-monotone excursions*. **Explicitly does NOT refute §2.9** — that is graph-popularity percentile, this is Spotify listeners. §9 records the second-opinion instrument's **failure mode** (name-based lookup returns the wrong entity for a renamed artist, and fails toward flattering the result) and `BYP-14` records that manual press-logging does not scale and was **mostly unnecessary** — a single-mechanism run is fully reconstructible by replaying truncations of one URL. §10 carries the length series and the reproducible URLs. The pause is intact; nothing proposed. |
| `superpowers/findings/2026-07-25-router-ascent-gradient.md` | **A re-reading of a committed measurement, not a new one — and it deliberately OWNS NO FIGURES.** The P8b harness review measured, one hop out from every node, how often the router's cheapest next step moves toward a *more popular* artist: for the production-like arm it is far above the share of the graph's own edges that point upward. That measurement is currently reachable only as **A17(c)**, a bound on how the corner arm `X` may be read, inside a pre-registration whose track returned a null — so nobody re-reads it. Its `ASC-1` (the router climbs about four times in five where the graph offers a coin flip) is a **single-arm measurement against a structural null** and is clean. Its `ASC-2` (the sweep's most aggressive dive-cheapening corner ascends *more*) is **explicitly confounded — three columns, not one** — and supports "the corner does not dive", never *why*. `ASC-3`/`ASC-4` are labelled **inference**: no arm in Track 2 or 2F was directional (the jump term is `w_jump·\|Δpop\|`, symmetric; the toll binds on the edge; the floor is one-sided on level, not direction), so the pull survives the whole span of repricing and probably originates outside the popularity terms — with `w_sim` the leading candidate, because similarity and popularity share a source. **Does not reopen §2.12**; sits underneath it. Names one cheap decisive test (`ASC-5`): re-read the gradient at path level and read the `X`-vs-`A7` contrast that already exists in the Track 2 run — **both re-reads of committed runs, no rebuild.** Figures owned by `../builder/analysis/2026-07-24-track2-p8b-harness-review/`; quoted in A17(c). ✅ **`ASC-5` DISCHARGED 2026-07-28** — results in `../builder/analysis/2026-07-28-asc5-path-ascent/` (its row above); its §4 carries the discharge note in place. |
| `superpowers/2026-07-26-RESUME-BRIEF-path-quality.md` | **✅ Its trigger FIRED 2026-07-28 — the owner resumed path-quality work**, and it was read in full first, as it instructs. The live governing document for the resumed work is the **Track 3 pre-registration** (row above), which addresses the fame axis; **this brief's §1 degree-floor decision and §2 arithmetic-vs-pricing confound on the *degree* axis remain open and unstarted** — the brief is still the entry point for *that* question. *(Original role: the entry point IF path-quality work is ever resumed — explicitly NOT a resume signal.)* Resuming is the owner's trigger. Written 2026-07-26 for a cold session months later; short by design and meant to be read in full. Carries five things: **the decision** (build a bounded degree floor, or not); **the one thing that blocks everything else** — nobody has separated whether barely-connected artists fail to appear because the router prices them out or because they arithmetically have fewer places to fit, and those point to opposite conclusions about whether more edges would help; **a correction** — the floor is a *larger* intervention than a consulting session implied, since nothing was delivered below four connections, making the receiving-side quota a real design problem; **a three-item reading order** with an instruction not to re-derive the chain from the analysis directories; and **what is closed** (loosening the cap, Track 2, Track 2F, the ceiling toll, Gate 1/F1) alongside **what may not be spent** without the owner (a blind listen, a rebuild — itself blocked by `acceptance.py`'s nameless-artist check — and any growth of `CLAUDE.md`). Names the two parked candidates so they are not lost. Proposes nothing and sets no threshold. |
| `superpowers/2026-07-26-HANDOFF-track-d-complete.md` | **⚠ SUPERSEDED ON NEXT ACTIONS** by `2026-07-26-HANDOFF-track-b-complete.md` and, through the chain, by `2026-07-27-HANDOFF-gate2-track-c.md`; still the record of Track D, and its do-not-revert list stands. *(This row read "The CURRENT handoff" until 2026-07-27, three handoffs after it stopped being one.)* — written at a clean seam after Track D was executed in full. Supersedes the Track A handoff below **on next actions only**; that one is still the record of Track A and **its "must not be reverted" list is still live**. Records Track D as DONE (frontend 65 → 78, e2e 3 → 4, api and builder unchanged, `pathfinding.py` never opened), the five claims an editor must not undo — chiefly that `Player.play()`'s **two-part** handler check is not redundant, because checking `disposed` alone is defeated by the StrictMode revival that clears the flag — what the session knew that is otherwise unwritten, and the open decision (write the Track B plan, and run `DEP-33` **inside** it rather than after it) **with the session's position**. Names two items that are genuinely the owner's: `uv.lock`, and whether `CLAUDE.md`'s next-action row should name the executed tracks. **Two detached dev servers are deliberately left running**, and the note explains why their pre-HEAD start time does *not* invalidate them. |
| `superpowers/2026-07-26-HANDOFF-track-a-complete.md` | **⚠ SUPERSEDED ON NEXT ACTIONS 2026-07-26** by the handoff above; still the record of Track A, and its do-not-revert list stands. **One bullet is struck in place**: its claim that the repo-root `.playwright-results/` was "the `DEP-30` / `TR-17` fix working" is false — see `TKD-3`. *(Original role: the handoff at the Track A completion seam, 2026-07-26 evening.)* — written at a clean seam after Track A was executed in full. Supersedes the Gate 2 design handoff below **on next actions only**; that one is still the record of the design and review work and **its "must not be reverted" list is still live**. Records Track A as DONE (api 153 → 180, `DEP-16` discharged, routing untouched), the five claims an editor must not undo — chiefly that the unreachable `count=` guard **stays with an honest comment** rather than being deleted as dead or re-labelled load-bearing — what the session knew that is otherwise unwritten, and **the one item awaiting the owner: whether `CLAUDE.md` gains `ARTISTPATH_GRAPH_SHA256`**, which is a real defect of absence whose fix grows the budgeted standing layer. Names the open decision (write the Track D plan, and write it for subagent execution) **with the session's position**, not a menu. **Two detached dev servers are deliberately left running**, PIDs recorded there and in the test queue. |
| `superpowers/2026-07-26-HANDOFF-gate2-track-a.md` | **⚠ SUPERSEDED ON STATUS 2026-07-26** by the handoff above — its "no code has been written" line is false and marked in place; everything else, including its do-not-revert list, stands. *(Original role: the handoff at the design/review/plan seam, 2026-07-26.)* **The CURRENT handoff, 2026-07-26** — written at a clean seam after the Gate 2 design, the team review, and the Track A plan. **No code was written.** Names the reading order for a cold session, the five claims a well-meaning editor must not revert (chiefly that `DEP-8`'s struck text stays visible and that telemetry logs facts rather than metrics), what the session knew that is otherwise unwritten, and what was decided against — including **why Track A is executed inline rather than by subagents**. Records that a full `closeout` was **not** run and what it would still add. |
| `superpowers/findings/2026-07-26-gate1-gate2-team-review.md` | **AUTHORITATIVE for its own measurements and triage.** The gate-boundary team review CLAUDE.md schedules at Gate 1 → 2, run 2026-07-26 **before any implementation plan existed**. Four reviewers — architect, security, quality, frontend; the graph analyst deliberately **not** staffed, since no scoring question is open. Identifiers **`TR-`**. **`TR-1`/`TR-2` are the most valuable output and are a correction to the same day's own work**: the design's flagship defect (`DEP-10`) was misdiagnosed — a truncated artifact **raises**, because the metadata blob is the last section — and its prescribed verification **passed before any fix**, the `FMS-P1` vacuous-check pattern recurring. **`TR-3`** names the genuinely silent case (header/metadata length disagreement); **`TR-4`** records that the builder's parser already had the checks the API's lacked, so the two APG1 parsers had **already drifted undetected**. `TR-5`–`TR-11` are seven blocking gaps in the design, notably that every shared link would 403 and that CloudFront's default cache policy would **resurrect C2**, a defect closed 2026-07-25. **`TR-16`** is why Track D exists. **§6 records what was declined and why**; §8 states the review's own weakest link — seven findings concern a design rather than code, so **`DEP-33` requires re-running it against the CDK stack before cutover.** |
| `superpowers/plans/2026-07-26-track-a-api-hardening-and-telemetry.md` | **The Track A implementation plan** — ten tasks, AWS-free, complete code and exact expected test counts per task. **EXECUTED 2026-07-26; do not execute again** — outcomes in `superpowers/2026-07-26-gate2-track-a-execution-log.md`. Implements Track A of the design's §11 only. Its header carries a **plan-specific execution recommendation: inline, not subagent-driven**, with the reasoning and the condition under which Track D reverses it. Carries a **handoff seam after Task 6** (defect fixes done, telemetry not started). Its Global Constraints include the standing instruction that **every new test must fail against unmodified source first, and for the right reason** — written because this plan's own predecessor prescribed a test that could not fail. |
| `superpowers/2026-07-26-gate2-track-a-execution-log.md` | **The record of Track A, 2026-07-26** — the plan above, executed. Identifiers **`TKA-`**. Ten tasks, inline; suites api 153 → 180, builder 115, frontend 64 → 65, e2e 3, every per-task count hit first time. **Touches no routing weight, no cost-function term, no graph** — `pathfinding.py` was not edited at all. **`TKA-6` records `DEP-16` as DISCHARGED**: two `path` events emitted through the live API on the **adopted** artifact replayed offline through `find_journey` and reproduced exactly, sequence and stop rule; the bypass event is the one that carries the weight, since a zero-exclusion path verifies only that the graph loaded. The service was booted with the checksum **set**, so Task 2's gate is proven against the real file. **`TKA-1` is the entry worth reading**: what the RED runs actually said, which is the half that cannot be reconstructed afterwards. Task 1's truncation test failed pre-fix with `Expecting ',' delimiter` — a `JSONDecodeError`, whose message does **not** contain "truncated" — so `pytest.raises(ValueError)` would have passed against unmodified source. **The corrected `DEP-10` is therefore confirmed by observation, not by argument, and `TR-2` was reproduced under control and avoided.** `TKA-2` records why Task 3's knowingly-passing verification is a named exception rather than a recurrence; `TKA-5` records the seam that was reached and not taken, and that this log was started late. Snyk clean on `api/` and `frontend/`. Nothing adopted, nothing rebuilt; the path-quality pause is intact. |
| `superpowers/plans/2026-07-26-track-d-frontend.md` | **The Track D implementation plan** — eight tasks, AWS-free, complete code per task. **EXECUTED 2026-07-26; do not execute again** — outcomes in `superpowers/2026-07-26-gate2-track-d-execution-log.md`. Covers the six frontend items the owner fenced at the team review (`TR-16`) **plus a seventh** he accepted the same day after running the Track A queue entry. Its header carries a **plan-specific execution recommendation — inline, not subagent-driven — which reverses the prediction the Track A completion handoff made**, with the reasoning and the reversing condition. Its Scope section **corrects two `TR-16` claims** against source; the correction that matters is that a grep-based verification of the layout fix passes before the fix. |
| `superpowers/2026-07-26-gate2-track-d-execution-log.md` | **The record of Track D, 2026-07-26** — the plan above, executed. Identifiers **`TKD-`**. Eight tasks inline; frontend 65 → **78** (this row said 77 until 2026-07-27; the log's own closeout corrects it to 78, and the log wins), e2e 3 → 4, api 180 and builder 115 **unchanged** *(counts as at 2026-07-26; later tracks have since added tests — for current counts collect the suites, do not read them off this row)*, which is the check that routing was untouched. Snyk clean. **`TKD-2` is the entry worth reading**: Task 1's e2e spec **passed on its own first RED run** because it measured an endpoint card, which carries no bypass buttons and was therefore never broken — the `FMS-P1` / `TR-2` vacuous-check pattern reproduced by the one spec written to prevent it, and caught by running the RED rather than by review. **`TKD-3` records that `DEP-30` was not fully fixed by Track A** — the repo root is under OneDrive too, so Playwright still `EPERM`ed on two consecutive runs; read `DEP-30` as discharged only from this track. **`TKD-4`** corrects the owner's own first telling of the seventh item in two ways that change the fix: it is **not** a race with the autocomplete, and the "New path" prefill did not introduce it. **`TKD-7`** records a suite count that did not match the plan and was **not** made to match. **§11 states what this track does not cover — no real phone has run it**, which is what the queued entry is for. Nothing adopted, nothing rebuilt; the path-quality pause is intact. |
| `superpowers/plans/2026-07-26-track-b-infrastructure.md` | **The Track B implementation plan** — thirteen tasks plus four owner-run prerequisites, complete code per task. **EXECUTED 2026-07-26; do not execute again** — outcomes in `superpowers/2026-07-26-gate2-track-b-execution-log.md`. Identifiers **`TKB-`**. Its header carries a **plan-specific execution recommendation: inline, not subagent-driven**, and it names its own seams. **Read its findings section before touching `infra/`:** `TKB-2` (the team review's prescribed SPA-fallback fix is **distribution-level** and would have rewritten the API's own 404s and 403s into HTML with status 200 — the finding stands, its mechanism is replaced); `TKB-4` (**the plan's own prescribed dependency scan was vacuous**, reporting clean while examining zero packages, caught with a positive control); `TKB-7` (the first deploy must be staged — **but see the execution log's §7: its stated mechanism is wrong and its conclusion right**); `TKB-8` (App Runner is closed to new customers and this account can still use it, measured). |
| `superpowers/2026-07-26-gate2-track-b-execution-log.md` | **The record of Track B, 2026-07-26 — the app is DEPLOYED.** The plan above, executed, plus the `DEP-33` review that followed it. **§1 owns the deployed identity** (site, API origin, buckets, image tag) — cite it, never restate it. **§2 is what was verified against the running stack** rather than the template, and the entry worth reading there is that the logged `journey_id` survived CloudFront: a stripped header would have degraded telemetry silently. **§4 lists the claims an editor must not revert**; **§6 the Windows/Git Bash traps**, chiefly that Git Bash rewrites `/aws/...` arguments and the resulting error names the wrong cause. **§7 carries two corrections to this project's own record**, including that `TKB-7`'s stated mechanism does not hold. **`TKA-11` and `TKA-12` both close here.** |
| `superpowers/findings/2026-07-26-track-b-cdk-review.md` | **AUTHORITATIVE for its own findings and triage — the `DEP-33` review, run against the DEPLOYED stack.** Four reviewers (`ARC-`, `SEC-`, `QUA-`, `FRO-`); the graph analyst deliberately not staffed. **45 findings, 10 blocking**, none invalidating the deploy — all concern the *next* deploy or the *first* user. **§1 is the headline and it is about method: ten of nineteen mutations went green**, including detaching the password function from the site's default behaviour, which would leave the whole site publicly readable with no test failing. The fourth instance of the project's signature defect after `FMS-P1`, `TR-2` and `TKB-4`, and **the first where the vacuity was distributed** — no single test obviously vacuous, the suite simply holed where the properties were. **§2 is an ordered work queue**, ranked by what breaks and how silently. §3 corrects two claims in the project's own record. §6 states what the review cleared; §7 what no reviewer could reach, since none touched AWS. |
| `superpowers/findings/2026-07-27-gate2-gate3-team-review.md` | **AUTHORITATIVE for its own measurements and triage — the Gate 2 → 3 boundary review CLAUDE.md schedules, run 2026-07-27 after the Gate 2 cutover and first phone run.** Four reviewers — architect, security, quality, frontend; the graph analyst deliberately **not** staffed, no scoring question open and path quality PAUSED. Identifiers **`G3-`** (`G3-A/S/Q/F` by reviewer). **Owns its own performance figures** — dev-laptop measurements, labelled, with the container-vs-laptop caveat in §4; **owns no path-quality figures**. **Headline (§6): four blocking findings (architect `G3-A1`–`G3-A4` ≈ security `G3-S1`–`G3-S3`) are one issue** — a ~1.5 req/s GIL-bound capacity ceiling with a health-check cascade, an unauthenticated clip proxy that triples its own outbound rate under throttling (its silent-failure half found independently by architect, security **and** frontend, §2), and unbounded request inputs — all held back only by the basic-auth password Gate 3 removes. Plus two untested wiring paths where every guard is tested but the code arming them is not (`G3-Q1`, `G3-Q2`, the project's signature vacuity class again), and an iOS-audio finding no test can reach (`G3-F2`). **§7 records the live-origin load test**: the calibration half ran and resolved the review's weakest link against the app — the container is ~2.5–3.5× *slower* per request than the laptop, closing the "maybe App Runner is faster" escape hatch; the concurrency-ladder half was blocked by the tool-permission classifier and stays inferred. **§6 separates the owner's calls** (custom domain, telemetry retention, borrow-an-iPhone) from the session's. Nothing adopted; no code changed; the pause is intact. |
| `superpowers/2026-07-26-HANDOFF-track-b-complete.md` | **Superseded on next actions by the 2026-07-27 handoff below; still the record of Track B's completion.** Written at a clean seam after Track B was executed, deployed and reviewed. Names the successor's job — the ten blocking findings, then Track C — and carries seven must-not-revert claims, what the session knew that is not in the durable record, and the open decision with the session's position. **⚠ Its must-not-revert claim 1 was REVERSED on measurement 2026-07-27 and is annotated in place**; the other six stand. |
| `superpowers/plans/2026-07-27-dep33-blocker-remediation.md` | **The `DEP-33` blocker remediation plan** — thirteen tasks, identifiers **`RMD-`**, executing the review's §2 queue. **ALL THREE STAGES EXECUTED 2026-07-27 — `RMD-0`…`RMD-13`; do not execute again.** §0 states why it carries **no factor table** (it compares nothing and runs no arm) and what replaces that discipline: a named mutation per task, which must go red before the test counts. §2 is a **pre-flight table resolving every function, file and config value the plan names** against the tree, including two that are absent *by design* where the absence is the finding. §3 names three stages and two seams at authoring time. **It corrects the queue's own length: nine blockers, not ten** — `QUA-4` and `ARC-9`/`QUA-5` were already closed in `6f49ab9`. |
| `superpowers/2026-07-27-dep33-remediation-execution-log.md` | **The record of `DEP-33` remediation stages 1–3, 2026-07-27.** Identifiers **`RMD-`**. **Stages 1–2 are PR #30, stage 3 is PR #32** — the log spans both. **§0 is the entry worth reading**: three read-only AWS checks run *before* planning, which closed the billing-alarm deferral, confirmed `ARC-2` live, measured `QUA-2`'s premise — and **found a new defect, `RMD-D`**: `ARTISTPATH_CORS_ORIGINS` never reaches the running service, so the deployed API serves `config.py`'s dev default. Measured by drift detection *and* a live probe with a negative control. **The first instance of the signature defect where the gap is between the template and AWS**, which no test in any package can see. Stage 1 rebuilt the suite until **16/16 mutations are caught** (12 previously green, 4 controls) and **records that review §1's own arithmetic does not reconcile**, without inventing a number. Stage 2's `cdk diff` gate **found `ARC-6` live** — a non-blocking finding one command from repointing the service at a floating tag. The closeout's `A4` records that **`cors_origins` is flipped in source and not in production**, so the work is not closed. **Stage 3's own section is appended at the end** and records the one thing worth carrying forward as method: **two mutations were MISSED on their first run, and both were against tests that stage had just written** — one asserted the plaintext password was absent from a page that would have leaked the base64 credential, the other was satisfied by the site's own name rather than by the instruction it was checking for. |
| `superpowers/2026-07-27-HANDOFF-dep33-stages-1-2.md` | **Superseded on next actions by the stage 3 handoff below** — stage 3 is done. Still the record of the plan's second planned seam: the one reversed claim and the six that still stand, and **the one defect that is fixed in source and still live in production**, which is *unchanged* and still live. |
| `superpowers/2026-07-27-HANDOFF-dep33-stage3.md` | **Superseded on next actions by the Track C _handoff_ below (`2026-07-27-HANDOFF-gate2-track-c.md`, not the execution log in the row between) — Track C is DONE.** Still the record of the plan's third and last seam: what must not be reverted, and the owner's decision to accept the live `RMD-6` exposure rather than deploy twice. **⚠ Two of its statements are now overtaken.** Its `RMD-6` section is spent — the exposure was closed by the Track C deploy. And its description of `infra/README.md` §6 as "three ordered passes" was true when written: §6 is now **one command**, because the open decision it put to the owner (script the passes) was answered yes. |
| `superpowers/2026-07-27-gate2-track-c-execution-log.md` | **The record of Track C, 2026-07-27 — the CUTOVER. The app is on the internet and reachable.** Gate 2's last build track. **§1 is the entry worth reading**: Track C was recorded everywhere as "sync the SPA to the bucket", and that is the smallest of its four parts — closing `RMD-6` needed a **new image**, because the fix lives in `config.py`, so a session taking the one-line description at face value would have published against an unfixed API and recorded the defect closed. **§2** covers the §6 script and its ten mutations. **§3 carries three corrections to this project's own record**, the first being that the drift gate would have fired on *every* future deploy: `RMD-6`'s fix deliberately leaves the empty variable in the template, so that drift row is permanent by design and §7 now expects three rows, not two. **§4 records that §8a ran for the first time in the project's history** — every previous check proved only that the site *refuses*. **§5 closes `RMD-6`, `RMD-11` and `FRO-1` with the measurement rather than the assertion**, each checked live on both sides of the deploy. §6 is four deferrals with conditions — **one of which, the double-encoded artist descriptions, was RETRACTED 2026-07-27 and is struck in place with the correction beneath it: the artifact was correct UTF-8 all along and the mangling was in the tool that read the live response**; §9 the image and graph provenance. |
| `superpowers/2026-07-27-HANDOFF-gate2-track-c.md` | **SUPERSEDED 2026-07-27 on next actions** by `superpowers/2026-07-27-HANDOFF-onedrive-migration.md`; **still ACTIVE for its four must-not-revert items**, which are untouched and still govern. Its "use the app, on a phone" is **spent, not wrong** — the phone run happened and passed. *(This row read "The CURRENT handoff" until 2026-07-27, while the row above it claimed the same title for a different document — the exact recurring defect recorded against the Track D row below. Caught by the doc audit, not by the session that created the collision.)* *(Original role: the seam handoff after the cutover.)* Carries four must-not-revert claims (chiefly that §7 now expects **three** drift rows and that `--prune` stays off), what the previous handoff had overtaken rather than got wrong, and **an open item the closeout could not discharge — `B1`'s doc audit was not run**, and the manual sweep substituted for it cannot find defects of omission. |
| `superpowers/TEST-QUEUE.md` | The async **use-the-app queue**. `closeout` appends; `session-start` reads it and flags stale entries. Catches the defect class tests structurally cannot. **⚠ Its standing no-phone constraint EXPIRED 2026-07-27 at the cutover**, and the mobile section deferred since 2026-07-26 is now runnable and folded into the newest entry. |
| `superpowers/specs/2026-07-23-defect-remediation-and-cost-retune-design.md` | **The governing design for resuming Phase 1** (owner-approved 2026-07-23). Track 1: builder fix for the §2.8 tie-break, rebuild, adopt by structural equivalence. Track 2: cost-function retune (currencies included) on the repaired graph, `known` mechanism, one blind listen. Records the owner's 2026-07-23 decisions in §1. |
| `superpowers/2026-07-23-repair-and-retune-execution-log.md` | Retained execution log for the repair+retune work. Track 1 record, the pre-Track-2 guards, and Track 2 continues it. |
| `superpowers/specs/2026-07-23-track2-preregistration.md` | **The Track 2 pre-registration.** Fixes the factor table, primary outcome, effect sizes, pair set, and the read of every possible result *including the null*, before any arm runs — the commit timestamp is the evidence it came first. Where it disagrees with the repair+retune design spec it says so inline (its §8 indexes the five disagreements); those are design corrections, not scope changes. Also the worked example for two CLAUDE.md rules (dormant-term check, pre-registration gate). **AMENDED 2026-07-23/24 — read §9 first:** eleven amendments (A1–A11). **A1–A7 predate any arm; A8 and A9 were made after the A0 gate ran, and A11 after the P4 pilot** — the ones that postdate a result (still before any factorial arm) — A8 resolves that gate (the floor column stays off; the first-path check is referenced to both P and A0), A9 gives the remaining gates effect sizes. **A10 fixes §5's fame-proxy sample before any label was collected** (an S1/S3 overlap that made the sample 29 not 33, and a false claim that S1's labels already existed). Six amendments (A1–A6) on the analyst review's D1–D3 and D5 — the first three had left arms provably incapable of moving; A6 fixes the fame-proxy scoring rule before the owner's one-shot labels are spent. §9's closing table lists what the amendments do **not** discharge (D4, D6, D7, PR-A, PR-B) with success conditions. Its §7 prerequisites are now P1, P4–P8; **P2 is discharged and P3 is withdrawn — do not execute either. P4 RESOLVED (A11): Deezer `nb_fan` failed §5 (2 pre-registered falsifiers); and Wikipedia pageviews fired the coverage falsifier — so the adopted fame proxy is Wikipedia pageviews with an unmatched interior scored at the fame floor (0), the owner's decision replacing §5's owner-labelling fallback, guarded at d15/d20. See A11 and the repair+retune execution log.** |
| `superpowers/plans/2026-07-23-pre-track2-guards.md` | Six structural guards (G1–G6) that had to land between the pre-registration and the Track 2 sweep. **Executed 2026-07-23; do not execute again.** Outcomes in the repair+retune execution log under "Pre-Track-2 guards". |
| `superpowers/specs/2026-07-25-track2f-toll-full-strength-preregistration.md` | **The Track 2F pre-registration**, committed before any arm ran and **EXECUTED 2026-07-25 — do not execute again.** Governs the ceiling-toll dose-response ladder and nothing else. Reuses Track 2's `C1`–`C4` by citation, unchanged; its `TF` identifier series is namespaced and collision-free. Carries a **continuation trigger with its own effect size** (a rebuild decision is not an adoption decision), and **three structural bounds stated before the run** — the toll tests the ceiling's *cheapness* and never its *ordering*, eight of the 24 endpoints are fully saturated so their first hop is unchangeable at any price, and a static toll cannot address F2's depth clause. Result and figures: `builder/analysis/2026-07-25-track2f-toll-ladder/`; narration in the repair+retune execution log's Track 2F entry. |
| `superpowers/specs/2026-07-26-gate2-deploy-and-telemetry-design.md` | **The governing design for Gate 2's AWS deploy and bypass telemetry** (owner-approved 2026-07-26). Identifiers **`DEP-`**. **Nothing implemented yet.** Where it and the roadmap disagree on Gate 2 sequencing, **this governs** — two deviations are named inline: **CI is deliberately out of scope** (the roadmap's own open judgment call 2, taken at the owner's instruction; `DEP-15` records the cost), and S3 graph loading uses an `s3://` URI in the existing `ARTISTPATH_GRAPH` rather than the roadmap's separate `GRAPH_VERSION`. Topology is App Runner + S3 + CloudFront + DynamoDB behind one distribution, which **removes CORS rather than configuring it** (`DEP-8`). **Its §4 verified every roadmap Phase 3 item against source**: three are real defects (`DEP-10` a truncated artifact loads silently because `np.frombuffer` gets no `count`; `DEP-11` sync boto3 blocking the event loop; `DEP-13` no `from == target` guard), **one — invalid `reason` coercion — is already built and needs nothing**, and **`DEP-12` is a defect the roadmap does not list**: cache failures become 500s on the clip endpoint, which `clips.py`'s own docstring forbids. **`DEP-7` is the design choice most worth arguing with** — the telemetry event logs **facts, not computed metrics**, because routing is deterministic so any metric can be derived offline, because choosing production's metric would be a scoring decision taken during the scoring pause, and because the costliest metrics are the ones Phase 1 log §3.8 records as the **worst** predictors of the owner's verdict. §5 states plainly what telemetry **cannot** answer, including value 3's length ceiling (an abandoned journey and a satisfied one are identical from the server). **§11 names three handoff seams at authoring time**; Track A is deliberately AWS-free and leaves the app better even if the deploy never happens (`DEP-18`). **Sets no threshold, adopts nothing, changes no routing weight, no graph and no cost function; the path-quality pause is intact** — §5 argues that building the instrument is not running the experiment. |
| `superpowers/specs/2026-07-25-f1-minimum-stop-design.md` | **The F1 design** (every journey needs at least one stop). Mechanism: run the search as today; if the result is exactly the two chosen artists, run it again with their direct connection forbidden — the existing cost function chooses the detour, so this is not path-quality work. Defines the `stop_rule` wire field (`natural` / `forced` / `adjacent_only`) and cites `findings/2026-07-25-mutual-knn-stranding.md` (`MKS-6`) for why some pairs cannot be given a stop at all. **Implementation has landed** on `f1-min-stop`, per the plan below; still ACTIVE because closure awaits the owner's queued use-the-app check, not because the design is unsettled. |

### Complete

| Document | Covers |
|---|---|
| `superpowers/specs/2026-07-21-phase2-path-quality-design.md` | Phase 2 design. **IMPLEMENTED and adopted 2026-07-22** (`capfix`); §8 risk 4 discharged. Supersedes the roadmap's Phase 2 content only. Its open questions were answered by the sweep — do not read it as a live agenda. |
| `superpowers/plans/2026-07-21-phase2-path-quality.md` | Phase 2 implementation, 16 tasks. **All executed; do not execute again.** Tasks 14–16 were superseded mid-flight by the revised plan below. Several of its inline test fixtures are arithmetically wrong — see the execution log §3 before trusting one. |
| `superpowers/plans/2026-07-22-phase2-revised-plan.md` | The mid-flight amendment governing Phase 2 Tasks 14–16, written by the owner after a full review. **Retained as the record of *why* the last three tasks differ from the original plan** — its §6 is what reclassified C3 into Phase 1. |
| `superpowers/specs/2026-07-19-artist-path-alpha-design.md` | Original alpha design. Still the reference for determinism (§9) and the `APG1` format. |
| `superpowers/specs/2026-07-20-stage3-web-frontend-design.md` | Frontend design. |
| `superpowers/plans/2026-07-19-graph-builder.md` | Builder implementation. Shipped. |
| `superpowers/plans/2026-07-20-path-engine-api.md` | API implementation. Shipped. |
| `superpowers/plans/2026-07-20-stage3-web-frontend.md` | Frontend implementation. Shipped. |
| `superpowers/findings/2026-07-22-doc-audit-context-layer.md` | Documentation audit, navigation layer. |
| `superpowers/findings/2026-07-22-doc-audit-technical-record.md` | Documentation audit, specs and plans. |
| `superpowers/2026-07-25-gate1-clips-and-ux-execution-log.md` | **The record of the Gate 1 clip and frontend-UX work, 2026-07-25** (PR #19). Both clip defects and all four dogfooding UX items. Carries three defects found **in the roadmap itself** — its stated cause for the wrong-artist bug was inaccurate as *located*, and its prescribed fix applied literally would have silenced every card. Also the mutation-check result, the deferred findings with success conditions, and the note that **C1/C2 are fixed but not closed** until the queued use-the-app check returns. Touches no routing, no graph, no weight. |
| `superpowers/findings/2026-07-25-doc-audit-gate1-clips-ux.md` | `doc-auditor` report from the 2026-07-25 closeout (B1). Five findings, four actioned; **its recommendation to expand bare identifiers inside `TEST-QUEUE.md`'s steps was rejected** — `closeout` C1 forbids naming identifiers in owner-facing steps, and the entry correctly has none. Its identifier census and previous-findings check are the parts worth citing. |
| `superpowers/2026-07-25-f1-minimum-stop-execution-log.md` | **The record of the F1 work, 2026-07-25** (PR #23) — every journey now gets at least one stop, or says why it cannot. Identifiers namespaced **`FMS-`**. Carries the four decisions with reasoning, **three defects found in the plan itself** (`FMS-P1` a verification check that could not fail; `FMS-P2` the false premise that adjacency implies the direct edge is cheapest; `FMS-P3` a figure restated under a citation pointing at the wrong identifier), the gate outcomes **including two failed reviews**, and the operational measurements it owns. **Restates no figure it does not own**; the graph figures are `findings/2026-07-25-mutual-knn-stranding.md`'s. Touches no weight, no config knob, no graph. |
| `superpowers/2026-07-25-HANDOFF-f1-minimum-stop.md` | **⚠ SUPERSEDED — Gate 1 is DONE and `F1` was discharged 2026-07-26.** Both this row and the file itself said F1 was "built but NOT discharged" until 2026-07-27; the discharge is recorded in `2026-07-26-HANDOFF-deliverability.md` and `NEXT.md`, which own status. Still the record of the F1 seam. *(Original role: **the CURRENT handoff for Gate 1, 2026-07-25**)* — written at a clean seam, nothing in flight. Supersedes `2026-07-25-HANDOFF-clip-playback.md` **on F1 only**. Records that F1 is **built but NOT discharged** (its condition is an observation — the queued use-the-app entry), the four things a successor must not get wrong, and what was decided against. |
| `superpowers/findings/2026-07-25-doc-audit-f1.md` | `doc-auditor` report from the 2026-07-25 F1 closeout (B1). Seven High, two Medium. **Its highest-value findings were two fresh identifier collisions in the F1 execution log itself** (`D1`–`D4` against the protocol review's defect series, `P1`–`P3` against the Track 2 prerequisites), caught within the hour and renamed to `FMS-` before the document was cited anywhere — the clearest instance yet of the rule that the audit is not skippable. All seven High actioned. |
| `superpowers/plans/2026-07-23-track1-tiebreak-remediation.md` | Track 1 implementation plan. **Executed 2026-07-23; do not execute again.** Outcomes in `2026-07-23-repair-and-retune-execution-log.md`. |
| `superpowers/specs/2026-07-22-c3-bypass-diagnostic-experiment.md` | C3 diagnostic protocol. **Executed** (Stage 0); Stage 1 superseded by Track 2. Results in the Phase 1 log §3. |
| `superpowers/specs/2026-07-22-c3-known-mechanism-blind-listen.md` | C3 `known`-mechanism blind-listen protocol. **Executed**, verdicts recorded. Results in the Phase 1 log §3.6–§3.9; the A-vs-C re-run condition is the 2026-07-23 spec §4.4. |
| `superpowers/findings/2026-07-27-doc-audit-full-project.md` | **The full-project documentation audit, 2026-07-27** — a one-time sweep of the whole corpus (~45,000 lines) run at the owner's instruction after `doc-auditor` moved from `haiku` to `sonnet`. Identifiers **`DAF-`**. **Owns no figures.** Ten parallel slices, because the corpus is ~11× the auditor's own single-pass budget and one broad run would have skimmed and reported as an audit; slice I was cross-cutting, since partitioning destroys the two checks that are inherently global. **`DAF-1` is the headline: the content is sound and the *status layer* is what decayed** — measurements reproduce, the one-record rule holds, code citations mostly resolve, but documents had stopped saying what they are. **`DAF-2`** found five merged plans reading as pending, worst of them a thirteen-task non-idempotent AWS plan still marked "not yet executed". **`DAF-3`**: the `DRV-1` provenance caveat sat inside the one section it does *not* cover, while two documents told readers to open one of the uncovered ones first. **`DAF-4` is the one to read on tooling** — a `haiku`-era `B1` run certified `docs/README.md` "entry complete" for a document this map had never mentioned, the absence class the auditor names as its own blind spot; it is recorded with an explicit note that one instance is not a controlled comparison. **§3 registers eleven previously unrecorded identifier collisions** (nothing renamed — forward-only), of which `DAF-C10` is the sharpest: a document's own collision census declared `T1` clean and was wrong. **§5 records two reported findings that did NOT survive verification**, deliberately. §6 lists what was changed; **§7 is what was left for the owner**, chiefly anything touching the budgeted standing layer. |
| `superpowers/findings/2026-07-26-doc-audit-context-layer-maintenance.md` | `doc-auditor` report from the 2026-07-26 closeout (B1), covering PR #26. Two High, two Medium. Its most valuable finding was the **absence class it was specifically pointed at**: `2026-07-25-HANDOFF-clip-playback.md` §5 still recommended "build F1 next" as a live position, unmarked, in a document whose top banner a mid-document reader never sees — actioned. **One of its findings is refuted and must not be re-actioned:** it carried forward a previous audit's claim that this map lacks a `**Role: AUTHORITATIVE**` marker in its first ten lines; the marker is on **line 3**. *(Corrected 2026-07-27: this row previously said the marker "has been throughout", which is wrong — git shows it was added on 2026-07-25 in `8a4450e`, as the fix for the 2026-07-25 audit's finding. The 2026-07-26 report's defect is therefore that it carried a fixed finding forward without re-checking, not that the finding was never true.)*. Its `export_paths.py` line-reference correction was right and was applied to both `FMS-N1` and `CLM-5`. Its `CLM-7` finding — that naming a gap without enumerating it leaves the successor no way in — produced the four-file table now in that log's §4. |
| `superpowers/2026-07-26-context-layer-maintenance-execution-log.md` | **The record of the skills-and-agents work, 2026-07-26** (PR #26). Identifiers namespaced **`CLM-`**. Touches no code, no graph, no weight. Exists mainly because closeout **D6** requires the standing-layer delta in a retained log rather than only in commit messages — §4 is that record, and it is where `CLM-7` was found: **`memory/` grew 435 → 469 lines with the delta recorded nowhere**, which is exactly the blind spot D6 names `memory/` for. Also carries **`CLM-5`** (a claim this session committed and then corrected: `FMS-N1`'s "adjacency is rare" was an inference presented as fact, and wrong twice over), **`CLM-6`** (`CLAUDE.md` may be wrong that `SKILL.md` **bodies** load unconditionally — if so D6 over-counts; not acted on, it is the owner's call, and the cheap check is named), **`CLM-1`–`CLM-4`** (four decisions taken *against*, which leave no other artifact), and **`CLM-8`–`CLM-10`** (what the session knew that was otherwise unwritten, including the owner's evidence that A5's detached relaunch survives a terminal closing). **`CLM-13` withdraws a claim from this log's own §7**: a file reported as unbacked is deliberately ignored by a `.gitignore` *inside* its directory, which the session could not see because it checked from a branch where that directory does not exist. **Whether a file is ignored is branch-dependent when the rule governing it is branch-scoped** — a trap that recurs across `builder/analysis/`'s dated directories, and one that `git status` alone cannot answer. |
| `superpowers/2026-07-26-stranding-causes-execution-log.md` | **The record of the stranding-causes work, 2026-07-26** (PR #25). Identifiers **`STC-`**. Touches no code, no graph, no weight. Carries the six decisions with reasoning — including **`STC-1` as a defect in the task's own framing** (the "crawl-frontier" cause it named is nearly empty, and the name invites a remedy that reaches almost none of the population it appears to name), and **D-4, the first report being fluent, self-consistent and wrong in its headline** until the source-offered denominator was added. Four gate outcomes, two of them designed to abort. **`STC-6` records a claim this session committed to draft and then corrected**, having run the falsifier it had named as open rather than shipping the open question. The two success conditions live here. |
| `superpowers/findings/2026-07-26-low-degree-synthesis.md` | **An INTERPRETATION of three committed measurements. AUTHORITATIVE for nothing, and it OWNS NO FIGURES** — every quantity is cited to `MKS-`, `CNS-` or `STC-` and their analysis directories; where it and a cited document disagree, **the cited one wins**. Identifiers **`SYN-`**. Assembles a reading produced by an independent consulting session across `findings/2026-07-25-mutual-knn-stranding.md`, `findings/2026-07-26-low-degree-census.md` and `findings/2026-07-26-stranding-causes.md`, none of which interpreted across the others. **`SYN-1`**: the low-connection population is three problems whose remedies are a rebuild, a full re-crawl, and nothing — costs that have been discussed as one decision. **`SYN-2`**: against the whole artifact the both-ways rule is a small cause by headcount and the dominant one among recognisable artists; it flags that *cap-stranded* and *rescuable* are different columns of the report's table and must not be swapped. **`SYN-3`** ranks the co-occurrence cut-off as the largest lever on headcount (mechanism is `STC-6`'s; only the ranking is new). **`SYN-4`** is a falsifier with its numeric expectation **committed before the measurement that tests it** — it fires if low-connection artists routinely appear as interior cards; resolved by `findings/2026-07-26-committed-walk-deliverability.md`. **`SYN-5`** records a wrong consulting prediction (right in shape, wrong in cause) rather than deleting it. **`SYN-6` and `SYN-7` were added 2026-07-26** and are **derivations from committed documents, not measurements**: `SYN-6` audits both blind listens against their own arm tables and finds that **neither varied the both-ways rule against a bounded-degree alternative** — Phase 2 log §12 varied reciprocity, the degree bound and the top-k ranking basis together, §16 ran `mutual_knn` on both arms, and the C3 `known`-mechanism listen pins every arm to one artifact — so **the ear has endorsed bounded degree and has never been asked about reciprocity**; inline pointers were added at the three places that lean on the shorthand (this map's `MKS-` row, that document's §3, and Phase 1 log §2.10's closing line), and **`MKS-5b`'s force is unchanged, only its currency**. `SYN-7` records that **both verdicts (2026-07-22) predate five of `WHAT-GOOD-LOOKS-LIKE.md`'s nine values** (5–7 dated 2026-07-23; 8–9 dated 2026-07-24), so a verdict cannot be cited as endorsing a criterion articulated after it; **neither item re-opens a verdict or argues for a third listen.** Its **§3 also records a methodological error — a consulting session proposed settling by ordinary use a question no amount of use could settle, because a missing connection and a priced-out connection look identical from the user's seat.** Its **§3 names `SYN-2` as the item most likely to fail** — "recognisable" leans on in-graph popularity as fame, and `CNS-2` contaminated the proxy that validated it. **§4 is a fenced opinions section**: a consulting position that the both-ways rule should *not* be loosened and that any targeted alternative must demonstrate its bound by simulation first. **Nothing adopted, nothing proposed, no threshold set; the path-quality pause is intact and this is explicitly not a resume signal.** |
| `superpowers/findings/2026-07-26-committed-walk-deliverability.md` | **AUTHORITATIVE for its own interpretation; OWNS NO FIGURES** — every quantity belongs to `../builder/analysis/2026-07-26-committed-walk-deliverability/`, whose `REPORT.md` is the deliverable. Identifiers **`CWD-`**. A read-only re-read of the `P` (user-facing baseline) arm in Track 2 and Track 2F, run to **resolve `SYN-4`**. **`CWD-1`: the falsifier did not fire** — across every interior card in the committed walks, none was a low-connection artist, against a graph where more than half of all nodes are. **`CWD-2` is the half that must be quoted with it**: the pair set was pre-registered with famous endpoints, so a low share was the *expected* result — the test could fail meaningfully and **could not pass meaningfully**, which `SYN-4` fixed in advance. Quoting `CWD-1` alone gets the finding backwards (`STC-3`'s shape). **`CWD-3`: the three committed runs are byte-identical and are one sample, not three** — recorded because the first version of the measurement counted all three and inflated every interior-card figure threefold, self-consistently. Explicitly **does not** show the router *cannot* deliver a low-degree artist, does not reopen §2.12, and its distinct-artist count is a **lower bound**. Nothing adopted, proposed, rebuilt or reconfigured; the pause is intact. **§7 was added 2026-07-26** when the follow-up below ran: `CWD-6` records that the **DO NOT APPEAR** branch fired, so `CWD-1` was *not* an artefact of its pair set, and `CWD-7` records a wrong prediction. §5's pricing-versus-arithmetic confound is **not** resolved by it. |
| `../builder/analysis/2026-07-26-obscure-pair-deliverability/` | **AUTHORITATIVE for its own measurements.** The missing arm for the committed-walk run above: same instrument, same production settings, same walk and depths (imported from the committed scorer, not transcribed), on **24 pairs sampled by node id from the graph's own connection-count distribution** — 12 obscure-endpoint and 12 ordinary-endpoint — instead of the famous pairs Track 2 pre-registered. **Its decision rule, thresholds, sampling seed and a stated prediction were committed BEFORE it ran (`8aa6f84`)**; the git order is the evidence. **Owns** the distinct-artist counts, the degree distribution, the per-stratum split and the minimum observed. Verdict: **DO NOT APPEAR** — low-connection artists stayed rare even when both endpoints were obscure. **Explicitly does not separate pricing from arithmetic** (a barely-connected artist has fewer chances to lie between two others), does not show these artists are undeliverable — eighteen distinct ones were delivered — and says nothing about path quality or famous journeys. **Not a `findings/` document, deliberately** — a second copy would restate its figures. Cite this directory; interpretation is `findings/2026-07-26-committed-walk-deliverability.md` §7. Nothing adopted, proposed or rebuilt; the pause is intact. |
| `superpowers/2026-07-26-deliverability-execution-log.md` | **The record of the synthesis and deliverability work, 2026-07-26** (PR #25). Identifiers **`DLV-`**. Touches no code, no graph, no weight. Carries the decisions with reasoning — including **`DLV-1`**, that both runs' reads were committed before the measurements that test them, and **`DLV-5`**, a deliberate and flagged exception to the one-figure rule. **`DLV-7`/`DLV-8` are defects found in the task's own framing**: a conflation of *cap-stranded* with *rescuable*, and a degree floor sized as a small change when nothing was delivered below four connections. **`DLV-9` is a correction this session made to its own work** — the first deliverability measurement counted three byte-identical copies of the same deterministic run as three samples and over-counted threefold, self-consistently; what exposed it was the three "independent" totals being identical, not code review. Also the gate outcomes, the B3 mutation check, and the D6 net-zero record. |
| `superpowers/2026-07-26-HANDOFF-deliverability.md` | **The CURRENT handoff for this branch, 2026-07-26 (later)** — a clean seam, nothing in flight. Supersedes `2026-07-26-HANDOFF-stranding-causes.md` **on next actions only**; that handoff remains the record of its own work. Carries the five claims a well-meaning editor must not revert — chiefly that `CWD-2`'s "could not pass meaningfully" caveat is **not** superseded by the second run agreeing with the first, and that §5's confound stays open. **Points a resuming session at the resume brief instead of itself.** |
| `superpowers/2026-07-26-HANDOFF-stranding-causes.md` | **The handoff for the stranding-causes work, 2026-07-26** — a clean seam, nothing in flight. Supersedes `2026-07-26-HANDOFF-low-degree-census.md` **on next actions only**; the census handoff is still the record of its own work. Carries the five claims a well-meaning editor must not revert, what the session knew that was otherwise unwritten, and the open position with what it would do. |
| `superpowers/2026-07-25-HANDOFF-clip-playback.md` | **COMPLETE — superseded on F1 by `2026-07-25-HANDOFF-f1-minimum-stop.md`, and Gate 1 has since closed.** The handoff at the clip-playback seam. **Added to this map 2026-07-27**: it had no row of its own and was reachable only through a mention inside another document's row, despite this map claiming to classify every document in `docs/`. |
| `superpowers/2026-07-26-HANDOFF-low-degree-census.md` | **COMPLETE — superseded on next actions by `2026-07-26-HANDOFF-stranding-causes.md`**; still the record of the census work's own seam. **Added to this map 2026-07-27**, same absence as the row above. |
| `superpowers/2026-07-26-low-degree-census-execution-log.md` | **The record of the low-degree census, 2026-07-26.** Identifiers **`CNS-`**. Touches no code, no graph, no weight; read-only by construction. **Added to this map 2026-07-27 — it was not mentioned anywhere in this file at all**, while its own closeout recorded the `B1` doc-auditor step as "CLEAN … `docs/README.md` entry complete." That audit ran on the `haiku` model and certified a map entry that did not exist: the absence class the auditor names as its own known blind spot. |
| `superpowers/plans/2026-07-25-f1-minimum-stop.md` | F1 implementation plan, six tasks. **Executed 2026-07-25** on branch `f1-min-stop`; do not execute again. **F1 itself was discharged 2026-07-26** — this row's closing sentence below describes the state before that happened. Design: `superpowers/specs/2026-07-25-f1-minimum-stop-design.md`. Task 6's verification script and use-the-app queue entry are the observation that closes F1 — the plan itself says not to mark F1 discharged until the owner runs it. |

### Partly historical — check before citing

| Document | Status |
|---|---|
| `superpowers/findings/2026-07-19-listenbrainz-probe.md` | **Superseded for scoring and normalisation** (it describes per-artist normalisation, deleted in `284366c`). Everything else — endpoint schema, rate limits, the 1,000-artist sitewide cap, the §6d–6f popularity-source eliminations — is still valid and load-bearing. **Read §6d–6f at its actual scope:** it eliminates external **popularity** from the **routing cost terms** (§6e's reason is that `|pop(a) − pop(b)|` and the floor compare two nodes of one graph, so the quantity must be defined on that graph's population), and §6f marks even that *"an alpha decision, not a permanent one"* with a named replacement condition. It says nothing about **fame**, a different currency — an external fame source has already passed a gate (Wikipedia pageviews, pre-registration amendment A11). **External fame sources are gated, not banned;** this row's earlier wording had been read three times as a general barrier. |
| `superpowers/findings/2026-07-21-architecture-review-and-path-baseline.md` | **Superseded for scoring and metrics.** Retained as narrative history of how the reviews unfolded. Its architect and QA findings in §1 (clip 500s, artifact length validation, sync boto3 on the event loop) are **not** superseded and remain actionable. |

### Narrative — never use as context

| Document | Status |
|---|---|
| `how-we-map-similar-artists.md` | A dev-blog journal of the modelling journey, written for humans. **Never cite it, never use it as context, never instruct development from it.** It is not maintained to the standard of project context and will lag reality. Update it when there is major progress worth journalling; correct factual errors when found. |

### External — not project documentation

| Document | Status |
|---|---|
| `reference/` | Third-party material consulted during early planning. **Not project documentation and not for routine context.** See `reference/README.md`. |

---

## Where else information lives

- **Persistent memory** — outside the repo, loaded into every session, at
  `~/.claude/projects/C--dev-music-app/memory/`. The
  `MEMORY.md` *inside that directory* is the index; each sibling file holds one fact.
  Memory holds **pointers and working preferences, not figures**.
- **`.claude/agents/ml-graph-analyst.md`** — a reusable analysis-only subagent for graph,
  scoring, and metric questions. It has no `Edit` tool by design.
- **`scripts/docs-lint.sh`** — the mechanical half of a documentation audit: role markers,
  this map's own completeness, dead relative links, plans claiming to be live, duplicate bare
  identifiers, restated figures. ~90 seconds, deterministic. **`closeout` B1 runs it before
  dispatching `doc-auditor`.** A document that legitimately *quotes* other documents' link
  syntax may opt out of the link check with `<!-- docs-lint: skip-links — reason -->`; one
  file does. **A green lint is not a clean audit** — it cannot see whether a banner is
  attached to the right section, which is what the 2026-07-27 sweep's best finding was.
  Its positive control is `scripts/docs-lint-selftest.sh`, which proves every hard check can
  go red; it caught a real bug on first run (`grep -i executed` matches "not yet executed",
  so the check would have cleared the exact plan it exists to catch).
- **`.claude/agents/consultant.md`** — **not a subagent, and a working session must never
  dispatch it as one.** It is launched as its own session (`claude --agent consultant --tools
  Read,Grep,Glob`), reads the committed project record but never the code, and gives one
  reasoned recommendation on one named decision, then stays in conversation. Its whole value
  is that its inputs are the owner's rather than another session's paraphrase — dispatching it
  from a working session destroys exactly that. **Added to this list 2026-07-27**: the agent
  had existed and been used for some time while appearing in none of the entry points, so it
  was discoverable only by listing `.claude/agents/` directly.
- **`.claude/agents/doc-auditor.md`** — the project-local fork of the global auditor, and it
  **shadows** the one in `~/.claude/agents/` inside this repo (project scope outranks user
  scope). It carries two checks the global one does not: **I**, the identifier census — no
  two load-bearing objects may share a name, and owner-facing text may not use a bare
  letter-number token without its plain-language sentence; and **J**, reads-of-results
  completeness for pre-registrations. It reports and never edits, and it may never propose
  renaming an identifier in a committed document. Dispatched by `closeout` B1.
- **The code is the truth about the code.** Where a document and the source disagree
  about behaviour, the source wins — and the document is a defect to be fixed.

---

## Adding a document

State its role in the first ten lines. Add it to the table above. If it supersedes
something, put a banner on what it supersedes **and mark the superseded claims inline**,
not only at the top of the file — a reader who lands mid-document never sees the banner.
