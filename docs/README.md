# Documentation map

> **📍 The deployed app is `https://unsung.fm`, since 2026-09-03.** Rows below describe frozen
> documents that name `musicapp.cmiller.io`; those descriptions are accurate to the documents
> and are not edited. The old host 301s to the new one with the path preserved. The app is
> still *named* "Artist Path" in the UI — only the address moved.

**Role: AUTHORITATIVE.** The current classification of every document in `docs/`. Where
another document disagrees with this one about a document's role, this wins.

**Read this before citing any document in `docs/`.** It says which documents are
authoritative, which are historical, and which must never be used as context.

Every document here has exactly one **role**. The role determines how much you can
trust it and whether you may act on it.

| Role | Meaning |
|---|---|
| **AUTHORITATIVE** | Current and correct. Cite it. If another document disagrees, this one wins. |
| **ACTIVE** | Work in progress. Current intent, but not yet reality. |
| **COMPLETE** | Describes work that shipped. Accurate as history; may not describe the code today. |
| **HISTORICAL** | Superseded. Retained so the reasoning can be audited. **Do not cite. Do not act on.** |
| **NARRATIVE** | Written for humans, not for engineering. **Never use as context for development.** |
| **EXTERNAL** | Not ours. Third-party reference material. **Not project documentation.** |

---

## The one rule that matters most

**Measured figures about similarity scoring, hub-seeking, and path quality live in
exactly one file:**

> ### `superpowers/findings/2026-07-21-scoring-adjudication.md`

Every other document **cites it by section number and does not restate its numbers.**
Its **§6** is a table marking prior claims as upheld, overturned, or unresolved —
check there before trusting any scoring claim you find anywhere else.

This rule exists because five documents each kept their own copy of the same figures
and drifted into three mutually contradictory positions. Two separate analyses were
invalidated before anyone noticed. **Restating a correct number is still a violation** —
that is how the drift began.

---

## Current state

> **⚠ For *what to do next*, read [`superpowers/NEXT.md`](superpowers/NEXT.md) — it owns
> status and sequencing, and this section does not.** The bullets below are retained for the
> **defect record and the standing hazards**, which are this map's job: what you must know
> before citing the documents it classifies. They lag: as of 2026-07-26 they do not mention
> Gate 2's Tracks A, D or B, and the handoff they name as current is superseded. Where they
> disagree with `NEXT.md` about status, `NEXT.md` wins; where they disagree about a *defect*,
> the owning document wins. Added 2026-07-26, when a status summary in `CLAUDE.md` went three
> tracks stale because three documents each kept a copy.
>
> ⚠ **2026-07-28: path-quality work is UNPAUSED by owner decision.** Every "the pause is
> intact" tail in the bullets and rows below was true when its document was written and is
> **not** a claim about today — the governing document for the resumed work is named in
> `NEXT.md` (the Track 3 pre-registration).

- ⛔ **Repair + retune is DONE. Track 1 was adopted; Track 2 (2026-07-24) and Track 2F
  (2026-07-25) both returned nulls; the ceiling **ordering** measurement (2026-07-25) came
  back **WIDE** — nothing adopted at any point.** Current handoff:
  [`superpowers/2026-07-25-HANDOFF-track2f-and-headroom.md`](superpowers/2026-07-25-HANDOFF-track2f-and-headroom.md),
  which supersedes the 2026-07-24 one **on next actions only**. **The execution log's last two
  entries are fresher than any handoff.** Track 2F ran the ceiling toll at full strength — the
  previous handoff's own recommendation, now executed — and exhausted that mechanism with its
  bound holding, which Track 2's corner arm never managed. The half it structurally could not
  reach, the ceiling's *ordering*, was then measured read-only and is WIDE. **Live candidate: a
  builder-side p99 rescale, still un-pre-registered and blocked behind the nameless-artist
  decision.** **Next action is the owner's.** **Before that rescale is pre-registered, read
  [`superpowers/findings/2026-07-25-router-ascent-gradient.md`](superpowers/findings/2026-07-25-router-ascent-gradient.md)** —
  a re-reading of the P8b review's ascent measurement, which argues the rescale is likelier to
  yield *better-chosen famous* artists than *more obscure* ones, and names two re-reads of
  already-committed runs that would settle it without a rebuild. It owns no figures and
  proposes no arm. The defect record and diagnosis below remain essential reading before touching
  Phase 1 — the three conflated quantities it warns about are still live hazards.
  **Read [`superpowers/2026-07-22-phase1-execution-log-and-graph-defect.md`](superpowers/2026-07-22-phase1-execution-log-and-graph-defect.md)
  §2 before any Phase 1 work — and read §2.12 first, because it retracts a central claim of
  §2.9 and corrects §2.10 and §2.11.**
  - **What is settled.** Two real defects in the **adopted-at-the-time** artifact (figures
    owned by the sections cited, not restated here): the most popular artists were among
    the least connected, caused by an MBID tie-break over p99-clipped scores (§2.8); and
    famous→obscure edges were depleted well below a structure-preserving null, caused by
    the mutual-k-NN reciprocity rule (§2.10). Routed paths never present an artist below a
    very high popularity percentile, at any bypass depth, on either graph tested (§2.9). ⚠ **§2.11's and §2.12's tables were measured on `graph-t15-capfix.bin`, not the adopted artifact** — see `superpowers/findings/2026-07-25-consulting-derivations.md` `DRV-1`, **narrowed 2026-07-26; it previously named §2.9–§2.12 and that was over-broad.** The conclusions are argued to survive (`DRV-2`); **those two sections'** individual figures must not be quoted as facts about the running app until re-measured. **§2.10 is NOT exposed, and its reciprocity attribution above stands for the graph in use:** its "§2.8's tie-break is not this" table measures the adopted artifact's own topology — the adoption record states the fix's topology is that of §2.8 Arm 2 and of `rankfix`, §2.13 C2 records those as identical, and the two manifest sidecars agree on node and edge counts while differing only in `similarity_rescale` — and §2.10's figures ride on a **fixed pre-cap popularity reference** rather than on emitted scores, so they do not turn on which rescale the artifact carries. **§2.9 needs no provenance caveat to be barred from quotation:** §2.10 supersedes its figures and §2.12 retracts its central claim, so it is already barred on its own terms.
  - **What it is NOT.** Not primarily a graph problem. Obscure artists are a small, fixed
    number of hops from every famous artist tested, and even the most-connected famous
    artists have a handful of admissible `known` substitutes (exact counts: §2.12).
    **§2.12 concludes this is a cost-function problem** — the router prices the exits
    correctly and declines them.
  - **Three quantities that are not interchangeable** and have each caused an error here:
    **degree ≠ fame** (§2.6), **popularity ≠ fame** at the top of the distribution (§2.11),
    and **raw popularity ≠ percentile** (§2.12). Do not trust any
    `top1pct_degree_frac` (named `hubfrac` before 2026-07-23) or payload figure,
    and check which currency a claim is in before acting on it.
  - **Decided 2026-07-23:** the owner chose **repair + retune** — governing design
    `superpowers/specs/2026-07-23-defect-remediation-and-cost-retune-design.md`; its §1
    also records the nine-names verdict (in-graph popularity ≠ fame at the top,
    confirmed) and the owner's relaxation of the listening-test prohibition.
  - **Track 1 is DONE, 2026-07-23: the §2.8 tie-break is fixed and the rebuilt artifact
    is adopted.** It routed on `graph-t15-tiebreakfix.bin` — identity and
    verification in `superpowers/findings/2026-07-23-tiebreak-fix-adoption.md` (the
    authoritative record for **that** artifact's checksum). **⚠ SUPERSEDED 2026-08-06:
    the app now routes on `graph-msw-tu50.bin`, adopted and deployed by `MSW-` Task 11 —
    its identity is owned by `superpowers/2026-08-05-msw-execution-log.md`'s Task 9
    section and the artifact's manifest sidecar. The 2026-07-23 record remains correct
    about the artifact it names and is no longer the live one.** Radiohead is back;
    famous-artist neighbourhoods are score-ranked, not MBID-ranked.
  - **Track 2 is COMPLETE and returned R0, a full null, 2026-07-24.** All 15 arms scored over
    two stages; the best reached −0.177 against a −1.0 threshold. **Nothing adopted, no shipped
    code changed, no blind listen run.** Firmest finding, and it needs no fame measurement: the
    **floor device is never pivotal** — the two floor arms are byte-identical to their base
    across every scored cell while the floor term fires 3× more often than production's, so the
    only depth-graduated device in the cost function cannot do its job. **R0 does NOT license
    "the repricing family is exhausted"** — A17(c)'s falsifier fired. Figures are owned by
    `builder/analysis/2026-07-24-track2-arm-scorer/`; the execution log narrates them.
- **Gate 1 (personal use).** Phase 2 (path quality) is **COMPLETE**, 2026-07-22. All 16
  tasks executed on branch `phase2-path-quality`. The blind listening test was run and the
  owner adopted the **`capfix`** arm — `cap_strategy="mutual_knn"`,
  `similarity_rescale="p99_log_clip"`, `similarity_damping=0.0`. See execution log §16
  (verdict) and §17 (adoption). **That adoption is not overturned** — but §2 of the Phase 1
  log is information that was not available when it was made.
- **Phase 1 resumed 2026-07-23; Track 1 is done and adopted, and Track 2 has now run and
  returned a null.** C3
  (`w_floor` is a no-op; `known` degrades to a bare hard exclusion — a *pathfinding*
  defect rather than the UX item it was filed as) is now folded into Track 2's
  cost-function retune, per
  `superpowers/specs/2026-07-23-defect-remediation-and-cost-retune-design.md` §4. Current
  sequencing: see that spec and
  `superpowers/2026-07-23-repair-and-retune-execution-log.md`. **Clips (C1, C2) and the four
  frontend UX items are DONE and CLOSED 2026-07-25** — built in PR #19, completed in PR #20,
  and **confirmed in use**. ⚠ **C1 is nonetheless not fully closed:**
  [`superpowers/findings/2026-07-25-bypass-depth-use-run.md`](superpowers/findings/2026-07-25-bypass-depth-use-run.md)
  `BYP-13` records a card that played a clip by a *different artist of the same name*, found in
  use on 2026-07-25. C1 was closed on a worked example where a band name collided with a song
  title; this is an **artist-name collision aggravated by a rename**, a different route to the
  same failure, and it is live. Not path-quality work, not paused. Record:
  `superpowers/2026-07-25-gate1-clips-and-ux-execution-log.md` (§15–§19 carry the clip
  closure and two playback defects found in that use). **F1 — zero-intermediary paths — is
  the remaining Gate 1 item**: the owner has decided every journey needs at least one stop.
  Implementation has landed, on branch `f1-min-stop` — design
  `superpowers/specs/2026-07-25-f1-minimum-stop-design.md`, plan
  `superpowers/plans/2026-07-25-f1-minimum-stop.md` — and it is not inside the path-quality
  pause (§16). Not yet closed: closure needs the queued use-the-app check (plan Task 6),
  which only the owner can run.
  Two items carry in from Phase 2 with success conditions; see the roadmap's Phase 1 section.
- **Two Phase 2 predictions did not survive measurement**, and older prose still asserts
  them. Damping ("C4") was tested at 0.25 / 0.5 / 0.75 and **rejected** — the undamped arm
  won. Neighbour-set Jaccard was **not** adopted as the primary objective; the
  overlap-family metrics sign-flip between slices at these effect sizes (adjudication §6
  claims 41–42). The p99 ceiling defect **survives adoption** and is carried to Phase 1.
- `superpowers/plans/2026-07-22-phase2-revised-plan.md` governed Tasks 14–16 and supersedes
  the remaining tasks of the 2026-07-21 plan. Both are now historical.

---

## Every document, by role

### Authoritative

| Document | Covers |
|---|---|
| `superpowers/NEXT.md` | **The next action, and the only document that states it.** Gate state, what is closed and must not be re-planned, what is waiting on the owner, and the path-quality pause. `CLAUDE.md`'s orient table points here rather than restating it — that row went three tracks stale as a summary, 2026-07-26, which is why status now follows the same one-document rule as figures. Rewritten wholesale at every `closeout`; owns no figures, and **owns no git state** (merge state is `gh`'s — it is written before the merge every time, so a session finding it silent there has found the design). Superseded status blocks are demoted to `superpowers/NEXT-ARCHIVE.md`, not kept inline; **2026-09-05 it was cut from 2,063 lines to 328** after five weeks of monotonic growth caused by demoting without distilling. **Where a handoff or an execution log disagrees with it about status, the fresher of those two wins and this document is stale** — fix it rather than working around it. |
| `superpowers/NEXT-ARCHIVE.md` | **HISTORICAL — frozen, and never edited.** The 38 superseded status blocks that were `NEXT.md`'s live top block between 2026-08-01 and 2026-09-01, split out 2026-09-05. **Never read it for status** — `NEXT.md` owns that, and nothing here is current by construction. **Never read it for standing constraints either**: anything that still binds belongs in `NEXT.md`'s *Closed* / *Must not be changed* / *Deferred* registries, and if something here looks load-bearing and is not in one of those, that is a distillation defect to fix forward in `NEXT.md` — not a reason to edit this file. The supersession annotations inside the blocks were accurate when written and are not maintained. |
| `superpowers/findings/2026-07-21-scoring-adjudication.md` | **All** scoring, hub-seeking, and path-quality figures. The single quantitative record. |
| `superpowers/PRODUCT-REQUIREMENTS.md` | **The owner's product requirements — Must / Should / Expect, 2026-07-29.** The requirements layer above `WHAT-GOOD-LOOKS-LIKE.md`: **where the two disagree, this governs**, and its §10 lists every known disagreement (value 2's lengthening clause superseded as a documentation conflation; value 5 promoted to a Must). Identifiers **`REQ-`**; open questions **`REQ-Q`** (one is live: the currency of discovery payload, `REQ-Q1`). Owns no figures and sets no thresholds; its Expects are calibration and must never be read as targets. Carries the operational chain (novelty is reached through obscurity, fame currency) and the recorded structural conflict between REQ-37 and `DD-F1`. **§11 added 2026-08-05 — the owner's 2026-08-04 no-commercialization ruling (`REQ-43`: none or donations only, no ads, no paid features, ever) and the confirmation gate on it (`REQ-44`: nothing is built on that ruling's consequences without confirming with the owner first).** §11 is where the supersession of the alpha design's Last.fm constraint lives, and it names three things the supersession does **not** settle — two unverified, one (setlist.fm) never ruled out in the first place. **Placed after §10 so that §10 keeps its number**, which is cited from `CLAUDE.md` and from this map. |
| `superpowers/WHAT-GOOD-LOOKS-LIKE.md` | **⚠ Partially superseded 2026-07-29 by `superpowers/PRODUCT-REQUIREMENTS.md` — where they disagree, that document governs; its §10 lists the disagreements. This file remains the calibration record (provenance, worked cases, protocol).** **What the owner means by a better path.** Calibration for the blind listening test — this project's strongest evidence class, which decided the graph twice where the offline metrics decided it zero times. Read before interpreting any listening verdict, **and before designing anything that scores a path**: it holds no thresholds and must not be mined for any, but a criterion that contradicts a value in it is wrong (worked example: the Track 2 pre-registration's C4). **Amended 2026-07-27 at the owner's instruction**, after the reference product it invokes was read for the first time: value 9's endpoint-tracking clause is **verified** and its caveat lifted; value 2's "Reference product: boilthefrog" claim is **struck in place** — that product's bypass does not do what value 2 describes — and a short table now delineates what is shared with boilthefrog, what is new here, and what is a deliberate deviation. **The values themselves are unchanged**; what was withdrawn is an appeal to precedent. Read that table before citing boilthefrog in either direction. **Value 8 CORROBORATED 2026-09-11** — the owner restated *novelty is delivered through coherence, not traded against it* from inside the `LBL-` blind listen, of the rows where his two answers pointed opposite ways, not knowing which map was which (`findings/2026-09-11-lbl-listen1-results.md` §1.5; verbatim at `../builder/analysis/2026-09-10-lbd-blind-listen/lbl_listen1_owner_notes.md`). **No value changed and no threshold may be read off it** — this file still records preference, not evidence. |
| `superpowers/findings/2026-07-22-phase2-sweep-results.md` | Every figure from the Phase 2 six-arm sweep. Owns its numbers; linked from the adjudication's §6 (claims 41–45). |
| `superpowers/plans/2026-07-21-alpha-rollout-roadmap.md` | Gate structure and phase ordering. Its C4 is a pointer only — it holds no figures. |
| `superpowers/findings/2026-07-23-tiebreak-fix-adoption.md` | Identity (sha256) and verification record of the **adopted** 75k artifact, post tie-break fix. |
| `superpowers/findings/2026-07-22-configuration-model-null.md` | Authoritative for its own figures — the exact configuration-model null referenced by Phase 1 log §2.10/§7.1 item 1. |
| `superpowers/findings/2026-07-23-track2-protocol-analyst-review.md` | **AUTHORITATIVE for its own §2 measurements; ADVISORY on the protocol.** The `ml-graph-analyst` review of the Track 2 pre-registration, discharging its prerequisite **P8** and the design's §4.6 review gate. Verifies the pre-registration's §0 resolution table **in code**, and raises numbered protocol observations (O-series) and recommendations (PR-series) that the Track 2 session must read before running any arm. Scope is the **protocol only** — the harness is a separate review (P8b). Remit is derivation, not judgement: it says nothing about whether the experiment is worth running. Measurements: `builder/analysis/2026-07-23-track2-protocol-review/`. |
| `superpowers/findings/2026-07-23-a0-gate-analyst-review.md` | **AUTHORITATIVE for its own measurements; ADVISORY on the protocol.** The `ml-graph-analyst` review of the step-4 A0-vs-P result. Establishes that the raw floor's *value* is arm-invariant but its *effect* is not (a first-path change alters who gets bypassed, and that carries to depth); that the unconditional dead-bound is **7** `known` bypasses, not 6; that the d0–d2 floor asymmetry between a diving arm and production is real and large; and it corrects the claim-23 wording before it enters the adjudication. Figures: `builder/analysis/2026-07-23-track2-a0-review/`. |
| `../builder/analysis/2026-07-24-track2-p8b-harness-review/` | **AUTHORITATIVE for its own measurements; ADVISORY on the harness.** The `ml-graph-analyst` review of the Track 2 **Stage A scorer**, discharging pre-registration prerequisite **P8b** — the companion to the P8 protocol review above, and the gate that stood between the harness and the arms. Verdict: fit to run stage 1, not fit to run stage 2 as it then stood. Fourteen findings, two HIGH. **Owns** the blank-name and duplicate-name figures, the floor-lifetime depths, and the measurement showing the `X` arm does not bound what §1.4 claims. **Not a `findings/` document, deliberately** — a second copy would restate its figures, which the rule above forbids; cite this directory. Its consequences live in pre-registration **A17–A19**. |
| `../builder/analysis/2026-07-25-track2f-toll-ladder/` | **AUTHORITATIVE for its own measurements.** The Track 2F dose-response ladder on the similarity-ceiling toll, run 2026-07-25 after Track 2's A17(b) established that the toll had only ever run at half its intended magnitude. **Owns** the ladder's `C1`–`C4` figures, the isolating contrasts, and **`TF-D1`, the ceiling-hop usage measurement** — the finding that production takes 85 % of its hops on ceiling edges and that no price drives that below 28.86 %, because those hops are structurally forced. Result **`TFR0`**, a null on the primary criterion at `M*` = −0.198; **nothing adopted**. Unlike Track 2's corner arm, **its bound holds** (`TFR4`), so the null may be stated at mechanism strength. **Not a `findings/` document, deliberately** — a second copy would restate its figures. Cite this directory. |
| `../builder/analysis/2026-07-25-ceiling-ordering-headroom/` | **AUTHORITATIVE for its own measurements.** How much edge ordering the p99 clip destroys, measured read-only from the archive on 2026-07-25 — the untested half of the ceiling hypothesis that Track 2F's toll structurally could not reach. **Decision rule committed before the measurement ran** (`0e39522`); verdict **WIDE**, median erased cost spread **10.01 × `w_hop`** against a 1 × threshold, and **100 % of the pre-registered endpoints above 7.5 ×** — a magnitude Track 2F proved changes routing. Carries an **identity gate**: the in-memory rebuild reproduces the adopted artifact's sha256, so the figures are about the graph in use. **Owns** the mechanism statement — at a fully saturated node the similarity term contributes nothing, so the router discriminates on popularity alone. Licenses *pre-registering* the builder-side rescale; **not** adoption. Cite this directory. |
| `../builder/analysis/2026-07-28-asc5-path-ascent/` | **AUTHORITATIVE for its own measurements.** The ASC-5 discharge, run 2026-07-28 after the owner unpaused path-quality work: the path-level ascent re-read and the `X`-vs-`A7` isolating contrast (`PLA-` series; decision rules committed at `8562d15` before the run). **Owns** the geodesic-null comparison showing famous-pair first-path interiors are structurally forced, the flat bypass-depth popularity profile, and the one-hop `A7` extension. Constrains how the parked p99 rescale may be pre-registered. **Not a `findings/` document, deliberately** — a second copy would restate its figures. Cite this directory. |
| `superpowers/findings/2026-07-25-mutual-knn-stranding.md` | **AUTHORITATIVE for its own measurements. Records a mechanism; proposes nothing.** Why recognisable artists (Meat Loaf, Elbow, The Cult, The Streets, Nada Surf, Pretenders) hold one or a handful of connections: `mutual_knn` requires both artists to rank each other in their top 50, so an artist whose nearest neighbours are all more listened-to than it loses every edge in both directions. **Owns** the reciprocity figures (`MKS-3`: 43.7 % mean survival, 9.3 % of artists left with ≤1 edge), the degree-by-popularity table (`MKS-4`), and the no-detour pair counts (`MKS-6`: 6,599 total, 203 non-leaf) that make **F1 unsatisfiable for those pairs on this artifact**. **Distinct from the tie-break fix** (`MKS-5`), which repaired the very top and left this band untouched. **The mechanism itself is NOT new — cite Phase 1 log §2.10, which isolates the reciprocity rule by factorial and owns those figures; this document's `MKS-5a` records that an earlier draft wrongly claimed otherwise.** What is new is the per-artist view (§2.10 measures a distribution, not who is affected) and the F1 consequence. Explicitly **not** evidence that a looser rule would be better — `mutual_knn` won a blind listen, and §2.10's coupling (dropping reciprocity restores unbounded degree) binds any redesign (`MKS-5b`). ⚠ **Read "`mutual_knn` won a blind listen" in the light of `findings/2026-07-26-low-degree-synthesis.md` `SYN-6` (added 2026-07-26):** neither blind listen varied the both-ways rule against a bounded-degree alternative, so what the ear endorsed is the **degree bound**, not the reciprocity rule. `MKS-5b`'s force is unchanged; only its currency is. Nothing adopted; the path-quality pause is intact. Measurements: `builder/analysis/2026-07-25-mutual-knn-stranding/`. |
| `superpowers/findings/2026-07-26-low-degree-census.md` | **AUTHORITATIVE for its own interpretation; OWNS NO FIGURES** — every quantity belongs to `../builder/analysis/2026-07-26-low-degree-census/`, whose `REPORT.md` is the deliverable. **Cite that directory for any number.** Which artists can never appear mid-journey: an artist with one connection can only ever be one of the two the user typed, so the app can never introduce anyone to it (`DRV-4` stated this as structural and unmeasured; this measures it). **Closes three open questions**: `DRV-4`'s denominator (the two populations are **nested, not alternatives** — `pipeline.py:131,184` make every artifact node a crawled artist, and the fractions differ immaterially); that the count is post-largest-component-prune (confirmed observationally, not argued); and that non-artist entities are excluded (checked, not assumed — the nameless nodes are the one contaminant). Fame is the **adopted proxy unchanged** (§5 + A11 + A15, imported not reimplemented); **graph popularity ranks nothing** (§2.11) and was used only to screen who to ask — sound because popularity is accumulated *before* the mutual-kNN cap, so the reciprocity rule destroys degree and leaves popularity untouched. **Does not establish** that the lists are the true top of all low-degree artists: the cut was deliberately not deepened (owner's call), and a famous artist with low in-graph popularity is carried as a **candidate crawl-coverage gap**, not filed as instrument noise. Carries **`CNS-1`** in §3, recorded in passing per the `MKS-7` precedent: an artist can be unfindable under the name users know it by, because the graph stores one name and no aliases (Pretenders / The Pretenders). Nothing rebuilt, adopted or proposed; the pause is intact. |
| `superpowers/findings/2026-07-26-stranding-causes.md` | **AUTHORITATIVE for its own interpretation; OWNS NO FIGURES** — every quantity belongs to `../builder/analysis/2026-07-26-stranding-causes/`, whose `REPORT.md` is the deliverable. **Cite that directory for any number.** Separates *why* the degree-1/degree-2 artists are low-degree, which the census (above) counted but did not explain. Identifiers **`STC-`**. **`STC-1`: the crawl-frontier cause, as usually phrased, is nearly empty** — the dominant cause is that the source named almost no similar artists for them at all, and `STC-2` records that practically everything it did name had already been crawled. **`STC-3` is the reading rule**: the set is dominated by unfixable artists *by headcount* and by rule-fixable ones *by who anyone would recognise*, the two are almost perfectly stratified by popularity, and quoting either alone misleads. **`STC-4` is the strongest thing cutting against a cap-rule change** — most of the set is named by four artists or fewer, so even the most permissive rule reaches few of them, and the ones it does reach are all at the popular end. **`STC-6` corrects a claim an earlier draft of this document made**: SOURCE-THIN means "nothing above the co-occurrence threshold **we chose**" (`BuilderConfig.algorithm`), not "the source knows nothing" — so a lower-threshold re-crawl is inside project control, with an unmeasured payoff; the length limit was checked and provably cannot cause short lists. ⚠ **`STC-6`'s remedy is KILLED as of 2026-07-29** — the `algorithm` parameter is a closed enum and production already sits at the lowest available threshold, so there is no lower threshold to re-crawl at; the document carries a correction banner at its §6 and `superpowers/2026-07-29-algorithm-selection-execution-log.md` §9 owns the closure. **`STC-6`'s underlying distinction still stands** — only its remedy is dead. **`STC-5` records a limitation in `reciprocity.py`** (it does not model the builder's special-purpose exclusion), corrected here, **not invalidating** `findings/2026-07-25-mutual-knn-stranding.md`. The boundary between the populations is the session's stated choice and the report prints eleven alternatives. Nothing rebuilt, adopted or proposed; the pause is intact. |
| `superpowers/findings/2026-07-27-boilthefrog-source-review.md` | **AUTHORITATIVE for its own interpretation; OWNS NO FIGURES** — every quantity belongs to `../builder/analysis/2026-07-27-boilthefrog-reconstruction/`, whose `REPORT.md` is the deliverable. **Cite that directory for any number.** A source review of **BoilTheFrog** (`plamere/BoilTheFrog` at `1f2cb60`), the product named as the reference in `WHAT-GOOD-LOOKS-LIKE.md` values 2 and 9 and never previously read. Identifiers **`BTF-`**. **`BTF-2` lifts value 9's standing "not independently verified" caveat on its first clause** — endpoint-tracking is confirmed from the author's own design text, the code, and measurement — and records that the author's stated *purpose* for the shared `w_jump`-shaped term was to **suppress** obscurity ("back alleys that no music fan should dare to tread"). **`BTF-3` is the finding that cuts hardest against this project's own record: value 2's progressive-bypass clause is NOT exhibited by the reference product it names** — BoilTheFrog's bypass is single-signal, node-local, relaxes no popularity term, and reaches a fixed point within a few presses. That does not make value 2 wrong (it is preference, and self-flagged as gut instinct) but it means artistpath's two-signal bypass is **novel, not a re-implementation**. **`BTF-1`** is the structural half of value 9 nobody had: the famous artists are **near-leaves** (Ed Sheeran, degree 4), so the router cannot pass through them. **`BTF-4` explicitly does NOT answer `SYN-6` or weaken `MKS-5b`** — BoilTheFrog is reciprocity-free *and* degree-bounded, but the factor table shows its source list is capped at 20 by Spotify, so the unbounded-degree regime cannot arise there at all; what survives is a **third option absent from the resume brief and `SYN-1`**: change what the cap *selects* (popularity proximity vs similarity rank), measured to halve max degree and cut stranding at a fixed cap. ⚠ **Do not pick that option up off this row.** It is a **graph** intervention, so the resume brief's §2 blocker binds it exactly as it binds the degree floor — **if the router prices these artists out, changing what the cap selects changes nothing** — and it is a rebuild, which is the owner's call and currently blocked behind the nameless-artist decision. It was measured on a **different graph from a different source**, and is recorded as a named option with an unmeasured payoff, not a candidate. **`BTF-5` corrects an expectation** — the reference strands artists at a broadly comparable rate; only the *distribution* differs. `BTF-6`/`BTF-7` are outside path work: `BYP-13`'s defect class is structurally absent there because graph and clips share one identifier space, and `de_norm` is a ready-made, search-side, rebuild-free answer to `CNS-1`. **`BTF-8`** records the reference's own headline mechanism (track energy smoothing) as **never called in any shipped generation** — the `FMS-P1` class in a six-year-old public product — and that this document reproduced the class in its own factor table. Nothing adopted, proposed or pre-registered; the pause is intact. |
| `superpowers/findings/2026-07-30-lb-algorithm-semantics.md` | **AUTHORITATIVE for the semantics of the source `algorithm` string's parameters** — read from ListenBrainz's own Spark job (`listenbrainz_spark/similarity/artist.py`, quoted verbatim), which outranks the 2024 staff forum comment that prompted it and corrects that comment twice (`session` is 300 **seconds** not minutes; the per-pair cap is `contribution`, per user, not a per-session 50). Identifiers **`LBS-`**. **Owns one measurement series**: the candidate-list-length table (`builder/analysis/2026-07-30-lb-source-semantics/`) showing `ALG-C`'s `limit_50` lists still reach 100 entries — reconciled by the source's own "instructive only, upto 2x" clause, so **the k-sweep ceiling of 100 and `NEXT.md`'s closed list-length item both stand**. `LBS-4` is a **labelled inference, not evidence**: lowering `contribution` 5→3 raises the minimum distinct co-listeners for a pair from 3 to 4, a supply cut aimed at the obscure end — the mechanism-shaped hole in `RC-R1`. `LBS-5` supersedes every earlier paraphrase of these parameters; the `filter` token is left open deliberately (absent from current master, and it cannot bear on the `ALG-B` decision). **The Track B design cites this rather than re-deriving it.** Changes interpretation only; enum closure, `MKS-5b`, and the parked adoption untouched. |
| `superpowers/findings/2026-07-30-track-b-cap-selection-results.md` | **AUTHORITATIVE for Track B's results** — the four-part report (measured / inferred in plain language / weakest link / options) over the 24-cell cap-rule grid. **Raw record: `../builder/analysis/2026-07-30-track-b-cap-selection/cb_scores.json`** (committed before any read); cite one of those two for any Track B number. Headline verdicts under the pre-registered bars: the k-curve is null on `ALG-E` and decisive on `ALG-B` (`R0`); the incumbent loses at matched bound 50 to `trimmed_union` on coverage criteria with no hub flag (`R1`); reciprocity alone is nearly free at k = 100 (`R1a`, null); `R2` is the pre-committed router null on `ALG-E` (quota edges verified present at famous nodes and declined at production weights — **not** "the quota rule cannot work") and decisive nonzeros on `ALG-B` including under the production rule; the survival tilt is directionally as predicted and immaterial (`R3`); un-boundedness is priced (`R4`). **Its weakest link is stated in the document**: path criteria rest on famous pairs only after the `CRS-G3` attrition. Licenses a recommendation input only; `REQ-38`'s blind listen owed before any adoption. ⚠ **FORWARD CORRECTION 2026-09-07 — `CRS-C3`'s "no hub flag" carried NO information, and `R1`'s summary above repeats it.** Where >= 1 % of a cell's nodes sit at its degree bound, `top1pct_degree_mass_frac` reduces algebraically to `bound / (100 x mean degree)` and cannot see edge arrangement; the identity holds on all ten `trimmed_union` cells to 1.6e-4, and **no selectable cell could have reached 73.5 % of its own flag threshold**. The two that would have fired are the **barred uncapped pair**. The document is unedited and carries the correction at its head; arithmetic is owned by `../builder/analysis/2026-09-07-degree-floor-at-admission/README.md` §9. **This touches nothing else: `CRS-C4` hub transit is a routing measure and stands, no conclusion here is overturned, and it is not evidence about any cap rule.** |
| `superpowers/findings/2026-08-04-cap-reevaluation-results.md` | **AUTHORITATIVE for the `CRE-` experiment's results** — the four-part report (measured / inferred in plain language / weakest link / options) over the sixteen swept cells, **written by a session that did not run the sweeps** (the prereg's §4 Stage 3 requirement, and the reason Seam 3 exists). **Raw record: `../builder/analysis/2026-08-03-cap-reevaluation/cre_scores.json`** plus `cre_screen.json`, `cre_d1.json`, `cre_d3.json`, `cre_gates.json` and the sixteen `cre_sweep_*.json`, all committed before this document was opened; cite one of those or this document for any `CRE-` number. Read determinations under the pre-registered bars: **`CRE-R2` fires for `B-S1-P1a` and `B-S1-P1b`** (candidate archive, trimmed union, `known` ramp at r₁ and r₂); **`CRE-R0`, `CRE-R1` and `CRE-R3` do not fire**; **`CRE-R4` finds no clear winner** because its clause (i) fails for both, so the document presents options and no recommendation. **Four qualifiers travel with every `CRE-R2` sentence and must not be dropped:** both passing arms trip §0.4's censoring trigger, so their `CRE-C1` is reported **"descent partly unmeasurable" and never as a clean pass**; on fully-measured pairs `B-S1-P1a` scores below the bar it passed on the full set, and `B-S1-P1b`'s matched-only set falls below the readable floor; the `w_floor` guard flags all nine supply comparisons, so **no sentence credits the connection rule alone**; and `CRE-C5`'s entry condition was not met with both scramble companions' CIs including zero, so **no "tags did this" or "votes did this" sentence is licensed**. Also records the one `CRE-C4` failure (`E-S1-P1a`, 0.646 — payload spent for no gradient, and **not** `CRE-R3`, which needs a `CRE-C1` pass alongside). **`CRE-R4` clause (ii) was computed in this document from the committed per-pair deltas and is not in `cre_scores.json`** — labelled as such where it appears. Its §5.3 names the unrun remainder, because **"the family space is exhausted" is a barred read**. Licenses a recommendation input only; `REQ-38`'s blind listen unspent and owed before any adoption. |
| `../CLAUDE.md` | How to work in this repo: commands, architecture, conventions. |

### Active

| Document | Covers |
|---|---|
| `superpowers/findings/2026-09-11-lbl-listen1-results.md` | **ACTIVE — AUTHORITATIVE for the `LBL-` listen-1 read, and it OWNS that listen's figures** (the tally, the per-pair breakdown, the ear-tracking counts); cite it by section and never restate a number from it. It owns **no map figures** — those stay with `../builder/analysis/2026-09-10-lbd-served-population/README.md`. The four-part report (measured / inferred in plain language / weakest link / what is left open) over the executed blind listen of the **served map against `LBD-A0V`**, **written by a session that neither ran nor prepared it**. Governed by `LBD-AM5-5` in `specs/2026-09-07-lbd-fidelity-and-supply-preregistration.md`, which wins wherever they disagree. **The read under the pre-registered bar: margin +1 on coherence and +1 on novelty against a bar of 8, so both axes are `no_detectable_difference` and the listen reads `LBL-R2` — the tie.** Its plain sentence, frozen before any journey existed: *"My ear cannot tell our recomputed lists from ListenBrainz's own."* **Four things travel with that sentence:** the two maps are structurally very different, so this is *inaudible*, not *identical*; **no row of 24 was lost to a clip problem** (the `CAU-` correction worked and `LBL-R4` never came near firing), so the resolution limit is the pair draw, not the harness; **two of eight pairs produced journeys too short to judge**, and most undecided rows were undecided because the owner knew everyone on both sides; and the **one pair that moved all three coherence rows went to `LBD-A0V`** — the obscure, long-journey pair, and the owner's only unprompted pair-level preference, which is a hint about where a difference might live and is not a read. §5 is the barred-reads list — no attribution to "the data" without `LBD-X4` beside it, none inside the `LBD-X5` bundle, no "equivalent" (`REQ-41`), no "passed", no "failed", no transitive claim to listen 2, **no adoption on any outcome** (`S4` owns that, and `V` is an experimental control, never a population rule), and `GBL-` §5's run-once rule bars re-listening this verdict. §4 records six instrument defects for the next listen; **§1.5 carries the owner's verbatim closing impression** — he diagnosed the familiarity limit himself, said his picks were bimodal (*"in those cases, the winner was very clear"*), and said the coherent side is the better path where the two answers split, **which corroborates `WHAT-GOOD-LOOKS-LIKE` value 8 from inside a blind setting and changes no number in this listen**. **Nothing adopted, no default changed, no shipped code touched.** |
| `superpowers/findings/2026-09-10-documentation-layer-strategy.md` | **ACTIVE — AUTHORITATIVE for its own measurements (`DLS-M`) and the `DLS-T1` pre-registration.** What sessions here pay for context and where, measured from the repo and the retained session transcripts; where the current Claude Code guidance disagrees with this project's conventions; the seven-item plan of work (item 1 landed as PR #117) and six questions that are the owner's (`DLS-Q1`–`Q6`). Proposes; decides nothing. Scripts: `../builder/analysis/2026-09-10-dls-context-layer/`. |
| `superpowers/specs/2026-09-08-unsung-redesign-scope.md` | **ACTIVE — the governing scope document for the `UXR-` set**, the Unsung.fm rename and UX redesign. Records the owner's seven decisions of 2026-09-08 over the Claude Design mockup (rebrand approved; reach pill **dropped**; "Save track" dropped; genres, "Why this stop" prose and the additional pages **deferred**) and eighteen session decisions `UXR-D1`–`UXR-D18` he can overrule one line at a time. **Where the mockup and this document disagree, this document governs.** `UXR-D12` (the landing copy) is its one flagged assumption. |
| `superpowers/plans/2026-09-08-unsung-redesign.md` | **Implementation plan for `UXR-T1`–`UXR-T11`. IN PROGRESS: `T1`–`T8` executed 2026-09-08 (branch `unsung-redesign`, PR #114), `T9`–`T11` unstarted.** Argues from the spec above; **both named handoff seams have now been taken** (after `T5`, after `T8`). ⚠ **Five small plan-vs-repository defects across `T1`–`T8`**, all recorded in the log — the two in `T6`–`T8` were an under-specified test edit and a shared MBID the clip cache would have bled. ⚠ **The plan's log-section numbers are one behind the log's own**, because the first seam's closeout took `§ 6`. |
| `superpowers/2026-09-08-unsung-redesign-execution-log.md` | **RETAINED EXECUTION LOG for `UXR-T1`–`UXR-T11`. ACTIVE. Owns no figures and no status.** §§1–5 are the first session's, through the plan's first seam; §6 is that seam's closeout; §§7–9 are `T6`–`T8`, through the second seam. Appended per task. **Its section numbers run one ahead of the plan's from §7 on** — forward-only, never renumbered. |
| `superpowers/2026-09-08-HANDOFF-unsung-redesign-t8.md` | **ACTIVE — the CURRENT handoff FOR THE `UXR-` TRACK.** *(Not the current handoff on next actions overall since 2026-09-10 — that is the `LBD-` build-stage handoff, which `NEXT.md` names as its entry point; this row claimed both until the 2026-09-10 evening closeout's audit caught it.)* Written at the plan's **second** seam (after `T8`); next task is `T9`. ⚠ **Two tracks are live and each has its own current handoff**; the other is [`superpowers/2026-09-07-HANDOFF-lbd-preregistration.md`](superpowers/2026-09-07-HANDOFF-lbd-preregistration.md) for `LBD-`, which advanced to Task 4 alongside this work. Its most load-bearing warnings: the card carries **no** facts, links, cycling or bypass (all four are in `ArtistDetail`); **every** artist has a detail button, endpoints included; `ArtistDetail.test.tsx` needs a **distinct MBID per test** because the clip cache is module-scope; the rail now runs **first dot to last** rather than full height, which changes half of an owner request from 2026-07-28 and was flagged to him first; `max-w-[1200px]` was **pulled forward from `T8` into `T7`** and must not be re-applied; and an artist's name must be queried **by role or test hook, never by text**, since an open detail renders it three times in jsdom. Records that **`e2e/responsive.spec.ts` is red until `T10`**, which owns it, and that **the owner's mark is in the tree but untracked**. |
| `superpowers/2026-09-08-HANDOFF-unsung-redesign-t5.md` | **⚠ SUPERSEDED ON NEXT ACTIONS by [`superpowers/2026-09-08-HANDOFF-unsung-redesign-t8.md`](superpowers/2026-09-08-HANDOFF-unsung-redesign-t8.md), 2026-09-08 — NOT the current handoff.** Its must-not-revert list for `UXR-T1`–`T5` still binds in full. *(Was:)* the current handoff for the `UXR-` track, written at the plan's first seam. |
| `../frontend/design/2026-09-08-unsung-redesign/README.md` | **The visual source for `UXR-`** — the owner's Claude Design mockup, direction 1a, exported verbatim 2026-09-08. Its PNG assets did not survive the export and are the owner's to add. The spec governs where they disagree; the mockup predates every `LUX-` feature and shows "Steer away". |
| `../builder/analysis/2026-09-08-lux-e6-tag-vocabulary/README.md` | **FIGURES OWNER for `LUX-E6`. ACTIVE. Never restate its numbers.** Ran 2026-09-08 at the owner's instruction; descriptive. Read: the MusicBrainz-vocabulary labels are fit to show as written, the Wikidata P136 labels are not, and the MusicBrainz genre list is the allowlist the spec asked about. **Changes nothing**: the `LUX-` scope's §4.6 deferral of genres stands until he lifts it. |
| `superpowers/specs/2026-09-06-own-similarity-design.md` | ⛔ **REVIEWED 2026-09-06 — §0 CONTAINS A FALSE CLAIM AND §4 RESTS ON IT.** `LBD-C2` is **NOT** population-independent (measured); §0's drop-list row cannot hold; §0's held-constant degree ceiling is a **dormant term** whose effect grows in exactly the arms that succeed; §4's `LBD-C1` never defines "our top-N". Outcome: [`superpowers/findings/2026-09-06-lbd-plan-review.md`](superpowers/findings/2026-09-06-lbd-plan-review.md). **The document is deliberately UNEDITED** — corrections belong to the pre-registration (plan Task 2), before results exist. Do not "fix" §0 or §4 in place. The track's ruling, stages, `LBD-D1`–`D8` and §9 all stand. **ACTIVE, governing for the `LBD-` track** — computing our own artist similarity from ListenBrainz's published Spark/parquet listens instead of the Labs endpoint. **Owner ruling 2026-09-06: stages `S1`–`S3` approved; `S4` (adoption) is a separate decision on `S3`'s read.** Identifiers `LBD-`, collision-checked. **Owns no figures** — `../builder/analysis/2026-09-06-lb-dump-feasibility/README.md` owns today's. **Read §0 before any comparison:** the population is NOT constant across arms, so every population-relative quantity recomputes, and **`LBD-C2` is stated over a fixed artist set (the `CXR` added artists) in degree** for that reason. **Read §6 before reading `LBD-C1`:** our recomputation at LB's parameters will not reproduce the archive's lists and the gap is a lineage fact, not a code defect, unless the synthetic sub-check says otherwise. Eight design decisions fixed now (`LBD-D1`–`D8`): reimplement LB's job faithfully first; DuckDB locally, not Spark; outputs land as an archive in the existing key scheme via a **second** `SimilaritySource`, never a seventh `PERMITTED_ALGORITHMS` value; **no currency change inside the track** (dump-derived fame is a future pre-registration); a total ordering, because `LBD-P2a` showed LB's own `ORDER BY listened_at` is not one; acceptance never widened; nothing written into `builder/scratch/`. **§9 is the CLOSED list** — fame rulings, `ALG-B` lineage, `DD-F1`, the router-side `CXR` fixes, the cap rule, MLHD+, any new formulation. Supply-side only: the fame-ramp squeeze is not this track's. **⚠ AMENDED 2026-09-07 by [`superpowers/specs/2026-09-07-lbd-fidelity-and-supply-preregistration.md`](superpowers/specs/2026-09-07-lbd-fidelity-and-supply-preregistration.md), whose §10 lists the six amendments** — including that §0's population-independence sentence is void, the degree ceiling moves out of held-constant into a named exposure, and **the owner stop moves earlier**. Still unedited here; read the amendment register, not this row, for what changed. |
| `superpowers/plans/2026-09-06-lb-dump-exploration.md` | ⛔ **REVIEWED 2026-09-06 — NOT EXECUTABLE AS WRITTEN** (eight findings; `LBDR-F1`/`LBDR-F2` are false premises). **Task 7 refuses to build on every arm including `A0`** — surfaces only after Task 4's full-history run and Task 5's per-arm hours. **Task 3's featured-weight bullet contradicts the SQL it quotes** and Task 2's synthetic fixture would inherit the error — surfaces **never**. **Task 5's two arms cannot support `LBD-R1`.** Outcome: [`superpowers/findings/2026-09-06-lbd-plan-review.md`](superpowers/findings/2026-09-06-lbd-plan-review.md); **deliberately unedited below its banner.** **ACTIVE implementation plan for the `LBD-` track, stages `S1`–`S3`; UNSTARTED.** Operational document for `specs/2026-09-06-own-similarity-design.md`, which governs where the two disagree. **Its Task 2 is the pre-registration, committed before Task 3 runs** — the design fixes the criteria's shape and plain sentences; the pre-registration fixes their values. Two seams (after the fidelity run; at the `S3` read, which is an owner stop). Every file, function and config value it names was grepped at `75d9642` and re-resolved after `LUX-4` merged (`422530b`). ~~**The owner has said it will be reviewed by another agent before execution.**~~ *(DISCHARGED 2026-09-06 — two independent reviews ran; see this row's head.)* Runs from a worktree: reads `builder/scratch/` by absolute path and writes only under `D:\unsung-large-data\`. **⚠ AMENDED 2026-09-07 by the pre-registration's §10** — Task 5 restructured (the first four arms are derivations of one materialised table, not four runs), Task 7 gains `drop_unlistenable=False`, a feasibility gate is added before the full pass, and **Tasks 6–7 become conditional on an owner read at the end of Task 4**. Tasks 1 and 2 are DONE. Still unedited here. |
| `superpowers/findings/2026-09-06-lb-dump-route-assessment.md` | **ACTIVE — the assessment that opened the `LBD-` track**, written before any design existed, in the four-part shape. **Carries a FORWARD CORRECTION to two frozen documents:** the "0.03 % of listens carry MBIDs" figure (`2026-07-19-listenbrainz-probe.md` §6c; restated in the alpha design) was measured on the JSON *listens* dump and is correct for it; the **Spark/parquet** dump LB's own similarity job reads carries LB's MBID mapping and measured **73.8 %** (`LBD-P1`). Names which half of the `CXR` degradation this route can address (supply) and which it cannot (the ramp squeeze; the unexplained depth-0 half). Owns no figures. |
| `../builder/analysis/2026-09-06-lb-dump-feasibility/README.md` | **FIGURES OWNER for the `LBD-` route assessment. Never restate its numbers.** Two frozen probes over one day's parquet incremental (dump 2653, sha-pinned, kept at `D:\unsung-large-data\`): `LBD-P1` MBID coverage and corpus shape; `LBD-P2` the session/pair cost of LB's job per day on the owner's machine. **`LBD-P2a`: ordering by `listened_at` alone is non-deterministic on this data** — two runs disagreed until the order was made total. Descriptive; no criterion; nothing adopted. The **full** dump was not opened. |
| `superpowers/findings/2026-09-06-lbd-plan-review.md` | **ACTIVE, and AUTHORITATIVE for the `LBD-` plan review's outcome.** Two independent reviews, run 2026-09-06 before any arm executed — a claims-against-the-repo pass and an `ml-graph-analyst` measurement derivation. **They did not see each other's work and converged on the same blocker by different routes**, which is why it is stated as certain. Verdict: **the plan is not executable as written and the design's §0 carries a false claim.** **Owns no figures** — `../builder/analysis/2026-09-06-lbd-plan-review/` owns them. **It changed no governing document, deliberately**: the corrections belong to the pre-registration (plan Task 2), which is committed before results exist. Carries the split of what is owed — **one open item is the owner's** (how much of today's sparsity is our own degree ceiling rather than missing listening data, which moves the track's premise), the rest belong to Task 2's author and to a session before Task 3. ⚠ **CORRECTED 2026-09-07:** this row read *"most of today's sparsity is attributable to our own degree ceiling"*; the falsifier that review named has since been run and moves it **from a majority to a minority**. Mechanism unchanged, magnitude only. The finding and `NEXT.md` carry the same forward correction; figures are owned by `../builder/analysis/2026-09-07-degree-ceiling-falsifier/README.md` §4. |
| `superpowers/findings/2026-09-07-doc-audit-two-branch.md` | **ACTIVE — the `doc-auditor` report covering PR #108 (`DCF-`) and PR #109 (`LBD-`) together**, which neither session could produce alone: they ran concurrently and each deliberately avoided reading the other to keep the `LBD-` pre-registration independent. Discharges the B1 success condition recorded in the `DCF-` execution log after a first attempt terminated on a rate limit. **⚠ Read its correction banner first: one finding is wrong** — git conflicts on the `NEXT.md` collision rather than silently reverting either side (verified with `git merge-tree`), and its advice to resolve directly on `main` is barred here. Everything else stands, including both HIGH cross-branch findings. **Owns no figures.** |
| `../builder/analysis/2026-09-06-lbd-plan-review/README.md` | **FIGURES OWNER for the `LBD-` plan review. Never restate its numbers.** Front matter for two verbatim reports (`claims-review.md`, `measurement-derivation.md`) and six committed probes that read artifacts through the shipped `GraphStore` and write nothing. All artifacts sha256-verified against their manifest sidecars before reading; ListenBrainz's own algorithm fetched from **master by URL and deliberately NOT vendored** (third-party; re-fetch rather than trusting a copy). **Read its "What is NOT established"**: no arm was run and no graph built, the population-drift figure is one artifact pair at one ratio with a second column changed, and nothing about the parquet dump on `D:\` was visible to either reviewer. |
| `superpowers/specs/2026-09-07-lbd-fidelity-and-supply-preregistration.md` | **ACTIVE, and GOVERNING for the `LBD-` track's criteria, arms, gates and reads.** Committed 2026-09-07 **before anything ran on real data** — the git timestamp is the evidence, and it cannot be reconstructed afterwards. The design's §4 fixed each criterion's *shape*; **this fixes the values, and governs where the two disagree on one**. **Owns no figures.** Carries every correction owed by [`superpowers/findings/2026-09-06-lbd-plan-review.md`](superpowers/findings/2026-09-06-lbd-plan-review.md) and the derivation's fourteen-item list; **its §10 is the register of what it amends in the design and the plan**, neither of which is edited. ⚠ **Read §1 before planning any run:** verified from ListenBrainz's own source that `threshold` and `limit` are the ONLY tokens applied after the cross-user aggregation, so `LBD-A0`–`LBD-A3` are **filters over one materialised pair table, not four runs** — while `contribution`, `session`, `skip`, `days` and pairing each need their own pass. ⚠ **Read `LBD-X1` before proposing a degree-ceiling change:** Track B already measured the cost of raising it and the bound-100 set fired the hub-transit bar, *and* both sweeps predate the crawl extension so its effect on the added set is unmeasured — the two point opposite ways and neither is discharged. ⚠ **CORRECTED 2026-09-08: the second half of that sentence is now STALE.** `DCF-` landed on `main` and measured the ceiling's effect on the added set directly; figures owned by `../builder/analysis/2026-09-07-degree-ceiling-falsifier/README.md` §4. **The pre-registration's `LBD-X1` still says "unmeasured" and is deliberately NOT edited** — that amendment is `LBD-` Task 3's, per `NEXT.md`, and §12 now exists to hold it. **`LBD-X1`'s operative consequence is unaffected**: the bar on reading a graph-level null as a supply null rests on the ceiling being an active term, which no measurement of its size removes. **The owner stop moves to the end of Task 4** (§10): the pair-table read answers `LBD-R1` with no build, so the emitter and builds are conditional on it. Identifiers `LBD-A0`–`A4`, `LBD-G1`–`G4`, `LBD-X1`–`X3`, collision-checked across every ref. ⚠ **AMENDED 2026-09-07 by `LBD-AM1`, before any arm ran** — its new **§12** is the register of amendments to itself, and the amendment's text sits at the end of **§3**. It adds a **reporting requirement only**: `LBD-C2a` is additionally reported stratified into the **residual set** (added artists still at <= 2 connections when our degree ceiling does not bind) and its complement. **No gate, no effect size, no threshold changed, `LBD-G2` untouched.** It adds one named result branch, `R12`, extending §9's own bare `R1`–`R11` series rather than sitting beside `LBD-R1`, which names something else. Reachability of that stratum was checked before the amendment was written; figures owned by `../builder/analysis/2026-09-07-lbd-am1-residual-reachability/README.md`. ⚠ **AMENDED 2026-09-10 by `LBD-AM3`, AFTER a result existed and saying so:** `LBD-G1` fired (top band 0.5844 < 0.60), the diagnosis named the input difference (ListenBrainz's deployed dataset was computed on roughly a third of today's corpus, shown by a single-knob dated rerun with pre-stated readings), and **the owner ruled the arms may be read anyway.** No value changes; the reading stands; it spends the commit-before-results property of `LBD-G1` and of nothing else. **Never cite `LBD-C1` as passed.** Figures owned by `../builder/analysis/2026-09-08-lbd-similarity/README.md` §5–§6. ⚠ **AMENDED 2026-09-10 by `LBD-AM4`** (the build stage over the fixed population `P`; §10 block, §12 row) **and 2026-09-10 (night) by `LBD-AM5`**, before anything it names was built and before any journey existed: two maps over the SERVED population `V` (`LBD-A0V`; `LBD-A5V` at threshold 3), drop lists pinned to the served lineage's own files because the `20260809` payload would drop 31 served artists, fame reused from the served map's own records under three checks, and **the `LBL-` blind listens with their pairs, protocol and reads `LBL-R1`–`R4` fixed** — served vs `LBD-A0V` first, `LBD-A0V` vs `LBD-A5V` only after the owner has heard it. **No value changes in either.** Read `LBD-X4` and `LBD-X5` before attributing a listen-1 verdict to "the data". ⚠ **`LBL-` LISTEN 1 HAS BEEN EXECUTED (2026-09-11) — run, unblinded and read; the result is `findings/2026-09-11-lbl-listen1-results.md`.** This document remains **governing** for the protocol and for every read; the findings note applied `LBD-AM5-5` and did not reinterpret it. **`GBL-` §5's run-once rule now binds that verdict: it may not be re-listened.** Listen 2 is registered and unrun. |
| `../builder/analysis/2026-09-07-lbd-inputs/README.md` | **FIGURES OWNER for the `LBD-` track's inputs. Never restate its numbers.** Task 1: the pinned dump's identity, the MusicBrainz side-tables converted to parquet, and three descriptive reads — corpus membership of the `CXR` added set, whether the 0.25 featured-artist weight is live on this corpus, and the heavy-user census (`LBD-R9`, cheap half). **Descriptive; no criterion, no gate, and the user census decides nothing.** ⚠ **Read its "what could not be verified"**: the full dump's published `.tar` is not on disk, so the plan's verify-the-tarball step could not be run and a per-file digest of the extracted tree stands in its place. Three frozen scripts beside it; outputs land under the large-data tree on `D:` and nothing is written into `builder/scratch/` (`LBD-D8`). |
| `../builder/analysis/2026-09-07-degree-ceiling-falsifier/README.md` | **FIGURES OWNER for the `DCF-` probe. ACTIVE. Never restate its numbers.** Runs the falsifier `2026-09-06-lbd-plan-review`'s Q4 named and did not run: build the extended ALG-B archive with `union_degree_ceiling` raised. **The falsifier fails** — the added set's dead-end share does not hold, so our ceiling really is a cause; but it is the **minority** one, which is why four documents carry a forward correction dated 2026-09-07. Also owns the composition split of the restored edges (overwhelmingly attachments to well-connected artists, not routes through the tail) and a per-fame-band read of which cap step deletes an edge. **⚠ Descriptive only: no path was built and `CRS-C4` hub transit was NOT re-measured, so Track B's `C4` result stands against any ceiling raise until someone re-measures it.** Its control reproduces the `CXA` population exactly and its edge-fate gate reproduces the served artifact's edge set exactly. **Read its "What is NOT established"** — a candidate-supply gain is not a delivered-connection gain, and neither is a journey the router would choose. ⚠ **FORWARD CORRECTION 2026-09-07 at its §3 — every number stands as measured, but its `top1pct_degree_mass_frac` figures are a concentration measurement ONLY on the non-binding arms.** On the ceiling 50 / 100 / 200 arms the statistic equals `ceiling / (100 x mean degree)` and cannot see edge arrangement, so the rise across those three is not evidence of hub concentration. **§3's conclusion about the non-binding arm is unaffected** — it rests independently on max degree and edge count. Reasoning owned by `../builder/analysis/2026-09-07-degree-floor-at-admission/README.md` §8. |
| `../builder/analysis/2026-09-07-degree-floor-at-admission/README.md` | **FIGURES OWNER for the `DFA-` probe. ACTIVE. Never restate its numbers.** One arm extending `DCF-`'s instrument (imported, not copied): instead of raising `union_degree_ceiling`, exempt an edge from the degree trim when deleting it would leave the artist at the **far end** with <= `F` connections, ceiling held at the shipped 50. **The floor at `F` = 2 rescues the SAME added artists the ceiling lift rescues** — set identity checked by MBID, Jaccard 1.0, not merely the same count — **and parks every one of them on exactly three connections**, where the ceiling lift gives them a median of five. `F` = 1 makes the dead-end share slightly worse (deletions are redirected, not prevented) and `F` = 3 buys nothing further while breaking the degree bound. **`F` = 2's perfect score is partly DEFINITIONAL** — a floor at `F` guarantees nobody is cut to `F` or below, and the dead-end criterion is exactly 2. ⚠ **Its §8 carries a commissioned `ml-graph-analyst` derivation that invalidates the cost side as usually read, here AND in `2026-09-07-degree-ceiling-falsifier`: where >= 1 % of nodes sit at the ceiling, `top1pct_degree_mass_frac` reduces algebraically to `ceiling / (100 x mean degree)` and cannot see edge arrangement at all.** The `DCF-` README is deliberately NOT edited; that is its own to annotate. **Read §8.4 before using any concentration figure from either probe.** ⚠ **Its own instrument gate reported FAIL and the original is left exactly as written**, with a dated addendum — the failing clause was the author's wrong prediction about the rule, not an instrument defect. **⚠ Descriptive only: no path was built, `CRS-C4` hub transit was NOT re-measured, and Track B's `C4` result stands against any looser bound.** **Its §9 tests that identity against Track B's committed `cb_scores.json` and finds `CRS-C3` was structurally unable to fire on any selectable cell** — that document is unedited and carries a forward correction at its head. The cap-rule decision stays parked and the owner's, and owes a blind listen. Identifiers `DFA-`, collision-checked across every ref. |
| `../builder/analysis/2026-09-07-lbd-am1-residual-reachability/README.md` | **FIGURES OWNER for `LBD-AM1`'s reachability read. ACTIVE. Never restate its numbers.** Commissioned by `LBD-AM1`, the amendment at the end of the `LBD-` pre-registration's §3, and **run before any `LBD-` arm**. Asks the one question that would have bounded the whole track had it gone the other way: is the **residual set** — the `CXR`-added artists still at <= 2 connections when our own degree ceiling does not bind, the part **raising the ceiling** cannot reach — present in ListenBrainz's corpus at all? **It is: absence is NOT a bound**, and the residual stratum is marginally better served than its complement on the arithmetic minimum a pair requires, while being **listened to less** overall. **Owns no other figures** — Task 1's whole-set read and the `DFA-` probe's degree figures are cited from their own READMEs. ⚠ **Enough listeners is necessary, NOT sufficient: nothing here measures co-occurrence, so it must not be cited as evidence that any arm will move `LBD-C2`.** It settles only the negative direction. Proposes no cap-rule change and depends on none. |
| `superpowers/2026-07-30-HANDOFF-track-b-runs.md` | **⚠ SUPERSEDED 2026-07-30 on next actions by `2026-07-30-HANDOFF-fame-proxy-coverage.md` (row below)** — *this row still claimed CURRENT until the coherence-probe closeout caught it; the file's own role line was already correct.* Still the record of Track B's completion seam: all 24 cells built and scored, raw outputs committed before any read, six reads written in prereg order, report committed, nothing in flight. Supersedes the Track B design handoff below **on next actions**. Names the next state (**nothing queued for a working session; the next action is the owner's** — the parked cap-rule and `ALG-B` decisions now have their pre-registered inputs) and the claims an editor must not revert: the two pre-committed nulls (`R1a`; `R2`-on-`ALG-E` as a *router* finding), `PS100` ≡ `MK100` byte-identical, the famous-pair-only scope of the path evidence, the closed no-redraw decision on the `CRS-G3` attrition, and the measured tie-domination. Carries what is otherwise unwritten: the gitignored raw path walks, the companion's max-endpoint resolution in count currency, and runtimes. |
| `superpowers/2026-07-30-track-b-runs-execution-log.md` | **The retained execution log for Track B's runs and reads (`CB-5`/`CB-6`). ACTIVE.** Owns the run-phase decisions and the six reads' verdict statements with their run-state sentences; **figures are cited from `cb_scores.json` and the results note, never restated as findings here.** Records: the gates re-run green and red at the scoring commit; the `CRS-A2` baseline protocol resolving to zero in both famous classes (no blind bar amendment needed); **the pair-set attrition decision** (82 → 39; five `× lower` classes unreadable per `CRS-G3`; no redraw, with the reasoning); the `CRS-H1` tie operationalisation fixed before any cell's ties were computed, with the trim instrumentation validated per cell against the built artifacts; the direct verification that quota cells hold their reserved sub-decile edges at famous nodes; and the exposure map that closed the reads with nothing escalated. |
| `superpowers/2026-07-30-HANDOFF-coherence-tag-probe.md` | **⚠ SUPERSEDED 2026-07-30 on next actions by `2026-07-30-HANDOFF-tag-discrimination.md`.** Its own probe record is untouched and remains accurate. *(Row corrected 2026-07-31; it had gone on claiming currency.)* Written at the coherence tag probe's seam: the kill gate fired, step 2's pre-registered retrodiction was **never run and stays unconsumed**, nothing in flight. Supersedes the fame-proxy-coverage handoff below **on next actions only**. Carries the claims an editor must not revert: the unexecuted `ct_retrodict.py` is the *outcome*; the kill is decisive and vocabulary-robust; **`COH-2` and `COH-3` travel together** the way `FPC-2`/`FPC-9` do; `COH-5` retires the 21-hour MB tag-collection cost assumption. |
| `superpowers/2026-07-30-coherence-tag-probe-execution-log.md` | **RETAINED EXECUTION LOG** for the coherence tag probe. **Owns no figures** (they live in the findings row below). Records: the gate outcome and the five-for-five confirmed predictions, the instrument decision (genre union, not raw tags) and its post-hoc vindication, the concurrent-collector glimpse and its handling, the two decided-against moves (running the retrodiction "descriptively" after the kill; re-gating on the `COH-3` population after seeing it), and the run-cost measurements. |
| `superpowers/findings/2026-07-30-coherence-tag-probe.md` | **ACTIVE — the coherence tag probe's findings. Identifiers `COH-`. Owns its figures** (instrument-coverage tables; probes in `builder/analysis/2026-07-30-coherence-tag-probe/`). The kill gate fired at band level; `COH-3` is the recorded counterpoint on delivered routes; `COH-4` is the committed-but-unrun retrodiction; `COH-5` the LB-transport result; `COH-6` the post-hoc vocabulary-robustness read. **Descriptive in the `CS-P0` sense — nothing adopted, no criterion fixed, neither open owner decision settled.** |
| `superpowers/2026-07-30-HANDOFF-fame-proxy-coverage.md` | **⚠ SUPERSEDED 2026-07-30 on next actions by `2026-07-30-HANDOFF-coherence-tag-probe.md` (row above)** — its §7 coherence thread's instrument half has now been probed and killed; its structural half stays parked. Supersedes `2026-07-30-HANDOFF-track-b-runs.md` **on next actions only**; Track B's own record is untouched and remains accurate. Names the two open owner decisions **with the retiring session's position on each** rather than as a menu (fix the instrument *before* the graph work, on `FPC-9`'s sequencing argument; leave prior fame-scored results standing, fixed in advance of any recompute), the claims a well-meaning editor must not revert (three REFUTED predictions recorded as refuted; `FPC-2` and `FPC-9` must be quoted together), and what is not otherwise in the durable record. |
| `superpowers/2026-07-30-fame-proxy-coverage-execution-log.md` | **RETAINED EXECUTION LOG** for the fame-proxy probes. **Owns no figures.** Its **§7 is the largest thing NOT done and the reason to read it**: the coherence thread — that the past failure of coherence metrics was *misdiagnosed* (Adamic–Adar and overlap coefficient are both topological, auditing the graph they were derived from, while the owner's §3.9 verdict notes are genre/era/scene judgments), and that the cost function assumes coherence is **additive along edges** while `PRODUCT-REQUIREMENTS`'s own second clause — "the whole path reads as one journey" — is not expressible as an edge sum. Names MusicBrainz tags as the untried independent-currency instrument **with the warning that its coverage thins in the same tail**. §2 tabulates seven pre-committed predictions and their outcomes; §8 records what was decided against. Neither half of §7 is a proposal; both are the owner's trigger. |
| `superpowers/findings/2026-07-30-fame-proxy-coverage.md` | **ACTIVE — descriptive scope probes on what the FAME instruments can see.** Identifiers **`FPC-`**. **Owns one measurement series**: the coverage and agreement tables (`builder/analysis/2026-07-30-fame-proxy-coverage/`). **Nothing is adopted, no criterion is fixed, no currency changed, and Track B is untouched**; a pre-registration is owed before any criterion is built on any instrument here. **Read its §0 first — it leads with a correction of the case that opened the work**: the Wikipedia floor is **not** actively corrupting fame-currency measurements. `FPC-2` measures floor reach among routed interiors *falling* with bypass depth (5.3%→2.4% distinct, d0→d20) because the router never descends into the blind region, and `FPC-1` finds the instrument correct 94.4% of the time — so **no Track 2 or Track 3 verdict changes**. What survives is `FPC-3`: **55.0% of the artifact carries no English article**, a **latent** blindness that binds on the population and would begin binding when the product succeeds at what `REQ-37`/`DD-F1` demand. `FPC-4`: ListenBrainz `POST /1/popularity/artist` covers **99.94%** of the artifact and 99.9% of the bottom band, MBID-keyed — the "drop-in improvement" the alpha design §4.1 named in advance. **Two of three pre-committed predictions were REFUTED and are recorded as such** (`FPC-2`, `FPC-5`). Standing hazard restated: LB and English Wikipedia share a population blind spot, so **their agreement is not corroboration**. |
| `superpowers/specs/2026-07-30-track-b-cap-selection-preregistration.md` | **⚠ EXECUTED 2026-07-30 — all 24 cells scored and all six reads written; read `findings/2026-07-30-track-b-cap-selection-results.md` beside it, which is fresher on outcomes.** It remains authoritative for *criteria, bars, gates and read order*, and its §8 amendments were all honoured as written (`CRS-A2`'s baseline measured zero in both famous classes, so no further §8 entry was needed). *(Original role: **ACTIVE pre-registration — Track B, cap-rule selection at production scale.** Identifiers **`CRS-`**, collision-checked. Committed before any comparative cell is scored; `CB-1`–`CB-3`'s instrument gates (green **and** red halves) and the design probe `CB-P1` ran first and are recorded in its §0. **Its §0 carries the per-family `DD-F1` bearing table** — fixed from `CS-P0c`'s own text plus `CB-P1`'s two-archive scan, the consultant items 1–2 validated: weakest-first trim provably deletes every reverse-only famous→obscure edge (checked in all 10 rows), so **`banded_quota` is the only selectable configuration bearing on `DD-F1`**, and a `CRS-C5` null elsewhere is confirmation, not a finding. 24 cells over three families × two archives + a barred `uncapped` reference row; matched-bound rule for cross-family reads; `CRS-C6` pre-registers the consultant's survival-split tilt with direction fixed by `LBS-4`. Resolves `PLA-R1` from its grounds (presence, not fame-of-interior). **Licenses a recommendation input only** — no adoption, no default change, no weight retune; `REQ-38`'s blind listen owed regardless.)* ⚠ **`CRS-C3` is defined here, and its flag was structurally unable to fire on any selectable cell** — see the forward correction at the head of `findings/2026-07-30-track-b-cap-selection-results.md`. Its other criteria are unaffected, `CRS-C4` included. |
| `superpowers/specs/2026-07-30-tag-discrimination-probe-preregistration.md` | **ACTIVE pre-registration — the tag discrimination probe.** Identifiers **`TAS-`** (criteria), **`TAS-AM`** (amendments, deliberately distinct so an amendment can never be misread as a criterion). Committed before any measurement; **owns its figures** (`builder/analysis/2026-07-30-tag-discrimination/`). Asks the one question both candidate architectures — build-time tag-aware selection and router-side tag pricing — rest on: **does genre agreement discriminate between the candidates an artist already has**, or is it the similarity score wearing a different hat. **Descriptive: adopts nothing, fixes no criterion, changes no weight, default or currency, and requires no rebuild.** Explicitly **not** the router-side pricing track's pre-registration, which still owes its own consuming `TB-P5H-7`. **Read §8 before acting on any criterion:** `TAS-AM1` withdraws `TAS-4`'s original bar as **false** (per-artist swaps understate map change ~2.11×; the median reads 0 while 6% of the map moves) and replaces it with edge turnover ≤ 1%, the owner's materiality line; `TAS-AM2` fixes the substrate per criterion — selection-side on the pre-cap capture, map-side on the adopted artifact, **never compared across**. `TAS-1` is a **narrow** gate that deliberately does **not** inherit `COH-2`'s coverage bar: that bar governed a *sensor*, this is an *actuator*, and silence where labels are missing is safe in a way blindness is not. |
| `superpowers/findings/2026-07-30-tag-discrimination.md` | **ACTIVE — the `TAS-` probe's findings, and the outcome record for the whole tag-discrimination thread. Identifiers `TAS-`. Owns its figures** (tag-discrimination only; probes and raw JSONs in `builder/analysis/2026-07-30-tag-discrimination/`). **Its §0 must be quoted whole:** both kill bars were cleared **and neither architecture has an adoption case**, which are decided by different pre-registered clauses rather than being in tension. The premise the thread rested on is **confirmed** — genre agreement discriminates between an artist's candidates (`TAS-2`) and is **not** redundant with the similarity score (`TAS-3`), so neither null is "the graph already knew". **Two things an editor must not soften:** `TAS-6`'s **selection** half is ADVERSE and §5 bars an adoption recommendation for the build-time side whatever else holds; and **`TAS-AM5c` FIRED** — permuting labels among labelled artists reproduces 98.2–99.1% of `TAS-5`'s journey change, so **that change is not attributable to genre structure** and every `TAS-5` figure carries the caveat. **`TAS-6`'s ROUTING half is VACUOUS, never a pass** — the baseline is zero sub-decile interiors across all 120 pairs, so `is_adverse` early-returns `False` (`tas_guard.py:111`); that zero is the sharpest corroboration of `DD-F1` in the record. Also carries the vocabulary decomposition: **`W4` dominates `W6`** on reach, spread and redundancy, so **`W6` must not be the frame any future amendment names.** **Nothing adopted; `w_coh` does not exist in `ApiConfig`; no rebuild and no blind listen.** |
| `superpowers/specs/2026-07-31-release-tag-coverage-preregistration.md` | **ACTIVE pre-registration — release-tag aggregation coverage.** Identifiers **`REL-`** (criteria), **`REL-C`** (instrument checks), **`REL-AM`** (amendments, distinct so an amendment can never be misread as a criterion). Committed **before any measurement ran**. Asks whether aggregating an artist's *release* tags labels the artists carrying no genre label today — **specifically in the obscure tail**, since a frame that only thickens labels where labels are already good changes nothing for `TAS-6`. Its **§0 declares prior knowledge honestly**: the release-existence ceiling and the unlabelled census already existed when it was written, and `REL-1`'s 50% bar (inherited from `COH-2`, same band and denominator) was fixed **in writing before** that ceiling was measured. **Descriptive: adopts nothing, changes no weight, default, currency or vocabulary, and does NOT amend `TAS-` §1** — a passing frame is a *candidate* for a `TAS-` §8 amendment, never an enactment. **Read §8 before relying on any instrument figure:** `REL-AM1` and `REL-AM2` both correct the same conflation (comparing MusicBrainz genres against `F0`, which is LB ∪ Wikidata P136 — an entire source apart), and **`REL-AM2` was written after `REL-1`'s passing figure was known and says so at its head.** |
| `superpowers/findings/2026-07-31-release-tag-coverage.md` | **ACTIVE — the release-tag probe's findings. Identifiers `REL-`. Owns its figures** (release-tag coverage only; probes in `builder/analysis/2026-07-31-release-tag-coverage/`). **Census over all 74,193 artists** from four local dumps — no sampling, no CI, and therefore no "within noise" escape in either direction. `REL-1` **PASSES** at 58.8% against `COH-2`'s 50% bar; `F6` (both sources) reaches 71.0%. **`REL-4` is the finding that inverted the design's framing**: Discogs, the arm expected to be awkward, is the *stronger* one in the tail, and the two independent sources agree on ≥ 1 label for **96.6%** of artists both reach. **What cuts against it, and must be quoted with the headline:** `REL-3`'s fidelity median is **0.250** with only 21.1% at half-overlap, its ratio bar is a **degenerate** pass (null median exactly 0.000), and **10,757 lower-half artists — 44.9% of the unlabelled — are reached by neither source.** **Nothing adopted; says nothing about whether `TAS-6` would flip**, which needs a rebuild this probe does not license. |
| `superpowers/2026-07-31-release-tag-coverage-execution-log.md` | **RETAINED EXECUTION LOG** for the `REL-` probe. **Owns no figures** (they live in the findings row above). Records the design decisions and their reasoning, the two amendments with their disclosure positions, what was decided against, the harness failures (an OS-killed Discogs collector; a comment describing an optimisation that did not exist), and the run-cost measurements. |
| `superpowers/2026-08-01-HANDOFF-tas5-routing.md` | **⚠ SUPERSEDED 2026-08-01 on next actions by `2026-08-01-HANDOFF-label-weighting.md`** (the `WGT-` probe, a successor investigation). Remains authoritative for the `TAS-` track's internals. **⚠ `TAS-` Task 8 has since been WRITTEN** (2026-08-01, latest — the findings row above), so its "Task 8 is owed" claims and its open decision are spent. *(Original role:)* **ACTIVE — the CURRENT handoff, 2026-08-01.** Nothing supersedes it. Supersedes `2026-07-31-HANDOFF-tas-am3-am4.md` on next actions; that document stays authoritative for its own work's internals. A **seam** handoff: `TAS-` Tasks 1–7 complete, only Task 8 (the findings document) owed, nothing in flight, tree clean. Carries the five claims an editor must not revert — **`TAS-AM5c` fired so `TAS-5`'s change is not attributable to genre**; **`TAS-6`'s routing half is VACUOUS** (zero baseline, so "not adverse" is a division-by-zero artifact) and must never be quoted as a pass; `TAS-6`'s **selection** half is still adverse and still bars adoption; the old plan's Tasks 5–7 **contain a trap** and are superseded; and `TAS-AM5` was written *before* its results, unlike `TAS-AM3`/`TAS-AM4`, so its disclosure must not be normalised away. Records three things not otherwise durable: the capture regenerates **byte-identically** against §14.1's recorded sha; a test can pass on a **tie-break** rather than the behaviour it names (closeout B3 caught one green with the term zeroed out); and a doc-auditor report can be **stale on arrival**, so findings need checking against current state rather than accepting or dismissing wholesale. |
| `superpowers/2026-08-02-HANDOFF-tail-drop-and-candidate-census.md` | **HISTORICAL on next actions — superseded 2026-08-02 by `2026-08-02-HANDOFF-deezer-id-clip-resolution.md`; stays authoritative for its own three threads' internals.** Supersedes `2026-08-01-HANDOFF-no-release-tail.md` on next actions; that document stays authoritative for its own two threads' internals. A **seam** handoff covering three pieces that all reached their end: the adopted drop rule **wired** into the builder (PR #62), the pipeline/mirror divergence that wiring opened **closed** (PR #63), and the candidate `ALG-B` population **censused** with its own drop list frozen. Carries six claims an editor must not revert, of which three are sharp: **the wiring deliberately contradicted the predecessor handoff's closing instruction** (reasoning in the log's §1 — do not "restore" it); **the probe mirrors must NOT gain the drop step**, because their value is byte-identical reproduction and Track B's grid is all pre-drop; and **Track B's identity gate proves reproduction, not fidelity**, so it cannot detect the pipeline gaining a stage and must not be cited as if it could. Records the owner's 2026-08-02 ruling that the drop rule is **not** re-validated on the candidate population, and names one blocking item: the builder applies **one drop list to any archive**. |
| `superpowers/2026-08-02-tail-drop-wiring-execution-log.md` | **RETAINED EXECUTION LOG** for the wiring, the harness repair and the candidate census. **Owns no figures** — the census and clip figures live in `builder/analysis/2026-08-02-candidate-tail-census/*.json`, cited by name. Records: the ordering decision that reversed a committed instruction and why the predecessor's stated reason did not transfer (§1); why the drop is a config flag, and both uses being exercised the same day (§2); **a cheap screen that was vacuous by construction and was caught before it entered the record** (§3 — the census population was itself produced by the rule the screen compared against, the same shape as `TAS-6`'s routing half); the two opposite treatments the pipeline/mirror divergence needed (§4); the owner's transferability ruling with its decisive argument (§5); **four of this session's own claims, made and corrected within it** (§6, including a withdrawn "re-pricing is the cheap first move" that was wrong twice over); and the closeout check outcomes (§9). |
| `superpowers/2026-08-02-HANDOFF-deezer-id-clip-resolution.md` | **⚠ SUPERSEDED 2026-08-02 (evening) on next actions by `2026-08-02-HANDOFF-fame-instrument.md`; PR #65 has since MERGED.** Remains authoritative for its own two threads' internals. *(Original role:)* **ACTIVE — the CURRENT handoff, 2026-08-02. Nothing supersedes it.** Supersedes `2026-08-02-HANDOFF-tail-drop-and-candidate-census.md` on next actions. A **seam** handoff: per-archive drop lists (merged, PR #64) and identity-based clip resolution for `BYP-13` (PR #65, open at writing — it changes the builder/api APG1 contract). Carries six claims an editor must not revert, of which three are sharp: **`FORMAT_VERSION` stays at 1 and `serialise` omits `deezer_ids` when empty**, because bumping stops every existing artifact loading and unconditional writing breaks the frozen mirrors' committed shas; **"strictly better by construction" is WITHDRAWN**, since the id path inherits MusicBrainz's link accuracy and duplicate pages exist; and **the `w_degree_hub` dormancy claim is corrected** — the term is dormant by *weight*, so it cannot self-activate in a rebuild, and a pre-registration must not design a control against a confound that cannot occur. |
| `superpowers/2026-08-02-deezer-id-clip-resolution-execution-log.md` | **RETAINED EXECUTION LOG** for the per-archive drop lists, `BYP-13`, and a cleanup pass. **Owns no figures** — coverage and link-quality figures live in `builder/analysis/2026-08-02-dsp-ids/*.json`, cited by name. Records: why the drop-list selector needed no new config field (§2); the owner's choice of the metadata blob over a sidecar and the sequencing argument behind it, plus the argument against that he overruled (§3); **four corrections to the prior record, the first of which is this session's own withdrawn claim** (§4); **five defects and the mechanism that caught each** (§5 — an implausible zero, a test written before the data was frozen, a mutation the whole suite survived); the `REQ-38` reasoning (§6); **`G3-F2` falsified, and precisely what that does not license** (§7); and the Snyk MEDIUM splitting into two hazards with different answers (§8). |
| `superpowers/2026-08-02-HANDOFF-fame-instrument.md` | **⚠ SUPERSEDED 2026-08-03 on next actions by `2026-08-03-HANDOFF-featured-credit-filter.md`** — the featured-credit filter track it named as first has run to completion. Remains authoritative for the fame-instrument track's internals. *(Original role:)* **ACTIVE — the CURRENT handoff, 2026-08-02 (evening). Nothing supersedes it.** Supersedes `2026-08-02-HANDOFF-deezer-id-clip-resolution.md` on next actions. A **seam** handoff: the fame-instrument track complete — construct redefined, proxy adopted, worldly fame retired. Carries five claims an editor must not revert, of which two are the sharp ones: **"fame" changed meaning 2026-08-02 and old documents keep the old meaning** (never "fix" either direction); and **the `FAM-` NO-ADOPTION verdict and the later `NOV-` adoption are both correct — they answer different constructs.** Also carries the cap re-evaluation's complete owner-input list, and the three residuals not otherwise durable (corpus reuse rules, snapshot identity, the ~73%-novel famous band). |
| `superpowers/2026-08-02-cap-reeval-design-inputs.md` | **ACTIVE — the single design-input address for the cap re-evaluation pre-registration**, written at the owner's closeout prompt when the enumeration found the conversation carried load-bearing nuance the summaries did not. **Owns no figures and takes no design decisions** — it records the owner's rulings, the candidate families (a)–(d) spelled out, the supply×pricing synthesis (three measured nulls triangulating to the untested cell), the LB-cap structural picture with the unrecoverable-pairs bound, the blind-listen lineage (confounded per `SYN-6`, pre-dating the requirements layer), the tag constraints (sole-source LB; growth dissolved by construction), the owner's **labelled-guesswork** hypothesis with its cheap descriptive first read, and the criteria re-weightings (first-path hub transit tolerable; the depth-indexed hub measure is new). Where it and a governing spec disagree, the spec governs. |
| `superpowers/2026-08-02-fame-instrument-execution-log.md` | **RETAINED EXECUTION LOG** for the fame-instrument track (§1–§9). **Owns no figures** — they live in `builder/analysis/2026-08-02-fame-instrument/*.json`, cited by name, except where a disclosure requires the number on the page. Records: the owner's rulings and the delegation pattern (§1); the pre-run design critique and its full disposition table (§2); the `FAM-1` failure with its exposure map and the powerless `FAM-5` (§3); the identity work and the `FAM-4` construct-divergence failure (§4); the `FAM-6` failure, the featured-credit mechanism, and the TJ Brown addendum (§5, §5a); the `RCS-` shootout reads (§6–§7); **the controller's own mis-designed criterion, exposed by the owner's marks** (§8); and the adoption (§9). |
| `superpowers/specs/2026-08-02-fame-instrument-adoption-preregistration.md` | **⚠ EXECUTED AND CONCLUDED 2026-08-02 — verdict NO ADOPTION under its committed construct (worldly fame), later superseded in effect by the construct redefinition and the `NOV-` adoption; authoritative for its criteria, bars, amendments and machinery.** Identifiers **`FAM-`**. Its §8 carries three amendments whose disclosure positions differ and must not be normalised: `FAM-AM1` (pre-run, from the design critique), `FAM-AM2` (post-result, owner-ruled population re-scope), `FAM-AM3` (pre-result, the `FAM-6` design). **The percentile formula, null rules, snapshot-identity rule and quantisation floor remain the adopted machinery** — the cap re-evaluation inherits them. |
| `superpowers/specs/2026-08-02-ruler-candidate-shootout-preregistration.md` | **⚠ EXECUTED AND CONCLUDED 2026-08-02 — rule 4's sentence stands: "no measured candidate orders the tail" (worldly fame); Wikipedia and Deezer are dead as fame candidates absent new grounds.** Identifiers **`RCS-`**. Its §6/`RCS-AM1` is post-result with maximal disclosure and a **closed** five-row list — no third widening exists. The 30-artist corpus it scored (`fi_corpus.json`) outlives it as the reusable validation asset. |
| `superpowers/specs/2026-08-02-novelty-proxy-validation-preregistration.md` | **⚠ EXECUTED AND CONCLUDED 2026-08-02 — `NOV-2` PASSED 15/15 and `fame_lb_pctl` is ADOPTED as the novelty-likelihood proxy.** Identifiers **`NOV-`**. Its §4/`NOV-AM1` is post-result with **maximum-strength disclosure** (the amended criterion's result was foreknown); what saves it is on the page — `NOV-1`'s two-way form contradicted the governing Definitions clause, which predates the marks. **`NOV-1` stands UNREADABLE and retired as mis-designed; never re-run it.** |
| `superpowers/2026-08-03-HANDOFF-featured-credit-filter.md` | **⚠ SUPERSEDED 2026-08-03 (later) on next actions by `2026-08-03-HANDOFF-cap-reeval-prereg.md`** — the cap re-evaluation pre-registration it named as next has been written, reviewed pre-run, frozen and amended once. Remains authoritative for the featured-credit filter track's internals. *(Original role:)* **ACTIVE — the CURRENT handoff, 2026-08-03. Nothing supersedes it.** Supersedes `2026-08-02-HANDOFF-fame-instrument.md` on next actions. A **seam** handoff: the featured-credit filter track complete — rule fixed cold, amended once (`FCF-AM1`, owner-ruled, post-result disclosure on the page), lists frozen per population, wired behind `drop_featured_credit`, **adoption ruled by the owner 2026-08-03 (no calibration hand-review) and enacted by merging PR #67**. Carries the claims an editor must not revert: TJ Brown's drop is a **named-in-advance false positive**, never a surprise to be repaired post-hoc; the pre-amendment lists are the record, not the shipped state; and the two-drops disjointness is by construction, census-verified. |
| `superpowers/2026-08-03-featured-credit-filter-execution-log.md` | **RETAINED EXECUTION LOG** for the featured-credit filter track (§1–§8). **Owns no figures** — they live in `builder/analysis/2026-08-03-featured-credit-filter/*.json`, cited by name. Records: the census design decisions including why the sole-credit split is re-derived rather than read off `rel_rg_raw.json` (§2); the two findings that shaped the rule — the worked keeper failing the mechanical keep-check, and Discogs presence as the predicate-deciding column (§3); **the owner's MB-inconsistency observation as design input, with the two failure modes mapped to the keep-check's two clauses** (§3a); the predicate fix (§4); the keep-check run, freeze, wiring and mutation outcomes (§5); **the owner's spot-check challenge to the Discogs exemption and the split measurement that vindicated it** (§6); and `FCF-AM1` executed end to end with the superset assertion (§7). |
| `superpowers/specs/2026-08-03-featured-credit-filter-rule.md` | **ACTIVE — the governing rule document for the featured-credit filter.** Identifiers **`FCF-`** (`FCF-AM1` the amendment, distinct so it can never be misread as a base clause), collision-checked. **Committed before the keep-check's network results existed; `FCF-AM1` committed before its incremental keep-check ran** — the freeze points are the commit timestamps. The rule: inside the class (≥ 1 MB release-group credit, none sole, no **sole-credit** Discogs release per `FCF-AM1`), keep only where a commercial-DSP link exists AND a clip resolves via the app's own path. **Its FCF-5 fixes both worked-instance outcomes in advance, TJ Brown as a named false positive** — a result showing him dropped must not be presented as a surprise or quietly repaired. **Adoption ruled by the owner 2026-08-03, without the calibration hand-review**; the frozen lists ship as sha-pinned package data (`featured_credit_drop.py`). |
| `superpowers/specs/2026-08-03-cap-reevaluation-preregistration.md` | **ACTIVE pre-registration — the cap re-evaluation, the governing document for the experiment.** Identifiers **`CRE-`** (sub-series `CRE-G`/`D`/`C`/`R`/`S`/`P`; `CRE-AM` amendments), collision-checked; bare tokens from colliding series are always written qualified. Authored cold from the design-inputs address; **frozen at `3d7b7d6` after one pre-freeze revision on two owner-dispatched pre-run reviews — its §9 is the revision record** and carries two result-bearing disclosures (the `CRE-D1` direction measured opposite to the owner's hypothesis; the pricing device measured inert on famous pairs at incumbent supply) that must never be tidied away or read as predictions. Primary outcome: the bypass novelty gradient in `fame_lb_pctl` (`REQ-42` shape), on both data sets until a clear winner (`CRE-R4`). Factor table with one-column isolating baselines; held-constant section with the `w_floor` dormant-term disclosure scoped to the supply knob; families (a)–(d) as ruled, `UC` staged-reference only (`SYN-6` halves always together); consumes `TB-P5H-7` via `CRE-C4`. **`CRE-AM1`** (post-result disclosure on the page): `S2`'s ceiling ranking gains within-artist vote weighting on the owner's 2026-08-03 ruling, with the vote-scramble attribution companion gated at the sweep. **Licenses a recommendation input only** — no adoption, no default change, no blind listen spent. |
| `superpowers/2026-08-03-HANDOFF-cap-reeval-prereg.md` | **⚠ SUPERSEDED 2026-08-03 (night) on next actions by `2026-08-03-HANDOFF-cap-reeval-exec-plan.md`** — the execution plan it named as next has been written, reviewed and revised (PR #69), and **PR #68 has since MERGED** (`cdf0033`), discharging its owner-action. Remains authoritative for the prereg track's internals and its claims-not-to-revert. *(Original role:)* **ACTIVE — the CURRENT handoff, 2026-08-03 (later). Nothing supersedes it.** Supersedes `2026-08-03-HANDOFF-featured-credit-filter.md` on next actions. A **seam** handoff: prereg authored, twice-reviewed pre-run, frozen, the `WAV-` read run and closed, `CRE-AM1` appended; nothing in flight, tree clean, draft PR #68. Names the next work (the execution plan, written by a fresh session reading the prereg cold; merging PR #68 is the owner's action) and the claims an editor must not revert — the freeze point and §9's disclosures; **the `WAV-` read stays CLOSED as FAIL** (`CRE-AM1` supersedes its consequence on the owner's authority, never the read); the two deliberate divergences from the analyst's proposals; the 0.01/0.03 ramp bracket; the `CRE-D3` carve-out being the closed-cell rule's only exception. |
| `superpowers/2026-08-03-cap-reeval-prereg-execution-log.md` | **RETAINED EXECUTION LOG** for the pre-registration track. **Owns no figures** — the `WAV-` figures live in `builder/analysis/2026-08-03-within-artist-votes/` and the review measurements in `builder/analysis/2026-08-03-cre-prereg-critique/` (whose analyst report owns what it states). Records: the authoring decisions including the two defects the self-review caught (§2); the two pre-run reviews' disposition, the zero-overlap reviewer split worth reusing, and the two review measurements that became §9 disclosures (§3); the `WAV-` read's design, outcome, and the defect in this session's own `WAV-3` bar (§4); the owner's ruling, the correction to his recollection about `WGT-`, and `CRE-AM1` (§5); corrections to the prior record (§6); and the closeout outcomes (§8). |
| `superpowers/plans/2026-08-03-cap-reeval-execution-plan.md` | **COMPLETE — fully executed 2026-08-04; `CRE-T11` was its last task and Seam 3 is reached. Accurate as the record of how the run was operated; no longer a live instruction.** ⚠ **Two of its own statements were found wrong in execution and are corrected in the execution log, not in the plan** (a frozen document's value is that it is frozen): its `CRE-T11` `Interfaces` block contradicts its own `CRE-C2` bullet on whether a graph artifact is needed — the prereg governs and it is — see the log §13. *(Its role while live:)* **the operational plan for executing the `CRE-` pre-registration**, tasks **`CRE-T1`–`CRE-T11`** (namespace verified unused). **The prereg governs wherever the plan disagrees**; the plan adds no criterion and moves no bar. Fixes nine executor-column device pins pre-run, each with a committed precedent, in its "Decisions this plan fixes" section. Its **Verification record** is the claims-vs-repo half of its review (every name the prereg cites resolves); its **Revision record** maps the `ml-graph-analyst` critique's sixteen surviving findings (1 BLOCKING, 7 MATERIAL, 8 minor — record at `builder/analysis/2026-08-03-cre-plan-critique/`) to their folded changes and records two deliberate divergences with reasons. **`CRE-AM2` is quoted verbatim inside T1 Step 2 but is NOT yet appended to the prereg** — the executor commits it on the run branch before any stage runs. Seams after T7, T9 and T11; the Stage-3 findings note is deliberately outside this plan. Runtime prose is measured (critique V1), on pre-drop cells. |
| `superpowers/2026-08-04-HANDOFF-cre-stage3-findings.md` | **⚠ SUPERSEDED 2026-08-04 (evening) on next actions by `2026-08-04-HANDOFF-gbl-plan.md`** — the decision it said was the owner's has been taken (findings §4 Option A), and its owed item 2 (the Snyk LOW) is closed as accepted, struck in place with a revival condition. Remains authoritative for the `CRE-` experiment's internals and claims-not-to-revert. *(Original role:)* **ACTIVE — the CURRENT handoff, 2026-08-04 (Stage 3). Nothing supersedes it.** Supersedes `2026-08-04-HANDOFF-cre-t11-seam3.md` on next actions. A **seam** handoff at the end of the whole `CRE-` experiment: Stage 3 was its last stage, the findings note is written and classified, nothing is in flight, tree clean, draft PR #75. **The `CRE-` experiment is COMPLETE and nothing is owed by any session — the next action is the OWNER'S**, and it is a decision (which passing arm, if either, is worth a blind listen; whether the candidate data set's advantage justifies the re-crawl). Names eight claims not to revert, of which four are the ones a well-meaning editor is most likely to break: **`CRE-R2` firing twice and `CRE-R4` finding no clear winner are not in tension** (clause (i) fails *because* two arms pass, each the other's counterexample); **§0.4's censoring trigger is a reporting constraint, not a firing condition** — it forbids "clean pass", it does not bar `CRE-R2`; **`B-S1-P1b`'s matched-only comparison is UNREADABLE, not favourable**; and **"arm" in `CRE-R4` means a CELL** — the prereg is genuinely ambiguous there (clause (iii) presupposes an arm carrying several ramp settings, which a cell cannot), and re-resolving it the other way manufactures a clear-winner call the experiment does not support. Its owed list is one open item (the pre-existing Snyk LOW in `cre_sweep.py`, whose "close as accepted when the track completes" branch is now arguably due) and one discharged. Also flags, as a non-`CRE-` item, **eight `TEST-QUEUE.md` entries dated 2026-07-22 to 2026-07-27 still marked `QUEUED` and never discharged.** **Nothing is adopted; `REQ-38` is unspent.** |
| `superpowers/2026-08-04-HANDOFF-gbl-harness.md` | **⚠ SUPERSEDED 2026-08-05 on next actions by `2026-08-05-HANDOFF-cau-audit-run.md`, and transitively by `2026-08-05-HANDOFF-cau-scoring.md` (the current one)** — the listen it hands over has been run, unblinded, written up, and followed by the `CAU-` coherence audit. Remains authoritative for the `GBL-` harness chunk's internals and its six claims-not-to-revert. *(This row claimed to be the CURRENT handoff until 2026-08-05, through three subsequent handoffs — the stale-CURRENT class an audit has flagged before.)* *(Original role:)* **ACTIVE — the CURRENT handoff, 2026-08-04 (later). Nothing supersedes it.** Supersedes `2026-08-04-HANDOFF-gbl-plan.md` on next actions. A **seam** handoff: the `GBL-` plan is executed to its last task — harness built, 30 tests green, `GBL-AM1` committed, branch pushed, draft PR #77 updated, nothing in flight, no listener on any port. **The listen has NOT been run and no journey exists on either arm.** Names the next work — **a fresh, mechanics-only RUNNER session** reading `RUNNER-BRIEF.md` and nothing else — and the further fresh session that writes up `gbl_result.json`. Carries six new claims not to revert on top of the plan handoff's six; the sharpest three: **`GBL-CORR1` is a correction, not an amendment** (§5's table said `≥ 3` while its own prose said `≥ 5`; do not restore 3 and do not renumber the series); **the hub set is frozen on V0 and mapped by MBID**, since node ids are not shared between artifacts and a per-arm threshold would let an arm score better by compressing its degree distribution; and **`assert_page_data_clean` must not go back to scanning artist names** — it would abort generation on The Cramps. Flags two open items that are **not** `GBL-`: the owner's no-commercialization ruling is in memory but not in the repo record, and the eight 2026-07-22→27 `TEST-QUEUE.md` entries are still unruled-on. |
| `superpowers/2026-08-04-gbl-harness-execution-log.md` | **RETAINED EXECUTION LOG** for the `GBL-` harness build session. **Owns no figures and no status.** Records: the seam constraint that shaped everything (§1); two governing-document defects — **`GBL-CORR1`** and **six defects in the plan's own code, four of them tests that would have passed while testing nothing** (§2); three decisions with reasoning, including the frozen-hub-set reversal and the accepted Snyk LOW with its revival condition (§3); gate outcomes including the five that were **never reached** (§4); **§5, the one thing this session could not verify** — `gbl_generate.py` has never run against the real artifacts, what was done instead, and what that does not cover; operational timings (§6); and the standing-layer delta, which is the **owner's** memory edit rather than this session's (§7). |
| `superpowers/2026-08-04-HANDOFF-gbl-plan.md` | **⚠ SUPERSEDED 2026-08-04 (later) on next actions by `2026-08-04-HANDOFF-gbl-harness.md`** — the plan it hands over has since been executed to its last task. Remains authoritative for the planning chunk's own internals and its six claims-not-to-revert, all of which still stand. *(Original role:)* **ACTIVE — the CURRENT handoff, 2026-08-04 (evening). Nothing supersedes it.** Supersedes `2026-08-04-HANDOFF-cre-stage3-findings.md` on next actions. A **seam** handoff: the owner chose findings §4 **Option A** and the `GBL-` blind listen is specified (spec, owner-reviewed twice) and planned (8 tasks); branch `gentle-arm-blind-listen`, draft PR open, nothing in flight. Names the next work — **a fresh session on Opus executes the plan inline** — and the three built-in session boundaries (Task 3 owner gate → `GBL-AM1`; the listen in a mechanics-only runner session; the write-up in a further fresh session). Carries six claims not to revert, the sharpest three: **V0 is the adopted production artifact, NOT the CRE `E-S0` cell** (drop flags differ; the V0 mirror check exists because `CRE-G1`(a)'s warrant was on `E-S0`); **the margin is ≥ 5 of 16, rounded UP from the approved 3-of-10**; and **clips are name-based for both arms by design**. |
| `superpowers/2026-08-04-gbl-planning-execution-log.md` | **RETAINED EXECUTION LOG** for the `GBL-` design-and-plan session. **Owns no figures.** Records: the owner's Option A decision and his three design-review calls (§1); seven decisions with reasoning, including the withdrawn two-instances recommendation and why the record sided with the owner, and **V0 ≠ `E-S0` with the drop-flag reasoning** (§2); five paths decided against (§3); three defects in this session's own output, each caught by the owner or by grounding (§4); the deferral discharge (§5); and the sealed-dir and approved-pairs conventions plus the observed Spotify export shapes (§6). |
| `superpowers/findings/2026-08-04-gentle-arm-blind-listen-results.md` | **AUTHORITATIVE for the `GBL-` read — it OWNS the `GBL-` figures**; cite it by section and never restate a number from it. **⚠ QUALIFIED BY `CAU-`, and NOT superseded — never cite this null without reading `findings/2026-08-05-coherence-audit-results.md` first.** Nothing here is stale and every figure stands, which is exactly why no staleness check catches this: the confound this instrument could not see — one clip cannot support a coherence judgement about an unfamiliar artist, **and that bites only in the arm that delivers unfamiliar artists** — was tested afterwards and the gentle arm's coherence **met the owner's bar**. **`CAU-` may NOT be used to reopen the comparison** (`CAU-` §6) and `GBL-` §5's run-once rule still binds the verdict; what moves is **how much weight this null carries as evidence about the rebuilt arm, not the verdict**. A session read the null accurately and incompletely because this row said none of that (2026-08-05). The four-part report (measured / inferred in plain language / weakest link / options) over the executed blind listen, **written by a session that did not run it** (the `CRE-` Stage-3 rule; harness log §8). **Raw record: `../builder/analysis/2026-08-04-gentle-arm-blind-listen/gbl_verdicts.json`, `gbl_result.json`, `gbl_owner_notes.md` and `gbl_page_data.json`** (the stimulus as presented), all committed before this document was opened. **The read under the pre-registered bar: margin 3 of 16 deep rows against a bar of 5, so the branch is `no_detectable_difference` — production stands and Option A closes without adoption.** **Four things travel with that sentence and must not be dropped:** `GBL-Q1` (novelty) went **8–0 to the rebuilt package** but §5 pre-registered **no threshold for it**, so no bar may be chosen now (its §4 option D is argued unavailable for exactly that reason); **five of the seven undecided deep rows were undecided because of clip defects**, so the instrument lost resolution rather than finding equivalence; **the blind did not hold** on the owner's side, volunteered before he read the result; and the **d0 anchor went 6–0 to the rebuilt package** but is excluded from the tally by §5 and rescues nothing. Also records that **neither arm collapsed** (`GBL-Q2`'s collapse clause, empty on all eight pairs) and that **none of the four hidden offline metrics predicted his picks** (fame 5 of 9 — a coin flip; payload and length run against the picks), which is fresh standing evidence for `REQ-38`. Its §5 is the barred-reads list — no single-factor attribution, no "equivalent" (`REQ-41`), no "passed", no "failed". Its §6 records five instrument defects for the next listen, the sharpest being that **the per-row pick had no question attached to it** in either the spec or the page. **Nothing adopted, no default changed, no shipped code touched; the re-crawl decision untouched; and `GBL-` §5's run-once rule now bars re-listening this verdict.** |
| `superpowers/2026-08-04-gbl-run-execution-log.md` | **RETAINED EXECUTION LOG** for the **blind runner** session — mechanics-only, driven entirely by `RUNNER-BRIEF.md`. **Owns no figures and makes no assessment**, deliberately. **⚠ Its §5 must be read before anything is built on the `GBL-` result:** it states plainly what the runner saw and did not see (never opened the sealed mapping; did read the clips map indirectly to check per-side coverage; saw the unblind script's one-line output only *after* all 32 rows were committed) and records **the owner's report that the blind did not hold on his side** — "almost always really easy to tell which side was the 'new' graph" — which the runner flags as the most load-bearing thing in the log and offers no view on. Also records: all five generation gates firing for the first time and **all passing**, with no pair swap needed (§1); what the smoke test actually confirmed beyond the brief's four items, including that the page's own save button writes through (§2); the **corrected ~15-minute Deezer preview TTL** and the wrong four-hour inference that preceded it, kept because the mistake is the useful part — **a duration claim needs both endpoints measured** (§3); the owner's three in-run observations recorded verbatim-in-substance and deliberately not characterised (§4); and four loose ends left deliberately for a session with context (§6). |
| `superpowers/2026-08-04-gbl-writeup-execution-log.md` | **RETAINED EXECUTION LOG** for the `GBL-` write-up session. **Owns no figures.** Records: the stale-`NEXT.md` reconciliation that opened the session (§1); the independent re-derivation of the tally and the two structural findings that came out of reading the verdicts rather than the result — **the primary read tallied an unlabelled pick that the owner answered on coherence**, and **the undecided rows were mostly clip-blocked** (§2); the decisions taken, including committing `gbl_page_data.json` and declining to propose any bar for `GBL-Q1` (§3); and the bookkeeping the blind runner correctly declined and this session discharged (§4). |
| `superpowers/specs/2026-08-04-gentle-arm-blind-listen-design.md` | **⚠ EXECUTED 2026-08-04 (night) — the listen has been run, unblinded and read; results at `findings/2026-08-04-gentle-arm-blind-listen-results.md`. Its §5 run-once rule is now BINDING: this verdict may not be re-listened.** Remains **the governing document** for the protocol and for every read; the findings note applied it and did not reinterpret it. *(Original role:)* **ACTIVE — the governing document for the `GBL-` blind listen.** Identifiers **`GBL-`** (collision-checked 2026-08-04). Pre-registers the workload and every read **before any journey exists** — the commit timestamps are the freeze evidence. Two arms (V0 = adopted production artifact + today's router; G = `B-S1` + the gentle ramp), both generated by the `CRE-G1`(a)-verified mirror; **package comparison, attribution barred**. Eight owner-approved pairs (`GBL-AM1`, committed at the plan's Task 3 owner gate); depths d0/d10/d20 with d0 an anchor excluded from the read; frozen one-axis claims `GBL-Q1` (novelty) / `GBL-Q2` (coherence); primary read over 16 deep rows with **margin ≥ 5**; run-once; sealed mapping; mechanics-only runner session; no Spotify lookups mid-listen. **Nothing adopts on any verdict; the `CRE-R2` qualifiers travel ("descent partly unmeasurable", never a clean pass); the re-crawl decision is out of scope.** |
| `superpowers/plans/2026-08-04-gentle-arm-blind-listen.md` | **COMPLETE 2026-08-04 (later) — executed to its last task; not live.** Accurate as the record of what was built, but six pieces of its inline code were wrong and were corrected in execution (harness log §2.2) — **read the code in the repo, never this document's listings.** *(Original role:)* **ACTIVE — the operational plan for the `GBL-` blind listen harness; the spec governs wherever it disagrees.** Eight tasks, all in `builder/analysis/2026-08-04-gentle-arm-blind-listen/`, reusing the frozen `CRE-` harness and frozen `api_src`; **no shipped code**. Task 3 is an **OWNER GATE** (pair approval → `GBL-AM1`). Task 4 carries the **five** hard generation gates (artifact identity; the **V0 mirror check** — real `find_journey` equals the mirror ladder on the adopted substrate; the ramp decomposition; the differential check; and the **run-state precondition** — every pair reaches all three depths on both arms, which is the one most likely to fire, since three pairs are the owner's own recombinations) plus the arm-leak assertion on page data. *(This row said "four" and omitted the run-state gate until 2026-08-04 (later) — a defect of absence, which no grep for a stale name can find.)* After Task 8 the listen is a **named handoff seam**: a mechanics-only runner session executes `RUNNER-BRIEF.md`, and the write-up belongs to a further fresh session. |
| `superpowers/2026-08-05-HANDOFF-ulf-filter-fix.md` | **ACTIVE — the CURRENT handoff, 2026-08-05 (night). Nothing supersedes it.** Supersedes `2026-08-05-HANDOFF-ulc.md` on next actions. A **seam** handoff: the filter remit ran to its end — the merged `ULF-` filter built, censused over both archive populations, wired and snapshot-tested; `ULC-F1`/`ULC-F2` discharged and struck in results §5; CROOVE struck from the acceptance canonical list by owner ruling; verification build green. **The next action is the owner's** (two merges in order, and the map-switch decision). Carries six claims not to revert, the sharpest three: the cut line stays `ULC-D2` as ruled with Keith Scott the named residual; the carried frozen verdicts are deliberately un-re-run; and `keep_clip_provenance` is `ULC-F4` instrument data with no read licensed. **Nothing adopted; the trial artifact is scratch evidence only.** |
| `superpowers/2026-08-05-ulf-filter-fix-execution-log.md` | **RETAINED EXECUTION LOG** for the `ULF-` filter-fix session. **Owns no figures and no status** — figures live in the frozen payloads and `builder/analysis/2026-08-05-ulf-census/ulf_census.json`; the one exception is §7's trial-build output, an operational measurement with no other home. Records: the design-before-mechanics reversal and the merge decision with its subset argument (§2); everything decided against, including re-running prior keep-checks (§3); the `ULC-F1` machinery and the 36-test refusal fallout with the mirrors-guard catch — `cre_build.py` as an unlisted era-pinned caller (§4); six census design decisions, including why the `ULC-` universe had to be re-derived from artifacts and why verdicts carry per artist (§5); the census run record and the Snyk adjudication (§6); and **§7, the acceptance-gate collision on CROOVE** — the facts, the owner's ruling, and the passing verification build. |
| `superpowers/plans/2026-08-05-msw-package-adoption.md` | **✅ COMPLETE 2026-08-06 — all twelve tasks run; the map switch is adopted and deployed. Not live; do not execute it again.** **⚠ It carries a CORRECTION block above Task 12 Step 1**: the task omitted `infra/README.md` §3 (build and push the image), and executing it as written would have deployed the new map under the old code. Read that block before reusing this as a deploy template. *(Original role:)* **ACTIVE — the operational document for the `MSW-` map switch**, the adoption of `ULC-A4` (candidate data + trimmed-union connection rule + gentle fame ramp) as the production map and router. Identifiers **`MSW-`** (collision-checked across every ref 2026-08-05). Twelve tasks with **named handoff seams at 5, 7, 10 and 12**; Seam 3 is an **OWNER STOP** — verification results go to him before any default is flipped. **⚠ This adoption is an owner OVERRIDE of the standing `GBL-` null**, taken knowingly and recorded as his authority, not as something `GBL-` or `CAU-` licensed. Gates `MSW-G1`–`G3`, verification checks `MSW-V1`–`V4` (`V4` is an `ml-graph-analyst` dispatch on the percentile-frame deviation, derivation only). Carries a **Named deviations** section — three places where the shipped package differs from the arm that was listened to and audited — which travels with any claim that this ships "what was heard". Three further deviations were taken during execution and are recorded in the log, not here. **Task 9's build command was corrected in execution 2026-08-06** and now carries `--algorithm`, `--cap-strategy` and `--require-fame`, none of them optional; `V4` has been **run** (2026-08-06). |
| `superpowers/2026-08-05-msw-execution-log.md` | **RETAINED EXECUTION LOG** for the `MSW-` map switch. **Owns no figures and no status.** Its **§0 is the authority record** — why this adoption overrides the `GBL-` null, on whose authority, and the explicit note that neither `GBL-` nor `CAU-` licensed it; **read it before describing this work as evidence-backed.** §1 records the scope correction found before any code was written (`ULC-` §4.1 said two implementation jobs; it is four). Per-task entries carry the reasoning, including **two vacuous tests caught by perturbation** — a frozen-equivalence test that could not see the tie-break it existed to pin, and `MSW-G2` measured over a fixture carrying no fame — and the finding that the mirror's "never as `+ 0.0`" rule is not meaningful in the shipped router. Three execution-time deviations from the plan are recorded here with their reasoning. **Also carries the Seam 2 closeout outcomes and the full Task 8 record (2026-08-05/06):** the fame-coverage run and its counts, the S3 backup location for the gitignored snapshot, the **owner's acceptance of the D6 growth**, and a **verification table covering Tasks 1–7** — six inherited claims exercised rather than read, four perturbed until they went red. **⚠ Its Task 8 section owns two defects found in the plan's own commands**, the sharper being a missing `--algorithm` that would have fetched nothing **while exiting 0**, with the transferable form named: an equality between two derived counts passes vacuously at zero. **Task 9 and Task 10 (2026-08-06, later):** a **third** plan-command defect — the build would have produced a **fameless artifact while exiting 0** — recorded with the **pattern behind all three**, that Tasks 8 and 9's commands were written before Tasks 3 and 4 built the CLI they invoke and were never reconciled against it; the remaining commands were swept once and the class is structurally exhausted at Task 9. Also: **`MSW-G3`'s first red firing**, the acceptance **rejection** and the owner's **Option A** recalibration (recorded as his decision), the **era-pinning** of the frozen calibration probe, the artifact's identity, and `MSW-V1`'s pass with its red control. **⚠ Its `MSW-V4` section carries a premise correction** — the frame deviation is **two knobs, not one** — and files the 21-node null-pricing gap under deviation **3**, not deviation 2. **Task 10 continued (2026-08-06, night) — the successor session:** `MSW-V2`'s exposure re-measure with its factor table now **machine-asserted**, `MSW-V2B` (**not in the plan**, folded in on the owner's authorisation), `MSW-V3`'s two boots, and `B2`/`B3`/`B4`. **⚠ Two entries there are guards against a wrong summary rather than a wrong number:** `MSW-V2`'s paired median reads **zero** while its mean moves by double digits — `ULC-R1`'s statistic defect reproducing, deliberately unpatched — and **two journeys got worse**. **⚠ `B4` found a defect in that session's own docstring** (held-constants asserted in prose that no code checked), fixed by making the code true. |
| `superpowers/2026-08-06-clip-cover-art-execution-log.md` | **RETAINED EXECUTION LOG** for the clip cover-art defect and `DEP-34`. **Owns no status and no scoring figures**; it *does* own the Deezer response-shape and id-coverage measurements, which have no other home. Records: the root cause — Deezer embeds a **different artist object per endpoint**, so `/artist/{id}/top` carries no picture key and the id path always produced an empty image (§1); the measurements, with the `pop_raw`-is-not-impressions caveat attached (§2); **§3, the four separate mechanisms behind the "inconsistency", only one of which is the bug** — and the note that the population was drifting from cache-hit to defect over 30 days, so it was getting worse with no commit behind it; the fix and the declined second-request alternative (§4); **§5, why 254 passing tests could not see it** — the fixtures invented the response shape, exactly as `clips.py`'s own docstring predicted — **including a near-miss whose obvious reading is WRONG**: the `GBL-` harness's 2026-08-04 fixture decision was *correct for its own call site*, and the transferable form is that **a fixture reconciled against one call site is evidence about that call site only**; **§6, `DEP-34`** (below); what was decided against (§7); two deferrals with conditions (§8); the operational and verification record (§9); and **§10, a defect B3 found in this session's own test** — an agreement assertion that survives uniform breakage. |
| `superpowers/2026-08-06-HANDOFF-clip-cover-art.md` | **ACTIVE — the CURRENT handoff.** Supersedes `2026-08-06-HANDOFF-msw-adoption.md` on next actions. **A SEAM handoff** — the work is finished, deployed and verified, so the cold-read check does *not* apply. The clip cover-art defect is **fixed and live** (`artistpath-api:641ced7`), with 404 stale cache entries cleared so the fix is visible now rather than over 30 days. **⚠ Its headline is NOT the clip fix but `DEP-34`:** an API-only deploy silently reverts the graph, because `ARTISTPATH_DEPLOY_GRAPH_KEY` defaults to the pre-`MSW-` artifact and `.env.deploy` does not set it — **this deploy would have undone the previous day's adoption**, and only `cdk diff` catches it. Carries four claims-not-to-revert, two of them guarding deliberately-impoverished test fixtures. **PR #85 MERGED 2026-08-06** at `fee46e2` — production and `main` agree. **Owed by the owner: `DEP-34-FIX`** (make both deploy variables required in `app.py`, before the next artifact adoption) **and the queued hand test.** Nothing owed by a session. |
| `superpowers/2026-08-06-HANDOFF-msw-adoption.md` | **⚠ SUPERSEDED on next actions 2026-08-06 (later) by `2026-08-06-HANDOFF-clip-cover-art.md`. Still AUTHORITATIVE for the `MSW-` adoption itself** — what was adopted, on whose authority, and the `GBL-`-override record, none of which the successor touches. **Its "two redundant local servers left running" is now STALE — both were stopped on the owner's instruction at `76d3b82`.** *(Original role:)* **ACTIVE — the CURRENT handoff.** Supersedes `2026-08-06-HANDOFF-msw-seam3.md` on next actions. **A SEAM handoff** — the `MSW-` plan is complete at all twelve tasks, so the cold-read check does *not* apply. **The map switch is ADOPTED AND DEPLOYED: four defaults flipped in `a06ab58` and the new map is live at `musicapp.cmiller.io`.** **⚠ The adoption is an OWNER OVERRIDE of the `GBL-` null, never evidence-backed adoption** — neither `GBL-` nor `CAU-` licensed it. Records the direction-of-staleness INVERSION that now governs this corpus: *"nothing is adopted" is stale wherever it describes the present, and correct inside a frozen findings row* — **not greppable, read for tense**. Carries six claims-not-to-revert, the new `CLIP-1` deferral (right artist, unrepresentative clip — distinct from `BYP-13`), `ULF-3` re-tested and now **half-due**, and a new deferral on the committed test fixtures, which predate the adopted graph. Names two redundant local servers left running at the owner's request. **The next action is the owner's and it is USE, not work.** |
| `superpowers/2026-08-06-HANDOFF-msw-seam3.md` | **⚠ SUPERSEDED on next actions 2026-08-06 by `2026-08-06-HANDOFF-msw-adoption.md`** — the owner gave the Seam 3 go and Tasks 11–12 both ran, so the decision it was waiting on is taken and the map switch is live. **Still authoritative for the Task 10 verification internals** (`MSW-V1`, `MSW-V2` and its two worse slots, `MSW-V2B`'s split result, `MSW-V3`) and for its eight claims-not-to-revert, all of which stand. Its Task 11 obligations — Step 0's era-pins and Step 0b's agent-definition correction — are **discharged**, and the Pino Palladino observation it calls the sharpest input to the decision is now tracked as **`CLIP-1`**. *(Original role:)* **ACTIVE — the CURRENT handoff.** Supersedes `2026-08-06-HANDOFF-msw-task10-midflight.md` on next actions. **A SEAM handoff** — Task 10 complete, **Seam 3 reached**, so the cold-read check does *not* apply and **the next action is the owner's DECISION, not work**. Records `MSW-V2` (exposure re-measured, a report row with **no branch assigned and none supplied**), **`MSW-V2B`** (**not in the plan** — folded in on the owner's authorisation; it closes the question the previous handoff named as the most decision-relevant thing unmeasured, and finds the frame deviation **does** change routes at depth), `MSW-V3` (the app boots, presses and plays), and `B2`/`B3`/`B4`. **⚠ Its eight do-not-revert claims are unusually load-bearing**, four of them guarding against a *summary* rather than a code change: the paired median reading zero is `ULC-R1`'s statistic defect reproducing and must not be patched or reported alone; two slots got **worse**; the share is an under-count in a known direction; `MSW-V2B` splits and both halves travel; it is **hypothetical until Task 11**; and it measures whether journeys **differ**, never whether they are **better**. **⚠ Its sharpest finding is that "the clip resolves" is NOT "this artist has music of their own"** — no automated check here distinguishes them. **Two detached servers are deliberately left running** with their PIDs recorded, for the owner's hand test. |
| `superpowers/2026-08-06-HANDOFF-msw-task10-midflight.md` | **SUPERSEDED on next actions 2026-08-06 (night)** by `2026-08-06-HANDOFF-msw-seam3.md`; **still authoritative for Task 9's internals** (the three plan defects, `MSW-G3`'s firing, the acceptance rejection and the owner's Option A recalibration), and **its "Claims that must NOT be reverted" list still stands in full**. **⚠ Its "next action is `MSW-V2`, then `MSW-V3`" is spent**, and the open question it names as unmeasured has since been **closed by `MSW-V2B`**. Supersedes `2026-08-06-HANDOFF-msw-task8-midflight.md` on next actions. **⚠ A MID-FLIGHT handoff** — Task 9 complete, **Task 10 at 2 of 4**, so **Seam 3 is NOT reached** and `session-start`'s **cold-read check applies**. Records **why** it stopped there, and the reason is a role conflict rather than the degradation tell: the plan requires that the Seam-3 session not be the one that reads the verification results, and this session **built the artifact and recalibrated the acceptance bound that admitted it**. Carries the artifact's identity, the owner's **per-session pre-authorisations** (flagged as NOT carrying forward) and his standing hard stops, an enumerated *decided-against* list, six do-not-revert claims, and a **stated position on the open decision** rather than a menu. **⚠ Names the one open question nobody has measured:** `MSW-V4` bounds what the router adds up, not what it chooses — no journey has been run on this artifact. **B2, B3 and B4 travel with the work to the successor**, deliberately not run against a half-finished Task 10. |
| `superpowers/2026-08-06-HANDOFF-msw-task8-midflight.md` | **SUPERSEDED on next actions 2026-08-06 (later)** by `2026-08-06-HANDOFF-msw-task10-midflight.md`; **still authoritative for Task 8's own internals** (the fame-coverage run, its two command defects, the Tasks 1–7 verification table). **⚠ Its "next action is Task 9" is spent** — Task 9 is done and the artifact exists. Supersedes `2026-08-05-HANDOFF-msw-seam2.md` on next actions. **⚠ A MID-FLIGHT handoff** — Task 8 of 12, two tasks short of Seam 3 — so `session-start`'s **cold-read check applies**: state the situation back before acting. Records **why** it stopped there rather than at the seam: Task 9 builds a gitignored artifact whose only identity is a hand-recorded sha256, and a session interrupted mid-Task-9 hands its successor a file it cannot identify. Carries the owner's **standing instruction to verify inherited work as you go** (said in conversation, in no other file), the three numbers computed but not otherwise recorded, and an enumerated *decided-against* list. **Task 8's outcome:** fame coverage 75,000 of 75,000; **two defects found in the plan's own commands**, one of which would have fetched nothing while exiting 0. **Tasks 1–7 verified by exercise; nothing wrong found in the previous session's code.** |
| `superpowers/2026-08-05-HANDOFF-msw-seam2.md` | **SUPERSEDED on next actions 2026-08-06** by `2026-08-06-HANDOFF-msw-task8-midflight.md`; **still authoritative for Seam 2's own internals** (Tasks 1–7: what was built, the six do-not-revert claims, the two operational facts). **⚠ Its "next action is Task 8" is spent** — Task 8 is done. Four of its six do-not-revert claims have since been **verified by exercise rather than assertion**. Supersedes `2026-08-05-HANDOFF-ulf-filter-fix.md` on next actions (every action that note owed is discharged: both merges landed and the owner took the map-switch decision). A **seam** handoff — Tasks 1–7 of 12 at the plan's own Seam 2, tree clean, nothing in flight. **The next action is WORK, not a decision**: Task 8, whose first step is backing up the gitignored single-machine fame snapshot. **⚠ Carries the override notice**: this adoption overrides the standing `GBL-` null on the owner's authority, and neither `GBL-` nor `CAU-` licensed it. Its "must not be reverted" list covers six claims a well-meaning editor would plausibly "fix" — chiefly `require_fame=False` being deliberate until Task 11, the fame percentile frame being the served artifact's own population, and the `GBL-` banner being a qualification rather than a reversal. |
| `superpowers/2026-08-06-HANDOFF-tcr-rerun.md` | **⚠ SUPERSEDED 2026-08-07 on next actions by `2026-08-07-HANDOFF-bypass-tray-ux.md`** — the UX track it did not anticipate has shipped and deployed. Remains authoritative for the `TCR-` re-run's internals and its claims-not-to-revert, all of which stand; **its Option C is still unblocked and still the owner's.** *(Original role:)* **ACTIVE — the CURRENT handoff.** Supersedes `2026-08-06-HANDOFF-tce-void.md` on next actions. **A SEAM handoff** — the `TCR-` re-run completed under its committed pre-registration, everything committed and pushed, nothing in flight, no listeners on any port (the stale Vite server the previous handoff flagged, PID 249084, is gone). **Its headline: `TCR-` is computed, nothing reads enriched, and the co-credit investigation closes** — with the do-not-revert list guarding the adjudication (no pre-registered branch fires as written; do not retro-assign a label), the `no_read_licensed`-is-not-`null` distinction for arm B, the still-open same-name population question, and the now-unblocked Option C being the owner's call. Its "what I know that is not in the durable record" is **empty and checked rather than asserted**. |
| `superpowers/2026-08-07-HANDOFF-bypass-tray-ux.md` | **⚠ SUPERSEDED 2026-08-08 ON NEXT ACTIONS by `2026-08-08-HANDOFF-cex-design.md` (row below).** Its own record of the bypass-tray track is untouched and remains accurate; only its ranking as "what to do next" has moved, and **`SNS-1` is still open and still the owner's**. *(Row corrected at the `CEX-` closeout; it had gone on claiming to be the current handoff, so the map named two at once.)* *(Original role: **ACTIVE — the CURRENT handoff.** Supersedes `2026-08-06-HANDOFF-tcr-rerun.md` on next actions. **A SEAM handoff** — the bypass-tray UX track complete, PR #89 merged at `a7a8b9a`, **deployed to production and verified**, nothing in flight, every port free. Carries the claims an editor must not revert, of which three are deliberate departures from the supplied design and are the ones most likely to be "fixed" back: the strip says **"Reroute from here"**, not the design's "Not this step"; the **clip-length line stays OFF** the landing page; and the landing page carries **no mechanism sentence**. Also: **`NOTICE_MIN_MS` is a floor on reading time, not an artificial delay**; the notice's lifetime **must not** be keyed on the artist list (that was the stranded-notice bug); and **the owner's `MSW-` map pass does NOT license the `GBL-` null**. Names one new open item, **`SNS-1`** — the billing alarm has zero subscribers, checked directly, pre-existing, and the owner's because it needs his inbox.)* |
| `superpowers/2026-08-07-bypass-tray-ux-execution-log.md` | **ACTIVE — retained reasoning** for the 2026-08-07 UX track and `DEP-34-FIX`. **Owns no figures.** Records why the design's own 1b annotation overrode its chosen 2c wording; the three gaps the design left; why `DEP-34-FIX` had to require **both** variables rather than one (the two defaults agreed with each other, so a reverted service passed every downstream check); and, in **§3, four defects in the session's own work** — a stranded notice found by writing the test, an e2e wait that depended on text being absent elsewhere, **that same defect class reintroduced hours later in a throwaway script, producing a false claim about the product that the owner's disbelief caught**, and a browser claim asserted without testing. §5 carries three deferrals with success conditions (`SNS-1`, `SMP-1`, `FE-SNYK-1`). |
| `superpowers/2026-08-06-tcr-rerun-execution-log.md` | **ACTIVE — retained reasoning** for the `TCR-` re-run. **Owns no figures** (`builder/analysis/2026-08-06-tcr-thin-catalogue-rerun/` does) and **states no status** (`NEXT.md` does). Successor in the co-credit chain to `2026-08-06-tce-thin-catalogue-execution-log.md`, whose §7 re-run requirements it executed item by item. Records the anchor-choice lesson proving the method (the zero-candidate guess measured at 4), **two defects in this session's own work** (a phase-1 crash fixed before any gate value existed, and the carried branch table's gap that the result landed in — recorded, not patched), the per-arm gate outcomes including `TCR-G3` firing for arm B as predicted, the instrument being **shown red, not only green** (tie machinery vs brute force, band assert fired deliberately), the correction of the `TCE-` spec row's stale "NOT YET EXECUTED", and `ULF-3` re-tested (still half-due) rather than copied forward. Its §7: Option C is now unblocked and is the owner's. |
| `superpowers/2026-08-06-HANDOFF-tce-void.md` | **⚠ SUPERSEDED on next actions 2026-08-06 (night) by `2026-08-06-HANDOFF-tcr-rerun.md`** — the re-run it ruled has happened and the stale Vite server it flagged is gone. **Still authoritative for the void run's internals; its do-not-revert claims all stand.** *(Original role:)* **ACTIVE — the CURRENT handoff.** Supersedes `2026-08-06-HANDOFF-cocredit-investigation.md` on next actions. **A SEAM handoff** — the probe reached a designed stopping point (a gate firing is a provided-for outcome), everything is committed, nothing half-built, and the degradation tell did **not** fire. **Its headline is that the next action is RULED, not open:** the owner's 2026-08-06 decision that `TCE-` be **re-run under a NEW pre-registration** with externally anchored gate thresholds by a **fresh session**; that the **fired `TCE-` record stands untouched**; and that a **same-name population probe is deferred until after the re-run**, deliberately. **The replacement pre-registration was deliberately not drafted** — its absence is intentional. Carries six do-not-revert claims, chiefly that **`TCE-` is VOID with no outcome and no inferable branch**, that **the instrument being correct and the threshold being wrong travel together**, and that **§6 item 1 is only PARTIALLY discharged**. Records one item needing the owner: a **stale Vite server on port 5174, PID 249084**, which the earlier sweep missed by checking 5173/8000 only. Its "what I know that is not in the durable record" is **empty and checked rather than asserted**. |
| `superpowers/2026-08-06-tce-thin-catalogue-execution-log.md` | **ACTIVE — retained reasoning** for the Laura Lee closure and the `TCE-` void run. **Owns no figures** (three `builder/analysis/2026-08-06-*` directories do) and **states no status** (`NEXT.md` does). Its **§2 records four defects in this session's OWN designs, all caught before any outcome** — a wrong base rate in an effect-size justification, a statistic that could be a mathematical **constant**, that same statistic being **blind to a concentrated effect** (`ULC-R1` reproducing inside a document that asserted it was not), and an **unsatisfiable `TCE-G2`**. Its **§7 is what a re-run must not repeat** and is the section to read before drafting the replacement. **§5 records a correction against itself:** both D6 standing-layer figures were written before being measured and were wrong, caught in the same closeout by running the commands — kept visible because it is this project's named failure class occurring in the log whose purpose is to be trusted. |
| `superpowers/2026-08-06-HANDOFF-cocredit-investigation.md` | **ACTIVE — the CURRENT handoff.** Supersedes `2026-08-06-HANDOFF-clip-cover-art.md` on next actions. **⚠ A MID-FLIGHT retirement**, triggered by the degradation tell rather than by unfinished work — the work reached its seam and both approved items are committed. **Read its enumerations rather than trusting its judgement**; that is the point of the mid-flight form. Carries the figures computed but never written to any analysis directory (the depth-census and ramp-control numbers, Laura Lee's top-6, the name-collision search), everything decided against, the owner's in-conversation rulings not yet in any file — **notably that Andrew VanWyngarden and Zach Condon should NOT have been dropped by the filter, which is what withdrew the drop-rule option and is in no rule document** — and a stated position on which open item to take first (the Laura Lee puzzle, not B2) with the reasoning to argue against. **Nothing in flight; ports 8000 and 5173 swept and free.** |
| `superpowers/2026-08-06-cocredit-investigation-execution-log.md` | **ACTIVE — retained reasoning** for `CCR-`, `RCC-` and B1. **Owns no figures** (the three `builder/analysis/2026-08-06-*` directories do) and **states no status** (`NEXT.md` does). Its §2 records three defects in this session's *own* designs, §4 records **three of its own wrong claims corrected in-session** — a wrong archive (contribution 5 vs 3), wrong drop-list variants (production vs ALG-B), and an overstatement about the rescale — and §6 lists what is closed and what is open. **Nothing authored by another session was overturned.** |
| `superpowers/specs/2026-08-06-shared-recording-preregistration.md` | **⚠ EXECUTED 2026-08-06 — results at `builder/analysis/2026-08-06-rcc-shared-recording/README.md`, which OWNS the figures.** Remains **the governing document**. **ACTIVE.** Identifiers **`RCC-`**, collision-checked across all refs 2026-08-06. Successor to `CCR-` and **explicitly not an amendment to it**. Asks whether the class's top similarity edge is a **shared recording credit**, the question `CCR-`'s instrument could not see. **`RCC-AM1`, committed before any outcome value was observed**, replaced an absolute rank-50–100 comparison partner with a **relative** one (80% depth of the artist's own list): archived list length is itself a property of the arm (CLASS median 100, CONTROL median 18), so the original rule left CONTROL at 15 of 200 and would have made the between-arm reading worthless while the CLASS half read fine. **Outcome: `RCC-C1` = `null`** (CLASS lift +6.5 against a 10-point floor); **`RCC-C2` = `general`** at −6.5, **sign opposite to the hypothesis** — ordinary obscure artists show *more* co-credit at rank 1 than the class does. **The instrument was validated** (Khruangbin + Leon Bridges → 9 shared recordings incl. *Texas Sun*), which is what makes this null a result about the world rather than about the tool. **Its §5 barred reads bind every citation**, including that `RCC-C2` compares "top of your list vs bottom of your list" across arms. **No adoption on any branch.** |
| `superpowers/specs/2026-08-06-cocredit-relationship-preregistration.md` | **⚠ EXECUTED 2026-08-06 — results at `builder/analysis/2026-08-06-ccr-relationships/README.md`, which OWNS the figures.** Remains **the governing document**. **ACTIVE.** Identifiers **`CCR-`**, collision-checked across all refs 2026-08-06. Tests the owner's hypothesis that ListenBrainz over-weights similarity between artists who record together, against the central-but-unlistened class he observed at depth. **Outcome: `null` — and the instrument is why, which is a defect in this probe, not a reading of the result:** `inc=artist-rels` carries band membership, not shared recording credits, so **Laura Lee, the motivating case, scores `False` on it**. **Read it as: the hypothesis is UNTESTED at population scale, not disconfirmed — and equally, unsupported. Both halves travel together.** Superseded as an instrument by `2026-08-06-shared-recording-preregistration.md` (`RCC-`); **its null is not amendable into that test and stands for what it measured.** Two defects recorded rather than fixed: its `CCR-C1` plain sentence said *"or have recorded with"* while §3 fetched only `artist-rels` — **the plain-sentence drift the rule exists to prevent** — and **`CCR-C3` is void** (tallies every relation type of a matching artist, not the matching relation's type; carries no branch). What it *did* establish: the class is far likelier to carry any documented relation, 79.5% vs 57.0%. |
| `superpowers/specs/2026-08-06-thin-catalogue-edge-preregistration.md` | **⚠ EXECUTED 2026-08-06 (night) — VOID: `TCE-G1` fired and no outcome was computed.** Results (the gate failure) at `builder/analysis/2026-08-06-tce-thin-catalogue/README.md`, which OWNS the figures. **The fired record stands untouched by owner ruling; the re-run happened under a NEW document, `specs/2026-08-06-thin-catalogue-rerun-preregistration.md` (`TCR-`), which carries `TCE-AM1`/`AM2` forward.** Remains the governing document of the void record. *(Original role, whose "NOT YET EXECUTED" went stale at the void run and was corrected 2026-08-06 by the `TCR-` session:)* **ACTIVE — governing document.** Identifiers **`TCE-`**, collision-checked across all refs 2026-08-06 (`TCE`, `CAT`, `TCS`, `CSZ` all free; `TCE` taken). The **third** mechanism tried against the owner's observed class, after `CCR-` (band membership, null and instrument-limited) and `RCC-` (shared recording credits, null and instrument-validated): does an artist with **nothing of their own recorded** acquire artificially strong links? **⚠ It is NOT a route to widening the drop rule** — the owner closed that 2026-08-06 and this probe proposes nothing about whether an artist appears, only about how strong a link is. **Its §0 is the load-bearing part:** the motivating comparison (two same-named Laura Lees — figures owned by the closure's §2 table) is a **cross-artist score comparison and is barred**; every comparison here is **within one artist's own list, by RANK**, which is `RCC-AM1`'s lesson applied. **§0.1 names the dormant term** — `p99_log_clip` is non-monotone at the top, so it creates ties exactly where the probe looks, and it is dormant in every arm but the one that matters. **Two arms differing by TWO columns** (filters *and* rescale), stated as such, with `R2` written so no result may be attributed to the drop filters alone. **Census, not sample** — no seed, no sampling error. `TCE-G1` voids the probe outright if the catalogue counter fails four known-answer cells; `TCE-G2` voids it below 95 % coverage. **⚠ `TCE-AM1`, committed 2026-08-06 before any outcome value was observed, GOVERNS where it and the original text disagree.** It replaced the fixed `±0.05 / ±0.10` bands, which a pre-run derivation showed were **unreachable and blind**: below a base rate of roughly 0.007–0.023 (list-length dependent) the census median is **exactly 0 at every effect size including a total one**, and being a median it is **invisible to an effect confined to a minority** — `ULC-R1`'s defect reproducing in a document whose §4.3 claimed it was not. **Verified independently before amending**, not taken from the derivation on trust: at base rate 0.03, an effect in 20 % of the census at maximum strength reads `+0.0000`. The amendment re-bands `TCE-C1` as a **fraction of the attainable ceiling `C`** (computable from list membership alone, before any placement is read); adds **`TCE-G3`**, a pinning gate that can bar any read of `TCE-C1`; adds **`TCE-C3`/`C4`/`C5`** whose nulls are **exactly 0.5 / exactly 1.0 / computed from run state** and therefore base-rate-free; and conditions **`R3` on `TCE-C1` and `TCE-C3` both landing null**, because `R3` closes an entire investigation. It also **rewrote §4.1's plain sentence** — which said "a *famous* artist's ten strongest connections" while §1 carries no fame criterion — rather than restricting the population, because **arm A's archive carries no fame figure at all** and naming fame as the concentrating variable would bake in the guess `TCE-C5` exists to measure. **`TCE-C1` is kept, re-banded and still reported, not removed.** **⚠ The amendment has a stated COST:** the median's blindness was also robustness against MusicBrainz editor accounts and placeholder MBIDs counting as thin, and `TCE-C3`/`C4` give that up — three mitigations are owed at run time. Derivation and figures: `builder/analysis/2026-08-06-tce-c1-statistic-behaviour/`, which OWNS them. **No adoption follows on any branch.** **Its premise is `builder/analysis/2026-08-06-laura-lee-closure/README.md`, which OWNS those figures — and the premise is PARTIAL:** that closure kills the same-name MBID collision hypothesis **for Laura Lee** and **not as a class**, so the execution log's §6 item 1 is **discharged for its worked case and OPEN as a population question**. **`TCE-` neither tests nor closes it**, and the motivating case does not evidence `TCE-` either. |
| `superpowers/specs/2026-08-06-thin-catalogue-rerun-preregistration.md` | **⚠ EXECUTED 2026-08-06 (night) — results at `builder/analysis/2026-08-06-tcr-thin-catalogue-rerun/README.md`, which OWNS the figures.** Remains **the governing document**. **ACTIVE.** Identifiers **`TCR-`**, collision-checked across all refs 2026-08-06. **The re-registration of the VOID `TCE-` probe under the owner's Option A ruling** — a new document, not an amendment; the fired `TCE-` record stands untouched, and no outcome value existed there to inherit. **Its `TCR-G1` redesign is the point:** five validation artists chosen for unambiguous catalogues (exactly one exact-name MusicBrainz match each), their release-group counts fetched from the **live MusicBrainz API first** (`g1_anchor_fetch.py`, committed with its timestamped output), tight bands set around those counts (± max(2, 5 %) for drift between live MB and the 2026-07-29 dump; the zero cell exact), the document committed, and **only then** the counter run — the reverse of the guessed-threshold order that voided `TCE-`. **Both Laura Lees are demoted to reported run state** (the known MusicBrainz mis-filing contaminates them as ground truth); Alana Haim is the zero-detection cell. **The anchor-choice lesson is recorded:** Andrew VanWyngarden was chosen as a zero candidate and the live fetch returned 4. Carries `TCE-AM1`/`AM2` in full (per-arm ceiling `C` and bands as fractions of it, the `TCR-G3` pinning gate, `TCR-C3`/`C4`/`C5` with exact nulls and fixed plain sentences, the three mitigations, the barred reads, the §6 run-state ordering) and adds **exact average-rank tie resolution** for the rank-10 boundary (the void run measured one arm B list in five with one). **Pre-registers the eyeball read's interpretation**: same-name/mis-merged domination would be the open population question surfacing, never noise and never the mechanism confirmed. **No adoption on any branch, and it does not reopen widening the drop rule.** |
| `builder/analysis/2026-08-06-tcr-thin-catalogue-rerun/README.md` *(outside `docs/`)* | **ACTIVE — results of record for `TCR-`, and it OWNS the figures.** Run 2026-08-06 (night) under the `TCR-` prereg, committed `4918eea` before the counter touched any anchor. **All gates passed** — `TCR-G1` 5 of 5 externally anchored cells (total live↔dump drift across four orders of magnitude: **one** release-group), `TCR-G2` coverage 0.99991 — and the outcomes computed. **Headline: no enrichment anywhere; every licensed signal points the opposite way** — arm A `TCR-C1` null, `TCR-C3`/`C4` **depleted**, `TCR-C5` *below* its own null (no hidden concentrated minority); arm B `TCR-C1` barred by `TCR-G3` exactly as predicted (85 % of its reference artists have no thin neighbour — **a finding about the drop filters, not a broken probe**: shipped-map thin base rate 14× lower than the raw feed's), `TCR-C3` null, `TCR-C4` depleted. **⚠ Its §4 adjudication is the part to read before citing a branch:** *no* pre-registered branch fires as written — `TCR-R3` required `C1` and `C3` both null and `C3` landed beyond null at depleted; `TCR-R4` read the primary and `C1` landed null — a **recorded gap in the carried-forward branch table**, not patched post hoc. What is licensed: both adjacent branches carry the identical consequence, so **the thin-catalogue hypothesis is unsupported, the direction is opposite (the §0 confound's prediction), the investigation closes, and nothing is adopted**. Three mechanisms have now been tried against the owner's class and none explains it. **The eyeball read found the pre-registered surfacing condition did NOT obtain**: top contributors are dominated by correctly resolved credited personnel (the `CAU-` class), not same-name junk; isolated common-name candidates are named and license no rate — **the same-name population question stays open, untouched in either direction.** ⚠ **FORWARD NOTE 2026-09-10 — the class IS now attributed, from the other side:** its members have **no artist credit line in MusicBrainz at all**, so no probe over our credits or relations could ever see the mechanism; ListenBrainz's *deployed* job scored them through something outside the recording credit. Figures and the named examples (Rammstein's drummer, Meg White, Aerosmith's drummer) are owned by `../builder/analysis/2026-09-08-lbd-similarity/README.md` §5a. This document is unedited. |
| `builder/analysis/2026-08-06-tce-thin-catalogue/README.md` *(outside `docs/`)* | **⛔ ACTIVE — results of record, and the result is `TCE-G1` FIRING. The probe is VOID.** Run 2026-08-06 under `TCE-` + `TCE-AM1` + `TCE-AM2`, all committed first. It stopped at phase 3 of 7: **no `TCE-C1`, no `TCE-C2`, no `TCE-C3`/`C4`/`C5`, no `Δ_R`, no branch fired and none may be inferred.** The failing cell required the 1945 Laura Lee to hold ≥20 release-groups and she holds **16**. **⚠ The instrument is CORRECT and the threshold was wrong** — the dump counter agrees **exactly** with the live MusicBrainz API (16 and 37), orders all four subjects correctly and returns 0 for the motivating case; `≥20` was anchored on a *recording* count without checking what it implies for *release-groups*. **That does not un-void the probe and the document refuses to treat it as doing so**: a threshold corrected after seeing the value it failed on is not a gate. Recalibration is the **owner's** call, on `MSW-G3`'s precedent. **⚠ Carries a finding the gate surfaced by accident:** MusicBrainz itself mis-credits between the two Laura Lees, inconsistently across levels — the *recording* of *Not Up for Discussion* to the Khruangbin member, the *release-group* to the soul singer. So **cell 1 passed partly because of a mis-credit**, and the ground truth the gate checked against is contaminated. That bears on the **open population question** at `superpowers/2026-08-06-cocredit-investigation-execution-log.md` §6 item 1 and is **not** evidence for the thin-catalogue mechanism. **Also worth carrying:** §0.1's dormant term was real — **20.4 % of arm B reference artists have a score tie spanning the rank-10 boundary.** Its §4 counts are structural and the release-group figure is **not** a base rate. |
| `builder/analysis/2026-08-06-tce-c1-statistic-behaviour/README.md` *(outside `docs/`)* | **ACTIVE — the derivation behind `TCE-AM1`, and it OWNS those figures.** Pure arithmetic: it reads **no archive, no artifact, no dump**, computes **no base rate**, and is **no part of running `TCE-`** — the base rate is a swept free parameter and remains run state. Produced by `ml-graph-analyst` on a bounded brief and **independently re-verified before `TCE-AM1` was written**. Establishes that the census median of `Δ_R` is **pinned at exactly 0** when over half of reference artists have no thin neighbour (`b < 1 − 2^(−1/L)`), and that its 50 % breakdown point makes it **blind to an effect in a minority** — the two failures `TCE-AM1` repairs. Supplies the **exact nulls** that make `TCE-C3` (0.500) and `TCE-C4` (1.000) pre-registerable without knowing the base rate, and **names a trap**: the naive "share exceeding expectation" statistic has a null of 0.33–0.48, not 0.5, so pre-registering it against 0.5 would read depletion under a perfect null. **Its stated conservative direction matters** — real thin-ness clusters, which makes the insensitivity worse, not better. |
| `builder/analysis/2026-08-06-laura-lee-closure/README.md` *(outside `docs/`)* | **ACTIVE — results of record for the Laura Lee case, and it OWNS its figures.** Discharges the success condition at `superpowers/2026-08-06-cocredit-investigation-execution-log.md` §6 item 1 — **partially, and the partiality is the point.** **⚠ Diagnosis of one named case, carrying no pre-registration BY DESIGN; do not retro-fit criteria and do not read `n=1` as a population result.** **Kills** the same-name MBID collision hypothesis for Laura Lee: both Laura Lees are separately present under distinct MBIDs, their lists are disjoint in character with no bleed either way, the soul singer is not missing her listens, and contamination would have had to invent a coherent modern scene. **So `BYP-13` is NOT operating on graph edges here** — the worst reading on the table is removed — and the case cannot reach a user anyway, being in `drop_mbids` of both ALG-B payloads and absent from the adopted artifact. **Does NOT settle:** the collision question as a class (one correctly-resolved pair is not a general demonstration, and no rate may be inferred); where her 214 actually comes from (the two-guest-credits explanation is consistent with every figure and demonstrated by none); and it is **not evidence for `TCE-`**, whose striking cross-artist comparison `TCE-` §0 bars. Also corrects a truncation artefact: her absence from Khruangbin's list is the 100-entry cap, not an asymmetry. |
| `superpowers/specs/2026-08-05-unlistenable-filter-rule.md` | **ACTIVE — the governing rule document for the filter fix (the `ULC-` track's follow-on).** Identifiers **`ULF-`**, collision-checked across all refs 2026-08-05. **Committed before any census or clip lookup under the rule exists** — the freeze point is the commit timestamp. The rule: class = `ULC-D2` verbatim (no sole-credited substantial release group; **the owner ruled the cut line 2026-08-05: `ULC-D2` as ruled, Keith Scott the named residual false negative**); **no Discogs clause, and its absence is the fix** (results §1.4 — even a sole credit admits a drum sample library); keep only where a commercial-DSP link exists AND a clip resolves, each keep recording whether the clip was id-verified (instrument data for the deferred `ULC-F4`, never a criterion). **Supersession without reversal:** both adopted classes are strict subsets, frozen verdicts carry, no adopted verdict reverses, old flags and lists stay functional for era-pinning. Its ULF-5 fixes the `ULC-F1` (census manifest = archive artist set; mismatched population refuses to build) and `ULC-F2` (per-signal coverage write-back) mechanics. **Its ULF-6 fixes worked-instance outcomes in advance** — a keep among the six must be reported, never quietly absorbed. Adoption is not decided here. |
| `superpowers/2026-08-05-HANDOFF-ulc.md` | **⚠ SUPERSEDED 2026-08-05 (night) on next actions by `2026-08-05-HANDOFF-ulf-filter-fix.md`** — the filter work it names as the next remit is done, and its owed `ULC-F1`/`ULC-F2` are discharged. Remains authoritative for the `ULC-` track's internals and claims-not-to-revert. *(Original role:)* **ACTIVE — the CURRENT handoff, 2026-08-05 (evening). Nothing supersedes it.** Supersedes `2026-08-05-HANDOFF-cau-scoring.md` on next actions. A **seam** handoff: the `ULC-` track ran to its end, nothing in flight, tree clean, no listener on any port. **The owner has set the next session's remit — the filter work** (`ULC-F1`, `ULC-F2`, the fix itself, then re-censusing both drop lists). Carries the seven claims that must not be reverted, chief among them that **`ULC-R1` did not fire and must not be "finished" by swapping its statistic**, and that **`ULC-V1` is 6 not 7**. Also records the owner's tabling of the production-data rebuild and what that costs. |
| `superpowers/findings/2026-08-05-unlistenable-class-results.md` | **AUTHORITATIVE for the `ULC-` read — it OWNS the `ULC-` figures**; cite it by section and never restate a number from it. Raw data: `../builder/analysis/2026-08-05-unlistenable-class/`. **Two things were measured and they answer differently, which is the finding**: the class occupies a similar share of every cleaned map and is *slightly smaller* on the candidate data, yet the production router never routes through it while the candidate router does, on one journey reaching most of the interior — **a routing problem, not a population problem.** Figures §1.2 and §1.3; none is restated here. **`ULC-G1` passed `separable`; `ULC-R1` did NOT fire** — run state 95/96 and, more importantly, its pre-registered statistic is a paired median over a zero-inflated distribution and could not have seen the effect. **That defect is recorded in §1.3c and deliberately not patched**, because switching statistics after seeing which one is interesting is the fishing the design exists to prevent. §0 states what cuts against the headline; §4.1 pins down that **"switch to the new map" is two different actions**, neither with a listen behind it, and that **no re-crawl is needed**; §5 carries four follow-on items with conditions, three gating any crawl extension; §7 is a correction owed to the `CAU-` note. **Nothing adopted, no default changed, no shipped code touched.** |
| `superpowers/2026-08-05-ulc-execution-log.md` | **RETAINED EXECUTION LOG** for the `ULC-` track. **Owns no figures and no status.** Decisions with reasoning — why the census sets no bar, why the predicate was derived from the definition rather than fitted to six cases, why `ULC-P5` is measured but has no baseline, and why `main` was merged rather than rebased (commit timestamps are this track's integrity property). **Its §2 lists five design defects caught before the stage each affected**, of which `ULC-AM4` generalises: the source note said "eight of the nine can't-tells" without naming which, so the target set had to be reconstructed from the owner's notes and one member was a different kind of can't-tell — **a count in prose is not a set.** §3 is the one defect that survived to the result and was not patched. §6 carries the reusable operational fact that Discogs **masters** answered the question 20× smaller and 32× faster than **releases**. |
| `superpowers/specs/2026-08-05-unlistenable-class-preregistration.md` | **⚠ EXECUTED 2026-08-05 — results at `findings/2026-08-05-unlistenable-class-results.md`.** Remains **the governing document** for the track and every read. *(Original role:)* **ACTIVE — GOVERNING**, written and committed before any count existed. Identifiers **`ULC-`** (collision-checked across every ref). Fixes the populations as a factor table with two licensed one-knob comparisons, the predicate, both gate clauses and every branch **before any data**. Five amendments, each committed before the stage it affects: **`ULC-AM1`** (a pre-run disclosure on Keith Scott, plus `ULC-D1` as a declared precision companion), **`ULC-AM2`** (the owner's `ULC-OG1` ruling and four corrections, two to his own earlier account — it supersedes §2.2a's *reasoning* while leaving its branch intact), **`ULC-AM3`** (`ULC-OG3` ruled — count songs, not releases; `ULC-D2` supersedes `ULC-D0` as primary), **`ULC-AM4`** (`ULC-V1` is 6 not 7, and the bar is held at 5 in absolute count rather than rescaled), **`ULC-AM5`** (the primary read pools the deep rows; `d0` is an excluded anchor). **`ULC-AM0`** records a correction owed to the `CAU-` findings note rather than to this one. §5's nine barred reads bind every sentence of the result. |
| `superpowers/2026-08-05-HANDOFF-cau-scoring.md` | **ACTIVE — the CURRENT handoff, 2026-08-05. Nothing supersedes it.** Supersedes `2026-08-05-HANDOFF-cau-audit-run.md` on next actions. A **seam** handoff: the `CAU-` audit is scored, written up, the sealed owner note opened and answered, closeout run; nothing in flight, tree clean, no listener on any port; branch `gentle-arm-blind-listen`, draft PR #77. **`CAU-` is COMPLETE and nothing is owed by any session — the next action is the OWNER'S** and it is a decision (the findings note's §4 options). Carries six claims not to revert, the sharpest three: **`CAU-C3` fired and the findings note both reports its pre-registered reading and argues against it** — deliberate, and neither half may be deleted to resolve the tension; **the 53 slots were never split by depth**, refused as a post-hoc denominator after the owner raised a depth hypothesis, so its absence is a decision rather than a gap; and **nothing above §5 of the findings note was revised** after the sealed note was opened, which is that document's main integrity property. Flags the two long-open non-`CAU-` items (no-commercialization ruling not in the repo record; eight `TEST-QUEUE.md` entries unruled-on) at their sixth consecutive closeout. |
| `superpowers/findings/2026-08-05-coherence-audit-results.md` | **AUTHORITATIVE for the `CAU-` read — it OWNS the `CAU-` figures**; cite it by section and never restate a number from it. The four-part report over the judged coherence audit, **written by a session that did not design, build or run it**, and **committed before `cau_owner_notes_SEALED.md` was opened** — that commit boundary (`184b3ac`) is the evidence the read was not shaped by the owner's direction. Raw record: `../builder/analysis/2026-08-04-coherence-audit/cau_judgements.json` (committed `cb11b38`, before anything was scored), `cau_result.json`, `cau_page_data.json`. **`CAU-G1` passed 12/12 so nothing is void; `CAU-C1` landed `meets_bar` by one card; `CAU-C2` did not fire; `CAU-C3` FIRED.** Three things travel with any citation: **the primary result's margin is a single judgement**, so the branch label is the weakest part of it; **eight of the nine can't-tells record that there was nothing to listen to**, which is a third cause `CAU-C3`'s pre-registered binary did not anticipate — the note reports that reading *and* argues against it, and both halves are load-bearing; and **§6's barred reads travel with every sentence** (no comparison with today's app, no whole-path claim, no novelty claim, no adoption, and "settled" barred even on a pass). Its **§5** is the response to the owner's sealed note, appended afterwards — it records his comparative and depth claims **as his statements, converted into no finding**. Its **§6** is an addendum answering an owner question and measuring nothing about any criterion: **the class this audit found slipped past both drop filters because a single sole credit exempts an artist from each**, verified against both committed `ALG-B` lists. **Nothing adopted, no default changed, no shipped code touched; the `GBL-` null untouched.** ⚠ **FORWARD NOTE 2026-09-10:** the eight can't-tells with nothing to listen to are the uncredited-personnel class that `LBD-` Task 4 identified in ListenBrainz's deployed lists — see `../builder/analysis/2026-09-08-lbd-similarity/README.md` §5a. This document is unedited. |
| `superpowers/2026-08-05-cau-scoring-execution-log.md` | **RETAINED EXECUTION LOG** for the `CAU-` scoring and write-up session. **Owns no figures and no status.** Records: **§1, that the build-and-run chunk left NO execution log** and why it is deliberately not backfilled — reconstructing another session's reasoning from commits produces something that reads like a record and is a guess; the decisions taken (§2), including reporting `CAU-C3`'s pre-registered reading while disagreeing with it, and **refusing the depth split with the reasoning written down, because a later session will find it obviously computable**; **§3, what the commit boundary bought** — the independent read and the owner's sealed note reached the same mechanism and named the same worked artist, and the procedure is cheap enough to reuse wherever he has a strong prior; the defects found (§4), covering the five lint failures, the builder's filter-coverage gap, and a `BYP-13` exposure inside the featured-credit keep-check that the census disclosed and never acted on; gate outcomes (§5); and the standing-layer delta, zero in both units (§7). |
| `superpowers/2026-08-05-cau-audit-build-and-run-execution-log.md` | **RETAINED EXECUTION LOG** for the `CAU-` design, build and run chunk (commits `c8e5494`–`adafde1`). **Owns no figures and no status.** **Written by the session that did that chunk, while it was still live, and committed before it read any result** — it had seen none of `cau_result.json`, the findings note, the sealed note, the scoring log or handoff, `NEXT.md`'s current top block, **or any commit message after `adafde1`**, that last being the real leak vector since the intervening messages state the outcome. Written *after* the scoring session had already recorded the missing log as a permanent gap and declined to backfill it; the owner pointed out the session had not gone. Records: build-side population and instrument measurements that exist nowhere else, several carrying an explicit bar against comparative use (§1); **everything decided against** — seven rejected red-control designs, six rejected instrument variants, three process refusals including scoring its own work (§2); **seven owner statements that never reached a file** (§3), of which §3.1 confirms as fact what the `GBL-` findings note carries as an inference; **eleven defects where the handoff listed five** (§4), including two method defects about reporting an edit as done without asserting it; **§5, the design rationale nothing else states** — why distance 3 and not 2 or 4, why the `None` band is its own band, why no clips on the page, and the reframing to a one-sided question that dissolved the `GBL-` run-once tension and that everything else follows from; and **§6's gate table, which records a gate that was NEVER REACHED** (the browser render check — the Chrome extension was not connected) plus the Snyk MEDIUM adjudicated a false positive and rescanned clean. |
| `superpowers/2026-08-05-HANDOFF-cau-audit-run.md` | **⚠ SUPERSEDED 2026-08-05 on next actions by `2026-08-05-HANDOFF-cau-scoring.md`** — the scoring and write-up it hands over are done and the seal is opened. **Remains authoritative for the build-and-run chunk's own internals**, alongside `2026-08-05-cau-audit-build-and-run-execution-log.md`, which that chunk's session wrote later and which supersedes this handoff's defect list (eleven, against the five recorded here). *(Original role:)* **ACTIVE — the CURRENT handoff, 2026-08-05. Nothing supersedes it.** Supersedes `2026-08-04-HANDOFF-gbl-harness.md` on next actions. A **seam** handoff: the audit is run to completion, all 77 judgements committed at `cb11b38`, **and nothing scored** — deliberately, so the session that built and ran it does not mark its own work. Names the next work (a fresh session scores `cau_score.py` and writes it up) and the sealed-note reading order with its reasoning. Its five must-not-misread items still stand: **`CAU-G1` is evaluated first and alone** (below 10 of 12 the audit is VOID, never a weak pass); **`D_all` is 53**, the `CAU-AM3` fix for a defect the owner caught, not a session shrinking the audit; **`CAU-C1`'s middle band is neither pass nor fail and no default may be supplied**; **`CAU-C3` can falsify the audit's own premise** — it did fire; and **`CAU-AM4` weakened the red control on purpose**, so a `CAU-G1` pass is a weaker demonstration of discrimination than it looks. |
| `superpowers/specs/2026-08-04-coherence-audit-preregistration.md` | **⚠ EXECUTED and SCORED 2026-08-05 — results at `findings/2026-08-05-coherence-audit-results.md`.** Remains **the governing document** for the audit and for every read; the findings note applied it and did not reinterpret it. *(Original role:)* **ACTIVE — the governing document for the `CAU-` coherence audit.** Identifiers **`CAU-`** (collision-checked 2026-08-04; `CAU-AM` amendments and a separate `CAU-CORR` corrections series, the `GBL-CORR1` precedent). Fixes the workload, instrument, red control and the read of every outcome **before any judgement was recorded** — and it is stricter than usual about the freeze because the result it follows was already known to everyone, including the listener. The one-arm follow-up to the `GBL-` null, built on the confound the owner identified: a 30-second clip of one arbitrary track cannot support a coherence judgement about an unfamiliar artist, **and that failure activates only in the arm that succeeds at delivering unfamiliar artists.** Four amendments, **all committed before any judgement existed**, and two of them raised by the owner: **`CAU-AM1`** (the unit of presentation becomes the whole journey, accepting a real loss of blinding to pose the question properly), **`CAU-AM2`** (the 75% bar ratified **by the owner**, plus the distance-≥-3 hardness condition without which `CAU-G1` is not a red control), **`CAU-AM3`** (controls contaminated their neighbours at 20 of 65 slots on a discarded build; question, placement and denominator all move, `D_all` → 53), and **`CAU-AM4`** (cards carry MBID, disambiguation and a MusicBrainz link, with the cost to `CAU-G1` stated in advance). **`CAU-CORR1`** corrected §4's verb — injections are inserted, not substituted — found by the build script's own self-check. **No adoption on any outcome; §6's barred reads bind every sentence of the result.** |
| `superpowers/2026-08-04-HANDOFF-cre-t11-seam3.md` | **⚠ SUPERSEDED 2026-08-04 on next actions by `2026-08-04-HANDOFF-cre-stage3-findings.md`** — the Stage-3 findings note it named as next has been written and the experiment is complete. **Remains authoritative for Seam 3's own internals** (the criteria figures, the run-state map, and its seven claims-not-to-revert, all of which still stand). Its **owed item 2 is struck in place: discharged** — the findings note makes no cross-cell `pop_raw` sentence at all, so the affine map was never needed. *(Original role:)* **ACTIVE — the CURRENT handoff, 2026-08-04 (Seam 3). Nothing supersedes it.** Supersedes `2026-08-03-HANDOFF-cre-run-stage2-midflight.md` on next actions. A **seam** handoff at the execution plan's own Seam 3: all eleven tasks run, the plan now COMPLETE, nothing in flight, tree clean, draft PR #73. **The criteria figures are a committed artifact** (`cre_scores.json`): all four criteria per cell over pin 9's partition with each group's dropped set committed beside it, and a run-state map whose `cre_r_readable` is `true` — **which asserts only that no unrun cell bars a read.** Names the next work (the **Stage-3 findings note, by a session that did not run the sweeps**, written from the JSON and the prereg and **not from any prose summary, including this handoff's**) and seven claims not to revert, three of them corrections to its predecessor. **No `CRE-R` read is licensed and none has been made; nothing is adopted; `REQ-38` is unspent.** |
| `superpowers/2026-08-03-HANDOFF-cre-run-stage2-midflight.md` | **⚠ SUPERSEDED 2026-08-04 on next actions by `2026-08-04-HANDOFF-cre-t11-seam3.md`** — the `CRE-T11` it named as next has run and Seam 3 is reached. **Remains authoritative for Stage 2's own internals** (the gates and the sixteen sweeps). ⚠ **Two of its claims were OVERTURNED at `CRE-T11` and must not be reverted**: "T11 needs no graph artifact" is false for `CRE-C2`, and its open decision was resolved *against* the position it takes — the absent-class counter is **live but unfired**, not structurally unexercisable, because its proof premise bounded the crawl rather than the pruned snapshot population (log §13). Its **owed items 1 and 3 are struck in place: both discharged**. *(Original role:)* **ACTIVE — the CURRENT handoff, 2026-08-03 (Stage 2, mid-flight).** Supersedes `2026-08-03-HANDOFF-cre-run-seam1.md` on next actions. A **mid-flight** handoff — `CRE-T10` complete, `CRE-T11` remains, Seam 3 not reached — retired on **context budget, not on the degradation tell**, which is stated because the two are read differently by a successor. Branch `cap-reeval-stage2`, draft PR #71, nothing in flight. **Stage 2's gates and all sixteen sweeps are committed artifacts**: `CRE-G1`(a) 22/22 on E-S0 plus a red control the plan did not specify, `CRE-G1`(c) frame N asserted, `CRE-G2`(a) clearing its bar in all three `P1`-carrying cells, `CRE-G1`(b) and `CRE-G2`(b) passing in every swept `P1` cell. Names the next work (`CRE-T11`, readable cold from the JSONs with **no graph artifact needed**) and the claims not to revert — the `G1`(a) red control, the `P0` decomposition check's deliberately separate name, **`walk_journey`'s padding is not measured infeasibility**, and the checked exclusion reconstruction. Carries the mid-flight enumerations (unwritten numbers, decisions against, owner conversation) and **an open decision with a position taken**: whether the absent-class counter's zero is structural. **No `CRE-R` read is licensed and none has been made.** |
| `superpowers/2026-08-03-HANDOFF-cre-run-seam1.md` | **⚠ SUPERSEDED 2026-08-03 on next actions by `2026-08-03-HANDOFF-cre-run-stage2-midflight.md`** — the `CRE-T8` it named as next has run, and Stage 2's gates and sweeps are complete. **Remains authoritative for Stage 0 and Stage 1's internals, and all seven of its claims-not-to-revert still stand** (the successor cites rather than restates them). Its **owed item 1 is struck in place and is now FULLY discharged** — the null half at `CRE-T10`, the absent half at `CRE-T11` (2026-08-04) and **against the position both later handoffs took**: the counter is live but unfired, not structurally unexercisable. Cited, not restated: execution log §13, correction 2. *(Original role:)* **ACTIVE — the CURRENT handoff, 2026-08-03 (Seam 1). Nothing supersedes it.** Supersedes `2026-08-03-HANDOFF-cap-reeval-exec-plan.md` on next actions. A **seam** handoff at the execution plan's own Seam 1: `CRE-AM2` appended and committed before any stage ran, `CRE-T1`–`CRE-T7` complete, nothing in flight, tree clean, draft PR #70. **Stage 0 and Stage 1 are committed artifacts**: `CRE-D3` = `inert_as_expected`, `CRE-D1` = `not_supported`, `CRE-G3` 22/22, `CRE-C3` disqualifies nothing, `CRE-C6` screens nothing out. Names the next work (`CRE-T8`, read cold from the committed JSONs) and the claims an editor must not revert: **`CRE-D1`'s branch consequence is binding** — the three `(D1-branch)` cells do not exist and no router-side tag arm does; the four plan defects were corrected, not worked around; **no `CRE-R` read is licensed and none was made.** |
| `superpowers/2026-08-03-cre-run-execution-log.md` | **RETAINED EXECUTION LOG** for the `CRE-` experiment run. **Owns no figures** — every measured number lives in a committed JSON under `builder/analysis/2026-08-03-cap-reevaluation/`, cited by file and key. Records, per task: the pre-run verification and the plan's one wrong figure citation (§1); the sixth, plumbing-only mirror delta and both `CRE-G2(b)` clauses driven red separately (§2); `CRE-D3`'s branch, and why the r = 1.0 liveness result makes the null a supply reading rather than a dead wire (§3); `CRE-D1`'s pin-5 aggregation divergence resolved to the prereg's words, with the branch robust either way (§4); the build gate, the determinism measurement, and the `pop_log_low` non-comparability caveat (§5); the `S2` degeneracy gate and the plan's non-constructible IEEE case, replaced by a stronger one (§6); the Stage-1 screens, the barred `C6`×`D3` cross-read, and the four items owed at Stage 2 (§7); the Stage-2 entry gates, the red control added because an identity gate is the shape that passes vacuously, and why `CRE-G2`(a) deliberately has none (§9); the `ALG-E` sweeps, the `sim`-share scare that was real saturation rather than a defect **and the unverified-`P0`-baseline gap it exposed**, and the two defects fixed pre-sweep (§10); the `ALG-B` sweeps and **owed item 1 splitting into a discharged half and an owed half** (§11); **`CRE-T11`'s criteria figures, and the two corrections to the mid-flight handoff** — `CRE-C2` does need the graph artifacts the handoff said it did not, and the absent-class counter is live-but-unfired rather than structurally unexercisable — plus this session's own `outcome`-vs-`branch` key defect, caught only by running the code (§13). |
| `superpowers/2026-08-03-HANDOFF-cap-reeval-exec-plan.md` | **⚠ SUPERSEDED 2026-08-03 (Seam 1) on next actions by `2026-08-03-HANDOFF-cre-run-seam1.md`** — the execution it named as next has begun and reached the plan's Seam 1, and **PR #69 has since MERGED** (`2e3b017`), discharging its owner-action. Remains authoritative for the execution-plan track's internals and its claims-not-to-revert. *(Original role:)* **ACTIVE — the CURRENT handoff, 2026-08-03 (night). Nothing supersedes it.** Supersedes `2026-08-03-HANDOFF-cap-reeval-prereg.md` on next actions. A **seam** handoff: prereg verified accurate, execution plan authored, analyst-reviewed and revised pre-run; nothing in flight, tree clean, draft PR #69. Names the next work (execution, by a **fresh session on Opus** — the owner's model ruling — running the plan inline and retiring at its seams, after the owner merges PR #69) and the claims an editor must not revert: the revision is review output, not drift; the two divergences from the analyst's proposals are recorded decisions; **`CRE-AM2` is specified but not yet appended**; the critique record is frozen as-executed and its cell figures carry pre-drop provenance. |
| `superpowers/2026-08-03-cap-reeval-exec-plan-execution-log.md` | **RETAINED EXECUTION LOG** for the execution-plan track. **Owns no figures** — the review measurements live in `builder/analysis/2026-08-03-cre-plan-critique/` (whose report owns what it states). Records: the two verification findings that shaped the plan — the pre-drop `_assemble` freeze forcing a new gated build mirror, and `fame_frame()` being the retired currency (§2); the one deviation from the recorded review split, with reasoning (§4); **the session's own blocking defect, named as a transferable class** — an instrument check authored beside its instrument trends tautological, and the fix is comparing against a quantity from a different mechanism (§5); the execution-mode decision and the owner's Opus ruling (§6); corrections to the prior record including the discharged PR #68 action (§7); and the closeout outcomes (§9). |
| `superpowers/2026-08-01-HANDOFF-no-release-tail.md` | **⚠ SUPERSEDED 2026-08-02 on next actions by `2026-08-02-HANDOFF-tail-drop-and-candidate-census.md`** *(reference made explicit 2026-08-02 evening when rows were inserted above this one)*. Remains authoritative for its own two threads' internals (`TAS-` Task 8 and the adopted drop rule). **⚠ Its closing recommendation — do not wire the drop list before the cap track resolves — was deliberately NOT followed**; the reasoning was examined and the owner ruled the other way. Read the successor's claim 1 before acting on that paragraph. *(Original role:)* **ACTIVE — the CURRENT handoff, 2026-08-01 (latest). Nothing supersedes it.** Supersedes `2026-08-01-HANDOFF-label-weighting.md` on next actions; that document stays authoritative for the `WGT-` probe's internals. A **seam** handoff covering two threads that both reached their end: **`TAS-` Task 8 completed the tag probe** (all eight tasks), and **the no-release tail became an ADOPTED drop rule** — keep a release-less artist only where a commercial-DSP link exists *and* a clip resolves; 7,035 of 7,686 dropped, list frozen at `builder/analysis/2026-08-01-label-weighting/tail_droplist.json`. Carries six claims an editor must not revert, of which three are the sharp ones: **the rule is adopted, not proposed** (apply it, do not re-open it); **the list is a snapshot that must never be re-resolved at build time**, because spec §9 requires byte-identical builds and `build_from_archive` is offline by a hard rule; and **⚠ the "a filter would cut real artists" argument is REFUTED by the owner** and must not be revived from the committed delivered-artist list, which is exactly what will tempt the next reader. Also fixes `REQ-42`'s reading (a **gradient**, not a floor — and the `DD-F1` defect ruling is *unaffected*) and records the measured `BYP-13` wrong-artist rate as a live defect independent of the tail decision. |
| `superpowers/2026-08-01-HANDOFF-label-weighting.md` | **⚠ SUPERSEDED 2026-08-01 (latest) on next actions by the row above**, which continues this thread — the tail sample this probe side-collected became an adopted drop rule. Remains authoritative for the `WGT-` probe's own internals. *(Original role:)* **ACTIVE — the CURRENT handoff, 2026-08-01 (latest).** Supersedes `2026-08-01-HANDOFF-tas5-routing.md` on next actions; that document stays authoritative for the `TAS-` track's internals. Its §"Status of `TAS-`" said Task 8 was owed and **is corrected in place: Task 8 landed 2026-08-01 (latest)** — the `WGT-` probe was a successor investigation, not that document. A **seam** handoff: probe designed, pre-registered, run, extended twice at the owner's direction, and written up in one session; tree clean, nothing in flight. Carries the five claims an editor must not revert — the style column closed **with a mechanism**, `EV-R` excluded as fame-shaped by a pre-registered rule, the device recommendation being input-only, the two near-miss bars whose proximity language must not be tidied away, and the owner's two manual records being **data** (preference records, `WGT-` findings §6). Records what is otherwise undurable: the capture's scratchpad location and regeneration sha, the cwd-persistence trap that misfired three launches, the versioned release-pass checkpoint schema, and where the fold rules live. |
| `superpowers/2026-08-01-label-weighting-execution-log.md` | **RETAINED EXECUTION LOG** for the `WGT-` probe. **Owns no figures** (the findings row below owns them). Records: the word-named scheme decision and why; the evidence frame-independence clarification that preserved one-knob isolations; the `EV-G` column decision; the deliberate mid-stream restart of the release pass to add the side-collection (schema-versioned checkpoint); one recorded execution-order deviation and what it protected; `WGT-AM1` caught at the design layer; the 0-for-3 tally on mechanistic label-source predictions; and the full closeout outcome table (SS15) including the demonstrated red capacity of the reproduction check. |
| `superpowers/findings/2026-08-01-label-weighting-probe.md` | **ACTIVE — the `WGT-` probe's findings. Owns the `WGT-` figures** (probes and raw JSONs in `builder/analysis/2026-08-01-label-weighting/`). **Its §0 pair must be quoted together**: weighting moves 5–15% of map connections (5–15× the owner's materiality line) **and** nothing measured says any weighted journey sounds better. `WGT-2` is **NOT REVERSED** under both schemes — equal weighting was never why the Discogs style column hurt; `W4` dominates on a third measurement. `WGT-4` fired **Branch 3**: release-level evidence is **EXCLUDED as fame-shaped** (pressings-per-album vs fame 0.406), so the release-group preference stands under the weighting lens for a sharper reason than the coverage argument that first made it. Instrument record all green, including bit-identical reproduction of the committed fields. Two bars resolved within 0.03 of their thresholds and §7 says so. Licenses one input to a future cap re-evaluation prereg — rarity over `W4` — **nothing adopted, no `TAS-` change, no blind listen spent.** |
| `superpowers/specs/2026-08-01-label-weighting-probe-preregistration.md` | **ACTIVE pre-registration — the label weighting and evidence probe.** Identifiers **`WGT-`** (gates, checks, readings) and **`EV-`** (evidence-table rows); weighting schemes named by *words* (`plain`/`rarity`/`evidence`) to stay outside the letter-number namespace, collision-checked. Owner-directed 2026-08-01. Committed before any `WGT-` figure existed; the committed figures it cites (`tas_weighting.json`, `tas_frame_split.json`, `REL-`) predate it and are named as baselines. Asks whether **rarity and evidence weighting** change what the agreement device discriminates, per frame with the Discogs style column isolated (`WGT-2`), and whether the release-group-over-release preference survives the weighting lens (`WGT-4`, the 322 GB release-dump scoping pass — which its §7 records as **present on disk**, correcting the deletion claim). **Diagnostic only: no `TAS-` change, no re-run of any guard, nothing adopted, no rebuild, no blind listen** — licenses a device recommendation as *input* to a future cap re-evaluation pre-registration, plus its record corrections. Its evidence table is **the owner's to strike row by row before commit**. Four instrument checks (`WGT-0a`–`0d`) gate every figure; the fame-leak gate (`WGT-1`) prices the vote-counting hazard before any cell may use votes. |
| `superpowers/2026-07-31-HANDOFF-tas-am3-am4.md` | **⚠ SUPERSEDED 2026-08-01 on next actions by the row above**, which continues the same track. Remains authoritative for the `TAS-AM3`/`TAS-AM4` work's internals; **its status claims are stale** — it says Tasks 5–8 not started and treats the routing instrument question as owed, and both have since been resolved. Supersedes **both** `2026-07-31-HANDOFF-release-tag-coverage.md` and `2026-07-30-HANDOFF-tag-discrimination.md` **on next actions only**; each stays authoritative for its own track's internals. A **seam** handoff — not the seam the plan named (Seam A sat after Task 4), but two material §8 amendments landed, and a material mid-flight amendment is a seam in its own right. Carries the three claims an editor must not revert: **`TAS-6` is ADVERSE and bars an adoption recommendation for the build-time architecture whatever else shows, and is not softened by the `REL-` frame existing**; the §4 red check is **withdrawn as unachievable**, not merely unfired, so the harness must not be "fixed" to make it fire; and `TAS-AM3`/`TAS-AM4` were appended **after results existed** and their disclosures must not be tidied away. Records that the instrument question is discharged for the **selection** side only — the routing side still owes its own — and what is otherwise unwritten: the `.npz` lazy-load trap that cost a run, that `td_turnover.py` sweeps after `--verify` and will overwrite the committed `TD-` record unless `--out` points at scratch, and that "audit clean" did not cover whether a named file exists. |
| `superpowers/2026-07-31-HANDOFF-release-tag-coverage.md` | **⚠ SUPERSEDED 2026-07-31 on next actions by `2026-07-31-HANDOFF-tas-am3-am4.md` (row above).** Its own probe record is complete, untouched and accurate, and it **remains authoritative for the `REL-` probe's internals** — its frames, aggregation device, validity filter and bars. Supersedes `2026-07-30-HANDOFF-tag-discrimination.md` **on next actions only**. A **seam** handoff: the `REL-` probe ran to completion, nothing in flight, tree clean. |
| `superpowers/plans/2026-07-31-tas5-routing-execution-plan.md` | **ACTIVE — fully executed 2026-08-01 (`TAS-R1`–`TAS-R4`).** **Supersedes Tasks 5, 6 and Task 7's routing halves of the plan below**, which could not be executed as written. Identifiers **`TAS-R1`–`TAS-R4`**, a series verified unused repo-wide before minting. Its value beyond sequencing is its opening table: **five things in the inherited tasks had drifted and one was a trap** — Task 7 Step 5 still instructed an executor to make the randomised-label red check fire and *stop* if it did not, which is the check `TAS-AM3` withdrew as **unachievable**, so a fresh executor would have "fixed" the harness until it fired. It also names the gap that produced `TAS-AM5`: `TAS-AM3`'s scope clause cannot transfer to the routing side, because its pass conditions live on the pre-cap capture while `TAS-AM2` puts `TAS-5` on the adopted artifact. **Owns no figures.** |
| `superpowers/plans/2026-07-30-tag-discrimination-probe.md` | **⚠ ACTIVE for Tasks 1–4 and Task 7's selection halves, all executed; its Tasks 5, 6 and Task 7's ROUTING halves are SUPERSEDED by the row above and must not be executed from this document.** **Task 8 (findings and the owner-facing read) was executed 2026-08-01 (latest), so the plan is now fully discharged** — its Step 1–5 outputs are the findings row above, execution log §17 and this map's own new row. The pre-registration above **governs wherever the two disagree** — it has already overtaken this plan's Task 4 once via `TAS-AM1`. Eight tasks with handoff seams named at authoring time. **Its value beyond sequencing is the record of four defects found in the plan itself by verifying rather than trusting** — a neutral-value formula computing the wrong quantity, two artifact accessors that do not exist, and a Wikidata assumption that was wrong twice over (`fp_wikidata.json` holds item presence and no genres; `ct_wikidata_genres.json` holds counts, not labels, so P136 **labels were never persisted** and needed their own collector). All four were confident prose about correct code, and none would have raised an error. |
| `superpowers/2026-07-30-HANDOFF-tag-discrimination.md` | **⚠ SUPERSEDED 2026-07-31 on next actions; the CURRENT handoff is `2026-07-31-HANDOFF-tas-am3-am4.md`, which continues *this* track** (the `REL-` handoff sits between them in date order but was a parallel investigation). **It REMAINS AUTHORITATIVE for the `TAS-` track's internals; its claim that `TAS-` Tasks 5–8 are owed and unrun is SPENT — all four ran (Tasks 5–7 on 2026-08-01, Task 8 on 2026-08-01 latest)** — but its own "must not revert" list is **no longer complete**, since Task 4, Task 7's two selection-side halves, `TAS-AM3` and `TAS-AM4` all landed after it — the `REL-` probe is a parallel investigation, not a continuation, and did not touch them. Supersedes `2026-07-30-HANDOFF-coherence-tag-probe.md` **on next actions only** (that probe's own record is untouched and accurate). A **seam** handoff: Tasks 1–3 ended with both gate reads resolved, nothing in flight, tree clean. Carries what the next session must not revert (no arm at bound 100 — ranking is provably never consulted there; the neutral rule is per-artist, never zero, and is the one dormant term; a `TAS-5` null is weak evidence about tags specifically), **the open architecture decision with the retiring session's position on it** rather than as a menu, and what is not otherwise in the durable record — including that the ~20 MB captures live in a session scratchpad and must be regenerated rather than hunted for. |
| `superpowers/2026-07-30-tag-discrimination-execution-log.md` | **RETAINED EXECUTION LOG** for the `TAS-` probe. **Owns no figures.** Its **§2 is the reason to read it**: four defects found in the plan *itself* by verify-against-source steps, none of which would have raised an error — the project's characteristic failure, tabulated. §1 records how the owner's three interventions changed the design (and why the bound-100 vote was reversed on evidence). §4 records the corrections this session made to its own committed pre-registration, including the withdrawal of a bar as **false** before it ran. §8 records the standing-layer measurement **and** that its own first draft fabricated both figures. |
| `superpowers/plans/2026-07-30-graph-rebuild-track-b.md` | **⚠ EXECUTED 2026-07-30 through its last task — the checkboxes are annotated in place; read the two execution logs beside it for what ran.** It remains authoritative for **its §0 owner scope rulings, which outlive the plan** — cap-RULE selection not k-tuning; the degree bound as a scoping constraint, not a settled product conclusion; the uncapped row barred from selection. *(Original role: **ACTIVE — the governing plan for Track B, the cap-rule selection simulation** (`MKS-5b`'s owed simulation, the successor plan Track A's strand 3 deferred). Identifiers **`CB-`**. **Its §0 records the owner's 2026-07-30 scope rulings — read them before touching anything cap-adjacent:** the track is cap-**rule** selection, not k-tuning (mutual k-NN is one candidate family among three, on `SYN-6`/`CS-P0f`/`BTF-4` grounds); **the degree bound is a scoping constraint of this track, not a settled product conclusion** — re-evaluating bounded-degree (router-priced unbounded graphs) is an open future track, owner's trigger, owing its own blind listen; and the uncapped graph runs as a **descriptive reference row barred from selection**. Six tasks, seam named at `CB-4` (the pre-registration, its own document, committed before any comparative cell). Everything analysis-only: no shipped default moves, archives are read-only (`GRT-A1`), and adoption still owes `REQ-38`'s blind listen regardless of outcome. Consumes `RC-A2`, `LBS-3`/`LBS-4`, `GRT-P3`/`GRT-P4`, and resolves `PLA-R1` applicability in the prereg rather than assuming it.)* |
| `superpowers/2026-07-30-HANDOFF-track-b-design.md` | **⚠ SUPERSEDED 2026-07-30 on next actions** by `2026-07-30-HANDOFF-track-b-runs.md` (row above) — the `CB-5`/`CB-6` work it named ran to completion the same day, its three order-sensitive obligations all discharged as written. Everything it records about the design phase remains accurate and its do-not-revert list stands. *(Original role: **The CURRENT handoff, 2026-07-30 — written at Track B's own named seam (`CB-4`): pre-registration committed, every instrument gate green AND red, nothing in flight.)*** Supersedes the Track A handoff below **on next actions**. Names the next work (`CB-5` runs then `CB-6` reads, fresh session, prereg read cold) and the three order-sensitive obligations that bind it (scorer dry-run first; `ALG-E`-`MK50`'s `C5` before any other `C5`, per `CRS-A2`; reads in the prereg's own order). Carries the claims an editor must not revert — the `LBS` corrections to the forum comment, the retracted "limit is not list length" read, the checked trim derivation, `CS-P0c`'s true scope, the band-pool baseline correction — and what is otherwise unwritten: the gitignored `cb-cells/` artifacts with manifest sidecars, the deliberately-unread `cb_bound_check.json` numbers, `CRS-A5`'s endpoint claim being the consultant's report not this session's verification, and the `CB-3` gate's two-class slice being deliberate. |
| `superpowers/2026-07-30-track-b-design-execution-log.md` | **The retained execution log for Track B's design phase. COMPLETE for the phase it records** — the runs phase has its own log (row above). *(Read "owns no comparative figures — those do not exist yet" as of its writing; they exist now and belong to the results note.)* LBS semantics → plan → `CB-1`–`CB-3` instruments → `CB-P1` probe → `CRS` pre-registration with `A1`–`A5`, one session, retired at the seam. Owns the design-phase gate-outcome table and the operational timings; **owns no comparative figures** — those do not exist yet by design. Records: the owner's widening of the track with grounds; this session's own mid-stream retraction ("limit is not list length", reversed by the source's 2x clause before commit); both consultant rounds with every item's verification outcome; four defects in this session's own output including the two blocking prereg/harness mismatches and **its own first draft asserting D4/D6 numbers from memory before running the commands**; the `banded_quota` origin (both proposed trim keys provably fail); and what was decided against (proximity-trim cells, a `q` axis, a second weight set). |
| `superpowers/2026-07-29-HANDOFF-graph-rebuild-track-a.md` | **⚠ SUPERSEDED 2026-07-30 on next actions** by `2026-07-30-HANDOFF-track-b-design.md` (row above) — the Track B work it named was designed through its seam, **and the owner re-scoped it on the way: cap-RULE selection, not k-tuning**, so its description of Track B as a k-simulation is dated. Everything it records about Track A remains accurate. *(Original role, retained in full: **The CURRENT handoff, 2026-07-29 — written at a seam after the graph rebuild plan was written AND executed to its last task in one session.** Supersedes the reciprocity handoff below **on next actions**. Names the next work (**Track B, the cap-selection simulation under `MKS-5b`**, which must consume `GRT-P2` and `RC-A2`; the `ALG-B` re-crawl decision remains PARKED and is the owner's trigger), and the claims an editor must not revert: **`RC-P2` is confirmed on its mechanism and REFUTED on its consequence** (R.E.M. holds 6 connections under `ALG-B` against 47 in the matched control, but the build does **not** refuse); **`GRT-P2`** — the guard cannot see the collapse it was written for, because popularity *is* score-weighted in-degree, so a collapsing artist drops out of the top-25 sample the degree floor inspects (**not established at production scale**); **`GRT-P1`** — `GRT-C4`'s ratio bar is undefined against a zero baseline, so the pre-registered read returned "not decisive" everywhere and is left standing as written; the production archive is **not** closed under one-hop neighbours (`GRT-A1`); a full build takes **~29 s**, not ~2 minutes. Records what is otherwise unwritten: **`snyk_code_scan` never ran and is owed** on three diffs; the scorer defect caught by dry-running the cheap arm while the expensive one collected; the cross-validation against the calibration harness; and that the production checkpoint no longer exists, so `GRT-A1`'s mechanism is hypothesis while its gap is measured.)* — Two claims inside that original description are dated by later work and correct in the handoff itself: `GRT-P2` **was** subsequently established at production scale (`GRT-P4`), and the owed `snyk_code_scan` **was** run and is clean. |
| `superpowers/plans/2026-07-29-graph-rebuild-track-a.md` | **⚠ EXECUTED 2026-07-29 — read the execution log beside it, which is fresher on everything that ran.** It remains authoritative for *task structure, the factor table and the strand ordering*. Its `GR-5` left two design decisions explicitly open (the control arm's archive; the trial target) and **the pre-registration closed both** — read that document, not this one, for what was actually run. *(Original role: ACTIVE, the governing plan for Track A.)* Identifiers **`GR-`**. Six tasks: the nameless-artist drop rule (pulled forward as the first build blocker), `--algorithm` selection, algorithm-scoped archive keys (`RC-H3`), a verification rebuild, the pre-registration, and the trial build. Declares its own seam after `GR-6` and defers strand 3 (cap selection) to a successor plan, because `RC-A2` shows the cap and the algorithm interact rather than compose. |
| `superpowers/2026-07-29-graph-rebuild-track-a-execution-log.md` | **The retained execution log for graph rebuild Track A. ACTIVE.** Owns this track's build-shape figures and nothing else; scoring and path-quality figures stay in `findings/2026-07-21-scoring-adjudication.md`. Records the baseline refusal captured **before** the drop rule landed (33 nameless, build refused, nothing written), the owner's MusicBrainz check that upgraded the drop decision from preference to only-available-option (all three sampled MBIDs return "Artist not found", so backfilling was never possible), the **36-vs-33 reconciliation** (different populations, both correct, and 3 stranded neighbours pruned — the clause's first production evidence), `GR-G1` passing, and the full `GRT` trial-build result with all three gates. Carries three findings against its own work in fixed wording: **`GRT-A1`** (the production archive is not neighbour-closed; a structural guard caught a naive control arm), the **scorer defect** found by dry-running the control while the trial arm still collected, and the cross-validation that followed. Corrects the record on build cost (~29 s, not ~2 min). §8 records that **`snyk_code_scan` was never run and is owed**. |
| `superpowers/specs/2026-07-29-algb-trial-build-preregistration.md` | **ACTIVE pre-registration — the `ALG-B` trial build.** Identifiers **`GRT-`**, namespaced and collision-checked. Committed **before any arm ran**; harness committed before either script produced a result. Its **§0 records the scope check that changed the design**: `RC-A2` already diagnosed reciprocation by hand and re-checking one artist costs ~101 requests, so the single irreplaceable output is **`GRT-C4`, component membership (`RC-H1`)**, which sampling structurally cannot reach — and without it the document says it would be withdrawn. Two arms differing in exactly one column (`ALG-B` vs a coverage-matched `ALG-E` control); every comparison against the adopted artifact is **barred** as a two-knob confound. Explains why the reads bypass `cmd_build` (a capped graph cannot satisfy `node_count`, and there is deliberately no CLI escape hatch). ⚠ **AMENDED three times, §8, append-only:** `GRT-A1` (pre-run — overlay archive, after the read-only guard refuted the document's own coverage assumption), `GRT-P1` and `GRT-P2` (post-hoc). No clause above §8 was edited. Licenses no adoption and no re-crawl. **⚠ STALE FIGURE, not edited and deliberately so (2026-08-06):** its §3 restates `PRODUCTION_ACCEPTANCE`'s node bound as "60,000–90,000". Those bounds were recalibrated at the `MSW-` map switch and the live values are owned by `builder/src/artistpath_builder/acceptance.py`. **The §3 argument is UNAFFECTED** — a 3,000-node graph fails the new floor as surely as the old — so only the quoted range is out of date. Left in place because this is a pre-registration whose value is that it is frozen; the correction lives here. |
| `superpowers/2026-07-29-HANDOFF-reciprocity.md` | **⚠ SUPERSEDED 2026-07-29 (latest) on next actions** by `2026-07-29-HANDOFF-graph-rebuild-track-a.md` (row above) — the plan it handed over was written **and executed**, so **its named next action is spent**. One claim it carries is now corrected rather than merely superseded: it states that `RC-P2` predicts a build refusal, and a real build shows **no refusal** for the reason `GRT-P2` gives. Everything else it records remains accurate. *(Original role: the CURRENT handoff, 2026-07-29 (later) — written at a seam after the reciprocity measurement ran and was read.** Supersedes the algorithm-selection handoff below **on next actions**. Names the next work (write the graph rebuild plan cold — **no experiment now gates it**), the instrument change an editor must not revert (**the `--target`-capped trial crawl is retired for `AS-H2`**: its readable core is structurally famous, so it cannot answer a question about obscure artists — retired for that question only, since it remains the sole route to component membership), the two items inside the plan now known to be harder (the nameless-drop rule **gates the plan's own first build**; the cap simulation **interacts with** any algorithm change), the claims not to revert (`ALG-B` strands obscure artists **and** must not be summarised as simply worse; the cause is supply not reciprocation; `RC-P3`'s mechanism is unsupported by this session's own data; `RC-P1`'s scorer defect biased toward the null and both readings are reported together; `RC-P2` predicts a build refusal), and what the session knew that is otherwise unwritten (full candidate lists stored so a different `k` needs no new requests; the two scorers re-derive degree independently; the corrected reading of `crawl.py`'s stopping rule). |
| `superpowers/2026-07-29-HANDOFF-algorithm-selection.md` | **⚠ SUPERSEDED 2026-07-29 (later) on next actions** by `2026-07-29-HANDOFF-reciprocity.md` (row above). **Its own named next action — `AS-H2`'s `--target`-capped trial crawl — is RETIRED as an instrument and must not be revived from this document**; the successor answered `AS-H2` by sampling instead and records why. Everything else it states remains accurate. *(Original role: the CURRENT handoff, 2026-07-29 — written at a seam after the algorithm-selection measurement ran and was read.)* Supersedes the requirements/Track 3b handoff below **on next actions**. Names the next work (write the graph rebuild plan cold; the agreed next experiment is `AS-H2`'s trial crawl, recommended and NOT run), **the re-sequencing an editor must not revert** (cap-selection simulation first → third, on `CS-P0c`), the two levers now known impossible (`limit` above 100; `threshold` below 10 — so `STC-6` is KILLED, not deferred), the claims not to revert (`AS-R1`/`AS-R5` did not fire; `ALG-B` is not unambiguously better; `AS-C1` is a lower bound), and what the session knew that is otherwise unwritten (the trial crawl's shape; the raw records being committed so re-scoring needs no new requests; the endpoint's real latency; `ALG-C`/`ALG-D` needing no re-measurement). |
| `superpowers/2026-07-29-HANDOFF-requirements-track3b.md` | **⚠ SUPERSEDED 2026-07-29 (later) on next actions** by `2026-07-29-HANDOFF-algorithm-selection.md` (row above) — that session ran and **re-sequenced the task order this handoff names: the cap-selection simulation moved from first to third**, because `CS-P0c` showed it cannot fix `DD-F1`. Everything it records about the requirements rewrite and Track 3b remains accurate, and its do-not-revert list stands. *(Original role: the CURRENT handoff, 2026-07-29 — written at a seam after the requirements rewrite and Track 3b both ran to completion.)* Supersedes the Track 3 handoff below **on next actions**. Names the next work (graph rebuild planning, owner's instruction, with entry points and the now-due nameless-drop rule), the parked decisions (candidate pool, recompute, listen — owner's trigger), which documents are now wrong and in which direction, the overturned claims an editor must not revert, and what the session knew that is otherwise unwritten (the recompute's cost; its position on the candidate pool; the mirror knobs staying 0.0). |
| `superpowers/2026-07-29-track3b-execution-log.md` | **The retained execution log for Track 3b. ACTIVE.** §1: TB-P1 discharged — 15 findings (4 HIGH), all amended into the pre-registration pre-arm, marked in place; the prereg's §9 is the index. Carries the review's headline in fixed wording: **the pre-run forecast says TB-R2 (shortening still dominates) is the likely outcome** — a designed outcome with a pre-committed reading, and the run proceeds. F4's 0/252 sub-decile-victims figure was independently re-verified by the dispatching session. |
| `superpowers/2026-07-29-algorithm-selection-execution-log.md` | **The retained execution log for algorithm selection. COMPLETE** for the measurement it records. All six arms clean (`AS-G1` 200/200 each); `AS-G2` passes. **`ALG-B` (`days_7500 · contribution_3`, one parameter from production) is the named re-crawl candidate** — sub-decile candidate yield at the very top 1.175 → 11.25, sub-median 0.075 → 4.025. Carries two findings against itself in fixed wording: **`AS-H1`** (a read-structure gap — neither designed read fires, recorded as not-fired rather than re-labelled toward the flattering one, same shape as `TB-P5H-7`) and **`AS-H2`** (`ALG-B` returns **58% fewer candidates** below the median, so it plausibly strands MORE obscure artists — the `MKS-6`/`F1` and `MKS-3` mechanism; unmeasured, deferred on "before any adoption decision"). **`AS-R3` fires: this is a replacement graph, not a patch** — Jaccard 0.42 at the top, 16.8 never-seen artists per artist, so every path-quality figure the project holds was measured under `ALG-E`. Nothing adopted; nothing here measures whether the new edges are any *good* (`REQ-38`). |
| `superpowers/specs/2026-07-29-track3b-thresholded-toll-preregistration.md` | **ACTIVE pre-registration — Track 3b, the thresholded toll. ⚠ AMENDED 2026-07-29 per TB-P1, pre-arm — 15 findings folded in, marked in place; §9 is the amendment index; the review is `../builder/analysis/2026-07-29-track3b-thresholded-toll/TB-P1-protocol-review.md`. Committed before any arm; no arm runs until its prerequisites discharge in order (TB-P1 ✅ → TB-P2a fame extension → TB-P3 ceiling go/no-go → TB-P4 harness → arms → TB-P2b → TB-P5 harness review → read).** Identifiers **`TB-`**. The named successor from the Track 3 execution log §7, owner-triggered 2026-07-29; the first track designed against `superpowers/PRODUCT-REQUIREMENTS.md` (REQ-13/14/9 and the REQ-Q1 currency ruling). Device: `w · k · max(0, pctl(v) − 0.90)` — zero on sub-decile interiors at any strength, so length-neutral where Track 3's DD-D5 confound lives. One axis (dose {0.10, 0.30, 1.00}, derived to match Track 3's realised 5×/15×/50× ladder); floor deliberately NOT an axis (DD-D4: dead from k=3 on this pair set); knee fixed at 0.90; pair set inherited frozen (`pairs_v2.json`). TB-C6 (length) is **promoted to a criterion** — TB-R1 (mechanism confirmed) requires a pass to be unflagged. Schedules no blind listen, no adoption, no rebuild. |
| `superpowers/2026-07-29-reciprocity-execution-log.md` | **The retained execution log for reciprocity sampling. COMPLETE** for the measurement it records. **OWNS NO CRITERION FIGURES** — every `RC` measurement belongs to `../builder/analysis/2026-07-29-reciprocity-sampling/` (`rc_scores.json`, the pre-registered read; `rc_posthoc.json`, the post-hoc corrections) and `../builder/analysis/2026-07-29-trial-crawl-calibration/`. **It does own two things nothing else can**: §6's operational measurements (the endpoint's latency then versus now, collection counts, the corrected stopping-rule reading) and §8's standing-context-layer totals, which are the figures the *next* closeout compares against. Records why the capped trial crawl was **retired as an instrument** before any pre-registration existed, the replacement design and its cost, `RC-G1` **not fired** with its figure, all five bands readable under `RC-G2`, and `RC-R1` **fired** on both readings. Carries three findings against its own design — **`RC-P1`** (a denominator defect in the pre-registered scorer that biased toward the null; corrected effect roughly doubles, verdict unchanged, both figures reported together), **`RC-P2`** (`RC-C3` pre-registered only the median floor, missing the top-25 minimum and the canonical-names clause, so an `ALG-B` artifact is predicted to be refused by the build) and **`RC-P3`** (the mechanism this session proposed, unsupported by its own data — candidate obscurity does not predict reciprocation). Also carries the corrected reading of `crawl.py`'s stopping rule (`done == discovered == target`) and the endpoint's measured latency. Nothing built, nothing adopted. |
| `superpowers/specs/2026-07-29-reciprocity-sampling-preregistration.md` | **ACTIVE pre-registration — does `ALG-B` strand obscure artists?** Identifiers **`RC-`**, namespaced and disjoint by role (`RC-ARM-*` arms, `RC-C*` criteria, `RC-G*` gates, `RC-R*` reads, `RC-H*` hazards, `RC-P*` post-hoc). Committed **before any arm ran**. Answers `AS-H2`'s own instruction to a successor — *pre-register the rate alongside the count* — by fixing candidate count, reciprocation rate and their product per fame band, paired across two settings differing in one request parameter. Its §0 carries the dormant-term analysis: the p99 clip is **genuinely** inert here because the cap ranks on unclipped strengths (verified in source, not assumed), while `filter_special_purpose` is **not** automatically constant and is handled explicitly. ⚠ **AMENDED twice, §8, append-only:** `RC-A1` **pre-run** (adopt the AS sample as the seed set, making the seed side free and the drift gate free and stronger) and `RC-A2` **post-run** (`RC-P1`/`RC-P2`/`RC-P3`, plus the diagnosis that the famous-side collapse is a `k` interaction — **explicitly not a licence to raise `k`**, which `MKS-5b` governs). No clause above §8 was edited. Licenses no adoption and no re-crawl. |
| `superpowers/specs/2026-07-29-algorithm-selection-preregistration.md` | **ACTIVE pre-registration — source algorithm selection.** Identifiers **`ALG-`** (arms) and **`AS-`** (criteria, gates, reads, noise floor), disjoint by construction. Committed **before any sampled arm ran**. Fixes the six-arm factor table over the ListenBrainz `algorithm` enum, which `CS-P0e` validated as **closed** — `limit` cannot exceed 100 and `threshold` cannot go below 10, so two previously-planned levers do not exist. `ALG-C` is **barred from every one-knob attribution** (four columns differ, and the enum's closure means no isolating baseline can be built). `ALG-A` vs `ALG-F` (1.4% apart in window) is adopted as the noise floor `AS-N`, giving calibrated effect sizes. Its §1.3 dormant-term check caught a real confound: sub-decile counts measured against the current artifact silently drop candidates *absent* from it — inert under production, not under `ALG-B` — so absence is promoted to `AS-C2` and `AS-C1` is declared a lower bound. ⚠ **AMENDED post-run** with `AS-H1` (neither `AS-R1` nor `AS-R5` fires; both assumed the strata would agree) and `AS-H2` (the criteria are counts, not rates); §7 is the amendment index and no clause was edited. Licenses no adoption and no re-crawl. |
| `superpowers/2026-07-28-HANDOFF-track3-preregistered.md` | **⚠ NO LONGER THE CURRENT handoff — superseded 2026-07-28 on next actions by [`2026-07-28-HANDOFF-track3-executed.md`](superpowers/2026-07-28-HANDOFF-track3-executed.md), which executed the work this one handed over.** Its direction-changes remain in force; **its next action (execute Track 3) is spent.** *(Original role: the CURRENT handoff, written at a seam after ASC-5 was discharged and Track 3 pre-registered.)* Supersedes the Track 2F/headroom handoff **on next actions** (the path-work chain) and the redesign-deployed handoff below **as the document to read first only** — a different body of work, discharging none of it. Names what the successor executes (Track 3, from its pre-registration, read cold), the direction changes that must not be reverted (the pause is over; famous-pair first-path fame is barred as a criterion; nameless artists are dropped, not backfilled), and the implementation knowledge folded in from the retiring session (the DD-P1 instrument mostly exists; DD-P4 extends the committed mirror; DD-G4 must land before the new pairs' fame fetch). |
| `superpowers/specs/2026-07-28-track3-depth-descent-preregistration.md` | **⚠ EXECUTED 2026-07-28, and AMENDED during execution — read the execution log beside it, which is fresher on everything that ran.** It remains authoritative for *design intent, criteria and thresholds*; it is **wrong in four places the log corrects**: it claims 4 both-famous carry-over pairs (there are 6, DD-D1), its DD-P1 branch trigger is **unsatisfiable by construction** (DD-D3), two of its six arms are **bit-identical to two others at every scored depth** and were struck (DD-D4), and its §5 has **no length control**, without which no criterion can attribute a DD-C1 movement to descent rather than shortening (DD-D5 → DD-C6 adopted). *(Original role: ACTIVE, governs the next path-quality work; no arm runs until discharged in order DD-P2 → DD-P1 → DD-P3 → DD-P4.)* Identifiers **`DD-`**. The first device that **rewards descent with bypass depth** rather than permitting it (`w_known_ramp_pctl · k · pop_pctl(v)`, zero at k = 0 so the first path is production's by construction, gated not assumed). Six arms over dose × floor with per-row isolating baselines; its §0 declares the raw floor a **live interacting term**, not a constant. Criteria reuse Track 2's committed calibration; coherence is a tripwire, never a gate (Phase 1 log §3.8). Its reads pre-commit the null: DD-R2 bars magnitude escalation and names the rescale and the `STC-6` re-crawl as the successors on a null with headroom. Schedules no blind listen, no rebuild, no owner spend. |
| `superpowers/2026-07-28-HANDOFF-track3-executed.md` | **⚠ SUPERSEDED 2026-07-29 on next actions** by `2026-07-29-HANDOFF-requirements-track3b.md` (row above) — the pending owner decision it hands over was taken and reframed (requirements rewritten; Track 3b run; candidates PARKED; next work is the rebuild plan). Still the record of Track 3's execution, and its do-not-revert list stands. *(Original role: the CURRENT handoff, 2026-07-28 — written at a seam after Track 3 ran to a read.)* Supersedes the Track 3 pre-registration handoff **on next actions**. All prerequisites discharged, all gates closed, DD-R1 fired, both halves of DD-P3 complete, and the owner's consultant review closed on all six items. Carries the fixed verdict wording that no summary may soften, the six overturned claims a well-meaning editor must not revert (**DD-A4/DD-A5 struck; `pairs_v2.json` is the pair set; anchors unscored and held-out gates nothing; production's negative depth gradient RETRACTED; guard-G constancy corrected to untested; DD-D8 must appear in every summary**), what is already updated, and what was decided against. Its last section carries the transferable lesson: **the pre-registration scheduled its own analyst review third, and nearly everything that review found needed no measurements at all.** |
| `superpowers/2026-07-28-track3-depth-descent-execution-log.md` | **The retained execution log for Track 3. ACTIVE — fresher than the pre-registration on everything that was actually run.** §1–§2 the pair set and the headroom measurement, including **DD-D3: the pre-registration's own branch trigger is unsatisfiable by construction** (any permitted pair set contributes 33.3 % against a 30 % trigger), and **DD-F1**, the structural finding that superstar endpoints have *zero* edges below the top decile. §3 the protocol review, which **struck two of six arms** (DD-D4), exposed the length/obscurity confound (DD-D5) and forced the currency check (DD-D6). §4 the ceiling probe and the **value tension it exposes between WGLL values 1 and 2**. §5 the arms and the verdict. §6 held-out confirmation and the **descriptive anchor table — the device does nothing on famous pairs at any strength**. §7 **Track 3b, a named UNSTARTED option** (the thresholded toll) that needs its own pre-registration. §8 the harness review, carrying **DD-D8** and **two retractions of this log's own earlier claims**. Figures owned by the analysis directory; cited, never restated. |
| `../builder/analysis/2026-07-28-track3-depth-descent/` | **AUTHORITATIVE for its own measurements.** Track 3's measurement record: the pair draw and its re-draw, the headroom instrument, the `w → ∞` ceiling probe, the four arm walks, the pre-registered scorer, and the supplementary held-out/anchor tables. **Owns** every Track 3 figure — the sub-decile degree series, the headroom cells, the ceiling fame gap, and the criterion table. `REPORT.md` is the deliverable and carries the verdict; `README.md` says what is imported rather than reimplemented and **what the directory cannot conclude**. Both DD-P3 reviews live here (`DD-P3-analyst-review.md`, `DD-P3-harness-review.md`) with their probes. **Not a `findings/` document, deliberately** — a second copy would restate its figures. Cite this directory. |
| `superpowers/2026-07-28-asc5-path-ascent-execution-log.md` | **The retained execution log for the ASC-5 discharge and Track 3 pre-registration, 2026-07-28.** §0 records the status change (path work UNPAUSED, owner decision, his stated goal verbatim); §1 why ASC-5 was the first move and not the degree floor or the rescale prereg; §4 which pre-committed branches fired, with figures owned by `builder/analysis/2026-07-28-asc5-path-ascent/`; §6 the Track 3 ordering decision; §7 the owner's nameless-artist decision (**drop**), recorded at the tripwire. Also records this session's two logged defects (a serialization failure before any figure existed; a prerequisite-order misstatement corrected the same hour). |
| `superpowers/2026-07-28-HANDOFF-redesign-deployed.md` | **⚠ No longer the CURRENT handoff — superseded 2026-07-28 (late night) *as the document to read first only* by the Track 3 handoff above, a different body of work discharging none of this one.** Everything it records about the deployed site remains true; **the `--prune` pass and the queued use-the-app test (including the iPhone script) are still owed exactly as it says.** *(Original role: written at a seam, after the work was finished, merged and DEPLOYED.)* Supersedes the row below **on everything**. All 13 tasks are done, plus three owner copy/rail changes and `path_length` telemetry; live at `https://musicapp.cmiller.io` (`d68ef7a`). Carries what the execution log cannot: that **the `--prune` pass is TRIGGERED but must be a SEPARATE LATER RUN**, never part of a publish, because pruning while the old `index.html` may still be cached re-opens `FRO-1`; the six do-not-revert items, including that **`PathIntro`'s count assertions must read `textContent`** and that the **landing clip-length test asserts ABSENCE** by the owner's decision; and that the loading screen's rail was deliberately left alone when the journey rail changed. Records what was decided against, including recording The Soft Boys find (offered, owner declined — **do not cite it as evidence**) and moving the reroll notice. |
| `superpowers/2026-07-28-HANDOFF-frontend-mockup-adoption.md` | **⚠ HISTORICAL — superseded 2026-07-28 (night) on EVERYTHING by the row above.** *(This row read "The CURRENT handoff" until then — the same collision recorded against the Track C and Track D rows below, caught here by the closeout rather than by an audit.)* Its Tasks 1–7 status, its "nothing is deployed" line and its next actions are **all spent**; its do-not-revert list is carried forward by the successor rather than cancelled. *(Original role: the handoff at the PLANNED Task 7 seam. Its body still reads "Tasks 8–13 are not started" and "nothing is deployed" — **both false since 2026-07-28 night**, left in the document because a frozen handoff is a record of what was handed over, and governed by its role banner. This row previously restated those claims as if current, which is the defect the banner exists to prevent.)* Still the only record of what the Task 7 seam handed over, and **its do-not-revert list is carried forward, not cancelled**: the deliberately MIXED `UI-7` test, the `data-testid` locator in `responsive.spec.ts`, `UI-D3`, and the oxlint exclusion of the design export. Also still true and worth knowing: **`npm run lint` exits 0 when oxlint fails to parse its own config.** For everything else read the row above. |
| `superpowers/2026-07-28-frontend-mockup-adoption-execution-log.md` | **The retained execution log for the mockup adoption plan. ACTIVE — all 13 tasks done, merged and DEPLOYED. Fresher than the plan on status; where they disagree, this wins.** **Owns the suite counts for this work**, stated in exactly two tables (§1 at the seam, §9 at completion) and nowhere else. §14–§17 cover the owner's post-use changes and the deploy: §15's gate table includes **the check that is not in the runbook** — a production journey returning the identical path to the same journey locally, which is the only direct evidence that routing did not move — and §17 is the deferral state, including that the App Runner tag deferral was **tested against reality and did not fire**. **Owns no path-quality figures**; the pause is intact. Its §4 records the finding of the session: **`B3` mutation testing caught a vacuous test in this session's own output**, guarding `UI-7`, the rule that a decorative lookup must not be able to turn a working page into an error page. Removing the `.catch()` that *implements* the rule left the suite green, because a rejected `Promise.all` never runs `.then`, so state stayed at its **initial** `null` — which is what the assertion read. **The fifth documented instance of this class here, and the first found by mutation rather than review.** It also records why a red-first test is not automatically a real test: this one went red because the module did not exist, not because the behaviour was absent. §3 lists three defects found **in the plan itself**, §6 records that the **e2e gate was not run** at the seam and why that is the honest weak point, and §8 is the five things a successor must not get wrong. |
| `superpowers/plans/2026-07-28-frontend-mockup-adoption.md` | **COMPLETE — fully executed 2026-07-28. The plan for the frontend redesign.** Thirteen tasks, identifiers namespaced **`UI-`**, one seam after Task 7 **named at authoring time** rather than discovered — and the seam worked: the successor picked up cold and finished. **All 13 done.** Its Global Constraints section is the load-bearing part: the field labels stay literally `From`/`To` and are uppercased in CSS only (four test files resolve them by exact text), the journey stays an `<ol>` of `<li>` (three e2e specs index into it), the bypass markup stays a single copy (Playwright strict mode), and `viewport-fit=cover` is **not** added. **Read the execution log's §3 before executing any remaining task** — two of the plan's own code and test snippets are known wrong there. Owns no figures. |
| `superpowers/specs/2026-07-28-frontend-mockup-adoption-design.md` | **AUTHORITATIVE for the redesign's intent; owns no figures.** The design for adopting the owner's Claude Design mockup across all five screens. **Not a re-skin:** `frontend/src/index.css` already defined eight theme tokens and the mockup uses those exact eight values, so the work is type scale, spacing, states and five new structural pieces. **Its §2 records nine owner decisions so they are not reopened**, and §7 fixes every user-facing string **before any implementation existed**, so wording could not be reshaped to fit what got built — including the two per-signal reroll messages, which are flagged there as *accurate to the router rather than decorative*, so a change to `dislike`'s neighbourhood penalty or `known`'s floor relaxation puts them in its blast radius. `UI-1` requires the mockup export to be **committed** (`frontend/design/2026-07-28-mockup/`) rather than cited from a Downloads folder. §12 names two deploy-time consequences: **this will be the next FRONTEND publish, which is the `--prune` deferral's trigger**, and the API change means an App Runner deploy. §14 names the weakest link. |
| `2026-07-28-doc-audit-frontend-mockup-adoption.md` | **COMPLETE — a point-in-time audit record. ⚠ THREE OF ITS SIX FINDINGS WERE ALREADY FALSE WHEN WRITTEN**, and it carries a banner saying which. The `doc-auditor` run for the mockup-adoption closeout. It was dispatched **while the closeout was still editing the documents it audits**, so its two HIGH findings (four documents unclassified; the handoff's lint claim untrue) and one MEDIUM (`NEXT.md` silent on the frontend work) are **race artifacts, verified false immediately after it reported**. **The one real defect it records is its own** — as written it had no role marker, was unclassified, and carried a dead link, failing three of the six checks it was auditing; all corrected in place. **Retained rather than deleted because its verification half is real evidence**: code references in the spec and plan resolve against source, and the `UI-` series is collision-free. Two findings survived and were acted on (the supersession axis, and a heading). **Cite it only through its banner.** |
| `superpowers/2026-07-28-HANDOFF-password-removal-complete.md` | **⚠ NO LONGER THE CURRENT HANDOFF — superseded 2026-07-28 (evening) on next actions ONLY** by the mockup-adoption handoff above, which is a different body of work and discharges none of this one. **Everything it states about the deployed site remains true, and the iPhone test it says is owed is STILL OWED.** Originally written at the password-removal plan's last implemented task, 2026-07-28 — written at the plan's last implemented task.** Supersedes the Track B handoff below **on next actions and on status**. `PW-5`→`PW-8` complete, deployed and verified; **`PW-9` is the only unrun task** and is gated on the owner's approval. **The site password is gone and `https://musicapp.cmiller.io` is public**, behind a Cloudflare front door with a rate limit; no application code changed. Carries what the execution log cannot: that **both plans are historical and still read as though nothing has run** (leave them); that the plan is wrong in two named places (`PW-6`'s check 4 aims at the wrong leg, and its rate-limit period assumes 60-second windows the owner's Cloudflare plan does not offer); that **`~1.9 s per path request` is CORRECT** and was briefly and wrongly reported as refuted, because latency is driven by the artist **pair** and not by bypass depth; and what was decided against, including Cloudflare's post-quantum origin handshake — deferred only so `PW-6`'s gates ran with one variable, still worth enabling. |
| `superpowers/2026-07-28-HANDOFF-password-removal-track-b.md` | **⛔ SUPERSEDED 2026-07-28 on next actions AND on status** by the row above — Track B ran to completion the same day. Retained as the record of *why Track B was shaped as it is*: the additive-reversible ordering, and four things it warned a cold session would get wrong, all of which held. **⚠ One of its bullets was WRONG and is struck in place** — it said the `--prune` deferral came due at `PW-5`'s deploy; that deferral belongs to the *frontend* publish and has not come due. **Do not act on its next actions.** Originally written at the plan's own Track A/Track B seam. Track A (PW-1→PW-4, API-only) is complete, committed and verified; **Track B is not started and the password is still on.** Supersedes the migration handoff below **as the document to read first only** — a *different* body of work, discharging none of it. **Two live handoffs, deliberately:** the migration's Tasks 9→11 remain owed on their own axis. Carries what the execution log cannot: that **the password must stay on until PW-7** and the additive-reversible ordering before it is a safety property rather than bureaucracy; that **`max_size=2` must NOT be raised** — it reads like the obvious capacity fix and is the cost ceiling, and not raising it is the only reason `G3-A3` does not fire; that **`G3-S7`'s fix is a swap, not a deletion** (deleting the gate also deletes the SPA fallback and 403s every shared link); that **PW-6 step 6 check 4 is not optional** — it proves the Cloudflare secret reaches the origin *while the password is still on*; and that **the `--prune` deferral comes due at Track B's first deploy** with nothing currently scheduled to read it. Also records what was decided against: AWS WAF, spending Cloudflare's five spare rules, and a `Literal` for `ExclusionIn.reason`. |
| `superpowers/plans/2026-07-28-password-removal-load-hardening.md` | **COMPLETE except `PW-9` (owner-gated, blocks nothing, status owned by `NEXT.md`) — executed through `PW-8` and deployed 2026-07-28; role corrected 2026-07-30 by doc audit after reading ACTIVE for two days post-ship.** *(Original role: ACTIVE — the governing plan for removing the site password.)* Nine tasks, identifiers namespaced **`PW-`**, one seam after PW-4. Implements a **subset** of the Gate 2→3 review — the minimum load-related set that lets the password come off — and **explicitly does not open Gate 3**; where it describes a finding, the review wins. **Owns no figures.** Track A (PW-1→PW-4) is DONE; Track B (PW-5→PW-9) needs the owner's AWS and Cloudflare accounts. Records the scope reasoning that two blocking findings were argued *out*: **`G3-A3` does not fire** under a shed-load-at-the-edge design, and **`G3-A1`'s capacity half is deferred** because a per-IP limit cannot help against a dozen legitimate simultaneous users. Contains one **in-place correction made during execution** (the circuit breaker's cooldown reset, annotated at the code block) — a plan defect, not a code defect. |
| `superpowers/2026-07-28-password-removal-execution-log.md` | **The retained execution log for the plan above, appended per task. ACTIVE — Track A DONE, Track B not started, the live site unchanged.** Fresher than the plan on status. Records the two **design errors caught by red steps** and kept rather than quietly fixed: a **vacuous test** whose 422 assertion passed before the fix existed, because `build_path` already 422s for "exactly two" — but only *after* materialising the array, which was the entire finding; and the **breaker cooldown reset** that would have let a dead catalogue take five calls a minute instead of one. Also records that tests written *after* their implementation were verified by mutation rather than trusted green, the Snyk result (one Low, pre-existing, already triaged), and the five things a Track B session must not get wrong. **Owns the suite counts** — its "State at the seam" table is the only copy; three documents restated them and disagreed until the doc audit caught it. **Records two closeout findings against this closeout itself**: `A4` found an untested wiring path (`build_default_app` relies on a constructor default to arm the circuit breaker, and every test injected one explicitly — `G3-Q1`'s exact shape), and `D1` ran before Part B's fixes and reported a false green. |
| `superpowers/2026-07-27-HANDOFF-migration-phase-d.md` | **⚠ No longer the CURRENT handoff — superseded 2026-07-28 by the one above *as the document to read first only*. ⚠ **NOW FULLY SPENT as of 2026-07-28 — do not act on its next actions.** Task 9 was discharged by restore, and Tasks 10 and 11 are **CLOSED by owner decision**: the OneDrive tree is kept indefinitely as an archive, so there is no deletion to gate and Task 10 existed only to gate it.** Written at the plan's own Phase D seam. Phases A–C are complete and verified; **Phase D is not started, and two owner-owned gates stand before it** (Backblaze upload *completed*, and Task 10's few days of use). Supersedes the handoff below on next actions **and** on status. Carries what the execution log cannot: that **`MIG-10` / Task 12 was deliberately deferred — ⚠ it is DONE as of 2026-07-28 and this clause is spent.** Its defence (the statements were still true of the tree that was still the rollback) expired when Tasks 10/11 closed and the tree became a permanent archive. The live documents now name `C:\dev\music-app`; only historical logs still say OneDrive, and those must stay as they are. The one earlier exception taken (`CLAUDE.md`'s project-memory path, corrected because it was actively misleading a live session); that **`MIG-11` could not be actioned** — two old-tree API servers on ports 8138/8139 are still running and will make Task 11's deletion fail partially, the kill having been blocked by the tool-permission classifier; that a **third listener on 53342 belongs to a different project** and must not be killed with them; and that the Task 4 evidence lives in five files under `C:\dev\` that **cannot be committed** and must not be deleted with the old tree. |
| `superpowers/2026-07-27-HANDOFF-onedrive-migration.md` | **⚠ SUPERSEDED 2026-07-27 on next actions AND on status** by the handoff above. Its "no task in it has run" is no longer true and its four don't-get-this-wrong items are all consumed. **Retained because it is still the record of *why the plan is shaped as it is*** — the `clamp` evaluation, the `MIG-5` firing, and the archive-is-the-asset framing are unchanged and still correct. Do not act on its next-actions section. | Supersedes `superpowers/2026-07-27-HANDOFF-gate2-track-c.md` **on next actions only**; that document's four must-not-revert items are untouched and still govern. Carries the four things a cold session would otherwise get wrong — Task 0 is not bookkeeping (the clone is silently lossy against uncommitted work), `clamp` looks like it does Task 8 and does not, `MIG-5` already fired and must **not** be repaired, and the archive is the asset rather than the artifacts. Its "what is owed" section records that **a full `closeout` was not run** and names what is missing. Also records the two unrelated items in the same PR: the queue entry DONE per-step, and the **mangled-descriptions retraction** — with the warning not to reinstate it from the two Track C records, which are annotated but still contain the original text. |
| `superpowers/plans/2026-07-27-onedrive-migration.md` | **The OneDrive migration plan — twelve tasks, identifiers `MIG-`. ⚠ COMPLETE as of 2026-07-28, and **Phase D never ran and now never will** — Phases A–C executed 2026-07-27; Task 9 discharged and Tasks 10/11 **closed by owner decision** (the tree is kept as an archive); Task 12 done. **Read it as history, not as work outstanding** — the plan's own text still reads as though nothing has run. For what actually ran, read the execution log row below, which is fresher than this one. Moves the tree to `C:\dev\music-app` (owner's decision, 2026-07-27). **Touches no application code, no graph, no routing and no config default** — its §5 says so explicitly and no test-queue entry is owed. Its §0 states what the job is actually about: every tracked byte is already on GitHub, so the whole risk sits in the 1.6 GB git ignores, and within that in **`builder/scratch/graph-archive/`** — 75,000 files that cost a 4¼-hour rate-limited crawl and **cannot be reproduced**, because a fresh crawl of a live API is a different input. §1 is the risk register; **three of the ten fail silently** (`MIG-1` partial copy, `MIG-2` project memory orphaned by the path-keyed `~/.claude/projects/` slug, `MIG-3` unverified Backblaze coverage). It **clones rather than copies the tracked tree** as the structural mitigation for the `.git`-under-OneDrive corruption incident, and **names a hard seam before Phase D**, which is where the rollback copy gets deleted. `MIG-9` is accepted, not fixed: **26** frozen `builder/analysis/` scripts will stop resolving, which **weakens the stated justification for the shipped-code read-only aliases** — evidence toward their removal condition, not an argument to remove them now. **That count read 16 until the doc audit corrected it**, and §4 keeps the error rather than just the fix: a regex alternation over two slash styles matched nothing on the backslash branch and returned a plausible wrong number. Count with `grep -F` per variant. |
| `superpowers/2026-07-27-onedrive-migration-execution-log.md` | **The retained execution log for the migration plan above, appended per task. ACTIVE — Phases A–C DONE, Phase D not started. Fresher than the plan and the handoff on status; where they disagree, this wins.** **Owns the migration's own inventory figures**, which live nowhere else: `builder/scratch` is **75,052 files / 1,460,266,074 bytes**, of which `graph-archive` is **75,000 / 900,518,765**. Records the Task 4 pass (per-file manifest diff, **0 differing files**; all 18 `.bin` sha256 identical source-to-destination; `/health` from the new tree serving `4cb84ef9…`) and all four suites green from `C:\dev\music-app` (builder 115, api 195, infra 58, frontend 80 + 5 e2e). **Four things a successor needs that the plan does not carry:** (a) `MIG-4` names two gitignored files and there are **six** — the clone also drops `.superpowers/`, which holds `sdd/BLIND-MAPPING.json`, the arm-to-identity key for the blind listening tests, gitignored precisely because a tracked copy would unblind them (§4); (b) **`TaskStop` does not kill the process tree here** — a killed inventory script kept running and raced a second writer into one output file, producing interleaved mid-line corruption caught by eye rather than by any control (§12); (c) **`robocopy` exit code 1 means success** and is surfaced as a failure (§8); and (d) **`MIG-11` — two API servers from the OLD tree are still running** on ports 8138/8139 since 2026-07-20 and **will make Task 11's deletion fail partially**, since they hold that tree's `.venv` open. **The most safety-critical of the four**, because it is the only one still live at this seam; stopping them was blocked by the tool-permission classifier and is an owner action. **Task 8 is CLOSED** (§11): memory was *copied* to the predicted slug `C--dev-music-app` rather than renamed, and a session launched in the new tree then read all eleven `MEMORY.md` entries from it — the duplicate under the old slug is retained deliberately until Task 11. **Task 9 (Backblaze) is IN PROGRESS — the owner's, and it gates Task 11**; its exclusion-list check passed (§13a), and acting on it uncovered a **pre-existing backup failure unrelated to this project** (§13b). |
| `superpowers/2026-07-21-phase2-execution-log.md` | **The audit trail for Phase 2.** Every decision and why, defects found in the plan and in the prior record, gate pass/fail state, open items, deferred findings. **§16 is the blind-test verdict; §17 the adoption; §18 the closeout triage; §19 the fixture-seed defect found after closeout — which supersedes §17's checksums.** Where any other document disagrees with §16–19 **on Phase 2**, this wins. **No longer the freshest truth in this directory** — as of 2026-07-23 that is `2026-07-23-repair-and-retune-execution-log.md` and `findings/2026-07-23-tiebreak-fix-adoption.md`, which govern current status and the adopted artifact. Marked ACTIVE rather than COMPLETE because Phase 1 consumes its carried-forward items. |
| `superpowers/plans/2026-07-21-alpha-rollout-roadmap.md` | *(also listed as Authoritative)* Its Phase 1 section gives the **phase structure**, including the two items carried in from Phase 2 with success conditions; current sequencing within Phase 1 is governed by the 2026-07-23 repair+retune spec, not this document. |
| `superpowers/2026-07-22-phase1-execution-log-and-graph-defect.md` | ⛔ **Start here for Phase 1, and read §2 before any implementation.** One document by design: the **graph-structure defect that halted the work** (§2 — facts, discovery mechanism, suspected cause; deliberately no solutioning) **plus** the full Phase 1 progress record (§3 — including two independent ML-analyst reviews and what each overturned), what is closed (§4), artifacts (§5), and state of play (§6). Splitting it would let the progress record be picked up without the context that stopped it. **Owns its figures** — cite from here, do not restate. |
| `superpowers/2026-07-22-HANDOFF-phase1.md` | Written at Phase 2 closeout for a cold session: what was overturned and must not be reverted, and what the previous session knew that is not otherwise in the record. **Superseded on Phase 1 status by the log above** — that document governs where they disagree. Still valid on Phase 2 outcomes. |
| `superpowers/2026-07-23-HANDOFF-track2.md` | **SUPERSEDED 2026-07-24** by `2026-07-23-HANDOFF-track2.md`'s successor below — its loose ends are closed or folded forward. Retained for audit; do not act on it. *(Original role: loose ends written at the 2026-07-23 mid-track retirement.)* |
| `superpowers/2026-07-24-HANDOFF-track2.md` | **SUPERSEDED 2026-07-24 by `2026-07-24-HANDOFF-track2-scorer.md` below** — its one open unit (the sweep) is now half-built (the scorer exists) and re-described there. Retained for audit; do not act on it. *(Original role: fame-proxy/sweep seam handoff, P4 closed, A11 committed.)* |
| `superpowers/2026-07-24-HANDOFF-track2-scorer.md` | **SUPERSEDED 2026-07-24 (same day) by `2026-07-24-HANDOFF-track2-complete.md` below.** Its open unit — P8b, then the arms — is closed: P8b was discharged and all 15 arms ran. Its "no factorial arm has run" statements are now false. Retained for audit; **do not act on it.** *(Original role: handoff at the scorer/P8b seam.)* |
| `superpowers/2026-07-25-HANDOFF-track2f-and-headroom.md` | **⚠ SUPERSEDED on next actions** — the chain ran on through the Track 3 handoffs (`2026-07-28-HANDOFF-track3-preregistered.md` names this one as what it supersedes) to the current handoff at the top of this table. *(This row claimed to be current until 2026-07-31, when the `REL-` closeout's doc audit found it; the claim was already stale when written past. Its own Track 2F record is untouched and remains accurate.)* *(Original role: **The CURRENT handoff, 2026-07-25 — written at the Track 2F / headroom completion seam.*** Track 2F ran the ceiling toll at full strength and returned **`TFR0`**, a null at `M*` = −0.198 — but **its bound holds**, unlike Track 2's corner arm, so the toll mechanism can be declared exhausted rather than merely untested at fifteen points. The ceiling's *ordering*, which no router-side arm can reach, was then measured read-only and came back **WIDE** (median 10.01 × `w_hop` erased). **Nothing adopted, no shipped code changed, no blind listen run, no threshold touched, no rebuild.** Carries the mechanism statement, the four things a successor must not get wrong, the open decision with the session's position, and what was decided *against*. Supersedes the 2026-07-24 handoff **on next actions only**. **Read the execution log's last three entries first**; where they disagree, the log wins. A seam handoff, not mid-flight.)* |
| `superpowers/2026-07-24-HANDOFF-track2-complete.md` | **⚠ PARTLY SUPERSEDED 2026-07-25** by the handoff above — **on next actions only.** Track 2's own result is untouched and this is still the record of it, but its §4 recommendation has been **executed** (that is Track 2F) and its §2 claim that the toll "has never run at full strength" is **no longer true**. Both are marked inline as well as bannered. *(Original role: the Track 2 completion-seam handoff, 2026-07-24.)* **Written at the completion seam.** The sweep ran to completion and returned **R0, a full null**: 15 arms, best at −0.177 against a −1.0 threshold, **nothing adopted, no shipped code changed, no blind listen run**. Carries what R0 does and does **not** license (A17(c)'s falsifier fired, so it does not reach "the family is exhausted"), the firmest finding (the floor device is never pivotal), the open owner decision with the session's recommendation, and the **three items now queued behind one rebuild**. **Read the execution log's Track 2 section first**; where they disagree, the log wins. A seam handoff, not mid-flight. |
| `superpowers/findings/2026-07-25-consulting-derivations.md` | **AUTHORITATIVE for `DRV-1` only; everything else is a labelled hypothesis with a named check.** Written when a consulting session was asked what it knew that was not written down. **`DRV-1` is verified and matters: §2.11's and §2.12's tables were computed on `graph-t15-capfix.bin`, while the app has run `graph-t15-tiebreakfix.bin` since 2026-07-23** (**narrowed 2026-07-26** from the original "§2.9–§2.12"; §2.10 measures the adopted topology directly and is not exposed, and §2.9 is barred by supersession rather than by provenance — see the Current State bullet above and the notice in `DRV-1` itself) — the scripts assert capfix's checksum, and `known_viability.py` was committed six hours *before* the tie-break artifact was adopted. The visible tell needs no tooling: §2.12's worked example is "The Beatles have 7 of 7 neighbours admissible", and `MKS-5` records the Beatles holding the full 50 on the adopted graph. **`DRV-2` argues this is a provenance defect, not a refutation** — the pricing leg is artifact-independent and the exposed legs move in the direction that *strengthens* §2.12 — **but any specific §2.9–§2.12 figure quoted as a fact about the current app is unsupported**, which this map's Current State does. Re-running two committed read-only scripts settles it. `DRV-3`–`DRV-7` were recorded at hypothesis strength, and **`DRV-4` is now RESOLVED (2026-07-26)** by `findings/2026-07-26-low-degree-census.md`, which measures the population its structural claim describes and closes its open denominator: the descent is a **round trip** and §2.12 priced it one-way; a **degree-1 artist can never be an interior card**, so it is undeliverable as a discovery (**`DRV-4`, resolved**); §2.12's substitute counts may be **leaf-inflated**; the **staircase model** that generated `BYP-11`; and **three discriminating signals go flat at the famous endpoints**, leaving popularity alone. §3 carries the session's opinions, fenced off from the findings. **§4 is the calibration**: the same session made three confident wrong claims that the owner caught, all from reasoning over summaries when the data was available — and it names which two hypotheses are most likely to fail the same way. Nothing adopted; the pause is intact. |
| `superpowers/findings/2026-07-25-bypass-depth-use-run.md` | **AUTHORITATIVE for its own measurements — the owner's own use of the shipped app, 2026-07-25.** **Two runs of 100 bypass presses** on one pair (Bob Dylan → Metallica): run 1 with the buttons mixed, run 2 **`dislike` only**. Dogfooding, not an experiment — one pair, one listener, nothing scored or pre-registered, and §1 states every protocol defect up front. **The headline (`BYP-3`): across 200 presses the app never presented an artist who is obscure by any ordinary reading** — floors of 372k and 168k Spotify monthly listeners. `dislike` finds more unfamiliar artists (10 v 7) and reaches ~2× deeper, confirming a prediction this document fixed *before* run 2 ran. **`BYP-11` is the most consequential reading**: genre departure precedes obscurity by ~50 presses, so the router appears to escape the famous stratum by crossing genres rather than descending within one — measured against `WHAT-GOOD-LOOKS-LIKE` value 8, that is buying novelty with incoherence. **`BYP-9`, confirmed and reproduced:** `dislike` held the path at 3 cards for nine consecutive presses, which at 3 cards is *forced* 1:1 swapping — the shape value 6 says `dislike` should show least. **`BYP-13` is a live defect and the most actionable item: a card played a clip by a different artist of the same name** (FERG / A$AP Ferg), which is **C1's class reached by a different route, so C1 is not fully closed** — not path-quality work, not paused. **Read §4 before citing any length claim**: `BYP-4` was written, corrected, then partly restored, and the settled statement is *an upward trend with large non-monotone excursions*. **Explicitly does NOT refute §2.9** — that is graph-popularity percentile, this is Spotify listeners. §9 records the second-opinion instrument's **failure mode** (name-based lookup returns the wrong entity for a renamed artist, and fails toward flattering the result) and `BYP-14` records that manual press-logging does not scale and was **mostly unnecessary** — a single-mechanism run is fully reconstructible by replaying truncations of one URL. §10 carries the length series and the reproducible URLs. The pause is intact; nothing proposed. |
| `superpowers/findings/2026-07-25-router-ascent-gradient.md` | **A re-reading of a committed measurement, not a new one — and it deliberately OWNS NO FIGURES.** The P8b harness review measured, one hop out from every node, how often the router's cheapest next step moves toward a *more popular* artist: for the production-like arm it is far above the share of the graph's own edges that point upward. That measurement is currently reachable only as **A17(c)**, a bound on how the corner arm `X` may be read, inside a pre-registration whose track returned a null — so nobody re-reads it. Its `ASC-1` (the router climbs about four times in five where the graph offers a coin flip) is a **single-arm measurement against a structural null** and is clean. Its `ASC-2` (the sweep's most aggressive dive-cheapening corner ascends *more*) is **explicitly confounded — three columns, not one** — and supports "the corner does not dive", never *why*. `ASC-3`/`ASC-4` are labelled **inference**: no arm in Track 2 or 2F was directional (the jump term is `w_jump·\|Δpop\|`, symmetric; the toll binds on the edge; the floor is one-sided on level, not direction), so the pull survives the whole span of repricing and probably originates outside the popularity terms — with `w_sim` the leading candidate, because similarity and popularity share a source. **Does not reopen §2.12**; sits underneath it. Names one cheap decisive test (`ASC-5`): re-read the gradient at path level and read the `X`-vs-`A7` contrast that already exists in the Track 2 run — **both re-reads of committed runs, no rebuild.** Figures owned by `../builder/analysis/2026-07-24-track2-p8b-harness-review/`; quoted in A17(c). ✅ **`ASC-5` DISCHARGED 2026-07-28** — results in `../builder/analysis/2026-07-28-asc5-path-ascent/` (its row above); its §4 carries the discharge note in place. |
| `superpowers/2026-07-26-RESUME-BRIEF-path-quality.md` | **✅ Its trigger FIRED 2026-07-28 — the owner resumed path-quality work**, and it was read in full first, as it instructs. The live governing document for the resumed work is the **Track 3 pre-registration** (row above), which addresses the fame axis; **this brief's §1 degree-floor decision and §2 arithmetic-vs-pricing confound on the *degree* axis remain open and unstarted** — the brief is still the entry point for *that* question. *(Original role: the entry point IF path-quality work is ever resumed — explicitly NOT a resume signal.)* Resuming is the owner's trigger. Written 2026-07-26 for a cold session months later; short by design and meant to be read in full. Carries five things: **the decision** (build a bounded degree floor, or not); **the one thing that blocks everything else** — nobody has separated whether barely-connected artists fail to appear because the router prices them out or because they arithmetically have fewer places to fit, and those point to opposite conclusions about whether more edges would help; **a correction** — the floor is a *larger* intervention than a consulting session implied, since nothing was delivered below four connections, making the receiving-side quota a real design problem; **a three-item reading order** with an instruction not to re-derive the chain from the analysis directories; and **what is closed** (loosening the cap, Track 2, Track 2F, the ceiling toll, Gate 1/F1) alongside **what may not be spent** without the owner (a blind listen, a rebuild — itself blocked by `acceptance.py`'s nameless-artist check — and any growth of `CLAUDE.md`). Names the two parked candidates so they are not lost. Proposes nothing and sets no threshold. |
| `superpowers/2026-07-26-HANDOFF-track-d-complete.md` | **⚠ SUPERSEDED ON NEXT ACTIONS** by `2026-07-26-HANDOFF-track-b-complete.md` and, through the chain, by `2026-07-27-HANDOFF-gate2-track-c.md`; still the record of Track D, and its do-not-revert list stands. *(This row read "The CURRENT handoff" until 2026-07-27, three handoffs after it stopped being one.)* — written at a clean seam after Track D was executed in full. Supersedes the Track A handoff below **on next actions only**; that one is still the record of Track A and **its "must not be reverted" list is still live**. Records Track D as DONE (frontend 65 → 78, e2e 3 → 4, api and builder unchanged, `pathfinding.py` never opened), the five claims an editor must not undo — chiefly that `Player.play()`'s **two-part** handler check is not redundant, because checking `disposed` alone is defeated by the StrictMode revival that clears the flag — what the session knew that is otherwise unwritten, and the open decision (write the Track B plan, and run `DEP-33` **inside** it rather than after it) **with the session's position**. Names two items that are genuinely the owner's: `uv.lock`, and whether `CLAUDE.md`'s next-action row should name the executed tracks. **Two detached dev servers are deliberately left running**, and the note explains why their pre-HEAD start time does *not* invalidate them. |
| `superpowers/2026-07-26-HANDOFF-track-a-complete.md` | **⚠ SUPERSEDED ON NEXT ACTIONS 2026-07-26** by the handoff above; still the record of Track A, and its do-not-revert list stands. **One bullet is struck in place**: its claim that the repo-root `.playwright-results/` was "the `DEP-30` / `TR-17` fix working" is false — see `TKD-3`. *(Original role: the handoff at the Track A completion seam, 2026-07-26 evening.)* — written at a clean seam after Track A was executed in full. Supersedes the Gate 2 design handoff below **on next actions only**; that one is still the record of the design and review work and **its "must not be reverted" list is still live**. Records Track A as DONE (api 153 → 180, `DEP-16` discharged, routing untouched), the five claims an editor must not undo — chiefly that the unreachable `count=` guard **stays with an honest comment** rather than being deleted as dead or re-labelled load-bearing — what the session knew that is otherwise unwritten, and **the one item awaiting the owner: whether `CLAUDE.md` gains `ARTISTPATH_GRAPH_SHA256`**, which is a real defect of absence whose fix grows the budgeted standing layer. Names the open decision (write the Track D plan, and write it for subagent execution) **with the session's position**, not a menu. **Two detached dev servers are deliberately left running**, PIDs recorded there and in the test queue. |
| `superpowers/2026-07-26-HANDOFF-gate2-track-a.md` | **⚠ SUPERSEDED ON STATUS 2026-07-26** by the handoff above — its "no code has been written" line is false and marked in place; everything else, including its do-not-revert list, stands. *(Original role: the handoff at the design/review/plan seam, 2026-07-26.)* **The CURRENT handoff, 2026-07-26** — written at a clean seam after the Gate 2 design, the team review, and the Track A plan. **No code was written.** Names the reading order for a cold session, the five claims a well-meaning editor must not revert (chiefly that `DEP-8`'s struck text stays visible and that telemetry logs facts rather than metrics), what the session knew that is otherwise unwritten, and what was decided against — including **why Track A is executed inline rather than by subagents**. Records that a full `closeout` was **not** run and what it would still add. |
| `superpowers/findings/2026-07-26-gate1-gate2-team-review.md` | **AUTHORITATIVE for its own measurements and triage.** The gate-boundary team review CLAUDE.md schedules at Gate 1 → 2, run 2026-07-26 **before any implementation plan existed**. Four reviewers — architect, security, quality, frontend; the graph analyst deliberately **not** staffed, since no scoring question is open. Identifiers **`TR-`**. **`TR-1`/`TR-2` are the most valuable output and are a correction to the same day's own work**: the design's flagship defect (`DEP-10`) was misdiagnosed — a truncated artifact **raises**, because the metadata blob is the last section — and its prescribed verification **passed before any fix**, the `FMS-P1` vacuous-check pattern recurring. **`TR-3`** names the genuinely silent case (header/metadata length disagreement); **`TR-4`** records that the builder's parser already had the checks the API's lacked, so the two APG1 parsers had **already drifted undetected**. `TR-5`–`TR-11` are seven blocking gaps in the design, notably that every shared link would 403 and that CloudFront's default cache policy would **resurrect C2**, a defect closed 2026-07-25. **`TR-16`** is why Track D exists. **§6 records what was declined and why**; §8 states the review's own weakest link — seven findings concern a design rather than code, so **`DEP-33` requires re-running it against the CDK stack before cutover.** |
| `superpowers/plans/2026-07-26-track-a-api-hardening-and-telemetry.md` | **The Track A implementation plan** — ten tasks, AWS-free, complete code and exact expected test counts per task. **EXECUTED 2026-07-26; do not execute again** — outcomes in `superpowers/2026-07-26-gate2-track-a-execution-log.md`. Implements Track A of the design's §11 only. Its header carries a **plan-specific execution recommendation: inline, not subagent-driven**, with the reasoning and the condition under which Track D reverses it. Carries a **handoff seam after Task 6** (defect fixes done, telemetry not started). Its Global Constraints include the standing instruction that **every new test must fail against unmodified source first, and for the right reason** — written because this plan's own predecessor prescribed a test that could not fail. |
| `superpowers/2026-07-26-gate2-track-a-execution-log.md` | **The record of Track A, 2026-07-26** — the plan above, executed. Identifiers **`TKA-`**. Ten tasks, inline; suites api 153 → 180, builder 115, frontend 64 → 65, e2e 3, every per-task count hit first time. **Touches no routing weight, no cost-function term, no graph** — `pathfinding.py` was not edited at all. **`TKA-6` records `DEP-16` as DISCHARGED**: two `path` events emitted through the live API on the **adopted** artifact replayed offline through `find_journey` and reproduced exactly, sequence and stop rule; the bypass event is the one that carries the weight, since a zero-exclusion path verifies only that the graph loaded. The service was booted with the checksum **set**, so Task 2's gate is proven against the real file. **`TKA-1` is the entry worth reading**: what the RED runs actually said, which is the half that cannot be reconstructed afterwards. Task 1's truncation test failed pre-fix with `Expecting ',' delimiter` — a `JSONDecodeError`, whose message does **not** contain "truncated" — so `pytest.raises(ValueError)` would have passed against unmodified source. **The corrected `DEP-10` is therefore confirmed by observation, not by argument, and `TR-2` was reproduced under control and avoided.** `TKA-2` records why Task 3's knowingly-passing verification is a named exception rather than a recurrence; `TKA-5` records the seam that was reached and not taken, and that this log was started late. Snyk clean on `api/` and `frontend/`. Nothing adopted, nothing rebuilt; the path-quality pause is intact. |
| `superpowers/plans/2026-07-26-track-d-frontend.md` | **The Track D implementation plan** — eight tasks, AWS-free, complete code per task. **EXECUTED 2026-07-26; do not execute again** — outcomes in `superpowers/2026-07-26-gate2-track-d-execution-log.md`. Covers the six frontend items the owner fenced at the team review (`TR-16`) **plus a seventh** he accepted the same day after running the Track A queue entry. Its header carries a **plan-specific execution recommendation — inline, not subagent-driven — which reverses the prediction the Track A completion handoff made**, with the reasoning and the reversing condition. Its Scope section **corrects two `TR-16` claims** against source; the correction that matters is that a grep-based verification of the layout fix passes before the fix. |
| `superpowers/2026-07-26-gate2-track-d-execution-log.md` | **The record of Track D, 2026-07-26** — the plan above, executed. Identifiers **`TKD-`**. Eight tasks inline; frontend 65 → **78** (this row said 77 until 2026-07-27; the log's own closeout corrects it to 78, and the log wins), e2e 3 → 4, api 180 and builder 115 **unchanged** *(counts as at 2026-07-26; later tracks have since added tests — for current counts collect the suites, do not read them off this row)*, which is the check that routing was untouched. Snyk clean. **`TKD-2` is the entry worth reading**: Task 1's e2e spec **passed on its own first RED run** because it measured an endpoint card, which carries no bypass buttons and was therefore never broken — the `FMS-P1` / `TR-2` vacuous-check pattern reproduced by the one spec written to prevent it, and caught by running the RED rather than by review. **`TKD-3` records that `DEP-30` was not fully fixed by Track A** — the repo root is under OneDrive too, so Playwright still `EPERM`ed on two consecutive runs; read `DEP-30` as discharged only from this track. **`TKD-4`** corrects the owner's own first telling of the seventh item in two ways that change the fix: it is **not** a race with the autocomplete, and the "New path" prefill did not introduce it. **`TKD-7`** records a suite count that did not match the plan and was **not** made to match. **§11 states what this track does not cover — no real phone has run it**, which is what the queued entry is for. Nothing adopted, nothing rebuilt; the path-quality pause is intact. |
| `superpowers/plans/2026-07-26-track-b-infrastructure.md` | **The Track B implementation plan** — thirteen tasks plus four owner-run prerequisites, complete code per task. **EXECUTED 2026-07-26; do not execute again** — outcomes in `superpowers/2026-07-26-gate2-track-b-execution-log.md`. Identifiers **`TKB-`**. Its header carries a **plan-specific execution recommendation: inline, not subagent-driven**, and it names its own seams. **Read its findings section before touching `infra/`:** `TKB-2` (the team review's prescribed SPA-fallback fix is **distribution-level** and would have rewritten the API's own 404s and 403s into HTML with status 200 — the finding stands, its mechanism is replaced); `TKB-4` (**the plan's own prescribed dependency scan was vacuous**, reporting clean while examining zero packages, caught with a positive control); `TKB-7` (the first deploy must be staged — **but see the execution log's §7: its stated mechanism is wrong and its conclusion right**); `TKB-8` (App Runner is closed to new customers and this account can still use it, measured). |
| `superpowers/2026-07-26-gate2-track-b-execution-log.md` | **The record of Track B, 2026-07-26 — the app is DEPLOYED.** The plan above, executed, plus the `DEP-33` review that followed it. **§1 owns the deployed identity** (site, API origin, buckets, image tag) — cite it, never restate it. **§2 is what was verified against the running stack** rather than the template, and the entry worth reading there is that the logged `journey_id` survived CloudFront: a stripped header would have degraded telemetry silently. **§4 lists the claims an editor must not revert**; **§6 the Windows/Git Bash traps**, chiefly that Git Bash rewrites `/aws/...` arguments and the resulting error names the wrong cause. **§7 carries two corrections to this project's own record**, including that `TKB-7`'s stated mechanism does not hold. **`TKA-11` and `TKA-12` both close here.** |
| `superpowers/findings/2026-07-26-track-b-cdk-review.md` | **AUTHORITATIVE for its own findings and triage — the `DEP-33` review, run against the DEPLOYED stack.** Four reviewers (`ARC-`, `SEC-`, `QUA-`, `FRO-`); the graph analyst deliberately not staffed. **45 findings, 10 blocking**, none invalidating the deploy — all concern the *next* deploy or the *first* user. **§1 is the headline and it is about method: ten of nineteen mutations went green**, including detaching the password function from the site's default behaviour, which would leave the whole site publicly readable with no test failing. The fourth instance of the project's signature defect after `FMS-P1`, `TR-2` and `TKB-4`, and **the first where the vacuity was distributed** — no single test obviously vacuous, the suite simply holed where the properties were. **§2 is an ordered work queue**, ranked by what breaks and how silently. §3 corrects two claims in the project's own record. §6 states what the review cleared; §7 what no reviewer could reach, since none touched AWS. |
| `superpowers/findings/2026-07-27-gate2-gate3-team-review.md` | **AUTHORITATIVE for its own measurements and triage — the Gate 2 → 3 boundary review CLAUDE.md schedules, run 2026-07-27 after the Gate 2 cutover and first phone run.** Four reviewers — architect, security, quality, frontend; the graph analyst deliberately **not** staffed, no scoring question open and path quality PAUSED. Identifiers **`G3-`** (`G3-A/S/Q/F` by reviewer). **Owns its own performance figures** — dev-laptop measurements, labelled, with the container-vs-laptop caveat in §4; **owns no path-quality figures**. **Headline (§6): four blocking findings (architect `G3-A1`–`G3-A4` ≈ security `G3-S1`–`G3-S3`) are one issue** — a ~1.5 req/s GIL-bound capacity ceiling with a health-check cascade, an unauthenticated clip proxy that triples its own outbound rate under throttling (its silent-failure half found independently by architect, security **and** frontend, §2), and unbounded request inputs — all held back only by the basic-auth password Gate 3 removes. Plus two untested wiring paths where every guard is tested but the code arming them is not (`G3-Q1`, `G3-Q2`, the project's signature vacuity class again), and an iOS-audio finding no test can reach (`G3-F2`). **§7 records the live-origin load test**: the calibration half ran and resolved the review's weakest link against the app — the container is ~2.5–3.5× *slower* per request than the laptop, closing the "maybe App Runner is faster" escape hatch; the concurrency-ladder half was blocked by the tool-permission classifier and stays inferred. **§6 separates the owner's calls** (custom domain, telemetry retention, borrow-an-iPhone) from the session's. Nothing adopted; no code changed; the pause is intact. |
| `superpowers/2026-07-26-HANDOFF-track-b-complete.md` | **Superseded on next actions by the 2026-07-27 handoff below; still the record of Track B's completion.** Written at a clean seam after Track B was executed, deployed and reviewed. Names the successor's job — the ten blocking findings, then Track C — and carries seven must-not-revert claims, what the session knew that is not in the durable record, and the open decision with the session's position. **⚠ Its must-not-revert claim 1 was REVERSED on measurement 2026-07-27 and is annotated in place**; the other six stand. |
| `superpowers/plans/2026-07-27-dep33-blocker-remediation.md` | **The `DEP-33` blocker remediation plan** — thirteen tasks, identifiers **`RMD-`**, executing the review's §2 queue. **ALL THREE STAGES EXECUTED 2026-07-27 — `RMD-0`…`RMD-13`; do not execute again.** §0 states why it carries **no factor table** (it compares nothing and runs no arm) and what replaces that discipline: a named mutation per task, which must go red before the test counts. §2 is a **pre-flight table resolving every function, file and config value the plan names** against the tree, including two that are absent *by design* where the absence is the finding. §3 names three stages and two seams at authoring time. **It corrects the queue's own length: nine blockers, not ten** — `QUA-4` and `ARC-9`/`QUA-5` were already closed in `6f49ab9`. |
| `superpowers/2026-07-27-dep33-remediation-execution-log.md` | **The record of `DEP-33` remediation stages 1–3, 2026-07-27.** Identifiers **`RMD-`**. **Stages 1–2 are PR #30, stage 3 is PR #32** — the log spans both. **§0 is the entry worth reading**: three read-only AWS checks run *before* planning, which closed the billing-alarm deferral, confirmed `ARC-2` live, measured `QUA-2`'s premise — and **found a new defect, `RMD-D`**: `ARTISTPATH_CORS_ORIGINS` never reaches the running service, so the deployed API serves `config.py`'s dev default. Measured by drift detection *and* a live probe with a negative control. **The first instance of the signature defect where the gap is between the template and AWS**, which no test in any package can see. Stage 1 rebuilt the suite until **16/16 mutations are caught** (12 previously green, 4 controls) and **records that review §1's own arithmetic does not reconcile**, without inventing a number. Stage 2's `cdk diff` gate **found `ARC-6` live** — a non-blocking finding one command from repointing the service at a floating tag. The closeout's `A4` records that **`cors_origins` is flipped in source and not in production**, so the work is not closed. **Stage 3's own section is appended at the end** and records the one thing worth carrying forward as method: **two mutations were MISSED on their first run, and both were against tests that stage had just written** — one asserted the plaintext password was absent from a page that would have leaked the base64 credential, the other was satisfied by the site's own name rather than by the instruction it was checking for. |
| `superpowers/2026-07-27-HANDOFF-dep33-stages-1-2.md` | **Superseded on next actions by the stage 3 handoff below** — stage 3 is done. Still the record of the plan's second planned seam: the one reversed claim and the six that still stand, and **the one defect that is fixed in source and still live in production**, which is *unchanged* and still live. |
| `superpowers/2026-07-27-HANDOFF-dep33-stage3.md` | **Superseded on next actions by the Track C _handoff_ below (`2026-07-27-HANDOFF-gate2-track-c.md`, not the execution log in the row between) — Track C is DONE.** Still the record of the plan's third and last seam: what must not be reverted, and the owner's decision to accept the live `RMD-6` exposure rather than deploy twice. **⚠ Two of its statements are now overtaken.** Its `RMD-6` section is spent — the exposure was closed by the Track C deploy. And its description of `infra/README.md` §6 as "three ordered passes" was true when written: §6 is now **one command**, because the open decision it put to the owner (script the passes) was answered yes. |
| `superpowers/2026-07-27-gate2-track-c-execution-log.md` | **The record of Track C, 2026-07-27 — the CUTOVER. The app is on the internet and reachable.** Gate 2's last build track. **§1 is the entry worth reading**: Track C was recorded everywhere as "sync the SPA to the bucket", and that is the smallest of its four parts — closing `RMD-6` needed a **new image**, because the fix lives in `config.py`, so a session taking the one-line description at face value would have published against an unfixed API and recorded the defect closed. **§2** covers the §6 script and its ten mutations. **§3 carries three corrections to this project's own record**, the first being that the drift gate would have fired on *every* future deploy: `RMD-6`'s fix deliberately leaves the empty variable in the template, so that drift row is permanent by design and §7 now expects three rows, not two. **§4 records that §8a ran for the first time in the project's history** — every previous check proved only that the site *refuses*. **§5 closes `RMD-6`, `RMD-11` and `FRO-1` with the measurement rather than the assertion**, each checked live on both sides of the deploy. §6 is four deferrals with conditions — **one of which, the double-encoded artist descriptions, was RETRACTED 2026-07-27 and is struck in place with the correction beneath it: the artifact was correct UTF-8 all along and the mangling was in the tool that read the live response**; §9 the image and graph provenance. |
| `superpowers/2026-07-27-HANDOFF-gate2-track-c.md` | **SUPERSEDED 2026-07-27 on next actions** by `superpowers/2026-07-27-HANDOFF-onedrive-migration.md`; **still ACTIVE for its four must-not-revert items**, which are untouched and still govern. Its "use the app, on a phone" is **spent, not wrong** — the phone run happened and passed. *(This row read "The CURRENT handoff" until 2026-07-27, while the row above it claimed the same title for a different document — the exact recurring defect recorded against the Track D row below. Caught by the doc audit, not by the session that created the collision.)* *(Original role: the seam handoff after the cutover.)* Carries four must-not-revert claims (chiefly that §7 now expects **three** drift rows and that `--prune` stays off), what the previous handoff had overtaken rather than got wrong, and **an open item the closeout could not discharge — `B1`'s doc audit was not run**, and the manual sweep substituted for it cannot find defects of omission. |
| `superpowers/TEST-QUEUE.md` | The async **use-the-app queue**, and since **2026-09-12 a CHECKLIST, not a log** — one `- [ ]` box per thing to press, budgeted at **70 lines** and checked at `closeout` `B6-budget`. It catches the defect class tests structurally cannot. **Count unticked boxes, never headings**, and **never grep the archives for a count** — discharged entries there keep an old `QUEUED` heading, which made six consecutive closeouts report a backlog that did not exist. **Two standing rules, both the owner's:** an entry is written only when there is something he can actually press, so an **empty file is valid and never evidence a session forgot**; and it holds only things that can be found *wrong*, **never "tell me how it feels"**, because his continuous judgement of the app has no completion state and such an entry could never be discharged. What a session did, the port sweep and "did my app move" belong in the **closeout report and the PR body**. It reached **255 lines on 2026-09-12**, 89 of them front matter, which is what the budget now prevents. **⚠ Its standing no-phone constraint EXPIRED 2026-07-27 at the cutover.** |
| `superpowers/archive/TEST-QUEUE-notes-2026-09.md` | **HISTORICAL — frozen, never work.** `TEST-QUEUE.md` as it stood on 2026-09-12, immediately before it became a checklist: three undischarged entries, ~55 lines each, behind 89 lines of front matter. **Kept because the prose says what each check is *for*** — a session that needs to know why a box exists reads this and the owner never has to. **Where it and the checklist disagree, the checklist governs**, because it resolved a contradiction these entries contain: the rename entry says the Spotify and Apple links moved off the card into the detail panel, the facts entry still says to look at the bottom of the card. **The panel is correct**, verified 2026-09-12 from the component tests. Verbatim except four relative links repointed for its new directory, stated in its own header. |
| `superpowers/archive/TEST-QUEUE-nil-entries.md` | **HISTORICAL — do not cite, do not act on, do not add to.** The 40 "nothing to test" entries stripped from `TEST-QUEUE.md` on 2026-08-05. **None asks the owner to press anything and none records anything he exercised**; each is a session narrating that its work did not reach the app, which is what the pre-2026-08-05 one-entry-per-closeout rule compelled. Retained so the record can be audited. **Nothing here is the sole record of anything** — verified before the move, including `G3-F2`, whose falsification is recorded in four other documents. Two "nothing to test" entries were deliberately **not** moved and remain live, each carrying a `✅` discharge or owner check of *another* entry. |
| `superpowers/archive/TEST-QUEUE-discharged.md` | **HISTORICAL — frozen, do not cite, do not add to.** The **discharged** use-the-app entries: queued, pressed, answered. Sibling of the nil-entries archive above, and the complement of it — that one holds entries that queued nothing, this one holds entries that were run. Split out 2026-09-05, when `TEST-QUEUE.md` had reached 1,996 lines of which **93% was discharged history** sitting behind one live entry: the same shape, cause and fix as `superpowers/NEXT-ARCHIVE.md`. **Evidence about the past, never work** — several project turns were decided here, most consequentially the 2026-09-01 report that fired the revert criterion on the extended map. An entry is live only if its **topmost** heading says so, which is why entries here still carry their old `QUEUED` heading underneath. |
| `superpowers/specs/2026-07-23-defect-remediation-and-cost-retune-design.md` | **The governing design for resuming Phase 1** (owner-approved 2026-07-23). Track 1: builder fix for the §2.8 tie-break, rebuild, adopt by structural equivalence. Track 2: cost-function retune (currencies included) on the repaired graph, `known` mechanism, one blind listen. Records the owner's 2026-07-23 decisions in §1. |
| `superpowers/2026-07-23-repair-and-retune-execution-log.md` | Retained execution log for the repair+retune work. Track 1 record, the pre-Track-2 guards, and Track 2 continues it. |
| `superpowers/specs/2026-07-23-track2-preregistration.md` | **The Track 2 pre-registration.** Fixes the factor table, primary outcome, effect sizes, pair set, and the read of every possible result *including the null*, before any arm runs — the commit timestamp is the evidence it came first. Where it disagrees with the repair+retune design spec it says so inline (its §8 indexes the five disagreements); those are design corrections, not scope changes. Also the worked example for two CLAUDE.md rules (dormant-term check, pre-registration gate). **AMENDED 2026-07-23/24 — read §9 first:** eleven amendments (A1–A11). **A1–A7 predate any arm; A8 and A9 were made after the A0 gate ran, and A11 after the P4 pilot** — the ones that postdate a result (still before any factorial arm) — A8 resolves that gate (the floor column stays off; the first-path check is referenced to both P and A0), A9 gives the remaining gates effect sizes. **A10 fixes §5's fame-proxy sample before any label was collected** (an S1/S3 overlap that made the sample 29 not 33, and a false claim that S1's labels already existed). Six amendments (A1–A6) on the analyst review's D1–D3 and D5 — the first three had left arms provably incapable of moving; A6 fixes the fame-proxy scoring rule before the owner's one-shot labels are spent. §9's closing table lists what the amendments do **not** discharge (D4, D6, D7, PR-A, PR-B) with success conditions. Its §7 prerequisites are now P1, P4–P8; **P2 is discharged and P3 is withdrawn — do not execute either. P4 RESOLVED (A11): Deezer `nb_fan` failed §5 (2 pre-registered falsifiers); and Wikipedia pageviews fired the coverage falsifier — so the adopted fame proxy is Wikipedia pageviews with an unmatched interior scored at the fame floor (0), the owner's decision replacing §5's owner-labelling fallback, guarded at d15/d20. See A11 and the repair+retune execution log.** |
| `superpowers/plans/2026-07-23-pre-track2-guards.md` | Six structural guards (G1–G6) that had to land between the pre-registration and the Track 2 sweep. **Executed 2026-07-23; do not execute again.** Outcomes in the repair+retune execution log under "Pre-Track-2 guards". |
| `superpowers/specs/2026-07-25-track2f-toll-full-strength-preregistration.md` | **The Track 2F pre-registration**, committed before any arm ran and **EXECUTED 2026-07-25 — do not execute again.** Governs the ceiling-toll dose-response ladder and nothing else. Reuses Track 2's `C1`–`C4` by citation, unchanged; its `TF` identifier series is namespaced and collision-free. Carries a **continuation trigger with its own effect size** (a rebuild decision is not an adoption decision), and **three structural bounds stated before the run** — the toll tests the ceiling's *cheapness* and never its *ordering*, eight of the 24 endpoints are fully saturated so their first hop is unchangeable at any price, and a static toll cannot address F2's depth clause. Result and figures: `builder/analysis/2026-07-25-track2f-toll-ladder/`; narration in the repair+retune execution log's Track 2F entry. |
| `superpowers/specs/2026-07-26-gate2-deploy-and-telemetry-design.md` | **The governing design for Gate 2's AWS deploy and bypass telemetry** (owner-approved 2026-07-26). Identifiers **`DEP-`**. **Nothing implemented yet.** Where it and the roadmap disagree on Gate 2 sequencing, **this governs** — two deviations are named inline: **CI is deliberately out of scope** (the roadmap's own open judgment call 2, taken at the owner's instruction; `DEP-15` records the cost), and S3 graph loading uses an `s3://` URI in the existing `ARTISTPATH_GRAPH` rather than the roadmap's separate `GRAPH_VERSION`. Topology is App Runner + S3 + CloudFront + DynamoDB behind one distribution, which **removes CORS rather than configuring it** (`DEP-8`). **Its §4 verified every roadmap Phase 3 item against source**: three are real defects (`DEP-10` a truncated artifact loads silently because `np.frombuffer` gets no `count`; `DEP-11` sync boto3 blocking the event loop; `DEP-13` no `from == target` guard), **one — invalid `reason` coercion — is already built and needs nothing**, and **`DEP-12` is a defect the roadmap does not list**: cache failures become 500s on the clip endpoint, which `clips.py`'s own docstring forbids. **`DEP-7` is the design choice most worth arguing with** — the telemetry event logs **facts, not computed metrics**, because routing is deterministic so any metric can be derived offline, because choosing production's metric would be a scoring decision taken during the scoring pause, and because the costliest metrics are the ones Phase 1 log §3.8 records as the **worst** predictors of the owner's verdict. §5 states plainly what telemetry **cannot** answer, including value 3's length ceiling (an abandoned journey and a satisfied one are identical from the server). **§11 names three handoff seams at authoring time**; Track A is deliberately AWS-free and leaves the app better even if the deploy never happens (`DEP-18`). **Sets no threshold, adopts nothing, changes no routing weight, no graph and no cost function; the path-quality pause is intact** — §5 argues that building the instrument is not running the experiment. |
| `superpowers/specs/2026-07-25-f1-minimum-stop-design.md` | **The F1 design** (every journey needs at least one stop). Mechanism: run the search as today; if the result is exactly the two chosen artists, run it again with their direct connection forbidden — the existing cost function chooses the detour, so this is not path-quality work. Defines the `stop_rule` wire field (`natural` / `forced` / `adjacent_only`) and cites `findings/2026-07-25-mutual-knn-stranding.md` (`MKS-6`) for why some pairs cannot be given a stop at all. **Implementation has landed** on `f1-min-stop`, per the plan below; still ACTIVE because closure awaits the owner's queued use-the-app check, not because the design is unsettled. |
| `superpowers/specs/2026-08-07-crawl-extension-design.md` | **ACTIVE, governing for the crawl-extension track. SUBSTANTIALLY REVISED 2026-08-08 — read its §10 amendment log first; two of its own claims were falsified by measurement.** Identifiers **`CEX-`**; the independent review is `CEXR-` and the growth probe `CXS-`, all collision-checked. **Owns no figures** — `builder/analysis/2026-08-07-cex-frontier/` and `builder/analysis/2026-08-08-cxs-growth/` own them. Designs the fix for **`ULC-F3`** (crawl resume cannot extend): a `refrontier` command rebuilding the lost frontier from the archive, a root-cause fix that stops discarding discovered neighbours, and a **refusal** so the failure stops reading as success. **`CEX-2` redefines `target_artist_count` from a cap on artists *discovered* to a cap on artists *fetched*** — intended; the old bound provably never fired. **Read §0 and §5 before comparing any candidate to the adopted artifact:** six columns move at once, **no one-knob baseline exists**, and `pop_raw`, `fame_lb_pctl`, `degree_hub_penalty` **and every rescaled similarity** are recomputed over a different population. **Two owner stops:** acceptance rejects on **both** bounds (nodes *and* edges — `acceptance.py:139` forbids a session widening them quietly; `MSW-G3` is the precedent), then adoption. **Step 0 is an owner precondition: snapshot the archive**, since the crawl's append makes today's graph unrebuildable from source. **⚠ Its §3 records a REMOVED remedy, and the removal is the finding:** seeding a named artist does **not** work — falsified by a matched-pair build (zero-node, zero-edge delta) and again at three degree ceilings including the most permissive selectable one. `CEX-T6` is **withdrawn**. `CEX-R1` (growth cannot reach an artist nobody names), `CEX-R3` (one "missing" artist was never missing — a literal-substring **search** defect, out of scope so this track is not credited with fixing it) and `CEX-R5` (**`config.py:17` is stale: the adopted lineage is ALG-B, not ALG-E**) all stand. Adopts nothing, changes no router weight and no cost function. |
| `superpowers/2026-08-08-HANDOFF-cex-design.md` | **⚠ SUPERSEDED 2026-08-08 ON NEXT ACTIONS by `2026-08-08-HANDOFF-cex-crawl.md` (rows below)** — which executed the plan it handed over. **⚠ One claim it carries is now FALSE rather than merely superseded: it flags `config.py:17` as stale in the tree, and plan Task 7 fixed it.** Every "must not be reverted" claim it names still stands in full. *(Original role: **ACTIVE — the CURRENT handoff.** Supersedes `2026-08-07-HANDOFF-bypass-tray-ux.md` **on next actions only**; that document's own record of the bypass-tray track is untouched and remains accurate. A **seam** handoff: design and plan committed, nothing in flight, no port listening. **The successor executes the plan cold, which is the intended shape.** Names the claims an editor must not revert — **seeding a named artist does not work** (falsified twice, `CEX-T6` withdrawn), **growth thickens rather than thins the obscure tail**, the `CXS-` cohort is frozen deliberately, `refrontier` must union rather than replace, and the archive snapshot is the owner's precondition. Flags `config.py:17` as **stale in the tree right now** (ALG-E vs the adopted ALG-B), fixed only at plan Task 7. Carries the reusable operational lesson: **`finally` does not survive SIGTERM**, so a probe against a shared artifact should not write to it at all.)* |
| `superpowers/2026-08-08-cex-design-execution-log.md` | **RETAINED EXECUTION LOG** for the `CEX-` design and plan. **Owns no figures** (the two `builder/analysis/` directories do). Its **§2 is the reason to read it**: two claims the authoring session made were falsified by measurement — that seeding a named artist would work (the argument used half the cap algorithm) and that growth would crowd the hubs and thin the obscure tail (it runs backwards). Both errors were confident inferences from partial mechanism, and neither was caught by reasoning harder. §1 records the cheapest-experiment payout that dissolved half the stated motivation before a plan existed; §4 what was decided against and why, including declining to build the uncapped case because it could not change a decision; §5 the four review findings that changed the design rather than the prose; §8 the measurements with no other home — **that file mtimes recover the true crawl order, which is what makes the `CXS-` subsets real crawl moments rather than samples** (the span and rate are owned by the `cxs-growth` README), and a build-time spread wide enough that any single timing is a bad basis for a timeout. |
| `superpowers/2026-08-08-HANDOFF-cex-crawl.md` | **⚠ SUPERSEDED 2026-08-09 ON NEXT ACTIONS by `2026-08-09-HANDOFF-cex-task11.md` (row below): Task 11 is now COMPLETE, so this note's "Start here" describes finished work. Its "claims that must not be reverted" list is NOT superseded and still stands in full.** *(Original role:)* **ACTIVE — the CURRENT handoff.** Supersedes `2026-08-08-HANDOFF-cex-design.md` **on next actions**. A **seam** handoff at the plan's own seam, after Task 10: Tasks 1–10 complete and pushed, the ALG-B archive extended, nothing in flight, every port free. **Names Task 11 as the next work and its owner stop** — the build will be rejected on BOTH acceptance bounds, and that rejection is the designed outcome rather than a failure to fix. Carries the claims an editor must not revert, of which three are the ones most likely to be "fixed" back: **`exhausted: false` in the checkpoint is CORRECT** (the crawl stopped on the fetch cap with a non-empty queue), **`grt_run.py` refuses to run deliberately** (its era pin is a refusal because the old behaviour was deleted rather than defaulted), and **`calibrate.py`'s `_replay` is deliberately left on the OLD stopping rule**. Records that `ULC-F3` is **discharged** and that `config.py:17` is **fixed**. Operational lesson: **stopping a background `npm run dev` task does not free the port** — the wrapper dies and Vite's child keeps listening, so sweep by listener, never by task list. |
| `superpowers/2026-08-08-cex-crawl-execution-log.md` | **RETAINED EXECUTION LOG** for executing `CEX-` Tasks 1–10. **Owns no figures** (`builder/analysis/2026-08-08-cex-g1/` does). Its **§3 and §4 are the reason to read it.** §3 records `CEX-G2`'s finding: the test suite stayed **green** through the `--target` semantics change, so a passing suite could not discharge that gate — and the two frozen probes it caught needed **different pins**, one a refusal (no config value restores a behaviour deleted from a loop body) and one a deliberate freeze, because **a caller can depend on a rule without naming any identifier the rule uses**. §4 records two tests in this session's own work that **passed while testing nothing**, both caught by running them against the old code first: a fame-key test that could not distinguish a scoped walk from a naive one, and an `elementFromPoint` test that could never observe the defect it was written for because the element it watched is `pointer-events-none`. §2 records three defects in the plan itself, one of which could not run at all. |
| `superpowers/plans/2026-09-05-lux-4-links-and-info-card.md` | **⚠ FULLY EXECUTED 2026-09-08 — all eleven tasks ran, `L4-T1`–`L4-T11`, across PR #105 (the artifact) and PR #112 (the wire and the card). Nothing in it is left to do**, and its own role line said "UNSTARTED; no task has run" through both sessions that executed it, until corrected here. ⚠ **The plan was wrong about the repository FIVE times** — four tabulated at [`superpowers/2026-09-06-lux-4-execution-log.md`](superpowers/2026-09-06-lux-4-execution-log.md) §2, and the fifth at its §8: **`L4-T10` step 6 claims the API layer already maps `snake_case` at the boundary and it does not**, which would have left `spotifyId` `undefined` at runtime, rendering *identically to a genuine absence*. **Three shipped interfaces differ deliberately from what its tasks specify** (§10 there tabulates them), so **read the log before trusting any task text**. *(Original role:)* **ACTIVE — the implementation plan for `LUX-4`, the last of the four launch features.** Identifiers **`L4-T1`–`L4-T11`** (tasks) and **`L4-D1`–`L4-D3`** (decisions it takes), collision-checked across every ref 2026-09-05. Argues from `specs/2026-09-03-launch-ux-scope.md` §4, which governs; executors read both. **Owns no figures** — `../builder/analysis/2026-09-05-lux-4-rebuild/` will own the rebuild's. **Written only after `LUX-E1` returned byte-identical**, so it plans a metadata change and not a graph adoption; if that ever looks wrong, re-read the gate rather than the plan. **⚠ `L4-T1` STOPS FOR AN OWNER DECISION and blocks everything else**: `check_acceptance` runs before `serialise` and its bounds were recalibrated for the reverted 117k population, so a correct rebuild is refused — recalibrate the bounds back (recommended) or wire the `--criteria` hook the CLI lacks. **Names two handoff seams at authoring time** (after `L4-T7`, the verified artifact; after `L4-T9`, the frozen wire contract) because 11 tasks is past the long-context threshold. **`L4-T7` carries a factor table with a CONTROL BUILD** — the shipping artifact's sha must differ (keys were added), so the sha cannot be the check; the control proves `L4-T1`–`L4-T6` reach no part of the graph, and `verify.py` compares every other field. **Three decisions flagged for the owner to overrule:** two maps rather than one (`L4-D1`), ids rather than URLs (`L4-D2`), and render-only-what-is-present with no placeholder rows (`L4-D3`). **Records that `LUX-E2` is OWED and BLOCKED** on the damaged `TAS-` sample, and why `L4-D3` is safe to take without it: the artifact carries the data either way and only the rendering is in question. |
| `superpowers/plans/2026-09-04-launch-ux-1-3.md` | **⚠ ALL SEVEN TASKS EXECUTED and reviewed clean; the branch's final whole-branch fix wave landed on top of it — see `2026-09-04-lux-1-3-execution-log.md` below for that record.** The plan's own Handoff section carries two in-place corrections (`PF-3`, added at the fix wave, on top of the existing `PF-1`/`PF-2`) — read them before trusting the plan's test snippets or its "two independent chains" claim. **The implementation plan for `LUX-1`, `LUX-2` and `LUX-3` only.** *(Original role:)* Identifiers **`LUX-T1`-`LUX-T7`** (tasks) and **`LUX-D1`/`LUX-D2`** (two decisions it takes), collision-checked across every ref 2026-09-04. Argues from `superpowers/specs/2026-09-03-launch-ux-scope.md`, which governs; executors read both. **Owns no figures** - `LUX-T7` writes the `LUX-E4` results README, which owns those. Seven tasks in **what the plan claimed were** two independent chains sharing no file — **`PF-1` found that FALSE**: `LUX-T6` and `LUX-T1` both edit `ArtistCard.tsx`, `JourneyList.tsx` and `ArtistCard.test.tsx`, so all seven tasks ran strictly in numeric order instead — `T1`->`T2`->`T3` (one bypass control, then the skipped-artists panel, then those artists on the path response) and `T4`->`T5`->`T6` (candidate lists in the clip cache and resolvers, then the endpoint's index and count, then the 'try another track' control), with `T7` running `LUX-E4` alongside. **Every code claim in it was grepped against the tree on 2026-09-04**, including the three the scope document rests on: all three clip resolvers fetch `clip_search_limit` and keep only the first row, `floor_relax_known` exceeds `floor_relax_dislike` (values in `ApiConfig`, cited never restated), and the fame ramp multiplies the `known` count alone. **Two decisions it takes rather than inherits, both flagged for the owner to overrule:** `LUX-D1` collapses the two-option tray to a single direct control, since a disclosure that reveals one button is a wasted press - the smaller diff that keeps the tray is written out as an alternative; and `LUX-D2` settles the defect spec section 2 says must be decided, not inherited - `_to_exclusions()` silently drops an MBID the graph does not have, so a shared link with a stale id builds a path as if that press never happened. It is **reported** now (`PathResponse.unresolved`) rather than 404-ing the request or leaving the panel to draw a row that lies. **`LUX-4` is deliberately NOT in this plan and must not be started from it** - spec section 6 names a handoff seam before it and it is gated on `LUX-E1`. **`LUX-E4` gates `LUX-3`'s WORTH, not its correctness**, so `T6` ships regardless of its result. **⚠ `LUX-E4` IS CLOSED as of 2026-09-05 (owner) and must not be re-run** — it ran, its read was **UNDEFINED and never a pass**, and its *threshold* was then retired as mis-specified: `LUX-3`'s control simply does not render when there is no second track, so no result could have changed whether it ships. The scope document's §5 governs; do not restore "a re-prioritisation trigger that is the owner's" from this row. |
| `superpowers/plans/2026-08-10-cxa-graph-adoption.md` | **COMPLETE 2026-08-10 — all nine tasks ran, both owner stops were taken, and the graph was deployed. ⚠ IT WAS REVERTED ON 2026-09-01** when the owner's queued test fired the revert criterion this plan's §0 records; see `../2026-09-01-cxr-revert-execution-log.md`. The plan executed correctly and the artifact it adopted is the one that lost — **do not read this row as a defect in the plan.** Outcome and defects in `../2026-08-10-cxa-adoption-execution-log.md`; **three of this plan's own claims did not survive execution** (its §3), so read the log before trusting a Task description here. *(Original role: ACTIVE — the operational document for adopting the extended 117k graph (`CXA-`, collision-checked across every ref 2026-08-10).)* Nine tasks, **strictly sequential — no task is independent of its predecessor**, so nothing here fans out. **Two OWNER STOPS: `CXA-S1`** (the acceptance bound values, his per the pre-registration §6 and the `MSW-G3` precedent) and **`CXA-S2`** (go/no-go before production). **Handoff seam after Task 3**, where the artifact is proven on the shipped path and nothing is deployed. **⚠ Its Task 2 carries the trap most likely to be got wrong:** the entry to repoint is `CANDIDATE_ALGORITHM`, not `PRODUCTION_ALGORITHM`, because ALG-B is the adopted lineage bound to the constant named *candidate* — `SEL-R1`–`R4`, deferred and live. **⚠ Task 6 exists because `MSW-` Task 12 had no image-build step** and would have shipped a new map under old code. **`CXA-G1` is the load-bearing gate:** a `cmd_build` with no override must reproduce `graph-cex-117k.bin` byte-identically, which is what proves Tasks 1 and 2 correct *together*. It records the owner's adoption decision and his revert criterion, both taken 2026-08-10 and **not to be re-litigated**. Evidence: `JFX-` (below); this plan owns no figures. |
| `superpowers/plans/2026-08-08-crawl-extension.md` | **COMPLETE — all 11 tasks executed, Task 11 finishing 2026-08-09 at its owner stop where acceptance rejected on both bounds as designed. ⚠ Its Task 11 is INCOMPLETE AS WRITTEN and the gap is recorded rather than patched into it:** Step 3 omits assembling the drop-list payloads and getting the ALG-B one where the builder reads it, so a session following the steps literally gets `unlistenable_drop.py`'s `ULC-F1` **refusal** — not the acceptance rejection the plan primes it to expect, and easily logged as the designed outcome. Step 5's `CEX-M1` comparison also had **no baseline**, Task 8 being what created the instrument. Both are written up in `2026-08-09-cex-task11-execution-log.md` §2. *(Original role:)* **ACTIVE implementation plan for the `CEX-` track, 11 tasks.** Operational document for `specs/2026-08-07-crawl-extension-design.md`, which governs where the two disagree. Tasks 1–8 are code (`refrontier`; the fetch-bound redefinition; the refusal plus its `exhausted` flag; atomic checkpoint writes; the ALG-E/ALG-B comment fix; `CEX-M1`'s rescale logging). Tasks 9–11 are operational. **Strictly sequential — no task is independent of its predecessor.** **⚠ Task 9 Step 1 is the owner's precondition: snapshot the archive**, because after the crawl appends, today's graph cannot be rebuilt from source. **⚠ Task 4 rewrites an existing test (`tests/test_crawl.py:97-103`) rather than adding one** — that test encodes the pre-`CEX-2` semantics, and rewriting it IS the red step. **Two stops: a handoff seam after Task 10** (the finished crawl is a durable artifact) **and an OWNER STOP at the end of Task 11**, where acceptance rejects on both node and edge bounds and a session must not widen them. **Its self-review names one gap it does NOT close:** `CEXR-6`'s `load_deezer_ids` has no population argument, so every new artist falls back to name-based clip resolution (`BYP-13`) — its own track. |
| `superpowers/2026-08-09-HANDOFF-cex-task11.md` | **SUPERSEDED on next actions by `2026-08-09-HANDOFF-jfx-amendment.md`** — still authoritative for `CEX-` Task 11's internals and for its own must-not-revert list, **every item of which stands in full**. ⚠ **Its "what I would do if continuing" is PARTLY SPENT**: the arms still have not run, but the pre-registration it points at was materially amended (`JFX-AM1`) and the run order changed — read the current handoff's version. Supersedes `2026-08-08-HANDOFF-cex-crawl.md` **on next actions**. A **seam** handoff: the plan's last task is complete, the track stopped at the owner stop it was designed to reach, nothing in flight, every port free, and a new governing document (`specs/2026-08-09-journey-fame-exposure-preregistration.md`) is committed and unrun. **Nothing adopted, no default flipped, no artifact shipped.** Carries the claims an editor must not revert, of which three matter most: **`CEX-M1`'s saturated-edge share is VACUOUS, not "unchanged"**; **`unlistenable_list_path` defaulting to `None` is deliberate and NOT unshipped work**, the default flip being an adoption decision on purpose; and **the regenerated ALG-E drop list is a by-product that must not be shipped** — its population never changed yet 30 artists became drops and 43 stopped being drops in four days, and the sha pin is what prevents that shipping by accident. Its "what I would do if continuing" names the `JFX-` run order and folds `CEX-M1`'s fix into the same artifact. |
| `superpowers/2026-08-09-cex-task11-execution-log.md` | **RETAINED EXECUTION LOG** for `CEX-` Task 11, `SEL-` and the `JFX-` pre-registration. **Owns no figures** (`../builder/analysis/2026-08-09-cex-recensus/README.md` does), except the D6 standing-layer numbers the closeout rule requires to live here — **0 delta in both layers**. **Its §2 is the reason to read it:** three defects in the plan itself, the sharpest being that Task 11 Step 4 could not run as written and would have failed with a **refusal** that the plan primes a reader to log as the designed **rejection**. §3 records why the drop-list fix is a per-invocation override rather than a repointed default — `pipeline.py` refuses only on artists OUTSIDE the censused set, so a larger list on a smaller archive passes **silently**, and the two lists disagree on 62 shared artists from clip drift alone. **That decision paid for itself the same session**: the baseline rebuild needed no flag precisely because the default still pointed at the 75,000-artist list, and had it been repointed the `CEX-M1` comparison would have been circular with nothing failing. §5 records `CEX-M1`'s vacuity and a session error worth keeping — reading `PRODUCTION_ALGORITHM` as "what production serves" with `CEX-R5`'s warning three lines below the constant. §7 records a D6 mis-measurement taken from the wrong working directory. |
| `superpowers/findings/2026-08-09-selector-identity-drift.md` | **ACTIVE — a problem statement awaiting a dedicated maintenance session, written at the owner's instruction as an INPUT to that session rather than as work done in flight. Nothing here is decided, specced or built.** Identifiers **`SEL-`** (collision-checked across every ref). **Owns the reference counts in `SEL-1` and the module table in `SEL-2`.** Records that two constants are named for a role that has since moved — `PRODUCTION_ALGORITHM` is **not** what production serves; `CANDIDATE_ALGORITHM` (ALG-B) is the adopted lineage — and that **the answer is NOT a rename**: 23 of the references are in frozen analysis scripts, the situation the 2026-07-23 `hubfrac` rename already paid for in read-only aliases still in the tree. Also that population identity is implemented in **one of three** sibling drop modules, and that the condition making the other two safe (one algorithm meaning one crawl) **expired** when this crawl extended ALG-B from 75,000 to 117,302. **`SEL-3` states plainly that no live defect is claimed** — the two unhardened modules can only under-apply and `unlistenable` is a superset — and asks the maintenance session to re-verify that rather than inherit it. Four recommendations, three with triggers; only `R1` (an additive `ADOPTED_ALGORITHM` pointer) is proposed for near-term action. |
| `superpowers/specs/2026-08-09-journey-fame-exposure-preregistration.md` | **ACTIVE pre-registration, COMMITTED BEFORE ANY ARM RAN — does the extended map put less famous artists into journeys?** ⚠ **SUBSTANTIALLY AMENDED 2026-08-09 by `JFX-AM1`, still before any arm ran — read the amendment before §2, §3 or §4, all three of which it changes, and it governs where they disagree.** Twelve clauses, of which four change what passes: the fame quantity is now **log-scaled** (`AM1.2`, owner decision — raw counts made a pooled median a single-stratum statistic); **`G1b` is reparameterised as the linear contrast `T = D_B − 0.67·D_A`** with a `D_A > 0` viability clause, because the ratio declares an equal map broken up to **42%** of the time when its denominator is weakly measured (`AM1.5`); **`G1a`'s three steps move to one simultaneous band** and gain an effect size, its false-stop rate being 7.5–8.2% against a nominal 5% (`AM1.6`); and **`C6`/`C7` get the effect sizes their branch triggers lacked** (`AM1.7`). **`AM1.1` WITHDRAWS a proposal to drop the famous–famous stratum** — the `DD-F1` barrier does **not** transfer to fame currency, though it survives in popularity, and the two halves must not be collapsed. **`AM1.3` names the routing instrument, which nothing did**, and records that `victim_key` press-selection makes `G1a` partly mechanical, so **no read may present it as evidence the product works for a user**. `AM1.9` measures the obscurity floor as **spent by press five**; the 67% bar is **unchanged** and the realised press-count equivalence is now reported beside it. **`AM1.12` wrote the §1 build script, which had never existed** despite the present tense. Identifiers **`JFX-`** (collision-checked across every ref; `JFX-AM` separately checked). Answers the owner's four product questions, **every criterion anchored on an existing `REQ-` rather than invented**. **Read §0.1 before §3.** Two confounds bind every read: the `known` ramp is priced in fame **percentile**, ranked against the served artifact's own population, so the two arms use **different rulers** and the ramp *is* the depth mechanism — hence **`JFX-G1` measures whether the extended map delivers a depth gradient, never whether adding artists changed it**; and **`w_floor` is dormant in the baseline for a reason the intervention may remove**, the Track 2 dormant-term pattern, measured as `C6` with every one-knob reading void if it fires. A third confound has a **stated direction**: nulls are a measured absence, never a floor (`FAM-AM1.8`), and excluding them pulls median fame **upward** in whichever arm has more — biasing **against** the extension (`C7`). **`JFX-G1b` is 67%, owner-set**, translated into press counts and anchored on REQ-18; **`JFX-C2`'s threshold was WITHDRAWN, not forgotten**, on the `MSW-V2` report-row precedent. §0 states the arms differ by ~~three~~ **four** things (`AM1.8` moved the re-censused drop payload out of "held constant" — the flag is `True` in both, the **payload is not**, and 32 artists can vanish for a reason that is not the crawl) and **bars any "adding artists caused it" read**. Pair set fixed in two stages with seed 20260809, over-selected so a shortfall cannot license a redraw. **⚠ The arms HAVE run** (2026-08-09/10) — this row said "No arm has run" until 2026-09-01, which had been false since the run. **⚠ QUALIFIED, NOT CORRECTED, 2026-09-01:** the design and the result both stand and **the run was not wrong**, but the extended map this was written to evaluate was **reverted** from production on 2026-09-01, and `JFX-G1b` is a stop-gate that never meant "no worse". **Read the banner before reusing this design for a future map switch** — the fame-ruler repricing that caused the regression is a standing trap these gates were not built to catch. |
| `superpowers/2026-08-09-HANDOFF-jfx-amendment.md` | **SUPERSEDED ON EVERYTHING by `2026-08-10-HANDOFF-jfx-run.md`, 2026-08-10 — the arms it was written to hand over HAVE NOW RUN**, so its "Start here" and its "what I would do if I were continuing" are both spent. **Its five must-not-revert claims stand in full** and are carried forward there. *(Original role: ACTIVE — the CURRENT handoff.)* Supersedes `2026-08-09-HANDOFF-cex-task11.md` **on next actions**. A **seam** handoff, and a deliberate one: the governing document was materially amended, and `CLAUDE.md` names a material mid-flight amendment as a seam in its own right because the successor is meant to read the new document cold. **The degradation tell did NOT fire.** **No arm has run**; nothing adopted, no artifact built, no pair routed. Carries five claims an editor must not revert, of which two matter most: **`DD-F1` is NOT overturned** (it survives in popularity currency and fails to transfer to fame — both halves travel together), and **the famous-to-famous measurement is STRUCTURE, not ROUTING**. Names the routing harness as the next work and records that it **does not exist**, while `jfx_stats.py` is tested and consumer-less by design. |
| `superpowers/2026-08-10-HANDOFF-jfx-run.md` | **SUPERSEDED ON NEXT ACTIONS by `2026-08-10-HANDOFF-cxa-adoption.md`, 2026-08-10 (later) — the `CXA-` plan it hands over IS EXECUTED, COMPLETE and DEPLOYED**, so its "Start here" is spent. **Its seven must-not-revert claims stand in full** and are carried forward there. ⚠ Its **"nothing adopted, no default flipped, nothing deployed"** is now FALSE of the present and true only of what `JFX-` itself did — the tense trap this project has hit before. *(Original role: ACTIVE — the CURRENT handoff.)* Supersedes `2026-08-09-HANDOFF-jfx-amendment.md` **on everything**. A **seam** handoff: the `JFX-` track is complete and its output is committed, so the successor reads a finished result rather than a live understanding. **The degradation tell did NOT fire.** **The arms RAN**; both gate clauses passed and the pre-registered read is §4 read 5. **Nothing adopted, no default flipped, nothing deployed** — the new artifact is gitignored and its manifest says `DO_NOT_DEPLOY: true`, correctly, because it was built against the old bounds. Names `plans/2026-08-10-cxa-graph-adoption.md` as the next work and says to execute it **inline, not with subagents**. Carries seven claims an editor must not revert, of which three matter most: **the shallower gradient is NOT established** (`G1b` tested against the bar, never against parity, and the difference spans zero — and this equally does not establish equality); **the d20 famous-to-famous drift is POST-HOC**, where to look and not a finding; and **`CRE-G1(a)` as originally run covers the six STATIC cost terms only**, having run at k = 0 with the ramp knob at 0.0. ⚠ Also records that **`TEST-QUEUE.md` on this branch LOOKS non-empty** because PR #92's discharge landed on `main` only — reconcile at merge, do not discharge twice. |
| `superpowers/2026-09-06-lux-4-execution-log.md` | **RETAINED EXECUTION LOG for `L4-T1`–`L4-T11` — the WHOLE plan. ACTIVE.** §§1–7 are the first session's, through the artifact; **§§8–13 were appended 2026-09-08** by the second, which finished the plan — the **fifth** plan defect (the frontend did NOT already map `snake_case` artists, so a declared `spotifyId` would have been `undefined` and rendered identically to a genuine absence), the three deliberate deviations from the plan's stated interface, and the truncation defect that only a screenshot of real data found. **Owns no figures** — the three `builder/analysis/2026-09-05-lux*` READMEs do, cited by section. Records: **the owner's rule on whose decision an acceptance-bound change is** (moving a bound to admit a NEW artifact is risk acceptance and his; moving one to re-admit the artifact already in production is bookkeeping and a session's), and that the session got this wrong in **both** directions on the same day; **four defects in the plan itself**, each caught by grepping rather than by reading, with the generalisable one being a factor table invalidated by an earlier task in its own plan; the decisions taken with reasoning, including why `artist_facts` ships as a list of dicts and what would revisit it; **`ULC-F4` and the Deezer-id gap** as work with conditions rather than notes; five corrections to the prior record including the dump's real date and licence; gate outcomes; and the D6 standing-layer figures. |
| `superpowers/2026-09-10-HANDOFF-lbd-task67-build-stage.md` | **ACTIVE — the CURRENT handoff FOR THE `LBD-` TRACK, and the current handoff on next actions overall.** A **SEAM** handoff: Tasks 6–7 finished under `LBD-AM4`, both arms emitted and built over the fixed population, every pre-registered read taken (`LBD-G2` clears at graph level with both controls; `R8`/`R9` do not fire), the threshold curve recorded. Carries the claims that must not be reverted (the override supersedes `LBD-X3` for the fixed population only; `A3` barred; the population is a control, not a rule; the `A0`-vs-served gap is a lineage bundle), what is on disk by checksum, and what is owed — **the next step is the owner's decision** (listen at threshold 0, amend for an intermediate threshold, or stop). Figures owned by `../builder/analysis/2026-09-10-lbd-supply/README.md` and the Task 4 README §6c; the log is `superpowers/2026-09-10-lbd-task67-execution-log.md`. |
| `superpowers/2026-09-10-HANDOFF-lbd-task4-owner-stop.md` | **SUPERSEDED on next actions 2026-09-10 (evening)** by [`superpowers/2026-09-10-HANDOFF-lbd-task67-build-stage.md`](superpowers/2026-09-10-HANDOFF-lbd-task67-build-stage.md) — the owner chose option 1 and the build stage ran to its own stop; its on-disk table and its must-not-revert claims still hold. Was: A **SEAM** handoff: Task 4 finished and the design's owner stop is reached, so nothing below it starts on a session's initiative. **Owns no figures** — `../builder/analysis/2026-09-08-lbd-similarity/README.md` §4–§6 does. Its three load-bearing claims: **`LBD-G1` fired and was overridden by the owner after diagnosis (`LBD-AM3`) — never cite `LBD-C1` as passed**; **`R4` fires** — the listening data is there and the threshold, not the cap, is the lever; and **the band-member class is identified** as uncredited personnel scored by ListenBrainz's deployed job outside the recording credit, the class three probes on our side could not attribute. Carries the on-disk inventory (everything landed under `C:\unsung-fast\`, gitignored, identified by checksum), six must-not-be-reverted claims including the correction of its predecessor's diagnosis, and the owner's decision with the three options tabled. |
| `superpowers/2026-09-08-HANDOFF-lbd-task4-midflight.md` | **SUPERSEDED on next actions 2026-09-10** by [`superpowers/2026-09-10-HANDOFF-lbd-task4-owner-stop.md`](superpowers/2026-09-10-HANDOFF-lbd-task4-owner-stop.md). Its must-not-be-reverted list still stands; **its open-decision section is overtaken** — the bucket-4 failure it read as a heavy tail was the pipeline at the margin of a 6 GB memory limit, and 12 GB cleared every bucket with no code change. *(Was: ACTIVE — the CURRENT handoff FOR THE `LBD-` TRACK.)* ⚠ **Two tracks are live and each has its own current handoff**; the other is [`superpowers/2026-09-08-HANDOFF-unsung-redesign-t5.md`](superpowers/2026-09-08-HANDOFF-unsung-redesign-t5.md) for the `UXR-` redesign *(the `LUX-4` plan finished 2026-09-08 and its handoffs are superseded)*. Neither supersedes the other, and a reader wanting "the" next action wants [`superpowers/NEXT.md`](superpowers/NEXT.md). ⚠ **A MID-FLIGHT handoff — the plan's seam is after Task 4 and Task 4 is NOT finished.** Task 3 (the reimplementation) is complete and verified; **no arm has run on real data, `LBD-C1` is unmeasured, and the owner stop was not reached.** **Owns no figures** — `../builder/analysis/2026-09-08-lbd-similarity/README.md` does. Its most load-bearing content: the **53.9 GB stage-0 intermediate on `C:` that must not be rebuilt**; a named, ordered recommendation for the one unsolved problem (the pair pass is heavy-tailed and bucket 4 exhausts memory); **five claims that must not be reverted**, two of which are DuckDB-versus-Spark divergences that would corrupt fidelity silently; and a full enumeration of numbers computed, options rejected, and what is in flight (nothing is). |
| `superpowers/2026-09-07-HANDOFF-lbd-preregistration.md` | **SUPERSEDED on next actions 2026-09-08** by [`superpowers/2026-09-08-HANDOFF-lbd-task4-midflight.md`](superpowers/2026-09-08-HANDOFF-lbd-task4-midflight.md); its account of Tasks 1–2 and its must-not-be-reverted list still stand. *(Was: the CURRENT handoff for the `LBD-` track.)* ⚠ **Two tracks are live and each has its own current handoff**; the other is [`superpowers/2026-09-06-HANDOFF-lux-4-artifact.md`](superpowers/2026-09-06-HANDOFF-lux-4-artifact.md) for `LUX-4`. **Neither supersedes the other** and neither blocks the other; [`superpowers/NEXT.md`](superpowers/NEXT.md) sequences both and is what a reader wanting "the" next action should open. A **SEAM** handoff, and the seam is that the pre-registration is a **material mid-flight amendment to two committed governing documents**. `LBD-` Tasks 1 and 2 are done; **next task is Task 3**, the reimplementation. **Owns no figures** — `../builder/analysis/2026-09-07-lbd-inputs/README.md` owns Task 1's. ⚠ Its most load-bearing warnings: **the full dump's published `.tar` is absent from disk so its checksum was never verified** and no claim may rest on byte-identity to the published dump; **the pair-table supply read is primary and the graph-level one is barred** from supporting the track's central risk; and **a `DCF-` session was working the degree ceiling in parallel**, which the next session must check against `LBD-X1` before Task 3. Records that **Snyk did not run** (expired credentials, the owner's hands) and that the scripts were hand-reviewed instead — reported as unscanned, never as clean. |
| `superpowers/2026-09-08-HANDOFF-lux-4-wire-and-card.md` | **⚠ SUPERSEDED ON NEXT ACTIONS by [`superpowers/2026-09-08-HANDOFF-unsung-redesign-t5.md`](superpowers/2026-09-08-HANDOFF-unsung-redesign-t5.md), 2026-09-08 — NOT the current handoff.** Its must-not-revert list for `LUX-4` still binds in full. *(Was:)* the current handoff for the `LUX-4` track. ⚠ **TWO tracks are live and each has its own current handoff**; the other is [`superpowers/2026-09-07-HANDOFF-lbd-preregistration.md`](superpowers/2026-09-07-HANDOFF-lbd-preregistration.md) for `LBD-`. Neither supersedes the other, neither blocks the other, and **a reader who needs "the" next action wants [`superpowers/NEXT.md`](superpowers/NEXT.md), which sequences both.** A **SEAM** handoff at the plan's last task; the degradation tell did not fire. **Supersedes `2026-09-06-HANDOFF-lux-4-artifact.md` on EVERYTHING**, status included, because that note's next action is spent. **`LUX-4` is FINISHED — `L4-T1`–`L4-T11`, across PR #105 and PR #112 — and nothing in it is left to build.** ⚠ Its most load-bearing warnings: **the plan was wrong about the repository a FIFTH time**, and that one would have SHIPPED — it claimed the frontend already mapped `snake_case` artists at the API boundary, and it did not, so a declared `spotifyId` would have been `undefined` at runtime and rendered *identically to a genuine absence*; **`ArtistOut` reaches the wire in FOUR positions, not three**, all from one construction site; **the facts line WRAPS and `truncate` must not be restored** (under it the life span was cut on most real artists at 390px, and the e2e assertion pinning it was shown to go red before being trusted); and the accessors are `spotify_id_of`/`apple_id_of`/`facts_of` returning `None`, both deliberate deviations from the plan. Records that **`LUX-E2` is still BLOCKED and was not touched**, that **the Deezer id gap deferral has NOT come due** (its condition is the first rebuild *after* `LUX-4` merges), and that **the deploy is NOT API-only** — the artifact changes, so a graph-variable `cdk diff` is expected rather than being the `DEP-34` warning firing. Its "what I know that is not in the durable record" is **non-empty and deliberately so**: `NEXT.md` got a **surgical edit rather than the wholesale rewrite `closeout` A2-next prescribes**, because a concurrent `LBD-` worktree would conflict on a wholesale rewrite — **so the outgoing-block demotion to `NEXT-ARCHIVE.md` is now one closeout in arrears** and the next session closing out without a concurrent track owes it. |
| `superpowers/2026-09-06-HANDOFF-lux-4-artifact.md` | **⚠ SUPERSEDED ON EVERYTHING by [`superpowers/2026-09-08-HANDOFF-lux-4-wire-and-card.md`](superpowers/2026-09-08-HANDOFF-lux-4-wire-and-card.md), 2026-09-08 — NOT the current handoff.** `L4-T8`–`L4-T11` are done and the plan is finished, so its "Start here" is spent and it is superseded on **status as well as next actions**. **Everything it records about `L4-T1`–`L4-T7` still stands**, artifact sha included — with one count moved forward: it says the plan was wrong about the repository **four** times, and a **fifth** was found during `L4-T10`. *(Original role:)* **ACTIVE — the CURRENT handoff FOR THE `LUX-4` TRACK.** ⚠ **TWO tracks are live and each has its own current handoff**; the other is [`superpowers/2026-09-07-HANDOFF-lbd-preregistration.md`](superpowers/2026-09-07-HANDOFF-lbd-preregistration.md) for `LBD-`. Neither supersedes the other, they block nothing of each other's, and **a reader who needs "the" next action wants [`superpowers/NEXT.md`](superpowers/NEXT.md), which sequences both.** A SEAM handoff at `L4-T7`, the seam the plan chose at authoring time. `LUX-4` is done through the artifact: built, verified two independent ways, `LUX-E5` passed. Next task is `L4-T8`. ⚠ Its most load-bearing warning: **the plan was wrong about the repository four times**, each caught by grepping rather than by careful reading, so **the remaining tasks are unverified**. Supersedes the 2026-09-05 handoff on next actions; owns no status (`NEXT.md` does). |
| `superpowers/2026-09-07-dcf-degree-ceiling-execution-log.md` | **RETAINED EXECUTION LOG for the `DCF-` probe. ACTIVE. Owns no figures** — the `builder/analysis/2026-09-07-degree-ceiling-falsifier/README.md` does. **No separate handoff note exists and that is deliberate**: the work is complete, nothing is in flight, and `2026-09-06-HANDOFF-lux-4-artifact.md` remains the current handoff for next actions. Records the four documents whose "mostly ours" characterisation was corrected and **why the notes must not be reverted** (the arithmetic was always right; only the characterisation was wrong, so a future editor will find no contradiction); a correction to this session's own output, where a mechanism was asserted before the denominators existed and the measurement refuted it; two instruction defects (a `tee` masking a traceback as exit 0; an uninterpretable crosstab withheld rather than retracted); build timings; and ⚠ **that `doc-auditor` did NOT complete — B1 is partially run, with a success condition.** |
| `superpowers/2026-09-07-dfa-degree-floor-execution-log.md` | **RETAINED EXECUTION LOG for the `DFA-` probe and the `LBD-AM1` amendment. ACTIVE. Owns no figures** — the three `builder/analysis/` READMEs it names do. **No separate handoff note exists and that is deliberate**: the work is complete, nothing is in flight, and it creates no next action, so `2026-09-06-HANDOFF-lux-4-artifact.md` and `2026-09-07-HANDOFF-lbd-preregistration.md` remain current for their own tracks. Records the ten decisions and their reasoning, **five defects in this session's own output and in its instructions** (a gate clause encoding a prediction rather than an instrument property; a docstring wrong about what a Track B column measures; two conflated windows; a record assumed to hold artist IDs that held counts; and an instruction framing broader than the probes support), the **instrument gate's FAILURE and its dated correction**, the two forward corrections and **why neither must be reverted**, build timings, and the `D6` figures. |
| `superpowers/2026-09-08-lbd-task34-execution-log.md` | **RETAINED EXECUTION LOG for `LBD-` Tasks 3 and 4. ACTIVE. Owns no figures** — `builder/analysis/2026-09-08-lbd-similarity/README.md` does. **Tasks 3 and 4 are both COMPLETE (2026-09-10) and the owner stop is reached** — see `2026-09-08-HANDOFF-lbd-task4-midflight.md`, superseded by `2026-09-10-HANDOFF-lbd-task4-owner-stop.md`, the current handoff for this track. The 2026-09-09/10 entries record: the bucket-4 diagnosis corrected by measurement (the memory limit, not a heavy tail); `LBD-G1` firing, its decomposition, the ~3× score ratio, the `created`-dated rerun (`C1-DIAG-1`) with its three readings committed before it ran, and the owner's override (`LBD-AM3`); the band-member class; and the `LBD-C2a` read (`R4`). Records **thirteen declared deviations** from ListenBrainz's SQL, of which three were unforeseen and two would have corrupted the fidelity run silently (DuckDB's cast rounds where Spark's truncates; DuckDB's `count_if` returns NULL where Spark's returns 0, deleting every user's first listen from every pair). Also records **six distinct scale obstacles none of the governing documents anticipated** — the dump is on a spinning disk, DuckDB materialises window inputs, the join build side did not fit, a partitioned write spilled 111 GB for a 4 GB output, the harness kills on reclaimable cache, and the work is heavy-tailed across users — with the measurements that settled each; **five wrong diagnoses of the same OOM and why measuring sooner was the cheaper path**; `LBDR-F4` discharged with a number (0.001 %); and **four instances of a check reporting green while doing nothing.** |
| `builder/analysis/2026-09-08-lbd-similarity/README.md` *(outside `docs/`)* | **ACTIVE — FIGURES OWNER for `LBD-` Tasks 3 and 4: the transcription's verification, `LBD-C3` (§4), `LBD-C1` and its diagnosis (§5–§5c), and `LBD-C2a` (§6).** Cite it by section; nothing in `docs/` restates a number from it. ⚠ **§5 records `LBD-G1` FIRING** and §5c the dated-corpus test that explains it; §6 records `R4` firing under `LBD-AM3`. Its §5a's per-artist table is the corrected one — the quantiles `lbd_reads.py --mode c1` prints are wrong (NaN in `sorted()`) and its gate statistic is not. Scripts alongside are frozen research code, `--created-before` is diagnostic only, and everything they produced sits under `C:\unsung-fast\`, gitignored and identified only by the checksums the README carries. ⚠ **§6c ADDED 2026-09-10 under `LBD-AM4-5`: the descriptive threshold curve from `T` — no gate, no effect size, decides nothing; it is context for the owner's threshold choice and must not be cited as one.** |
| `superpowers/2026-09-10-lbd-task67-execution-log.md` | **RETAINED EXECUTION LOG for the `LBD-` build stage (plan Tasks 6–7 under `LBD-AM4`). ACTIVE. Owns no figures** — `builder/analysis/2026-09-10-lbd-supply/README.md` and the Task 4 README's §6c do. Continues the Tasks 3–4 log from the owner stop. Records the `LBD-AM4` decisions (population fixed to the `CXA` node set; `A0` then `A2`; `A3` barred; the drop lists measured inert), a DuckDB access-violation trap (two DuckDB processes beside each other on this machine), the emitter's three memory shapes and the determinism check, and the reading of `LBD-C2b`/`R8`/`R9`. Branch `lbd-build`, PR #115. |
| `superpowers/2026-09-10-lbd-listen-prep-execution-log.md` | **RETAINED EXECUTION LOG for the `LBD-AM5` session — the amendment, the served-population builds, and the `LBL-` listen materials. ACTIVE. Owns no figures.** Records why the pairs are NOT `GBL-AM1`'s (a remembered journey would identify a side) and that the draw rule was committed before it ran; why the drop list departs from `LBD-AM4`'s file (the `20260809` payload drops 31 served artists); the protocol decisions taken against `GBL-` results §6 and `CAU-` §2.3; and that `LBD-D6` (`LBD-A4` before a further arm) is surfaced to the owner, not waived. Branch `lbd-listen`. |
| `superpowers/2026-09-10-HANDOFF-lbd-listen-prep.md` | **SUPERSEDED on next actions 2026-09-11 by `superpowers/2026-09-11-HANDOFF-lbl-listen1-read.md`** — the listen it prepared has since been run, unblinded and read. ⚠ **Its "the listen has NOT been run — no journey exists on any map" is now FALSE** and is left as written, because it was true when written. **The rest stands and is still the only record of what was built and why**: its "Claims that must NOT be reverted" all still bind, and **`LBD-D6` is still unruled**. *(Original role:)* the CURRENT handoff for the `LBD-` track only, a seam at the owner's stop: `LBD-AM5` committed, `LBD-A0V` and `LBD-A5V` built and serialised, the `LBL-` listen-1 harness ready. Supersedes `2026-09-10-HANDOFF-lbd-task67-build-stage.md` on `LBD-` next actions. Owns no figures. |
| `superpowers/2026-09-11-HANDOFF-lbl-listen1-read.md` | **ACTIVE — the CURRENT handoff. Nothing supersedes it.** A seam: the `LBL-` listen 1 was run, unblinded, read and written up, and nothing is in flight. Supersedes `2026-09-10-HANDOFF-lbd-listen-prep.md` on next actions. **Carries the owner's ruling of 2026-09-11 that no further blind listen starts for now** and that he is taking the route decision to a separate session — which is why the four routes are set out in `superpowers/NEXT.md` rather than left in a conversation. Names what must not be reverted (the run-once rule on the verdict; that his closing note changed no number; the owner-notes provenance caveat; `LBD-X4`/`LBD-X5`; `V` as a control). Owns no figures. |
| `superpowers/2026-09-11-lbl-listen1-read-execution-log.md` | **RETAINED EXECUTION LOG for the `LBL-` listen-1 run and its read. ACTIVE. Owns no figures.** Covers two sessions — the mechanics-only runner (which deliberately left no log of its own) and the write-up. Records what the two-commit order does and does not establish about the run; that the tally was recomputed by hand and `lbl_unblind.py` checked clause by clause against `LBD-AM5-5` before the result was opened; why no re-tally was done on the owner's "slight"/"strong" wording; the provenance decision on his closing note; the three `docs/README.md` rows this work made due; and **§5, one dated `DLS-T1` observation — a spec was opened repeatedly and the path-scoped rule did not load, confounded by this session reading through Bash rather than the `Read` tool. That is NOT the `DLS-T1` read.** §6 carries the `closeout` D6 figures. |
| `../builder/analysis/2026-09-10-lbd-served-population/README.md` | **ACTIVE — FIGURES OWNER for `LBD-AM5`'s builds**: `LBD-A5`'s derived table, the two archives over the served population `V`, the maps' counts, retention and pre-existing-set degree against the served map, the three fame checks, and the two artifacts' checksums. ⚠ **Two edge-count units** (connections vs CSR entries, a factor of two) — every figure there is labelled; never compare across them. Says nothing about journeys. |
| `../builder/analysis/2026-09-10-lbd-blind-listen/` | **ACTIVE — the `LBL-` blind-listen harness** (pairs, generation with its gates, clips, page, unblind, tests) and `RUNNER-BRIEF.md`. Governed by `LBD-AM5-5`. **A runner session reads only the brief.** `lbl_pairs.json` is the pinned pair set for both listens; listen 2's materials are deliberately not prepared. **Listen 1 was run 2026-09-11**: `lbl_listen1_verdicts.json` (the owner's saved picks and notes), `lbl_listen1_page_data.json` (the stimulus as presented) and `lbl_listen1_result.json` (the unblinded mapping and mechanical tally) and `lbl_listen1_owner_notes.md` (his closing impression, verbatim) are the raw record, and the read is `findings/2026-09-11-lbl-listen1-results.md` — **the figures owner; nothing here interprets them.** ⚠ **The owner notes were given to the runner session directly and committed a day later by the write-up session**, so their "written before he read the read" rests on his account and **not** on a git timestamp — the file states this itself; do not cite it as timestamped. |
| `builder/analysis/2026-09-10-lbd-supply/README.md` *(outside `docs/`)* | **ACTIVE — FIGURES OWNER for the `LBD-` build stage under `LBD-AM4`: the emitted `A0`/`A2` archives' identity, `LBD-M1`, `LBD-C2b` with both controls, `R8`, `R9`, and the differences against the ceiling probe's §4 bridge control (= `CXR-P2`'s population).** Cite by section; nothing in `docs/` restates a number from it. Built over the FIXED population `P` (the `CXA` node set), so each arm differs from the bridge control in the data alone — **read its §4 warning: the `A0`-vs-control difference bundles corpus age, `filter_True`, mapping and tie-break, none separated.** `LBD-G2` clears at graph level for `A2` with both controls; `R8` and `R9` do not fire; the residual stratum moves most. **Read its §5 — no path was built, no routing read, no blind listen; `P` is an experimental control and not a population rule.** Scripts beside it are frozen research code; archives live under `C:\unsung-fast\lbd-archives\`, gitignored, identified by the checksums it carries. |
| `superpowers/2026-09-05-HANDOFF-lux-e1-and-lux-4-plan.md` | **SUPERSEDED on next actions — NOT the current handoff.** Succeeded by [`2026-09-06-HANDOFF-lux-4-artifact.md`](superpowers/2026-09-06-HANDOFF-lux-4-artifact.md), which is current. Its "must not revert" list still binds **with one exception: "the rebuild is ~40 s" is OVERTURNED** — two rebuilds on 2026-09-06 took far longer on the same archive; the figure is owned by [`builder/analysis/2026-09-05-lux-4-rebuild/README.md`](../builder/analysis/2026-09-05-lux-4-rebuild/README.md) §5. Supersedes `2026-09-05-HANDOFF-doc-layer.md` on **next actions only**. A **SEAM** handoff; the degradation tell did not fire. **No app code changed** — a maintenance session that also ran one offline eval, so its closeout was the maintenance tier with `A2` added because it produced a seam that tier assumes it will not. **Names the one assigned action and it is the owner's: `L4-T1`, the acceptance-bounds decision, which blocks every task in the `LUX-4` plan.** Carries seven claims an editor must not revert, of which three matter most: **`LUX-E4`'s read is UNDEFINED and only its *threshold* was retired**; **its empty denominator is `DD-F1` and must not be re-opened as a fresh path-quality question**; and ~~**the rebuild is ~40 s**~~ (**overturned 2026-09-06 — see this row's head**). Records **three `CXA-` leftovers, not one**. Its "what I know that is not in the durable record" is **non-empty and deliberately so**: the plan's code claims rest on this session's own grepping rather than a second reader's, because the auditor read the plan only at task boundaries and said so. Also carries **the owner-requested RCA of why the `CXR-` revert was incomplete** — adoption changed two sides, the revert touched one, and its `/health` verification structurally could not see build-input drift; the adoption plan had a revert criterion but no revert procedure, and had itself warned that *"a wholesale revert is self-consistent and passes"*. |
| `superpowers/2026-09-05-HANDOFF-doc-layer.md` | **⚠ SUPERSEDED ON NEXT ACTIONS, transitively — NOT the current handoff.** *(Corrected 2026-09-07: this row read "ACTIVE — the CURRENT handoff" while two later handoffs had taken over the chain.)* It is superseded by `2026-09-05-HANDOFF-lux-e1-and-lux-4-plan.md`, which is itself superseded by `2026-09-06-HANDOFF-lux-4-artifact.md`. Its own content below still stands on everything except next actions. Supersedes `2026-09-04-HANDOFF-lux-1-3.md` on **next actions only**. A **SEAM** handoff from a **maintenance** session: **no app code changed**, so there is no execution log and the reasoning is in the commit messages. Records the `NEXT.md` and `TEST-QUEUE.md` restructures and the seven claims an editor must not revert — the sharpest being that **`NEXT.md` owns no git state** (re-adding merge status restores a false alarm that fires every closeout), that **both archives are frozen**, and that **`BYP-13`'s clip fix is LIVE, not inert**. Also owns the one measurement with no other home: `docs/README.md` — **this file** — is 336 KB across 275 rows at a median 1,180 characters, and a probe found its rows restate the documents they describe rather than routing to them. That is deferred with a condition in `NEXT.md`, on the owner's explicit decision. |
| `superpowers/2026-09-04-HANDOFF-lux-1-3.md` | **⚠ SUPERSEDED ON NEXT ACTIONS by `2026-09-05-HANDOFF-doc-layer.md`, 2026-09-05** — a maintenance session that changed no app code. **Everything this note records about `LUX-1..3` still stands in full**, including the zero-diff promise that keeps every `?dislike=` link resolving, the `REQ-` retirements, `LUX-E4`'s **undefined** threshold, and the damaged `TAS-` sample. Only its ranking as the current handoff has moved. Supersedes `2026-09-01-HANDOFF-cxr-revert.md` on **next actions only**; that note stays authoritative for the `CXR-` revert's internals and its must-not-revert list. A **SEAM** handoff — all seven `LUX-T` tasks executed, each individually reviewed, then a whole-branch review, one batched fix wave and a scoped re-review (16/16 addressed). **The degradation tell did NOT fire.** **Nothing is deployed**: branch `launch-ux-1-3`, PR #101 open against `main`, and **#101 supersedes PR #100 rather than sitting beside it** — closing #100 is the owner's. Carries six claims an editor must not revert, of which three matter most: **the `dislike` mechanism, wire contract and `?dislike=` URL parameter all still exist and are deliberately unreachable from the UI** (removing them breaks every shared link — `exclusions.ts` and `pathfinding.py` have **zero diff** across the branch); **`REQ-30`/`31`/`32` are RETIRED, not overturned**; and **`LUX-E4`'s threshold is UNDEFINED, not passed**, its denominator having been empty. Also records that the committed `TAS-` pair sample is **damaged on the served map** and that `LUX-E2` reuses it. **⚠ FORWARD CORRECTION 2026-09-05: this note's "what to do about `LUX-E4`'s unreadable threshold" is NO LONGER AN OPEN OWNER DECISION.** `LUX-E4` is **closed** and its threshold **retired as mis-specified** — `LUX-3`'s control does not render when there is no second track, so no result could have changed whether it ships. The document is frozen and its **UNDEFINED read is still correct**; only the open-decision status has moved. The damaged-sample half survives the closure and now blocks **`LUX-E2`**, where the warning has been carried. See `NEXT.md`'s top block and the scope document's §5. Its "what I know that is not in the durable record" is **empty and checked rather than asserted**, with one judgement recorded there deliberately: **the single bypass control's wording was never put to the owner**, though the scope document says wording is his. |
| `superpowers/2026-09-01-HANDOFF-cxr-revert.md` | **⚠ SUPERSEDED ON NEXT ACTIONS by `2026-09-04-HANDOFF-lux-1-3.md`, 2026-09-04** — the `LUX-1`/`LUX-2`/`LUX-3` work is built and in review, so this note's "Start here" is spent. **It remains authoritative for the `CXR-` revert's internals and for every must-not-revert claim it carries**, all of which still stand; and its queued confirmation test was **discharged 2026-09-01 (later)** when the owner ran it and found nothing. *(Original role:)* **ACTIVE — the CURRENT handoff.** Supersedes `2026-08-10-HANDOFF-cxa-adoption.md` on **everything**, status included, because that note's central claim went false. A **seam** handoff: the revert is deployed and verified, the mechanism is measured, the record is written. **The degradation tell did NOT fire.** **The extended 117k graph is REVERTED** — the owner used the app for three weeks and it fired the revert criterion he set before adoption. Names the **queued confirmation test as the next action and it is the OWNER'S**. Carries five must-not-revert claims, of which two matter most: **`graph-cxa-adopted.bin` is the REJECTED artifact and not "the newer one"** (an editor updating to the newest artifact would re-ship the regression), and **the `JFX-` run was not wrong** — its gate was a "the map is broken" stop-gate that never meant "no worse", so the reading changed and the record should not be. ⚠ Records that **one half of the owner's report is UNEXPLAINED** (the first journey, before any press) and must not be quietly closed. **⚠ FORWARD CORRECTION 2026-09-04: that half is now CLOSED by owner ruling** — he judges his single offhand report on the first journey unreliable and not a defect absent a mechanism. The document is frozen and correct as written; **the status it implies has moved.** See `NEXT.md`'s top block. Its "what I know that is not in the record" section carries the **graph-only revert command sequence**, which the runbook does not hold, and the structural fact that **`--require-approval never` is unavoidable on every graph swap** rather than a one-off judgement. |
| `superpowers/2026-08-10-HANDOFF-cxa-adoption.md` | **SUPERSEDED — NOT the current handoff.** Superseded on **everything** by `2026-09-01-HANDOFF-cxr-revert.md`: **the graph it announces as live was REVERTED on 2026-09-01.** Its record of what was done and why stands; its present-tense claims about production are false. Supersedes `2026-08-10-HANDOFF-jfx-run.md` **on next actions**. A **seam** handoff: the `CXA-` track ran to its last task, deployed, and was verified in production, so the successor reads a finished result. **The degradation tell did NOT fire.** **The extended 117k graph is ADOPTED and LIVE**; both owner stops were taken by the owner, and PR #92's landing-dot fix shipped with it at his instruction. Both gates passed — **`CXA-G1` byte-identically**, and **`CXA-G2` verified from inside the built image**. Names the **queued use-test as the next action and it is the OWNER'S**. Carries six must-not-revert claims, of which the newest matters most: **the EDGE FLOOR in `acceptance.py` is now the only bound that can catch a silent cap-rule revert** — the node bound cannot see one, so widening the floor to admit a build removes the protection with nothing going red. ⚠ Also records that **`--require-approval never` is NOT routine**: it was passed because CDK prompts on IAM broadening and stdin is null here, and only after the owner had seen the full IAM delta. |
| `superpowers/2026-09-03-unsung-cutover-execution-log.md` | **RETAINED EXECUTION LOG** for the `unsung.fm` cutover, 2026-09-03. **Infrastructure and documentation only — no application code, no graph, no cost function, and the served bytes are unchanged**, so it supersedes nothing about path quality or the map. **Owns no figures and no status** (`NEXT.md` owns status). **Mints no identifiers.** **Three sections are the reason to read it.** §3 records a defect this session introduced **in its own work and caught before it shipped**: it persisted the three per-deploy inputs into `infra/.env.deploy`, which is `ARC-6`/`DEP-34` again — `_require()` checks only that a variable is *set*, so a stale persisted value satisfies the guard while naming last month's graph, converting `DEP-34`'s loud failure back into the silent revert. §4 records that **`infra/README.md`'s `DEP-34` block was four weeks stale and asserted the opposite of the truth** — it said making the graph variables required was "deliberately not done", which `DEP-34-FIX` did on 2026-08-07 (`2930fac`); **do not revert that block to describing defaults.** §2 records why the old hostname is redirected at Cloudflare rather than kept as a second CloudFront alias, and why three live documents got a standing note instead of a find-and-replace. §6 records that **`localStorage` does not survive an origin change**, so the bypass explainer reopens once for every existing user — expected, and queued as *not* a fault. `G3-A5` was checked and does not reopen. |
| `superpowers/2026-09-01-cxr-revert-execution-log.md` | **RETAINED EXECUTION LOG** for the `CXR-` revert and diagnosis. **Owns no figures** (`../builder/analysis/2026-09-01-cxr-regression-diagnosis/README.md` does) and **no status** (`NEXT.md` does). **Four sections are the reason to read it.** §4 records that the session's **committed prediction was backwards** on the mechanism that turned out to matter — it expected the newly crawled artists to carry no listener data, which would have made the fame ruler immovable, and they carry it at 99.89 %. §3 records that **PR #91 was never merged, so production ran an unmerged branch's image for three weeks** while `main` described the older map — a gap, not a design. §2 records what was verified live rather than inferred, and why `--require-approval never` is unavoidable on **every** graph swap (the IAM statement is scoped to the object key). §5 lists what was deliberately NOT done, including the three candidate fixes that each need their own pre-registration. **⚠ It records one half of the owner's report as UNEXPLAINED** and that must not be quietly dropped. **⚠ FORWARD CORRECTION 2026-09-04: that half is now CLOSED by owner ruling** — he judges his single offhand report on the first journey unreliable and not a defect absent a mechanism. The document is frozen and correct as written; **the status it implies has moved.** See `NEXT.md`'s top block. |
| `../builder/analysis/2026-09-01-cxr-regression-diagnosis/README.md` | **FIGURES OWNER for the `CXR-` diagnosis — why the extended 117k map was worse at finding unknown artists. Never restate its numbers.** Descriptive probes over the two artifact files; **no routing was measured, no arm ran, nothing is adopted.** Predictions were committed before any figure existed (`PREDICTIONS.md`, `09a5656`). **Two interlocking mechanisms.** `CXR-P2`: the ~29,900 added artists have a fraction of the connections the existing population has, and a fifth of them **cannot be an interior card at any price** because a journey enters and leaves every artist in the middle. `CXR-P1`/`M4`: the fame ruler the `known` ramp steers on is framed on **the served artifact's own population**, so growing the map repriced every artist in it — unevenly, because artists near the top of the scale have nowhere to rise — costing a fifth of the only device steering toward the unfamiliar once the obscurity floor is spent. **⚠ `CXR-P3` did NOT fire**, so the owner's report that the *first* journey is also worse is **unexplained by anything here**, and the document says so. **⚠ Standing trap for every future map switch:** a map switch is never a "more artists, same routing" change, whatever the node-set arithmetic says. |
| `superpowers/2026-08-10-cxa-adoption-execution-log.md` | **RETAINED EXECUTION LOG** for the `CXA-` adoption. **⚠ The adoption it records was REVERTED on 2026-09-01** — everything here stands as history, but its present-tense claims about what production serves are false; `../2026-09-01-cxr-revert-execution-log.md` supersedes it on status. **Owns no figures** — `../builder/analysis/2026-08-09-jfx-prereg-critique/README.md` and `../builder/analysis/2026-08-10-cxa-acceptance-bounds/README.md` do. **Two sections are the reason to read it.** §3 records **three defects in the plan itself**: Task 2 named one constant to move when three had to (the snapshot test pins count, population *and* sha against whatever the registry resolves to, so executing it as written would have turned the suite red on a "one-line change"); Task 3's promise that the manifest would record `passed: true` is false, because `build_manifest` has no acceptance key at all; and Task 3 is not "the first exercise of the `manifest.py` fix", which fires only on override builds. §2 records **why the fourth acceptance artifact was built rather than waived** — the other three rows all vary crawl size, so none of them tests the cap rule, and the previous band demonstrably could not discriminate it. §6 records the `cdk diff` read, the `--require-approval never` decision, and live verification against the public address rather than inferred. §1 records the owner's `--prune` call and its reasoning. |
| `../builder/analysis/2026-08-10-cxa-acceptance-bounds/README.md` | **FIGURES OWNER for the fourth acceptance artifact** — the mutual-kNN build of the extended 117k archive, built at `CXA-` Task 1 because the plan had it marked NOT RUN. **Never restate its numbers.** One knob from the shipped build (`mutual_knn` instead of `trimmed_union`; same archive, same `ULF-` payload, same k=50), which is what makes it an isolating baseline for the **cap rule** rather than for crawl size. **Establishes that the recalibrated band discriminates the cap rule, which the previous band did NOT** — and that **the edge floor does that alone**, because the mutual-kNN build's node count sits inside the node band. **It serialises no artifact**, deliberately: the row needs counts and a verdict, so `mknn_result.json` is the record and there is no sha to cite. Establishes nothing about path quality, journey length, or what a user sees. |
| `superpowers/2026-08-10-jfx-run-execution-log.md` | **RETAINED EXECUTION LOG** for the `JFX-` run. **Owns no figures** (`../builder/analysis/2026-08-09-jfx-prereg-critique/README.md` does), except the D6 standing-layer numbers the closeout rule requires to live here — **0 delta in both layers**, compared against the recorded previous figures rather than inferred. **Three sections are the reason to read it.** §4 records a defect in the session's **own analysis code**: it implemented `AM1.11` read 9's trigger from the amendment's *rationale* rather than its *condition*, which would have silently suppressed a pre-registered read at the one depth it fires. §6 records the session **misreporting build progress** from a `| tee`-buffered 0-byte log, corrected only because the owner questioned the duration. §2 records that the `CRE-G1(a)` gate `AM1.3` names **could never have caught the thing `AM1.3` was worried about** — it ran doubly inert on the ramp. §3 records a mid-session reversal; §9 answers why a session bassist with 86 listeners is a routable waypoint, and identifies the structural signature of the co-credit class three probes failed to attribute. **⚠ QUALIFIED, NOT CORRECTED, 2026-09-01:** the artifact this run validated was **reverted** from production on 2026-09-01 and its reasoning is unchanged — the gate it verified passing is a stop-gate, never a quality bar. *(Corrected here rather than in the log, which is a frozen record.)* |
| `superpowers/2026-08-09-jfx-prereg-amendment-execution-log.md` | **RETAINED EXECUTION LOG** for `JFX-AM1`. **Owns no figures** (`../builder/analysis/2026-08-09-jfx-prereg-critique/README.md` does), except the D6 standing-layer numbers the closeout rule requires to live here — **0 delta in both layers**, compared against the recorded previous figures rather than inferred from "nothing was touched". **Two sections are the reason to read it.** §2 records the session **reversing its own recommendation under owner challenge**: it proposed dropping the famous–famous stratum from the gate on a record that turned out to be pre-`MSW-`, in popularity currency, under the retired fame construct — and the owner caught it from *using the app*, which is the evidence class this project rates highest. §4 records a claim the session had to correct **inside its own amendment**: `AM1.10` asserted shared RNG state would move every later interval, and **the test written to prove it refused to go red**. §3 lists six defects in the pre-registration itself, two of which left parts of it undefined rather than imprecise. §8 carries two shell traps and the fact that `GraphStore` discards raw fame at load. |
| `../builder/analysis/2026-08-09-jfx-prereg-critique/README.md` | **FIGURES OWNER for the famous-to-famous reachability measurement**, cited by `JFX-AM1.1` and by `PRODUCT-REQUIREMENTS.md` §8's forward correction. **Never restate its numbers.** Measured on the adopted artifact (sha `43dd82bb…`, matched against its sidecar by a script that **refuses** on a mismatch). **Settles whether `DD-F1`'s "superstar endpoints have zero edges below the top decile" still binds: it does NOT in fame currency** — almost no top-decile artist is boxed in, and none at all at the very top — **and DOES in popularity**, where a large minority still are. Magnitudes in its "Result" section. **⚠ Both halves travel together — `DD-F1` is not overturned**, it survives where it was measured and fails to transfer, which is the §2.6/§2.11/§2.12 currency trap catching a session again. **⚠ Measures STRUCTURE, not ROUTING** — it licenses "famous endpoints *can* descend", never "journeys *do*". Records that the `MSW-` switch addressing this was **by design** (owner statement) and that **no `MSW-` record mentions it**, so that adoption is written up on narrower grounds than it had. The directory also holds the `JFX-` statistics module and its 20 tests, and the §1 diagnostic build script. **⚠ QUALIFIED, NOT CORRECTED, 2026-09-01 — its `JFX-` RESULTS half only.** The extended map those arms scored was **reverted** from production on 2026-09-01; `JFX-G1b` is a stop-gate and **passing it never meant "no worse"**, so a PASS there is not a quality result. **The reachability half above it is NOT qualified** — it was measured on `graph-msw-tu50.bin`, the artifact the revert restored. See the `CXR-` rows. |
| `superpowers/specs/2026-08-08-crawl-growth-subset-preregistration.md` | **ACTIVE pre-registration — does growing the crawl thin the obscure tail?** Identifiers **`CXS-`** (`CXS-AM1` for its one amendment, deliberately distinct). **Committed at `7f4b77b` and amended at `43f0ce0`, both before any cell was built** — the timestamps are the evidence. **Owns no figures** (`builder/analysis/2026-08-08-cxs-growth/` does). **EXECUTED 2026-08-08 — do not run again.** Result: **`CXS-C1` rose materially and monotonically — growth makes the obscure tail BETTER connected**, falsifying the session's own prior claim that growth crowds the hubs; `CXS-C2` null. **Its subsets are not proxies** — file mtimes recover the true crawl order, so each cell is the archive exactly as it stood at that moment. **Its §4 cohort is frozen at the 25k frame deliberately**: `pop_raw` is population-dependent, so re-deriving "the obscure half" per cell would measure the ruler and not the artists. **`CXS-AM1` was written at execution time before any figure existed**, because `CXS-C2`'s baseline is 0 by construction and a relative threshold against zero cannot be evaluated; the cohort was **not** narrowed to rescue it. **Read its §7 before citing:** no path walked, no journey scored, nothing said about `DD-F1` or about how the app sounds; **every cell is at or below today's population, so the extension's target size is an assumption rather than a measurement**; and no cell is production's configuration. |

| `superpowers/specs/2026-09-03-launch-ux-scope.md` | **ACTIVE — the governing scope document for the `LUX-` set**, four pre-launch UX features scoped in a brainstorming session 2026-09-03. Identifiers **`LUX-`** (features `LUX-1`–`LUX-4`, evals `LUX-E1`–`LUX-E6`), collision-checked across every ref. **A SCOPE document, not a pre-registration and not an implementation plan** — no experiment has run, nothing is adopted, no default changed, no code written; a session picking it up **writes** the plan. **Owns no figures** (`builder/analysis/2026-08-02-dsp-ids/` and `findings/2026-07-31-release-tag-coverage.md` own the coverage figures it cites). Records four owner decisions and, for each, what the repo already contains: remove the `dislike` button while **keeping** the mechanism and the `?dislike=` URL parameter (§1); the route-history panel, which `LUX-1` makes contract-free because the two-list URL encoding loses press order **across** signals and one signal has none to lose (§2); alternate clips, where all three resolvers already fetch `clip_search_limit` rows and discard all but the first (§3); and streaming links plus a structured-fields info card, scoped together because they read the same MusicBrainz dump (§4). **⚠ §4.4 is the reason to read it before planning `LUX-4`:** the live artifact is ALG-B lineage (`CEX-5`) and `CXA-` Task 2 repointed ALG-B's unlistenable-filter payload **after** it was built, so a rebuild may not reproduce `43dd82bb…` — which would make `LUX-4` a **graph adoption**, not a metadata change. `LUX-E1` settles it and gates the item. §5 fixes a plain-language sentence and a threshold for every eval before any result exists — **⚠ with two 2026-09-05 amendments in place: `LUX-E4` is CLOSED (read UNDEFINED, threshold retired as mis-specified, not to be re-run), and `LUX-E2` carries a BLOCKER because the 120-pair `TAS-` sample is damaged on the served map exactly where that threshold is stated**; §7 carries the `PRODUCT-REQUIREMENTS.md` amendment **ready to apply but deliberately unapplied** (retiring `REQ-30`/`31`/`32`, adding two entries), for the implementing session to land in the same commit as the code. **§8 closes two things that must not be re-opened:** a prose artist description is **dropped, not deferred** (Wikipedia carries `COH-2`'s fame floor; Last.fm is barred; generation's error rate is inversely correlated with fame, which is the population the app exists to serve), and genre tags are **deferred, not dropped**. |
| `../builder/analysis/2026-09-05-lux-e1-drift-source/README.md` | **FIGURES OWNER for the drop-list drift measurement. ACTIVE. Never restate its numbers.** Measures what the `CXR-` revert left behind: `CXA-` Task 2 repointed the ALG-B unlistenable drop-list default at a payload re-censused over the extended population and the revert did not repoint it back, so **a rebuild from HEAD's default would drop 31 artists that are in the live map, and none return**. **Nothing served is affected** — the API never reads a drop list and the artifact is prebuilt. Its §3 is the part to read before citing it: it establishes that **one** build input moved and explicitly **not** that it is the only one — `LUX-E1-AM2` later found the archive had moved too, and running the eval found a third in the acceptance bounds. Its §4 flags that second one in advance. *(Row added 2026-09-05; the directory had no map row.)* |
| `../builder/analysis/2026-09-05-lux-4-rebuild/README.md` | **FIGURES OWNER for the `LUX-4` artifact's identity and for `LUX-E5`. ACTIVE. Never restate its numbers.** Produced by `L4-T7`. ✅ **The rebuild is the served graph plus exactly three metadata keys**, proven two independent ways: a CONTROL arm (post-LUX-4 code with the maps patched off) returns `43dd82bb` byte-identically, and a SUBTRACTION check strips the three new keys and reproduces the served artifact byte for byte, sha included. ⚠ **The plan's control arm could not be used** — `armb_sha.py` calls `build_from_archive`, which now wires the maps, so it can never return byte-identical again; `control_empty_maps.py` replaces it. ✅ **`LUX-E5` PASSES with ~8× headroom**: 261 MiB peak boot against App Runner's 2 GB, 12.8%. **Nothing is dropped and `area` stays.** Carries the finding that 5.70 MiB of JSON costs ~66 MiB of RSS (~10×, Python object overhead) — **that multiplier, not the byte count, is what should govern `LUX-E6`**. Build cost measured at 616 s; neither "~40 s" nor "~23 min" is usable for planning. |
| `../builder/analysis/2026-09-05-lux4-extract/README.md` | **FIGURES OWNER for `LUX-E3` and the `LUX-4` payload coverage. ACTIVE. Never restate its numbers.** Produced by `L4-T2`. **`LUX-E3`'s read: Spotify beats Apple in every popularity band and the gap widens with obscurity** (69.1% vs 50.9% over the served map), so the feature is "both services" with a search-URL fallback. ⚠ **Its §1 is why this directory exists rather than an edit to `2026-08-02-dsp-ids/`: that script's population is the 2026-08-02 union and MISSES 2,687 served artists (4.6%)** — its "superset of any final ALG-B build" claim is true for the 2026-07-30 archive and false across the crawl extension after it. ⚠ **Its §4 records a LIVE DEFECT: those same 2,687 artists carry no Deezer id at all**, so the app must resolve their clips by name search — the `BYP-13` exposure. **Not fixed by `LUX-4`**, because refreshing that key would break `L4-T7`'s control arm; metadata-only, so it needs a rebuild but no listening test. **Its §3 must NOT be cited as a `LUX-E2` read** — `LUX-E2` is stated over delivered cards and remains BLOCKED. |
| `../builder/analysis/2026-09-05-lux-e1-armb/README.md` | **FIGURES OWNER for `LUX-E1`'s result. ACTIVE.** ✅ **Arm B′ ran 2026-09-05 and was BYTE-IDENTICAL** to the served `graph-msw-tu50.bin` — artists, edges, bytes and sha all match — so **the gate on `LUX-4` is DISCHARGED and `LUX-4` is a metadata change as priced, not a graph adoption.** Determinism (spec §9) holds across a month and a reverted expansion. Both pins (`LUX-E1-AM1`'s drop list, `LUX-E1-AM2`'s archive) were committed **before** the run; the git timestamps are the evidence. **Arm A was not run and does not need to be** — both its inputs are identified. **Never restate its numbers.** ⚠ **Records a THIRD `CXA-` leftover found by running it: `check_acceptance` runs before `serialise` and its bounds were recalibrated for the extended population, so `artistpath-build build` REJECTS a correct rebuild of the served map** (`artist count 58838 outside bounds [70900, 106400]`). **That blocks `LUX-4`**, which must rebuild to add its fields. **Not a correctness problem** — acceptance governs admission, never content, which is why the probe's bypass cannot change a byte and the comparison stays valid. **Which fix is the owner's**; a session must not widen a bound to admit its own build. Also **retro-validates that `grt-archive-algb.pre-cex-snapshot` is the tree the live map was built from**, which no manifest records and inspection could not confirm. |
| `../builder/analysis/2026-09-04-lux-e4-candidate-counts/README.md` | **FIGURES OWNER for `LUX-E4` — how many of the cards a user sees can offer another track, by popularity band and by resolution route. Never restate its numbers.** Routes the committed 120 `TAS-` pairs with production weights against the adopted artifact (sha `43dd82bb…`, matched against its sidecar by a script that **refuses** on a mismatch) and resolves every **interior** card through the real `ClipResolver` against the **live** catalogues; endpoints are excluded because they are the user's own picks. Counts by band, by route, and by band × route in its §2; the threshold read in §3; the bounds in §4. **⚠ The pre-registered threshold could not be read, and was neither cleared nor fired: its denominator is EMPTY.** The router delivered **no lower-half artist onto any card at all**, so "fewer than half of lower-half cards carry ≥ 2 candidates" is **undefined here — not a pass**, and **recording it as passed would be the same error as recording it as failed.** What it does establish, on the bands it reached: the fraction of cards carrying ≥ 2 candidates is **flat and high from the very top of the map down to the upper half**, and the button would appear on almost every card. **⚠ Two limits bind every read, and both are the reason the threshold has no denominator.** The `TAS-` pair sample was drawn against the **pre-MSW** artifact and is **damaged on the currently-served map** — a large minority of its pairs cannot be routed at all because an endpoint is gone, and **the attrition falls almost entirely on the two obscure classes while the famous–famous class survives intact**, so **any future eval reusing `tas_pairs.json` against this map inherits the same hole**. And these are **depth-zero** journeys, before any bypass press: the press is the device that drives a journey obscure, so the missing lower-half cards are partly a property of measuring the un-pressed path rather than of the resolver. Also records that candidate counts are **censored at `clip_search_limit`** and that de-duplication is by normalised title only, so "Song" and "Song (Remastered 2011)" are two candidates. Establishes nothing about what a given user's own journeys contain. |
| `superpowers/2026-09-04-lux-1-3-execution-log.md` | **RETAINED EXECUTION LOG** for the `LUX-1`/`LUX-2`/`LUX-3` plan and the final whole-branch fix wave that followed it. **Owns no figures and no status** (`../builder/analysis/2026-09-04-lux-e4-candidate-counts/README.md` and `NEXT.md` do). Written because the session's ledger recording every ruling lives only under `.superpowers/sdd/`, which is gitignored and dies with the session — the final review's finding **I4**. **Three sections are the reason to read it.** §1 carries all eight rulings made across the run, in order, each with what it costs if wrong — three from the pre-flight conflict scan (`PF-1`–`PF-3`, on task ordering, a temporary bridge in `app.py`, and the plan's test code being illustrative not literal) and five from individual tasks, including promoting candidate de-duplication into `LUX-3`'s scope on the **spec's** authority rather than the plan's letter. §3 is an **⚠ operational note for whoever deploys this branch**: every existing clip-cache item becomes a miss at once, because the cache's item shape changed, so the first view of each artist afterward costs a Deezer *search* rather than a cheap re-sign — no infrastructure change needed, spec-sanctioned. §4 records that `snyk_code_scan` is **owed and undischarged** on the touched `api/` code because the tool is unauthenticated in this environment and authentication is the owner's to run — the only such gap across the branch. **⚠ FORWARD CORRECTION 2026-09-05: that debt is DISCHARGED.** The owner ran `snyk_auth`, and `api/` and `frontend/` both scanned **clean of shipped-code findings** — the five reported are a self-labelled test fixture constant and four in `frontend/design/**`, committed canvas exports referenced from nothing in `src/`, `index.html` or `vite.config.ts`. The log is frozen and was correct when written; only the status has moved. §5 traces the shared-link contract end to end and confirms it intact: `exclusions.ts` and `pathfinding.py` both have zero diff across the whole branch. |

### Complete

| Document | Covers |
|---|---|
| `superpowers/specs/2026-07-21-phase2-path-quality-design.md` | Phase 2 design. **IMPLEMENTED and adopted 2026-07-22** (`capfix`); §8 risk 4 discharged. Supersedes the roadmap's Phase 2 content only. Its open questions were answered by the sweep — do not read it as a live agenda. |
| `superpowers/plans/2026-07-21-phase2-path-quality.md` | Phase 2 implementation, 16 tasks. **All executed; do not execute again.** Tasks 14–16 were superseded mid-flight by the revised plan below. Several of its inline test fixtures are arithmetically wrong — see the execution log §3 before trusting one. |
| `superpowers/plans/2026-07-22-phase2-revised-plan.md` | The mid-flight amendment governing Phase 2 Tasks 14–16, written by the owner after a full review. **Retained as the record of *why* the last three tasks differ from the original plan** — its §6 is what reclassified C3 into Phase 1. |
| `superpowers/specs/2026-07-19-artist-path-alpha-design.md` | Original alpha design. Still the reference for determinism (§9) and the `APG1` format. ⚠ **Its §1 Last.fm constraint reads as live guidance and its stated reason is superseded** — "Last.fm must never become load-bearing" rests on "the roadmap includes paid subscribers", and the owner ruled 2026-08-04 that there will be no paid anything (`PRODUCT-REQUIREMENTS.md` §11, `REQ-43`). **This does not make Last.fm permitted:** §11 names three things the removal does not settle, and `REQ-44` requires owner confirmation before anything is built on it. Frozen document, not edited — §11 is where the supersession lives. **⚠ FORWARD CORRECTION 2026-09-06:** its §1 and §4.1 say "raw listen dumps carry MBIDs on 0.03% of records". That figure came from the JSON *listens* dump; the **Spark/parquet** dump carries LB's MBID mapping and measured **73.8 %** (`LBD-P1`, `../builder/analysis/2026-09-06-lb-dump-feasibility/README.md`). **Do not cite it as a reason the dump route is closed** — `findings/2026-09-06-lb-dump-route-assessment.md` carries the correction; the `LBD-` track is built on it. Its §6b's "$30–50 AWS processing job" is likewise obsolete (`LBD-D2`). Still frozen, still not edited. |
| `superpowers/specs/2026-07-20-stage3-web-frontend-design.md` | Frontend design. |
| `superpowers/plans/2026-07-19-graph-builder.md` | Builder implementation. Shipped. ⚠ **Its constraints list restates the alpha design's Last.fm bar** ("do not add it as a source in this plan") **with the same superseded reason** — see the alpha design's row above and `PRODUCT-REQUIREMENTS.md` §11. The instruction still binds this frozen plan; it is not a standing rule about the builder today, and `REQ-44` governs any move to change that. |
| `superpowers/plans/2026-07-20-path-engine-api.md` | API implementation. Shipped. |
| `superpowers/plans/2026-07-20-stage3-web-frontend.md` | Frontend implementation. Shipped. |
| `superpowers/findings/2026-07-22-doc-audit-context-layer.md` | Documentation audit, navigation layer. |
| `superpowers/findings/2026-07-22-doc-audit-technical-record.md` | Documentation audit, specs and plans. |
| `superpowers/2026-07-25-gate1-clips-and-ux-execution-log.md` | **The record of the Gate 1 clip and frontend-UX work, 2026-07-25** (PR #19). Both clip defects and all four dogfooding UX items. Carries three defects found **in the roadmap itself** — its stated cause for the wrong-artist bug was inaccurate as *located*, and its prescribed fix applied literally would have silenced every card. Also the mutation-check result, the deferred findings with success conditions, and the note that **C1/C2 are fixed but not closed** until the queued use-the-app check returns. Touches no routing, no graph, no weight. |
| `superpowers/findings/2026-07-25-doc-audit-gate1-clips-ux.md` | `doc-auditor` report from the 2026-07-25 closeout (B1). Five findings, four actioned; **its recommendation to expand bare identifiers inside `TEST-QUEUE.md`'s steps was rejected** — `closeout` C1 forbids naming identifiers in owner-facing steps, and the entry correctly has none. Its identifier census and previous-findings check are the parts worth citing. |
| `superpowers/2026-07-25-f1-minimum-stop-execution-log.md` | **The record of the F1 work, 2026-07-25** (PR #23) — every journey now gets at least one stop, or says why it cannot. Identifiers namespaced **`FMS-`**. Carries the four decisions with reasoning, **three defects found in the plan itself** (`FMS-P1` a verification check that could not fail; `FMS-P2` the false premise that adjacency implies the direct edge is cheapest; `FMS-P3` a figure restated under a citation pointing at the wrong identifier), the gate outcomes **including two failed reviews**, and the operational measurements it owns. **Restates no figure it does not own**; the graph figures are `findings/2026-07-25-mutual-knn-stranding.md`'s. Touches no weight, no config knob, no graph. |
| `superpowers/2026-07-25-HANDOFF-f1-minimum-stop.md` | **⚠ SUPERSEDED — Gate 1 is DONE and `F1` was discharged 2026-07-26.** Both this row and the file itself said F1 was "built but NOT discharged" until 2026-07-27; the discharge is recorded in `2026-07-26-HANDOFF-deliverability.md` and `NEXT.md`, which own status. Still the record of the F1 seam. *(Original role: **the CURRENT handoff for Gate 1, 2026-07-25**)* — written at a clean seam, nothing in flight. Supersedes `2026-07-25-HANDOFF-clip-playback.md` **on F1 only**. Records that F1 is **built but NOT discharged** (its condition is an observation — the queued use-the-app entry), the four things a successor must not get wrong, and what was decided against. |
| `superpowers/findings/2026-07-25-doc-audit-f1.md` | `doc-auditor` report from the 2026-07-25 F1 closeout (B1). Seven High, two Medium. **Its highest-value findings were two fresh identifier collisions in the F1 execution log itself** (`D1`–`D4` against the protocol review's defect series, `P1`–`P3` against the Track 2 prerequisites), caught within the hour and renamed to `FMS-` before the document was cited anywhere — the clearest instance yet of the rule that the audit is not skippable. All seven High actioned. |
| `superpowers/plans/2026-07-23-track1-tiebreak-remediation.md` | Track 1 implementation plan. **Executed 2026-07-23; do not execute again.** Outcomes in `2026-07-23-repair-and-retune-execution-log.md`. |
| `superpowers/specs/2026-07-22-c3-bypass-diagnostic-experiment.md` | C3 diagnostic protocol. **Executed** (Stage 0); Stage 1 superseded by Track 2. Results in the Phase 1 log §3. |
| `superpowers/specs/2026-07-22-c3-known-mechanism-blind-listen.md` | C3 `known`-mechanism blind-listen protocol. **Executed**, verdicts recorded. Results in the Phase 1 log §3.6–§3.9; the A-vs-C re-run condition is the 2026-07-23 spec §4.4. |
| `superpowers/findings/2026-07-27-doc-audit-full-project.md` | **The full-project documentation audit, 2026-07-27** — a one-time sweep of the whole corpus (~45,000 lines) run at the owner's instruction after `doc-auditor` moved from `haiku` to `sonnet`. Identifiers **`DAF-`**. **Owns no figures.** Ten parallel slices, because the corpus is ~11× the auditor's own single-pass budget and one broad run would have skimmed and reported as an audit; slice I was cross-cutting, since partitioning destroys the two checks that are inherently global. **`DAF-1` is the headline: the content is sound and the *status layer* is what decayed** — measurements reproduce, the one-record rule holds, code citations mostly resolve, but documents had stopped saying what they are. **`DAF-2`** found five merged plans reading as pending, worst of them a thirteen-task non-idempotent AWS plan still marked "not yet executed". **`DAF-3`**: the `DRV-1` provenance caveat sat inside the one section it does *not* cover, while two documents told readers to open one of the uncovered ones first. **`DAF-4` is the one to read on tooling** — a `haiku`-era `B1` run certified `docs/README.md` "entry complete" for a document this map had never mentioned, the absence class the auditor names as its own blind spot; it is recorded with an explicit note that one instance is not a controlled comparison. **§3 registers eleven previously unrecorded identifier collisions** (nothing renamed — forward-only), of which `DAF-C10` is the sharpest: a document's own collision census declared `T1` clean and was wrong. **§5 records two reported findings that did NOT survive verification**, deliberately. §6 lists what was changed; **§7 is what was left for the owner**, chiefly anything touching the budgeted standing layer. |
| `superpowers/findings/2026-07-26-doc-audit-context-layer-maintenance.md` | `doc-auditor` report from the 2026-07-26 closeout (B1), covering PR #26. Two High, two Medium. Its most valuable finding was the **absence class it was specifically pointed at**: `2026-07-25-HANDOFF-clip-playback.md` §5 still recommended "build F1 next" as a live position, unmarked, in a document whose top banner a mid-document reader never sees — actioned. **One of its findings is refuted and must not be re-actioned:** it carried forward a previous audit's claim that this map lacks a `**Role: AUTHORITATIVE**` marker in its first ten lines; the marker is on **line 3**. *(Corrected 2026-07-27: this row previously said the marker "has been throughout", which is wrong — git shows it was added on 2026-07-25 in `8a4450e`, as the fix for the 2026-07-25 audit's finding. The 2026-07-26 report's defect is therefore that it carried a fixed finding forward without re-checking, not that the finding was never true.)*. Its `export_paths.py` line-reference correction was right and was applied to both `FMS-N1` and `CLM-5`. Its `CLM-7` finding — that naming a gap without enumerating it leaves the successor no way in — produced the four-file table now in that log's §4. |
| `superpowers/2026-07-26-context-layer-maintenance-execution-log.md` | **The record of the skills-and-agents work, 2026-07-26** (PR #26). Identifiers namespaced **`CLM-`**. Touches no code, no graph, no weight. Exists mainly because closeout **D6** requires the standing-layer delta in a retained log rather than only in commit messages — §4 is that record, and it is where `CLM-7` was found: **`memory/` grew 435 → 469 lines with the delta recorded nowhere**, which is exactly the blind spot D6 names `memory/` for. Also carries **`CLM-5`** (a claim this session committed and then corrected: `FMS-N1`'s "adjacency is rare" was an inference presented as fact, and wrong twice over), **`CLM-6`** (`CLAUDE.md` may be wrong that `SKILL.md` **bodies** load unconditionally — if so D6 over-counts; not acted on, it is the owner's call, and the cheap check is named), **`CLM-1`–`CLM-4`** (four decisions taken *against*, which leave no other artifact), and **`CLM-8`–`CLM-10`** (what the session knew that was otherwise unwritten, including the owner's evidence that A5's detached relaunch survives a terminal closing). **`CLM-13` withdraws a claim from this log's own §7**: a file reported as unbacked is deliberately ignored by a `.gitignore` *inside* its directory, which the session could not see because it checked from a branch where that directory does not exist. **Whether a file is ignored is branch-dependent when the rule governing it is branch-scoped** — a trap that recurs across `builder/analysis/`'s dated directories, and one that `git status` alone cannot answer. |
| `superpowers/2026-07-26-stranding-causes-execution-log.md` | **The record of the stranding-causes work, 2026-07-26** (PR #25). Identifiers **`STC-`**. Touches no code, no graph, no weight. Carries the six decisions with reasoning — including **`STC-1` as a defect in the task's own framing** (the "crawl-frontier" cause it named is nearly empty, and the name invites a remedy that reaches almost none of the population it appears to name), and **D-4, the first report being fluent, self-consistent and wrong in its headline** until the source-offered denominator was added. Four gate outcomes, two of them designed to abort. **`STC-6` records a claim this session committed to draft and then corrected**, having run the falsifier it had named as open rather than shipping the open question. The two success conditions live here. |
| `superpowers/findings/2026-07-26-low-degree-synthesis.md` | **An INTERPRETATION of three committed measurements. AUTHORITATIVE for nothing, and it OWNS NO FIGURES** — every quantity is cited to `MKS-`, `CNS-` or `STC-` and their analysis directories; where it and a cited document disagree, **the cited one wins**. Identifiers **`SYN-`**. Assembles a reading produced by an independent consulting session across `findings/2026-07-25-mutual-knn-stranding.md`, `findings/2026-07-26-low-degree-census.md` and `findings/2026-07-26-stranding-causes.md`, none of which interpreted across the others. **`SYN-1`**: the low-connection population is three problems whose remedies are a rebuild, a full re-crawl, and nothing — costs that have been discussed as one decision. **`SYN-2`**: against the whole artifact the both-ways rule is a small cause by headcount and the dominant one among recognisable artists; it flags that *cap-stranded* and *rescuable* are different columns of the report's table and must not be swapped. **`SYN-3`** ranks the co-occurrence cut-off as the largest lever on headcount (mechanism is `STC-6`'s; only the ranking is new). **`SYN-4`** is a falsifier with its numeric expectation **committed before the measurement that tests it** — it fires if low-connection artists routinely appear as interior cards; resolved by `findings/2026-07-26-committed-walk-deliverability.md`. **`SYN-5`** records a wrong consulting prediction (right in shape, wrong in cause) rather than deleting it. **`SYN-6` and `SYN-7` were added 2026-07-26** and are **derivations from committed documents, not measurements**: `SYN-6` audits both blind listens against their own arm tables and finds that **neither varied the both-ways rule against a bounded-degree alternative** — Phase 2 log §12 varied reciprocity, the degree bound and the top-k ranking basis together, §16 ran `mutual_knn` on both arms, and the C3 `known`-mechanism listen pins every arm to one artifact — so **the ear has endorsed bounded degree and has never been asked about reciprocity**; inline pointers were added at the three places that lean on the shorthand (this map's `MKS-` row, that document's §3, and Phase 1 log §2.10's closing line), and **`MKS-5b`'s force is unchanged, only its currency**. `SYN-7` records that **both verdicts (2026-07-22) predate five of `WHAT-GOOD-LOOKS-LIKE.md`'s nine values** (5–7 dated 2026-07-23; 8–9 dated 2026-07-24), so a verdict cannot be cited as endorsing a criterion articulated after it; **neither item re-opens a verdict or argues for a third listen.** Its **§3 also records a methodological error — a consulting session proposed settling by ordinary use a question no amount of use could settle, because a missing connection and a priced-out connection look identical from the user's seat.** Its **§3 names `SYN-2` as the item most likely to fail** — "recognisable" leans on in-graph popularity as fame, and `CNS-2` contaminated the proxy that validated it. **§4 is a fenced opinions section**: a consulting position that the both-ways rule should *not* be loosened and that any targeted alternative must demonstrate its bound by simulation first. **Nothing adopted, nothing proposed, no threshold set; the path-quality pause is intact and this is explicitly not a resume signal.** |
| `superpowers/findings/2026-07-26-committed-walk-deliverability.md` | **AUTHORITATIVE for its own interpretation; OWNS NO FIGURES** — every quantity belongs to `../builder/analysis/2026-07-26-committed-walk-deliverability/`, whose `REPORT.md` is the deliverable. Identifiers **`CWD-`**. A read-only re-read of the `P` (user-facing baseline) arm in Track 2 and Track 2F, run to **resolve `SYN-4`**. **`CWD-1`: the falsifier did not fire** — across every interior card in the committed walks, none was a low-connection artist, against a graph where more than half of all nodes are. **`CWD-2` is the half that must be quoted with it**: the pair set was pre-registered with famous endpoints, so a low share was the *expected* result — the test could fail meaningfully and **could not pass meaningfully**, which `SYN-4` fixed in advance. Quoting `CWD-1` alone gets the finding backwards (`STC-3`'s shape). **`CWD-3`: the three committed runs are byte-identical and are one sample, not three** — recorded because the first version of the measurement counted all three and inflated every interior-card figure threefold, self-consistently. Explicitly **does not** show the router *cannot* deliver a low-degree artist, does not reopen §2.12, and its distinct-artist count is a **lower bound**. Nothing adopted, proposed, rebuilt or reconfigured; the pause is intact. **§7 was added 2026-07-26** when the follow-up below ran: `CWD-6` records that the **DO NOT APPEAR** branch fired, so `CWD-1` was *not* an artefact of its pair set, and `CWD-7` records a wrong prediction. §5's pricing-versus-arithmetic confound is **not** resolved by it. |
| `../builder/analysis/2026-07-26-obscure-pair-deliverability/` | **AUTHORITATIVE for its own measurements.** The missing arm for the committed-walk run above: same instrument, same production settings, same walk and depths (imported from the committed scorer, not transcribed), on **24 pairs sampled by node id from the graph's own connection-count distribution** — 12 obscure-endpoint and 12 ordinary-endpoint — instead of the famous pairs Track 2 pre-registered. **Its decision rule, thresholds, sampling seed and a stated prediction were committed BEFORE it ran (`8aa6f84`)**; the git order is the evidence. **Owns** the distinct-artist counts, the degree distribution, the per-stratum split and the minimum observed. Verdict: **DO NOT APPEAR** — low-connection artists stayed rare even when both endpoints were obscure. **Explicitly does not separate pricing from arithmetic** (a barely-connected artist has fewer chances to lie between two others), does not show these artists are undeliverable — eighteen distinct ones were delivered — and says nothing about path quality or famous journeys. **Not a `findings/` document, deliberately** — a second copy would restate its figures. Cite this directory; interpretation is `findings/2026-07-26-committed-walk-deliverability.md` §7. Nothing adopted, proposed or rebuilt; the pause is intact. |
| `superpowers/2026-07-26-deliverability-execution-log.md` | **The record of the synthesis and deliverability work, 2026-07-26** (PR #25). Identifiers **`DLV-`**. Touches no code, no graph, no weight. Carries the decisions with reasoning — including **`DLV-1`**, that both runs' reads were committed before the measurements that test them, and **`DLV-5`**, a deliberate and flagged exception to the one-figure rule. **`DLV-7`/`DLV-8` are defects found in the task's own framing**: a conflation of *cap-stranded* with *rescuable*, and a degree floor sized as a small change when nothing was delivered below four connections. **`DLV-9` is a correction this session made to its own work** — the first deliverability measurement counted three byte-identical copies of the same deterministic run as three samples and over-counted threefold, self-consistently; what exposed it was the three "independent" totals being identical, not code review. Also the gate outcomes, the B3 mutation check, and the D6 net-zero record. |
| `superpowers/2026-07-26-HANDOFF-deliverability.md` | **⚠ SUPERSEDED on next actions** by the current handoff at the top of this table; its branch was merged and the deliverability work it hands over is long closed. *(Found claiming currency at the `REL-` closeout's doc audit, 2026-07-31 — the "for this branch" qualifier did not stop it reading as current in a table where exactly one row may be. Its own record is untouched and accurate.)* *(Original role: **The CURRENT handoff for this branch, 2026-07-26 (later)*** — a clean seam, nothing in flight. Supersedes `2026-07-26-HANDOFF-stranding-causes.md` **on next actions only**; that handoff remains the record of its own work. Carries the five claims a well-meaning editor must not revert — chiefly that `CWD-2`'s "could not pass meaningfully" caveat is **not** superseded by the second run agreeing with the first, and that §5's confound stays open. **Points a resuming session at the resume brief instead of itself.**)* |
| `superpowers/2026-07-26-HANDOFF-stranding-causes.md` | **The handoff for the stranding-causes work, 2026-07-26** — a clean seam, nothing in flight. Supersedes `2026-07-26-HANDOFF-low-degree-census.md` **on next actions only**; the census handoff is still the record of its own work. Carries the five claims a well-meaning editor must not revert, what the session knew that was otherwise unwritten, and the open position with what it would do. |
| `superpowers/2026-07-25-HANDOFF-clip-playback.md` | **COMPLETE — superseded on F1 by `2026-07-25-HANDOFF-f1-minimum-stop.md`, and Gate 1 has since closed.** The handoff at the clip-playback seam. **Added to this map 2026-07-27**: it had no row of its own and was reachable only through a mention inside another document's row, despite this map claiming to classify every document in `docs/`. |
| `superpowers/2026-07-26-HANDOFF-low-degree-census.md` | **COMPLETE — superseded on next actions by `2026-07-26-HANDOFF-stranding-causes.md`**; still the record of the census work's own seam. **Added to this map 2026-07-27**, same absence as the row above. |
| `superpowers/2026-07-26-low-degree-census-execution-log.md` | **The record of the low-degree census, 2026-07-26.** Identifiers **`CNS-`**. Touches no code, no graph, no weight; read-only by construction. **Added to this map 2026-07-27 — it was not mentioned anywhere in this file at all**, while its own closeout recorded the `B1` doc-auditor step as "CLEAN … `docs/README.md` entry complete." That audit ran on the `haiku` model and certified a map entry that did not exist: the absence class the auditor names as its own known blind spot. |
| `superpowers/plans/2026-07-25-f1-minimum-stop.md` | F1 implementation plan, six tasks. **Executed 2026-07-25** on branch `f1-min-stop`; do not execute again. **F1 itself was discharged 2026-07-26** — this row's closing sentence below describes the state before that happened. Design: `superpowers/specs/2026-07-25-f1-minimum-stop-design.md`. Task 6's verification script and use-the-app queue entry are the observation that closes F1 — the plan itself says not to mark F1 discharged until the owner runs it. |

### Partly historical — check before citing

| Document | Status |
|---|---|
| `superpowers/findings/2026-07-19-listenbrainz-probe.md` | **Superseded for scoring and normalisation** (it describes per-artist normalisation, deleted in `284366c`). Everything else — endpoint schema, rate limits, the 1,000-artist sitewide cap, the §6d–6f popularity-source eliminations — is still valid and load-bearing. **Read §6d–6f at its actual scope:** it eliminates external **popularity** from the **routing cost terms** (§6e's reason is that `|pop(a) − pop(b)|` and the floor compare two nodes of one graph, so the quantity must be defined on that graph's population), and §6f marks even that *"an alpha decision, not a permanent one"* with a named replacement condition. It says nothing about **fame**, a different currency — an external fame source has already passed a gate (Wikipedia pageviews, pre-registration amendment A11). **External fame sources are gated, not banned;** this row's earlier wording had been read three times as a general barrier. **⚠ FORWARD CORRECTION 2026-09-06 to §6c:** "0.03 % of raw listens carry MBIDs" was measured on the JSON *listens* incremental and is correct for that file — but §6c's own last line already said the parquet dump was "unverified", and it has now been verified: the **Spark/parquet** dump is written with LB's MBID mapping applied and measured **73.8 %** (`LBD-P1`). **§6c's "unusable" verdict on the dump route is superseded**; §6d–6f's popularity elimination is unaffected, resting on its own reasons. Correction owned by `findings/2026-09-06-lb-dump-route-assessment.md`. Frozen, not edited. |
| `superpowers/findings/2026-07-21-architecture-review-and-path-baseline.md` | **Superseded for scoring and metrics.** Retained as narrative history of how the reviews unfolded. Its architect and QA findings in §1 (clip 500s, artifact length validation, sync boto3 on the event loop) are **not** superseded and remain actionable. |

### Narrative — never use as context

| Document | Status |
|---|---|
| `how-we-map-similar-artists.md` | A dev-blog journal of the modelling journey, written for humans. **Never cite it, never use it as context, never instruct development from it.** It is not maintained to the standard of project context and will lag reality. Update it when there is major progress worth journalling; correct factual errors when found. |

### External — not project documentation

| Document | Status |
|---|---|
| `reference/` | Third-party material consulted during early planning. **Not project documentation and not for routine context.** See `reference/README.md`. |

---

## Where else information lives

- **Persistent memory** — outside the repo, loaded into every session, at
  `~/.claude/projects/C--dev-music-app/memory/`. The
  `MEMORY.md` *inside that directory* is the index; each sibling file holds one fact.
  Memory holds **pointers and working preferences, not figures**.
- **`.claude/agents/ml-graph-analyst.md`** — a reusable analysis-only subagent for graph,
  scoring, and metric questions. It has no `Edit` tool by design.
- **`scripts/docs-lint.sh`** — the mechanical half of a documentation audit: role markers,
  this map's own completeness, dead relative links, plans claiming to be live, duplicate bare
  identifiers, restated figures. ~90 seconds, deterministic. **`closeout` B1 runs it before
  dispatching `doc-auditor`.** A document that legitimately *quotes* other documents' link
  syntax may opt out of the link check with `<!-- docs-lint: skip-links — reason -->`; one
  file does. **A green lint is not a clean audit** — it cannot see whether a banner is
  attached to the right section, which is what the 2026-07-27 sweep's best finding was.
  Its positive control is `scripts/docs-lint-selftest.sh`, which proves every hard check can
  go red; it caught a real bug on first run (`grep -i executed` matches "not yet executed",
  so the check would have cleared the exact plan it exists to catch).
- **`.claude/agents/consultant.md`** — **not a subagent, and a working session must never
  dispatch it as one.** It is launched as its own session (`claude --agent consultant --tools
  Read,Grep,Glob`), reads the committed project record but never the code, and gives one
  reasoned recommendation on one named decision, then stays in conversation. Its whole value
  is that its inputs are the owner's rather than another session's paraphrase — dispatching it
  from a working session destroys exactly that. **Added to this list 2026-07-27**: the agent
  had existed and been used for some time while appearing in none of the entry points, so it
  was discoverable only by listing `.claude/agents/` directly.
- **`.claude/agents/doc-auditor.md`** — the project-local fork of the global auditor, and it
  **shadows** the one in `~/.claude/agents/` inside this repo (project scope outranks user
  scope). It carries two checks the global one does not: **I**, the identifier census — no
  two load-bearing objects may share a name, and owner-facing text may not use a bare
  letter-number token without its plain-language sentence; and **J**, reads-of-results
  completeness for pre-registrations. It reports and never edits, and it may never propose
  renaming an identifier in a committed document. Dispatched by `closeout` B1.
- **`.claude/hooks/` and `.claude/rules/`** — project hooks, wired in `.claude/settings.json`:
  a repo-state report at session start (branch, uncommitted paths, other worktrees), a
  once-per-session context-size note, and the `DLS-T1` instruction-load logger; plus the
  `DLS-T1` probe rule. The owner's rulings behind them are
  `superpowers/findings/2026-09-10-documentation-layer-strategy.md` §5a. A git-safety hook
  that blocks whole-tree staging lives in the owner's user-level settings, outside the repo.
- **The code is the truth about the code.** Where a document and the source disagree
  about behaviour, the source wins — and the document is a defect to be fixed.

---

## Adding a document

State its role in the first ten lines. Add it to the table above. If it supersedes
something, put a banner on what it supersedes **and mark the superseded claims inline**,
not only at the top of the file — a reader who lands mid-document never sees the banner.
